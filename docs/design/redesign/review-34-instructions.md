# Review 지침: 디자인 개편 3·4단계 (2026-09-24)

Build와 **독립적으로** 검증한다. **코드는 고치지 않는다.** 결과는 `docs/design/redesign/review-34.md` 하나.

## 읽을 것
`CLAUDE.md`(과학 차시 앱 규칙), `docs/design/redesign/spec.md`(끝 "개정 1"), `build-3-instructions.md`, `build-4-instructions.md`, `build-3-report.md`(개정 1 포함), `build-4-report.md`(개정 1 포함), 커밋 `8eb7a94` 변경(`git show --stat 8eb7a94`, `git diff 8eb7a94~1 8eb7a94` — 읽기만).

## 검증 항목
1. **과학 앱 동작 불변(최우선)**: 공통 틀 변경은 CSS + `persist.js` 맨 위의 글꼴 `<link>` 주입뿐이어야 한다. `diff`로 확인하고, 주입 코드가 예외를 던지지 않는지·한 번만 실행되는지·SRI 값이 실제 파일과 맞는지(해시를 직접 계산) 확인. 저장 키·`app_progress.state`·`detail.qa` 모양 불변. 23개 앱 사본이 정본과 같은지(`diff -r`).
2. **과학 앱 화면**: 앱 최소 5개(`sci-6-1-1-2`, `sci-6-1-2-3`, `sci-6-2-1-4`(각도 슬라이더), `sci-6-2-2-2`(시간 바), 조사 `sci-6-1-1-6`)를 끝까지. 태블릿 가로·세로·375·다크, 3D 장면·그래프 색이 전과 같은지, 되짚기/차단 카드·제출 안내·완료 화면, 기록하기 버튼 가림 없음, 휴대폰 가로 머리말. 글꼴 서버를 막았을 때·늦을 때 첫 화면이 바로 뜨는지.
3. **관리자 화면**: 모든 기능이 그대로인지(회원 추가·엑셀·탈퇴·비밀번호 초기화·잠금 스위치·학생 응답·피드백·일괄 칭찬·신고·글쓰기), 위험 버튼이 분명히 빨간지, 회원 표 ⋮ 메뉴가 어떤 폭에서도 닿는지, 관리자 전용 채도 조정이 학생 화면에 새지 않는지.
4. **접근성**: 대비 AA(밝음·어두움), 키보드 초점, `prefers-reduced-motion`, 44px 터치 영역.
5. **정적 export·basePath**: `out/`을 `/class1` 경로 정적 서버로 띄워 앱·관리자 리소스 200.

## 작업 환경
자기 전용 headless 브라우저·자기 포트 서버만. **실제 Supabase 주소는 처음부터 막고**(요청 가로채기) 가짜 세션·가짜 응답만. git은 읽기(log/show/diff)만. `localStorage.clear()` 금지. 끝나면 서버·브라우저 종료.

## 형식
요약(심각도별 개수) / 문제 표(심각도, 위치 파일:줄, 현상, 재현, 수정 제안) / 확인한 것 vs 못 한 것. 최종 응답 10줄 이내 한국어.
