# 진행 현황 (docs/STATUS.md)

> 마지막 갱신: 2026-09-26. 규칙은 `CLAUDE.md`, 여기에는 **현황·확정 결정·남은 일**만 적는다. 작업이 끝나거나 결정이 바뀌면 이 파일을 갱신한다.
>
> **새 세션 시작 문구**: "CLAUDE.md와 docs/STATUS.md를 읽고, 지금 적용 중인 규칙과 남은 작업을 요약해줘"
>
> **지금 상태 한 줄 요약**(2026-09-26 오후): 배포 끝 — 실험 앱 개편 A·B·C·크게 보기(개정 6)·머리말 자동 접기·가로 기본(개정 7), 조사 앱 저장 수정, 자유게시판 로그인 전용, 사이트 이름 "모두의 배움터", 체험 모드 Gemini 판정, 검색(실험 앱·게시판·게임)·홈 좌우 배치, **반별 구분 + 학급별 선생님 글**(SQL ①~④ 사용자 실행·함수 3개 재배포·화면 `e4d33ce` 배포, 두 사이트 확인). **진행 중(커밋 안 됨)**: 확대 3D 이동 화살표(겉모양 수정 + Review), `sci-6-2-1-3` 위선 삭제·태양 고도 그림(Build). **사용자 결정 대기**: Review M1(과제·글 수정·삭제를 쓴 교사·총괄만으로 — 두 번째 담임 지정 전). → 아래 **▶ 이어서 할 일**부터. 배포 여부는 `git log origin/main..main`(비어 있으면 모두 배포됨).

## 배포
* 사이트: https://eversunk2-tech.github.io/class1/ (저장소 `eversunk2-tech/class1`, main에 push하면 GitHub Actions로 배포, 약 1~2분)
* 로컬 개발: `.claude/launch.json`의 `blog-dev`(`npm run dev`) → http://localhost:3000/class1/
* 배포 뒤 학생 태블릿은 앱 탭을 닫았다 다시 열어야 새 화면이 보일 수 있다(HTML 캐시 최대 10분).
* **Vercel(2026-09-26, 설정 끝)**: 프로젝트 `class1`(같은 저장소, main push로 자동 배포) — **`https://class1-chi.vercel.app/class1/`**. `vercel.json`: `/`→`/class1/`, `/class1/…/index.html`→폴더 주소(Vercel은 `index.html` 주소를 404로 줌 — 사이트 링크는 `/apps/<앱>/`라 원래 영향 없음). 사용자가 한 것: Vercel 환경 변수 `NEXT_PUBLIC_SUPABASE_URL`·`NEXT_PUBLIC_SUPABASE_ANON_KEY`, Supabase Redirect URLs에 `https://class1-chi.vercel.app/class1/**`, Secret `EXTRA_ALLOWED_ORIGINS`, 함수 4개 재배포. Claude 확인: 페이지 200·최신 커밋, 올바른 Supabase 프로젝트로 요청, 함수 4개가 Vercel·GitHub Pages 허용·다른 주소 거부. 사용자 확인: 메인·관리자 대시보드·Google 로그인. 두 사이트는 같은 Supabase(글·학생 기록·잠금 스위치 공통), 로그인 상태는 주소마다 따로. **push 뒤에는 두 사이트 모두 새 코드인지 확인**한다.

## 과학 차시 앱 — 23개, 모두 배포됨
앱 폴더 `public/apps/sci-{학기}-{단원}-{탐구}/`, 차시 연결은 `src/data/science-curriculum.ts`. 실험 앱 20개(공통 틀 `science-sim`) + 조사 도우미 3개(`science-guide`: 6-1-1-5, 6-1-1-6, 6-1-2-6).

### 6학년 1학기
| 단원 | 탐구 | 앱 | 종류 | 기준 | 상태·메모 |
|---|---|---|---|---|---|
| 1. 산과 염기 | 1~4 | sci-6-1-1-1~4 | 실험 | 새(09-26 간략화) | 학생 사용함 — **09-26 간략화 배포**: 예상 1·분석 1~2·정리 1, 발전 질문 삭제, '더 탐구하고 싶은 점'은 정리하기 안 필수 한 줄. 관찰 칸·분류·측정값 그대로(사용자: 하나하나 측정), 6-1-1-1 거품 5초는 빨리 감기(모형). 저장 키 `sci611sim1:v2`·`sci611sim2:v2`·`sci611sim3:v2`·`sci6114sim:v3`(진행 중이던 학생·이미 마친 학생 모두 다시 열면 처음 화면, 마친 결과는 선생님 화면에 "간략화 전"으로 그대로), `detail.questionSet: 2`, 매핑 v2. 예상 시간 7분 넘음(보통 7:15~12:44 — 관찰 칸 전부 측정) |
| | 5·6 | sci-6-1-1-5·6 | 조사 | 새(09-26 간략화) | 같음(도입 1·정리 퀴즈 2·결론, '더 탐구하고 싶은 점'은 정리 단계 안). 5는 정리 틀 2줄, 6은 원인·피해·대책 세 칸 + 출처 고르기(사용자 결정). 저장 키 `sci6115guide:v2`·`sci611guide6:v2`. 6은 "참고 자료 열어 봄" 값을 항상 false로 저장(대시보드 "알 수 없음") |
| 2. 물체의 운동 | 1·2 | sci-6-1-2-1·2 | 실험 | 이전 | 수업에 사용 안 함 — 그대로 둠 |
| | 3 | sci-6-1-2-3 | 실험 | 새 | 5.0초 1회 → 135.0/122.0 cm, `:v4` |
| | 4 | sci-6-1-2-4 | 실험 | 새 | 100.0 cm 차마다 1회 → 4.2/5.8초, `:v4` |
| | 5 | sci-6-1-2-5 | 실험 | 새 | 파랑·초록만 계산(24.4/23.8 cm/s), 빨강·노랑 교과서 값(27.0/17.2), `:v4`. 느린 학생 8~9분 — 사용자가 그대로 두기로 결정 |
| | 6 | sci-6-1-2-6 | 조사 | 이전 | 수업에 사용 안 함 — 그대로 둠 |
| 3. 식물의 구조와 기능 | 1~7 | — | — | — | **앱 만들지 않음**(사용자 결정). 차시 목록만 |
| 4. 지구의 운동 | 1~5 | — | — | — | **앱 만들지 않음**(사용자 결정). 차시 목록만 |

### 6학년 2학기 (3단원까지, 4단원 "과학과 나의 진로"는 사용자 결정으로 메뉴에서 삭제)
| 단원 | 탐구(차시) | 앱 | 측정(실험관찰 조건 전부) · 메모 | 저장 키 |
|---|---|---|---|---|
| 1. 계절의 변화 | 1 (2) | — | 제외(사용자 결정) | — |
| | 2 (3~4) | sci-6-2-1-2 | 9:30~15:30 7칸 + 시간 바(눈금 사이 값은 모형). 8:30은 사용자 결정으로 삭제. **09-25 개편**: 나무(측정 대상)·작은 사람·백엽상 장면, 태양 끌기 ↔ 시간 바, 평행 빛줄기 + 나무 꼭대기→그림자 끝 한 줄 + 작은 호, 값 패널(크게 보기 막대), **"옆에서 본 모습" 칸**(사용자 결정) | `sci621sim2:v1` |
| | 3 (5~6) | sci-6-2-1-3 | 1~12월 21일 12칸(다음 달 자동 재생 — **09-26 사용자 요청으로 없앰(fix-3 진행 중)**). 조사 기간 표기 생략(사용자 결정). **09-25 개편**: 궤적 "이번 달만"(기본)/"계절 대표 3개", 3D·2D 모두 남쪽 하늘(동쪽 왼쪽), 크게 보기 연결 | `sci621sim3:v1` |
| | 4 (7) | sci-6-2-1-4 | 30°·80° 필수 + 각도 슬라이더(10°~90°). **09-25 개편**: 예상 질문 "여름과 겨울 중 언제 기온이 더 높았나요? 그리고 그 때 한낮의 태양의 높이는 어떠했나요?"(사용자 문구, id `q1b` — 진행 중이던 학생은 예상하기부터 다시, 실험 기록은 유지), 손전등 끌기, 크게 보기 연결 | `sci621sim4:v1` |
| | 5 (8~9) | sci-6-2-1-5 | 자전축 수직·기울임 × 가·나·다·라 8칸. **09-26 사용자 지적 — 옮길 때 자전이 처음 잠깐 동→서로 돌던 것을 고침(늘 서→동, 매 프레임 기록으로 확인, 배포 끝 09-26 `e774b11` — 사용자 요청, Actions 성공·두 사이트 확인)**. **09-25 개편**: 확대 화면이 전체 화면과 좌우 같게(사용자 지적 — 전등 방향 유지), 확대 칸(그림자 길이를 표 값에 비례), 지구본 끌기 | `sci621sim5:v1` |
| 2. 물질의 연소 | 1 (2) | sci-6-2-2-1 | 밀가루·탄산수소 나트륨 × 2관찰 4칸 | `sci622sim1:v1` |
| | 2 (3) | sci-6-2-2-2 | 양초·알코올 × 3항목 6칸 + 시간 바(모형) | `sci622sim2:v1` |
| | 3 (4~5) | sci-6-2-2-3 | 산소(시간 바)·발화점 4칸, 나무도 더 가열하면 탐(지도서 207쪽) | `sci622sim3:v1` |
| | 4 (6) | sci-6-2-2-4 | 염화 코발트 종이·석회수 2칸, 분석 1은 정답 2개 | `sci622sim4:v1` |
| | 5 (7) | — | 제외(사용자 결정) | — |
| 3. 전기의 이용 | 1 (2) | sci-6-2-3-1 | 회로 가~라 4칸 + 플라스틱·고무·금속 3칸 | `sci623sim1:v1` |
| | 2 (3) | sci-6-2-3-2 | 전구·전동기·버저 × 전지 1·2개 직렬 6칸. 버저 소리(작게, 끄기 가능), 병렬연결은 기록 없는 팝업 | `sci623sim2:v1` |
| | 3 (4~5) | sci-6-2-3-3 | 스위치 1 + 전지 1·2개(클립 4/8개, 자연수) 2 + 전지 방향×나침반 4 = 7칸. **09-26 문구**(사용자 요청): 조건 "바꾸기 전 · 왼쪽 나침반" 등, 관찰 질문 "오른쪽 나침반의 바늘을 보고, 전자석의 끝이 무슨 극인지 골라 봅시다.", 나침반을 가리킬 때 '끝' 뺌(저장 영향 없음) | `sci623sim3:v1` |
| | 4·5 | — | 제외(사용자 결정) | — |

* 예상 소요(느린 학생, 글자 수 추정): 여러 앱이 8~9분 — 실험관찰 조건을 모두 재는 규칙 때문(사용자에게 알림, 허용).
* 설계·보고 문서: 앱 폴더의 `spec.md`(끝 "개정 1" 우선)·`build-report.md`·`review.md`·`fix-1-report.md`, 단원 공통 지침 `docs/science/6-2-unit{1,2,3}/`.
* 지도서 PDF 인쇄 쪽 = PDF 쪽 + 오프셋: 1학기 산과 염기 +103, 물체의 운동 약 +179~180 / 2학기 계절의 변화 +103, 물질의 연소 +179, 전기의 이용 +253 (단원 지도 계획은 PDF 5쪽). 2학기 실험관찰은 `실험관찰-6-2.pdf`.

### 과학 앱 공통 기능 (공통 틀, 23개 모두 적용)
* **로그인 잠금 연동**: 잠금 꺼짐 + 비로그인 → **체험 모드**(끝까지 가능, 이 기기에만 저장, 기록 저장 서버 호출 0 — 답 판정 Gemini는 부름(09-26), "로그인하면 기록이 저장돼요"). 로그인하면 체험 기록은 이어받지 않고 지운다. 잠금 켜짐 + 비로그인 → 로그인 안내.
* 로그인 학생: 진행 상황 `app_progress` 자동 저장·이어서 하기, 로그아웃 시 `sci6…` 로컬 삭제, 완료 시 `app_results`(+`detail.qa`).
* **학생 답 되짚기·차단**(`answer-check.js` + Edge Function `check-answer`): 통과 / 다시 생각(질문당 1회, 그대로 제출 가능) / 통과 불가(무의미·주제 무관). 로컬 규칙은 확실한 무의미 5종만, 주제 무관은 Gemini만(**체험 모드도 — 09-26 사용자 결정**). Gemini 실패면 차단 없음(로컬 규칙만). 같은 질문 3번째 차단부터 "🙋 선생님과 확인했어요". "✔ 잘 적었어요"는 검사 통과 뒤에만. 안내 문구(외부 검사 사실)는 넣지 않음(사용자 결정) — 학생에게 개인정보를 적지 않도록 교실에서 안내.
* **마치기 조건**: 정리하기 답이 통과 불가면 '학습 마치기' 거부, '더 탐구하고 싶은 점'은 **필수**(느슨 판정: 무의미·주제 무관만 거부). 버튼 위에 "'학습 마치기'를 눌러야 선생님에게 제출돼요" 상시 표시.
* 디자인: 사이트와 같은 보라·알약 버튼·본문 Pretendard(jsDelivr)·제목 G마켓 산스 Bold + 부드러운 그림자(사이트의 `public/fonts/gmarket-sans/`를 상대경로로). `persist.js`가 글꼴 `<link>`를 비차단으로 붙여 글꼴이 막혀도 첫 화면 즉시. 3D 장면·그래프 색은 그대로.
* **실험 화면(2026-09-25 개편 단계 A, 실험 앱 20개)**: 기본은 예전 구조(가로: 장면 왼쪽·조작 오른쪽), 장면 왼쪽 위 "⛶ 전체 화면 보기" → 크게 보기. **태블릿·PC는 가로·세로 모두 크게 보기가 기본**(09-26 사용자 결정 — 전에는 세로만; 휴대폰(세로 폭 600px 미만·가로 낮은 화면)과 막대에 넣을 것이 없는 앱은 끔), 선택은 방향별로 기억(`ssUiPref:sceneMode:portrait|landscape`, `sci6` 접두사 아님 — 로그아웃해도 남는 화면 설정). 머리말 "▲ 접기"(계정 줄+단계 메뉴, 23개 앱, `ssUiPref:headerCollapsed`) — **크게 보기가 보이는 동안은 가로·세로 모두 자동으로 접힘**(09-26 개정 7, 그동안 누른 펼치기는 저장 안 함, 끄거나 다른 단계로 가면 원래대로). 알림은 머리말 위.
  * 단계 A의 첫 크게 보기(장면 가로 전체, 관찰 카드·조건은 장면 아래 → 페이지 스크롤 필요 — 사용자 지적)는 아래로 바뀜.
  * **크게 보기 = "장면 + 옆 조작 칸" 한 화면 실험실(09-26 spec 개정 6, 배포됨)** — 머리말~아래 이동 막대 사이에 고정, 가로 화면은 조작 칸 오른쪽·세로는 아래, 조작 칸(조건·관찰 카드·알아 두기·안내 줄·시간 바 카드 등)만 안에서 스크롤, 페이지 스크롤 없음, 장면 안 막대(실행·기록하기·측정값) 유지, Esc로 기본 화면. '알아 두기'는 크게 보기에서 제목 줄만(학생 선택으로 저장 안 함, 끄면 원래대로) — 조작 칸 첫 화면에 고르는 버튼이 보임(세로 19/20·가로 20/20). 보고 `docs/science/sim-redesign/dock-report.md`·`review-dock.md`·`dock-fix-report.md`.
* 공통 틀 개선 이력: `docs/science/template-fix-1-report.md`(가짜 충돌 창·기록하기 가림·휴대폰 가로 머리말·숫자 조사·알림 클릭 통과), `docs/science/answer-check/`(되짚기·마치기 조건·체험 모드 연동), `docs/design/redesign/build-3-report.md`(겉모양·글꼴), `docs/design/redesign/build-5-report.md`(제목 글꼴·그림자), `docs/science/small-fix-2-report.md`(움직임 줄이기 스피너, 예전 파랑 7곳, `sci-6-2-1-4` 기록하기 버튼).

## 블로그·관리자 기능 (모두 배포됨)
* 블로그 글(관리자 작성), 댓글·좋아요·조회수, 검색/태그, 로그인(아이디/비밀번호 + GitHub/Google OAuth)
* **학급별 '선생님 글'(09-26 사용자 결정, docs/classes/spec.md 개정 2·2 보충 — 반별 구분과 함께 Build 중, 지침 `docs/classes/build-notices-instructions.md`)**: 새 표 `class_notices`(제목·본문만), 로그인한 학생 = 자기 담임 글만(다른 반 학생은 총괄 글도 안 봄), 비로그인 = 총괄 글(안내 한 줄 없음), 관리자 '글 관리·새 글 작성' 대신 학급별 '선생님 글'(제목·본문 두 칸), 날짜·읽음 기록·조회수 없음. 블로그 코드는 지우지 않고 화면 진입점만 뺌.
* **검색(09-26 사용자 결정)**: 상단 돋보기 = 블로그 글이 아니라 **실험 앱(사이트 안 차시 자료)·자유게시판(로그인한 사람만)·학습게임(잠금 스위치대로)**을 함께 찾음, 종류별 묶음·5~10개씩 더 보기, 태그 칩 = 종류(#실험 앱 #자유게시판 #학습게임), 설명 "필요한 글의 제목, 요약, 태그로 찾아요"(사용자 문구). 홈 '최근 과학 수업'과 '선생님 글'은 자유게시판·학습게임처럼 넓은 화면에서 좌우 반씩(09-26 사용자 요청).
* (배포됨 `f16703f`·`07e5fc4`) 홈 '선생님 글'(09-26 사용자 결정): 관리자 화면에서 쓴 공개 글을 홈에 **제목만** 목록으로, 제목을 누르면 본문이 펼쳐짐(날짜·조회수 없음, 왼쪽 메뉴 없음, 5개씩 더 보기). '최근 과학 수업'은 실험 앱 현황만, 과학수업 페이지의 '과학 수업 글' 칸 삭제. 읽음 기록 없음(`07e5fc4`에서 뺌), 지금은 로그인한 사람에게만 — 학급별 '선생님 글'로 바뀔 예정(위).
* 학생: 내 학습 활동 `/me/learning/`(앱 이름은 차시 제목으로 표시), 피드백 대화, 비밀번호 강제 변경 `/reset-password/`
* 커뮤니티: 자유게시판 `/board/`, 학습게임 업로드 `/games/`(샌드박스 실행), 신고, 관리자 신고 관리 `/admin/community/` — **자유게시판은 로그인한 사용자만**(09-26 사용자 결정: 목록·글·글쓰기 로그인 안내, 홈 미리보기·글 수도 로그인 안내, RLS `board_login_only` — 사용자 실행·확인 완료)
* 관리자 대시보드:
  * 회원 관리: **회원 추가**(한 명 / 엑셀 일괄 — 아이디·비밀번호·역할(학생=user, 교사=admin), 서식 `public/templates/members-template.xlsx`, 첫 로그인 비밀번호 변경 강제, 감사 로그 `member_create_log`), 비밀번호 초기화, **완전 탈퇴**(auth 계정 삭제, 학습 기록은 "탈퇴한 학생"으로 보존, 복구 없음, 이름 입력 확인, 관리자 대상 불가, 로그 `member_withdrawal_log`)
  * **로그인 잠금 스위치**(`site_settings.login_required`, 기본 꺼짐): 켜면 블로그 글·학습게임 글·과학 앱이 로그인 필요(자유게시판은 09-26부터 스위치와 상관없이 늘 로그인 필요), 홈·메뉴는 항상 보임(RLS 게이트 6곳: posts·comments·community_posts·community_comments·community_likes·game-uploads)
  * 학습 현황, **학생 응답**(질문별·학생별, 개별 피드백, 일괄 칭찬 `praise_presets`)
* 설계·검증 문서: `docs/admin/`(spec, responses-spec, admin-tools/, create-members/)

## 사이트 디자인 (2026-09-24 개편, 모두 배포됨)
* 참고: "똑똑! 수학탐험대"의 **느낌만**(그림·로고·문구·캐릭터는 가져오지 않음). 설계 `docs/design/redesign/spec.md`(끝 "개정 1"·"개정 2"), 보고 `build-1~5-report.md`, 검증 `review-12.md`·`review-34.md`·`review-5.md`.
* 보라 주색 + 연보라 배경 그라데이션, 메뉴별 파스텔 그라데이션 카드, 본문 Pretendard(OFL, npm 자체 호스팅, 가변 글꼴), **제목 G마켓 산스 Bold + 부드러운 그림자**(5단계, 2026-09-24 사용자 선택 — 주아체 대신, 공식 OTF 원본 `public/fonts/gmarket-sans/`, 관리자 화면은 그림자 없음), 왼쪽 사이드바 유지, 다크모드 유지. 관리자 화면만 보라 채도 약 25% 낮춤(`src/app/admin/admin-theme.css`), 위험 버튼은 진한 빨강(`src/lib/danger-button.ts`).
* 마스코트 "부엉이 과학자"(Canva AI 생성, 사용자 선택): `public/illustrations/mascot/owl-{wave,tablet,think,cheer}.webp`(홈·과학수업·빈 화면·완료), 원본·배경 제거 스크립트 `docs/design/redesign/mascot-src/`. 09-25 사용자 지적("흰 배경이 매끄럽지 못하게 남음")으로 배경 제거를 v2로 다시 함(가장자리 색에서 흰 배경을 걷어 냄 — 흰 테두리·돌기·발밑 그림자 없음, 원본 JPEG에서 4장 모두 `cutout.cjs` 자동 설정). 표시할 때 쓰던 "가장자리 1px 깎기" SVG 필터는 뺐다.
* 사이트 맨 아래 문구: "© {연도} PSC. All rights reserved."(09-25 사용자 요청, `src/components/site-footer.tsx`).
* **사이트 이름 "모두의 배움터"**(09-26 사용자 결정 — 다른 선생님 반도 쓸 수 있게, 전 "우리 반 배움터", 커밋 `3d63f67` 배포 완료·두 사이트 확인): 홈 소개 "친구들과 함께 배우고 탐구하는 공간이에요. 궁금한 메뉴를 눌러 시작해 봐요!"(사용자 문구), "배움터 한눈에 보기", 로그인 안내·메뉴·게시판·사이트 설명의 '우리 반' → '친구들'/'새 소식'(칭찬 문구 '우리 반 학생'은 그대로).
* **반별로 나눠 보이게 하기 — 사용자 승인(09-26), 배포 끝(09-26 `e4d33ce`)**: 사용자가 "전반적으로 추천대로" + 개별 결정(`docs/classes/spec.md` 끝 **개정 1** — 학습 기록은 **담임만**(총괄도 다른 반 기록은 못 봄), 비밀번호 초기화·탈퇴는 총괄도 모든 반, 총괄 = 부엉이반 담임 겸임, 총괄이 담임 지정 → 담임이 학급 개설(이름 직접)·학생 등록, 학급 이름은 관리자 화면에서만, 학생 화면 그대로, 지금 학생은 **"부엉이반"**). Build 에이전트 2명 동시(DB·함수 — **끝**: `build-db-report.md`, 새 SQL `supabase/migrations/20260927000000_classes_schema.sql`·`20260927010000_classes_rls.sql`(정책 18개·함수 8개, 끝에서 학생 기록·계정 정책에 `is_admin()`이 남으면 전체 되돌림), 함수 3개 수정 / 화면 — **끝**: `build-ui-report.md`(가짜 서버 109개 점검 통과, 새 SQL이 없으면 예전 동작 + "학급 기능 준비 전" 한 줄), 스크린숏 `…/scratchpad/classes-ui/shots/`) → 학급별 '선생님 글' Build 작업 중(`build-notices-instructions.md` → `build-notices-report.md`) → 그다음 반별 구분 전체 Review → 별도 Review → 사용자 SQL 실행(① schema ② `docs/classes/setup-owl-class.sql`(이메일은 대화로 — 저장소 공개라 파일에 안 씀) ③ rls) → 함수 3개 재배포 → push. 이전 기록: 계획 단계(09-26 사용자 요청): `docs/classes/spec.md`(Plan 에이전트 sonnet 작성 + Claude 검토·수정 4곳). 추천: `role`(admin/user) 그대로 + `profiles.is_super_admin`·`profiles.class_id`·`classes`·`class_teachers`, 함수 `teaches_student()` 등으로 학생 기록 RLS 약 13곳의 `is_admin()`을 좁힘, 과학 앱 코드 변경 없음, Edge Function 3개 수정. 사용자가 정할 질문 11개(§2.6 — 역할, 게시판·학습게임·과제·잠금 스위치 반별 여부, 아이디 규칙, 교사 계정·반 만들기 권한, 반 이동 기준) → 승인 뒤 단계 1~5(스키마 → RLS 치환+이관 → 함수·화면 → Review → 선택 확장).
* 3D 아이콘 15개: Microsoft Fluent Emoji 3D(MIT) `public/illustrations/3d/`(고지 `LICENSE-fluent-emoji.txt`).

## Supabase (모두 사용자가 실행·배포 완료)
| 마이그레이션 | 내용 |
|---|---|
| 20260921000000_init_blog.sql | 블로그 기본 스키마 |
| 20260921010000_fixes.sql | 1차 리뷰 수정 |
| 20260921020000_admin_learning.sql | 관리자·학습 스키마(`member_directory` 트리거 동기화) |
| 20260922000000_admin_learning_fixes.sql | 리뷰 수정(비밀번호 변경 트리거 등) |
| 20260922010000_app_progress.sql | 진행 상황 저장 |
| 20260922020000_praise_presets.sql | 칭찬 문구 |
| 20260922040000_community.sql | 자유게시판·게임·신고·Storage |
| 20260922050000_game_upload_policy_fix.sql | 게임 업로드 정책 재귀 오류(42P17) 수정 |
| 20260923000000_member_withdrawal.sql | 완전 탈퇴(기록 보존: profiles↛auth.users FK 제거, app_progress→profiles) |
| 20260923010000_login_required.sql | 로그인 잠금 스위치 |
| 20260923020000_login_required_scope.sql | 잠금 범위 조정(게이트 6개 확인) |
| 20260923030000_member_create_log.sql | 계정 생성 감사 로그 |
| 20260926000000_board_login_only.sql | 자유게시판 로그인 전용(글·댓글·좋아요 SELECT 정책 — 비로그인은 `kind='board'` 못 읽음, 학습게임은 스위치대로) — 사용자 실행(09-26), Claude가 anon 읽기로 확인: 게시판 글·댓글·좋아요 0, 학습게임 글 2(스위치 꺼짐) |
| 20260927000000_classes_schema.sql | ① 학급 표 `classes`·`class_teachers`, 열 `profiles.class_id`·`is_super_admin`(공개 열 아님)·`member_directory.class_id`, 판별 함수·RPC — 사용자 실행(09-26) |
| (docs/classes/setup-owl-class.sql) | ② 부엉이반 + 총괄 지정(이메일은 대화로, 저장소엔 자리표시만 — 처음 한 번만 실행) — 사용자 실행(09-26) |
| 20260927010000_classes_rls.sql | ③ 학생 기록 = 그 반 담임만(총괄 예외 없음), 명단·감사 로그 = 담임(자기 반)·총괄, 잠금 스위치·담임 지정 = 총괄만 — 사용자 실행(09-26). **이 뒤로 옛 마이그레이션 재실행 금지**, 비상 되돌리기 `docs/classes/rollback-classes-rls.sql` |
| 20260927020000_class_notices.sql | ④ 학급별 선생님 글(비로그인 = 총괄 글, 잠금 켜지면 없음) — 첫 실행 42883 오류(`any ((select …))`) → 고쳐 다시 실행 성공(09-26). Claude anon 확인: `class_notices` 읽기 200·`classes`·`class_teachers` 거부 |
| 20260927030000_content_owner_only.sql | ⑤ 과제·블로그 글 고치기·지우기 = 쓴 교사 또는 총괄, 만든 사람 열은 아무도 못 바꿈(열 권한) — 사용자 실행(09-26) |
| 20260927040000_class_assignments.sql | ⑥ 과제 학급별(`assignments.class_id` not null, 학생 = 자기 반 공개 과제만·비로그인 없음, 만들기 = 자기 학급에만) — 사용자 실행(09-26), 화면 `f2cbc8c` |

| Edge Function | 용도 | 비고 |
|---|---|---|
| admin-reset-password | 비밀번호 초기화 | Verify JWT, 관리자 검증 |
| admin-delete-member | 완전 탈퇴 | 관리자 검증, 기록을 지울 FK가 있으면 삭제 전 거부 |
| admin-create-member | 회원 추가(단건·일괄) | 관리자 검증, 비밀번호 기록 안 함 | 09-26 학급 규칙(학생 = 호출자가 담임인 학급, 교사 계정 = 총괄만)으로 재배포
| check-answer | 학생 답 판정(Gemini) | 로그인 학생 + **체험 모드(공개 키, 09-26 사용자 결정 — 허용 출처만·IP별 1분 60/1시간 600·전체 1분 300, Secret `CHECK_ANSWER_ALLOW_ANON=off`로 끔)**, 비밀 `GEMINI_API_KEY`(무료 등급), 모델 `gemini-flash-lite-latest`(`GEMINI_MODEL`로 변경 가능). 09-26: Gemini 대기 7초(앱 쪽 9초), 생각 끔(`thinkingBudget:0` → 400이면 `thinkingLevel:"low"` → 설정 없음), 로그에 켜질 때 모델 정보·판정마다 `ms`·`modelVersion`·`thinking` |
* 함수 코드를 고치면 사용자가 `npx supabase functions deploy <이름>`으로 다시 배포해야 한다(`--no-verify-jwt` 금지). 4개 모두 09-26 최신 코드로 배포됨(사용자).
* 함수 4개의 허용 출처(CORS): GitHub Pages·`http://localhost:3000` + Secret `EXTRA_ALLOWED_ORIGINS`(쉼표 구분 https 출처 — 지금 `https://class1-chi.vercel.app`). 새 도메인을 더하면 Secret만 바꾸고 함수 4개 다시 배포.
* **Supabase CLI**: 사용자 Mac에서 `npx supabase login` 해 둠(09-26, 토큰은 macOS 키체인 — 키체인 창의 암호는 Mac 로그인 암호), 프로젝트 연결됨(`supabase/.temp/`는 커밋하지 않음). 배포 때 "Access token not provided"가 나오면 사용자가 `npx supabase login`을 다시(브라우저 승인 → 확인 코드). Docker 경고는 무시해도 됨. Claude는 토큰·암호·확인 코드를 묻거나 입력하지 않는다.

## 사용자가 실제로 확인한 것
* (09-23) Gemini 피드백 품질·말투 괜찮음, 로그인 잠금 스위치 동작, 회원 추가(단건·엑셀)·감사 로그, 완전 탈퇴(계정 삭제 뒤 학습 기록 유지), 무의미 입력 차단
* (09-24) 디자인 개편 1단계 방향 승인("더 화려하게" 반영 후 "좋아")
* (09-24) 제목 글꼴·그림자 비교 이미지(글꼴 4개 × 그림자 3안)를 보고 G마켓 산스 Bold + A 부드러운 그림자 선택, 글꼴 파일 받기 허락
* (09-26) Vercel 사이트(`https://class1-chi.vercel.app/class1/`): 메인 화면, 관리자 대시보드(학생 결과), Google 로그인 정상
* (09-26) 자유게시판 로그인 전용: 학생 계정으로 로그인하면 게시판이 잘 보임(비로그인 차단은 Claude가 anon 읽기로 확인 — 게시판 글·댓글·좋아요 0)
* (09-26) 학생 답 되짚기: 학생 계정으로 GitHub Pages·Vercel 모두 "집에 가고 싶다"에 막기 카드("아직 이 질문과 어울리는 답이 아니에요")(시간 초과 수정·함수 재배포 뒤)
* (09-26) **반별 구분 배포 뒤 실제 계정**: 관리자 대시보드의 학급 구분(총괄·부엉이반), 선생님 글이 로그아웃 상태 홈에 보이는 것까지 확인

## 확인하지 못한 것 (사용자 확인 필요)
* 실제 iPad/태블릿: 3D 드래그, 시간 바·각도 슬라이더, 세로 화면 스크롤, 게임 무한 루프 시 정지 버튼, 버저 소리 크기(3단원 탐구 2)
* 실제 학생 소요 시간(7분 기준) — 지금 시간표는 글자 수 추정
* 새 크게 보기(한 화면 실험실)를 실제 iPad Safari로: `position: fixed` + 화면 키보드(조작 칸 안 입력칸), `html:has()` 스크롤 잠금, 회전, 손가락 끌기(태양·손전등·지구본)
* 개편된 디자인을 실제 로그인 상태·실제 태블릿으로 본 것(모두 가짜 세션·headless 브라우저로 확인). 새 제목 글꼴(G마켓 산스, 약 0.9MB 한 번 받음)이 학교 망·iPad Safari에서 뜨는 속도와 모양도 포함

## 남은 작업·메모

### ▶ 이어서 할 일 (2026-09-26 밤, 세션 압축 전 정리 — 여기부터 본다)
**사용자 사전 허락(09-26 "검토 끝나면 바로 배포해줘")**: 지금 진행 중인 세 가지 — ③ 확대 3D 이동 화살표, 선생님 글 링크(`be83554`, 별도 Review `review-links.md`)·쪽 나누기, ⑤ `sci-6-2-1-3` 옆에서 본 모습 — 는 각각 별도 Review에서 막히는 문제가 없거나 고친 뒤 **확인 없이 커밋·push**(push 뒤 Actions·두 사이트 확인). 새 단계에는 적용하지 않는다.
**작업 트리 주의**: 커밋 안 된 변경에 서로 다른 작업 3개가 섞여 있다 — 커밋할 때 `git add`는 경로를 골라서(① 확대 화살표: `scripts/templates/science-sim/{sim3d.js,style-common.css,README.md}` + 20개 사본 `public/apps/*/science-sim/` ② 반별 구분: `supabase/migrations/2026092700*`, `supabase/functions/admin-*`, `src/`(관리자·lib·hooks·types), `docs/classes/*` ③ 학급별 선생님 글: `supabase/migrations/20260927020000_class_notices.sql`(예정), `src/components/dashboard/teacher-posts.tsx`·`stat-tile.tsx`, 관리자 '선생님 글' 화면(예정)). `supabase/.temp/`는 커밋하지 않는다.
1. **[끝 09-26] 커밋 `7802fb2` push** — 검색(실험 앱·자유게시판(로그인)·학습게임, 태그 = 종류, 설명 "필요한 글의 제목, 요약, 태그로 찾아요") + 홈 '최근 과학 수업'·'선생님 글' 좌우 반씩. 사용자 요청으로 배포(문서 커밋 `9993566`·`20f2313` 함께), Actions 성공, 두 사이트 새 코드 확인.
2. **[확정 09-26]** 다른 반 학생(로그인)은 총괄 선생님 글을 보지 않는다(자기 담임 글만) — "총괄 글"은 비로그인에게만. 비로그인 목록 아래 안내 한 줄("로그인하면 우리 반 선생님 글을 볼 수 있어요")은 **넣지 않는다**(사용자). 두 가지 모두 선생님 글 Build 에이전트에게 전달함.
3. **[끝 — 배포 09-26 `78cbd65`, 후속은 사용자가 취소] 확대한 3D 장면 이동 화살표**(spec `docs/science/sim-redesign/spec.md` 개정 8): Build **끝**(`pan-report.md` — 20개 앱 × 6크기 × 두 모드 240/240, 겹침 0, 좌우 뒤집힘 없음, 배치 회귀 0, 알려진 한계: `sci-6-2-2-3` '실험해요 2'는 처음 시점이 가까워 화살표가 처음부터 보임 · 캔버스 안 3D 이름표는 피하지 못함 · 휴대폰 가로 크게 보기는 좁음). 사용자 피드백(09-26): 모양(원 + 꺾쇠) 그대로 · 원 바탕을 훨씬 옅게(흰 20%) · 확대 뒤 아래 안내 "👆 화살표: 화면 옮기기 · 두 번 탭: 처음 방향"(처음 방향이면 원래 안내). Review(`review-pan.md` 배포해도 됨, 높음 0·중간 1·낮음 9) → **배포 끝(09-26 `78cbd65`, Actions 성공·두 사이트 확인 — 쪽 나누기 커밋보다 먼저 올리려고 커밋 순서를 바꿈)**. **후속**: M1(확대 직후 화살표가 끌기·고르기 3D 물체를 덮음 — 예 6-2-1-5 지구본) + L1(빠른 연타)·L2(느린 기기 바닥 한도)·L9(README 절 번호) → Build 수정 끝(`pan-report.md` 9절 — 물체를 덮는 앱 7/20 → 0/20, 연타 = 누른 횟수만큼, 느린 기기도 바닥까지·▼ 흐림, 커밋 전) → 같은 Review 담당 재확인(`review-pan.md` 끝 "재확인" — M1·L1·L2·L9 모두 고쳐짐, 그러나 **새 문제 N1(높음)**: 화살표로 옮긴 뒤 2.5초 쉬면 물체를 다시 재며 화살표가 4~252px 옮겨 가 보던 자리를 누르면 빗나감 31/200·물체 눌림 → 배포 보류) → **사용자 결정(09-26): "화살표가 투명도가 더 높도록 설정했기 때문에 3d 물체를 좀 가려도 괜찮은거 같아. 화살표 위치 조정은 취소하고 기존의 것으로 복귀해줘."** → 후속 작업 멈추고 공통 틀·20개 사본을 배포된 판(`78cbd65`)으로 되돌림(diff 0) — 끝. 배포된 판에 남는 낮은 점은 `review-pan.md` 끝 "사용자 결정" 절.
4. **[배포 끝 09-26 `e4d33ce`] 반별 구분 + 학급별 선생님 글**(`docs/classes/spec.md` 개정 1·2·2 보충):
   * Build 끝(커밋 안 됨): DB·함수 `docs/classes/build-db-report.md`(SQL ① `supabase/migrations/20260927000000_classes_schema.sql` ③ `20260927010000_classes_rls.sql`, 함수 3개 수정), 화면 `docs/classes/build-ui-report.md`(가짜 서버 109개 점검 통과, 스크린숏 `…/scratchpad/classes-ui/shots/`).
   * Build 끝(커밋 안 됨): 학급별 선생님 글 `docs/classes/build-notices-report.md`(가짜 서버 117개 점검 통과, SQL ④ `20260927020000_class_notices.sql`). 담당이 정한 것: **로그인 잠금 스위치가 켜지면 비로그인에게 총괄 글도 안 보임**(블로그·게시판·게임과 같은 잠금 규칙 — **사용자 확정 09-26**: "담당이 정한대로 해줘"), 사용자 메뉴의 '글 관리'·'새 글 작성'도 '선생님 글'로, 읽기 정책은 비로그인용·로그인용 2개(로그인한 다른 반 학생·학급 없는 회원은 총괄 글 안 봄), 비로그인 열 권한은 `id,title,body,created_at,updated_at`만(정책 안 `author_id`는 열 권한 검사 대상 아님에 기댐 — Review 확인).
   * **Review 끝**(`docs/classes/review.md` — 배포해도 됨, 높음 0·중간 1·낮음 11) → Claude가 낮음 9건 고침(`docs/classes/fix-1-report.md`: 쓰던 글 보호·44px 버튼·문구·잠금 중 방문자 개수 요청 0·단계 번호 ①~⑥ 통일·setup 한 번만·옛 마이그레이션 재실행 금지) → 다시 시험 선생님 글 117/117·관리자 109/109 → **커밋 `e7487ce`(push 안 함)**. 비상용 ③ 되돌리기 `docs/classes/rollback-classes-rls.sql`(옛 정의와 글자 대조 38/38, `build-rollback-report.md`) 준비됨.
   * **[끝 09-26]** 사용자가 SQL ①→②→③→④ 실행(④는 첫 실행 42883 오류 → Claude가 고쳐 `e4d33ce` → 다시 실행 성공), 함수 3개 재배포, Claude 확인(anon: `class_notices` 200·학급 표 거부 / 함수 3개: 사전 요청 200·토큰 없음·공개 키 401) → push·Actions 성공·두 사이트 새 코드. **남은 것**: 사용자 실제 계정 확인(총괄 화면·부엉이반 학생 수·선생님 글 쓰기 → 비로그인·학생 홈에 보이는지·학생 화면 그대로), **Review M1 — 사용자 결정(09-26): "과제·글 수정은 쓴 선생님과 총괄만 하게 해줘"** → Build 끝(`build-owner-only-report.md` — 정책 6개(만들기 = 교사 본인 이름, 고치기·지우기 = 쓴 교사 또는 총괄) + 고칠 수 있는 열을 화면이 보내는 열로 좁힘(만든 사람 열은 아무도 못 바꿈), 관리자 과제·제출 현황·블로그 관리·편집기에서 남의 것은 버튼 꺼짐 + "쓴 선생님과 총괄만 고칠 수 있어요.", 시험 98/98) → 커밋(순서 바꿔 `ef23318`, push 안 함) → 별도 Review(`review-owner-only.md` — **실행 부탁해도 됨**, 23개 문장 실행 오류 없음·화면 179/179, 낮음 L1 머리말 확인 쿼리의 update·delete 줄을 따로 실행하면 실제로 바뀜 → `current_user = 'authenticated'` 조건 붙여 고침 `40390fd`, L2·L4 블로그 편집기 작은 점·L3 `/post/` 상세 삭제 버튼은 범위 밖) → ⑥과 함께 사용자 실행 안내 예정 → 사용자 SQL 실행 → push(새 단계라 push 전 사용자 확인). **사용자 결정(09-26): "1번으로 과제도 학급별로 나눠줘"**(spec 개정 3-2) → Build 끝(`build-class-assignments-report.md` — 시험 127/127, 지금 과제는 만든 선생님의 보관 안 된 학급·없으면 부엉이반, 외래키 restrict, 읽기 정책 학생·교사 둘, 만들기 = 자기 학급에만, 제출은 과제 읽기 정책을 따라 자동으로 좁아짐, ⑥ 실행 뒤 예전 화면에서는 새 과제 저장 실패 → 곧바로 push) → **커밋 `0d9d8e1`(push 안 함)** → 별도 Review(`review-class-assignments.md` — **실행 부탁해도 됨**, 문장 17개 오류 없음·점검 헛실패 없음, 낮음 L1 머리말 오류 설명·L4 ⑤ 되돌리기 순서 주석 고침 `f2cbc8c`, L2 지우기 창 개수에 ⑥ 전 다른 반 제출물 빠짐·L3 교차 제출물 과제 이름 표시는 지금 학생이 모두 부엉이반이라 해당 적음) → **사용자가 SQL ⑤·⑥ 실행(09-26)** → Claude anon 확인(`assignments.class_id` 열 있음 = ⑥ 적용, ⑥ 가드가 ⑤를 확인하므로 ⑤도 적용, 비로그인 과제 0) → **배포 끝(09-26 `f2cbc8c`, Actions 성공·두 사이트 확인)** → 사용자 SQL ⑤ → ⑥ 실행 → push(새 단계 — push 전 사용자 확인). 지금 공개 과제 0개(anon 확인).
   * **사용자 실행 순서**(단계마다 확인 쿼리): ① classes_schema.sql → ② `docs/classes/setup-owl-class.sql`의 `'총괄_이메일'`을 **사용자 로그인 이메일**(대화 정보에 있음 — 저장소가 공개라 파일에 쓰지 않음)로 바꿔 실행 → ③ classes_rls.sql → ④ class_notices.sql → 함수 3개 재배포(`npx supabase functions deploy admin-create-member` / `admin-reset-password` / `admin-delete-member`) → 화면 push. 거꾸로 하면 새 학생이 학급 없이 만들어지거나 관리자 화면이 막힌다.
   * 실제 계정으로 사용자 확인(build-ui-report "To verify"): 총괄 표시·부엉이반 학생 수, 담임 지정 → 그 교사로 학급 개설·학생 등록, 담임에게 다른 반 학생이 안 보임, 총괄이 다른 반 학생 기록은 못 보고 초기화·탈퇴는 됨, 담임 해제 거부 사유, 선생님 글(학생 = 자기 담임 글, 비로그인 = 총괄 글).
   * 결정 확인 필요(보고서): 담임 해제 = "학생 있는 학급을 혼자 맡으면 거부, 빈 학급은 보관 후 해제"(지침 1-4 글자 그대로는 "학급이 남으면 거부" — 목적(1-2)에 맞춰 구현). 블로그 글·과제를 교사 누구나 고칠 수 있는 문제(개정 1-5)는 나중에 사용자에게.
   * **[진행 중] 홈 '선생님 글' 후속(09-26 사용자 요청)**: ① 본문 주소 링크(`src/components/linkified-text.tsx` — http·https만, 새 창, rel noopener, 끝 문장부호·짝 없는 괄호·붙여 쓴 한글 제외, 관리자 목록에도, 글쓰기 안내 문구) — Claude 구현·시험(조각 12/12, 화면 10/10) → 별도 Review(`docs/classes/review-links.md` 배포해도 됨, 낮음 L1 복사 글자·L2 접힌 본문 링크 초점·L4 안내 문구 고침 `8713e32`) → **배포 끝(09-26 `8713e32`, Actions 성공·두 사이트 확인)**. ② 제목 알록달록 색 — **사용자가 취소**. ③ 좌우일 때 '최근 과학 수업' 높이를 넘으면 쪽 나누기 — Build 끝(`build-notice-pages-report.md` — 시험 600/600, 최신 100개를 한 번에 불러와 화면에서 나눔, 휴대폰 4쪽 이상은 "2 / 6") → 별도 Review(`review-notice-pages.md` 배포해도 됨, 높음 0·중간 1·낮음 3 — M1 펼친 글이 쪽을 넘기면 접히며 화면이 튐 → Claude가 쪽 넘기기 위치를 기억해 되돌리게 고침, 재현 시험 20/20·조작 36/36) → **배포 끝(09-26 `9a9909d`·`a46f7c3`, Actions 성공·두 사이트 확인)**. 남은 낮음: 90~100자 제목 3개가 연달아 있으면 한 쪽 최소 3개 규칙 때문에 오른쪽이 42px 김, 휴대폰 쪽 넘기기 모양이 쪽 수에 따라 달라짐, 윈도 고대비에서 지금 쪽 구별 안 됨(사이트 공통).
5. **[배포 끝 09-26 `845b4b7`] `sci-6-2-1-3` 위선 삭제 + 태양 고도 그림 바로잡기**(사용자 요청 09-26, 앱 `spec.md` 개정 4 — "천구의 가로로 그어져 있는 위선은 필요없으니 삭제", "태양 광선이 막대 끝을 지나 지표면의 그림자 끝과 이루는 각도로 측정하는 것으로 그림을 수정"): Build(`public/apps/sci-6-2-1-3/fix-2-instructions.md` → `fix-2-report.md`, 같은 단원 탐구 2의 빛줄기·호 표현) → 별도 Review → 커밋(그 앱 `app.js`·`style.css`·`spec.md`·fix-2 문서 — `science-sim/`은 확대 화살표 작업 것) → push는 사용자 확인. 2D의 가로 격자선도 없애고 왼쪽 눈금 숫자는 남김(Claude 판단 — 사용자에게 알림).
   * **Build 끝**(`public/apps/sci-6-2-1-3/fix-2-report.md`, 커밋 안 됨): 3D·2D 위선 삭제(2D는 눈금 숫자만), 남중 그림 = 막대 끝을 지나 그림자 끝에 닿는 주황 빛줄기 + 옅은 평행 빛줄기 4 + 그림자 끝 빨간 호(12달 호 각 = 고도, 오차 ≈ 0), 측정기 막대 1.0→3.0·받침판 2.5×6.56(12월 그림자까지 판 위)·정남쪽으로 2.362 옮김(빛줄기가 태양 가까이 지나게), 태양 원반·빛무리 조금 키움, 기록 12개는 고치기 전과 글자까지 같음. 사용자 결정(09-26): **2번 — "옆에서 본 모습" 칸 추가 + 옅은 평행 빛줄기 삭제** → Build 보충 끝(옆 그림은 3D와 같은 값 `noonSun(month)`, 왼쪽 = 남·오른쪽 = 북, 값 칸 아래·크게 보기는 조작 칸 달 버튼 아래, 12달 호 각 = 고도 ±0.013°, 기록 그대로) → 별도 Review(`review-2.md` 배포해도 됨, 높음 0·중간 0·낮음 4 — L1 겨울 빛줄기가 태양 원반 조금 밖에서 시작·L2 기본 화면에서 옆 그림이 기록하기 아래·L3 휴대폰에서 크게 보기를 직접 켜면 자동 스크롤로 달 버튼이 밀림·L4 재생 중 새로고침 안내 문구) → **배포 끝(09-26 `845b4b7`, Actions 성공·두 사이트 확인)**.
   * **[진행 중] 재수정 fix-3(09-26 사용자 요청, 앱 `spec.md` 개정 5)**: ① 궤적 보기 확인 — 기본 "이번 달만"(`pathMode` 기본 `current`), 계절 대표 3개는 골랐을 때만(기억됨) — 바꿀 것 없음(Claude 확인) ② 재생 중에는 태양만 움직이고 빛줄기·호·옆 그림·값은 끝에 남중 자리에서 **한 번만**(해 진 뒤 거꾸로 움직이지 않게) ③ 빛줄기가 12달 모두 태양 원반을 지나게(싱크) ④ 광선 느낌 ⑤ 기록하기 = 기록만(다음 달 자동 재생 없앰, 안내 문구 고침) → Build 끝(`fix-3-report.md` — 재생 중 0번·끝에 1번, 측정기 막대 0.75·태양 원반 0.9로 빛줄기가 12달 모두 원반 안(중심 거리 ≤ 반지름의 44%), 빛나는 심·번짐·화살촉·뻗어 나감, 기록하기 = 기록만·다음 달 버튼 깜박임, 재생 약 2.2초) → 별도 Review(`review-3.md` 배포해도 됨 — 높음·중간 0, 12달 정확성·싱크·재생 중 0번/끝 1번·기록 그대로, 낮음 L1 연출 다시 돎·L2 태양 두 색 → Claude가 고침(태양 픽셀 한 색 확인), L3 휴대폰 가로는 그대로. 알림: 3D 측정기가 1/4 크기라 여름 3D 호는 작음 — 각은 옆 그림 칸) → 사용자 사전 허락대로 커밋·push.
6. **[나중] 실험 앱 개편 단계 D**(나머지 12개 + 조사 1개) — 시작 전 사용자 확인(아래 "진행 중: 실험 앱 개편"의 D 항목), 그다음 E(마무리: 두 정본 어긋남 — `stage-nav.js`·`lesson.js` — 맞추기 포함).

* **진행 중: 실험 앱 개편**(2026-09-25~) — 세션이 압축·새로 시작돼도 이 항목만 보면 이어서 할 수 있다.
  * 문서: `docs/science/sim-redesign/` — `spec.md`(계획, **끝의 개정 1~6이 본문보다 우선**), 단계별 지침 `build-*`·`fix-*`·`review-*-instructions.md`·`dock-instructions.md`, 보고 `*-report.md`, 검토 `review-*.md`, 첫 점검 `audit.md`.
  * **끝난 것(커밋·배포됨)**:
    * 단계 A 공통 틀 `f93f716`: "⛶ 전체 화면 보기" 토글·방향별 기억·태블릿 세로 기본 켬, 머리말 접기(23개), 알림은 머리말 위, 단계 메뉴 칸 수=단계 수, 도우미 `SciSim.Experiment.enlarge()`·`v.draggable()`(`opts.pad`)·기록 버튼 훅(`exp.recordButton`·`exp.revealRecord()`), 답 알려 주기 문구 5곳, '더 탐구하고 싶은 점'의 "비워도 돼요" 문구를 화면에서 뺌(`lesson.js`, 표시만).
    * 단계 B 2학기 1단원 `9d2dc16`: 2-1-2 나무·작은 사람·백엽상, 태양 끌기 ↔ 시간 바, 평행 빛줄기·작은 호, 값 패널, "옆에서 본 모습" 칸(사용자 결정 — 좌우는 3D 처음 시점 기준) / 2-1-3 궤적 "이번 달만·계절 대표 3개", 남쪽 하늘(동쪽 왼쪽) / 2-1-4 새 예상 질문(id `q1b`)·손전등 끌기 / 2-1-5 확대 화면 좌우 유지·확대 칸·지구본 끌기.
    * 단계 C 1학기 1단원 6개 간략화 `9c13199`(위 표). 사용자 결정(09-26): 관찰 칸 그대로(하나하나 측정), 6-1-1-1 거품 5초는 형식상 빨리 감기, 6-1-1-5 정리 틀 2줄, 6-1-1-6 출처 고르기. 6개 모두 여전히 7분 넘음(사용자에게 알림 — 그대로).
    * Vercel 준비 `3ad7de2`, 되짚기 시간 초과 수정 `3a45060`(push됨)·`ce3c80b`(함수 코드 — 사용자가 함수는 배포함, **저장소 push는 아직** → 크게 보기 개편과 함께 push).
  * **크게 보기 개편 — 끝·배포(09-26)**(사용자 요청, spec 개정 6): Build(`dock-report.md`) → Review(`review-dock.md`: 배포해도 됨, 높음 0·중간 2·낮음 6) → Claude가 M1(첫 화면에 고르는 버튼이 안 보임 — '알아 두기' 크게 보기에서 제목 줄만 + 조작 칸 여백 촘촘히)·L5 고침·확인(`dock-fix-report.md`) → 커밋·push(사용자 결정: 검토 끝나면 바로)·두 사이트 배포 확인. `ce3c80b`(check-answer 로그)도 함께 push됨.
    * **09-26 후속(사용자 결정, 배포 완료)**: ① M2(세로 장면이 작음) → **크게 보기 머리말 자동 접기**(가로·세로 모두, 개정 7) ② **가로도 크게 보기가 기본**(태블릿·PC, 휴대폰은 끔) ③ R3 → **조사 앱 3개 `persist.js`를 science-sim판과 같게**(가짜 충돌 거르기 — 고치기 전 3개 앱 모두 가짜 충돌 창이 떴고, 고친 뒤 안 뜸·진짜 충돌은 여전히 물음, Claude가 가짜 로그인으로 확인). ④ '실험 고르기'를 장면 안 막대로 옮기는 안은 지시 뒤 **사용자가 취소**("전체화면 개선은 직전 개편된 것으로") — 구현 담당을 파일 변경 전에 멈춤, 배치는 개정 6 그대로. ⑤ 홈 '최근 자유게시판' 미리보기 로그인 전용. ⑥ `sci-6-2-3-3` 나침반 문구(위 표). 커밋 `ab5b50f`(①~⑤)·`08f2fa8`(⑥) — **배포 완료(09-26, 사용자 요청, Actions 성공, 두 사이트 새 코드 확인)**. ⑦ **확대한 3D 장면 이동 화살표**(사용자 요청 09-26, spec 개정 8: 확대하면 장면 가장자리 가운데에 반투명 ▲▼◀▶, 누르면 한 칸·누르고 있으면 계속, 좌우 방향 유지, 한도, 더블탭·처음 방향으로 초기화) — Build 중(`pan-instructions.md` → `pan-report.md`) → Review(별도 에이전트) → 커밋. 확인: 20개 실험 앱 × 세로·가로 머리말·기본값 시험 실패 0, 기본 화면(끔) 요소 위치 `3ae7dc4`와 같음, 조사 앱 머리말 동작 그대로, 휴대폰 기본 끔.
    * **자유게시판 로그인 전용(09-26 사용자 요청)**: 화면(`LoginGate` `/board/`, 홈 글 수 칸) 커밋 `5b1831a`·**배포 완료(09-26, 두 사이트 확인)**, SQL `20260926000000_board_login_only.sql` **사용자 실행 완료(09-26)** — Claude가 anon 읽기로 확인(게시판 글·댓글·좋아요 0, 학습게임 2). **사용자 확인(09-26): 학생 계정으로 로그인하면 게시판이 잘 보임** — 끝.
    * 남은 낮음(단계 D에서): L1 1024×768 조작 칸의 '기록한 칸 한눈에 보기' 표 끝 열(6-1-1-1·-2·-4, +36~46px, 옆으로 밀면 보임), L2 키보드가 창을 줄이는 옛 안드로이드 브라우저에서 세로 크게 보기가 입력 중 꺼졌다 켜짐(최신 Chrome·iPad는 해당 없음 추정), L3 2D 대체 화면 + 크게 보기에서 16개 앱은 2D 그림이 장면 칸 안에서 스크롤(2-1-2~5는 맞춤), L4 휴대폰에서 크게 보기를 켜면 장면이 좁음(휴대폰은 기본 끔), R1 6-2-2-3 3D 위쪽 이름표 잘림, R2 6-2-2-3 휴대폰 기본 화면에서 빨리 감기 배지 ↔ 토글 겹침.
  * **다음: 단계 D** — 나머지 실험 앱 12개(1학기 2단원 `sci-6-1-2-1~5`, 2학기 2단원 `sci-6-2-2-1~4`, 2학기 3단원 `sci-6-2-3-1~3`)와 조사 `sci-6-1-2-6`(공통 틀만). **시작 전에 사용자에게 범위·배포 방식(검토 뒤 바로 push할지) 확인.** 지침 `build-D-instructions.md`는 아직 없음(Claude가 씀 — 단원별 3묶음, 앱마다 Build, 묶음마다 별도 Review). 넣을 것:
    * `audit.md`의 앱별 문제(실행 뒤 화면까지 열어 보기), 두 모드(기본·새 크게 보기) 점검
    * 직접 조작(spec §3.3 — 장면 물체를 눌러 고르기 등, 조작 단계를 늘리지 않음), 시점을 옮겨도 좌우 유지, 바뀌는 값의 3D 이름표 → 값 패널
    * 관찰 카드의 '본 모습' 글이 정답 보기와 같은 말(review-A L8: `sci-6-2-3-1`·`sci-6-2-3-2`·`sci-6-2-2-1`·`sci-6-2-2-2`) → 6-2-2-3처럼 학생이 고른 뒤에만 확인
    * `sci-6-2-2-3` 빨리 감기 배지, `sci-6-1-2-5` 이름표↔토글 겹침(휴대폰), 휴대폰 가로 겹침
    * `sci-6-2-3-2`의 따로 떨어진 '궁금한 점' 단계를 다른 새 기준 앱처럼 정리하기 안으로 옮길지 — **사용자에게 먼저 묻기**(옮기면 저장 키 올림·응답 매핑 새 변형)
    * 1학기 2단원 탐구 1·2(수업 미사용)는 문항 그대로, 화면(공통 틀)만
  * **마지막: 단계 E**(마무리) — 정본↔23개 사본 `diff -r`, STATUS·CLAUDE 정리, 실제 태블릿 확인 부탁. 두 정본 사이 어긋남도 맞춘다: `stage-nav.js`는 조사판에만 "잠긴 단계에는 완료(✓) 안 보이기" 수정이 있고(실험판에 옮길지 확인), `lesson.js`는 실험판에만 `--ss-footer-h`가 있다(조사판에 필요한지 확인).
  * 사용자 결정 모음: 순서 A→B→C→D→E, 단계마다 별도 Review 에이전트. 기본 실험 화면은 예전 구조, 크게 보기 = 한 화면 실험실(개정 6). **B·C·크게 보기 개편은 사용자가 "검토 끝나면 바로 커밋·푸시"를 허락**(단계마다 따로 말함 — D는 시작할 때 다시 확인).
* **체험 모드도 Gemini 답 판정(09-26 사용자 요청)**: `check-answer`가 공개 키 호출을 받음(허용 출처·IP별 한도·끄기 Secret), 앱은 `answer-check.js`(두 정본)·`class1-record.js`(`callFunction`의 `allowAnon`)로 체험 모드에서도 부름 — 23개 사본 동기화, 가짜 응답으로 확인(비로그인=anon·로그인=authenticated, 식별 정보 없음, 차단 카드). 사용자가 `check-answer` 재배포(09-26) → Claude가 확인(체험 모드 요청 받음·다른 출처 403·토큰 없음 401, Gemini 호출 없이) → 커밋 `4ac4c1e` push·두 사이트 새 코드 확인 — **배포 완료**.
* **학생 답 되짚기(Gemini) 시간 초과 — 해결(09-26)**: 증상은 학생 계정으로 "집에 가고 싶다"가 막히지 않고 되짚기 기본 문구만 뜸, 함수 로그 `check-answer: Gemini 호출 실패 timeout`. 고침 ① `3a45060`: 서버 Gemini 대기 3→7초, 앱 대기 3.5→9초, 대답을 못 받으면 그 질문을 "되짚음"으로 기록하지 않음(전에는 한 번 늦으면 그 기기에서 그 질문 검사가 꺼짐) ② `ce3c80b`: 생각 끄기 + 폴백, 로그에 모델 정보·`ms`. 사용자가 함수 재배포 → 두 사이트 모두 막기 카드 확인. **다시 느려지면** 함수 로그의 `ms`·모델 정보를 보고 Secret `GEMINI_MODEL`만 바꾼다(코드 수정 불필요).
* 참고(09-24 해결): 게임 업로드 안내 문구의 "index.html"을 실제 규칙("이름은 무엇이든, `.html`/`.htm`으로 끝나면 됨")에 맞게 고침(`game-upload-field.tsx`, `community-post-form.tsx`, `games/page.tsx`, `lib/community.ts` 오류 문구).
* 참고(09-24 해결): 낮은 지적 3개 — `sci-6-2-1-4` 기록하기 버튼 가림(태블릿 가로, 카드 여백 조정), 움직임 줄이기에서 멈춰 보이던 스피너(2.4초로 천천히 돎), 앱 선택 표시에 남은 예전 파랑 7곳(보라로) — 모두 고침(`docs/science/small-fix-2-report.md`).
* 참고(해결됨): 학생 계정 202603·202622가 "로그인 기록 없음"으로 보였던 것은 DB 오류가 아니라 **그 계정으로 로그인한 적이 없음**(세션·토큰·활동 0) — 태블릿이 다른 계정이었거나 체험 모드였을 가능성. 필요하면 태블릿에서 계정 확인.
* `src/data/apps.ts`의 `webApps`는 비어 있음(과학 앱 목록 원천은 `science-curriculum.ts`, `appTitle()`이 그쪽도 찾음).
* 서브에이전트가 테스트 중 실제 Supabase로 요청을 보냈을 가능성이 있던 적이 한 번 있음(09-24, 가짜 토큰이라 쓰기는 거부됨). 이후 규칙: 테스트 시 실제 Supabase 주소를 첫 로드부터 막는다(CLAUDE.md).
