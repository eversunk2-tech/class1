# Fix-1 보고: 학생 응답 · 일괄 칭찬 review 1차 수정

지침: `docs/admin/responses-fix-1-instructions.md` · 대상 review: `docs/admin/responses-review.md` · 기준: `docs/admin/responses-spec.md`(§12 우선).
커밋·푸시 안 함. 실제 DB에는 아무것도 쓰지 않음. 앱 폴더·공통 틀·기존 마이그레이션은 건드리지 않음. **새 SQL 없음.**

## 1. 항목별 처리

| # | 처리 | 위치(파일:줄) |
|---|---|---|
| **H1** 질문별 보기에서 버전이 다른 답이 섞임 | 질문 정체성 = (앱 버전 + 질문 key + 질문 문구). 매핑 변형 id가 버전이고, `detail.qa` 결과는 `schema.qaVersion`(qa만 더한 같은 앱 버전: 2-3·2-4 `v4`, 2-5 `v3`)으로 본다. 가장 최근 결과의 버전을 "지금 버전"으로 두고, 나머지는 `이전 버전 질문 · {버전 이름}` 묶음으로 따로 보여 준다. 선택 인원·정답 수·답한 학생 수는 같은 질문끼리만 센다. 다른 버전으로 답한 학생은 표에 "(다른 버전 질문에 답함 · 버전)"으로 보이고 집계에서 빠진다. URL `q=`는 `key~버전~문구해시`. 예전 링크(`q=analyze:q1`)는 지금 버전의 그 질문으로 열린다 | `src/lib/app-responses.ts:223-247`(versionLabel·questionIdentity), `:183-213`(qa·매핑에 version 부여) · `src/data/app-responses/types.ts:62-71,109-115` · `responses-view.tsx:288-410` · `sci-6-1-2-3.ts:122`, `sci-6-1-2-4.ts:89`, `sci-6-1-2-5.ts:47` |
| **M1** 이름 폴백이 학생에게 그대로 전송됨 | `praiseNameOf()`: 표시 이름이 없거나, `@`가 있거나, 영문·숫자로만 되어 있으면서 숫자가 섞인 아이디 모양(`s2024001`)이거나, "이름 없음"이면 null. `fillPraise(t, null)`은 문장 앞의 부름말("{이름} 학생, ")을 통째로 뺀다(예: "오늘 활동을 …"). 문장 가운데의 "{이름} 학생"·"{이름}"은 "우리 반 학생"으로 바꾼다. 다이얼로그에서는 대상 목록에 "이름 없음" 배지를 달고, 작성 화면 미리보기를 이름 있는 학생 1명과 이름 없는 학생 1명으로 나눠 보여 준다. 확인 화면에는 **학생별로 실제로 나갈 문장 전체**와 "이름 없이 보냄" 배지가 나온다. 화면의 표시 이름(이메일 앞부분)은 관리자 화면에만 쓴다 | `src/lib/praise.ts:85-121` · `praise-dialog.tsx:120-131,236-265,450-495` · `responses-view.tsx:133` |
| **M2** sci-6-1-1-6 "참고 자료를 열어 봤나" | 앱 코드를 확인하고 **실제 브라우저로 재현했다**. 조사 네 칸을 채우면 카드가 저절로 펼쳐진다(open=true, 본문 표시). 그런데 `refOpen`은 끝까지 `null`이었고 저장값은 `openedFacts:false`였다. 원인: `ref-cards.js`는 잠기지 않은 상태의 toggle 이벤트에서만 값을 저장하는데, 잠금이 풀릴 때는 카드가 이미 펼쳐져 있어 이벤트가 나지 않는다. 그래서 매핑을 이렇게 바꿨다. 질문은 "'참고 자료 속 사실과 비교해 보기' 카드(정리 틀 네 칸을 채우면 저절로 펼쳐져요)". 값이 true면 "펼친 상태로 기록됨(자료를 볼 수 있었어요)", false면 "알 수 없음(저절로 펼쳐진 뒤 그대로 두었거나, 학생이 접었어요)". 뜻을 설명하는 주석도 달았다. 앱은 수정하지 않았다 | `src/data/app-responses/sci-6-1-1-6.ts:17-21,74-89` |
| **M3** 1단원 실제 기록 브라우저 확인 | 가짜 로그인 학생으로 **sci-6-1-1-1(sim)**과 **sci-6-1-1-6(guide)**를 실제 앱에서 끝까지 진행했다. 1-1-1은 관찰 23칸, 내 분류 기준, 분석 1, 분류 3라운드(1라운드는 한 번 틀림), 정리 3개, 궁금한 점. 1-1-6은 주제, 조사 4칸, 대본, 확인 2개, 알게 된 점, 빈칸, 퀴즈 2(1번은 한 번 틀림), 서술형·발전·결론, 궁금한 점. 저장된 `app_results` 행을 가짜 DB에 넣고 학생 응답 화면에서 확인했다. 입력한 글·고른 보기·정답/확인 횟수(예: 분류 1 "확인 2번")·기록 표가 모두 맞는 질문 아래에 보였다. "그 밖의 저장 값"은 없었다 | 검증(아래 §2) |
| **L6** "이미 보냄"이 최신 결과만 봄 | 그 학생의 그 앱 결과 **전부**(다시 한 것 포함)에 달린 선생님 메시지 수로 판정한다(카드 배지·다이얼로그·기본 선택). 보낸 뒤 목록 갱신도 학생 id 기준으로 한다 | `responses-view.tsx:72-86,109,132` · `praise-dialog.tsx:106-113` |
| **L7** 응답 유실·동시 전송 시 중복 | ① 보내기 직전에 `recheck`로 서버에서 "이미 보냄"을 다시 센다. 확인 화면을 연 뒤 다른 곳에서 메시지를 받은 학생이 생겼으면 아무것도 보내지 않는다. 대신 그 학생 이름을 경고로 보여 주고 한 번 더 누르게 한다. ② 학생마다 insert 직전에 그 대화를 다시 조회한다. 24시간 안에 선생님이 보낸 **같은 문장**이 이미 있으면 보내지 않고 "건너뜀(중복 방지)"으로 보고한다. 저장은 됐는데 응답이 유실돼 "다시 보내기"를 누른 경우, 탭 두 개에서 동시에 보낸 경우 모두 막힌다 | `praise.ts:131-192` · `praise-dialog.tsx:145-185,316-325` · `responses-view.tsx:83-86,650` |
| **L1** qa가 있으면 매핑 보조 정보가 사라짐 | qa와 같은 앱 버전의 매핑 변형이 맞으면 두 가지를 보탠다. qa 항목에는 매핑의 항목 주석(자기 기록 채점)을 붙인다. qa에 없는 짧은 값(열어 본 힌트 수)은 항목으로 더한다. 매핑이 읽지 않은 최상위 키는 "그 밖의 저장 값"으로 보인다. 기록 표·질문은 qa에 이미 있어 겹치지 않게 짧은 값만 보탠다 | `src/lib/app-responses.ts:141-213` |
| **L2** 확인 안 한 퀴즈가 "오답" | `detail.qa`에서 `correct:false`이면서 `tries:0`이면 채점 전으로 보고 정답 표시를 하지 않는다(null). 공통 틀은 수정하지 않았다 | `src/lib/app-responses.ts:61-72` |
| **L3** 미제출 글에 "(비어 있음)"과 "입력 중이던 글"이 함께 표시됨 | 빈 글이고 `submitted:false`이면 "(제출하지 않음)"만 보인다. "제출하기 전 입력 중이던 글"은 글이 있을 때만 보인다 | `src/components/learning/response-panel.tsx:71-80` |
| **L4** sci-6-1-1-5 notes 부정확 | '새롭게 알게 된 점'도 제출한 글만 저장된다는 점을 추가했다. 이름 비교 규칙을 실제 `worksheet.js` 규칙으로 고쳤다(띄어쓰기 무시, 별칭, 2글자 이상 부분 일치, 넓은 말 제외) | `src/data/app-responses/sci-6-1-1-5.ts:23-26` |
| **L5** 2-5 v3가 "20.0 km/h"로 보임 | v3와 v4는 구분할 수 없다. 그래서 분석 2에 "v4 전에 한 학생은 '20 km/h'로 보았어요(보기와 정답은 같아요)" 주석을 달았다 | `src/data/app-responses/sci-6-1-2-5.ts:81-82` |
| **L8** 관리자 시험 기록이 칭찬 대상에 들어감 | 명단(role=user)을 불러왔으면 명단에 없는 계정은 칭찬 대상에서 뺀다. 카드에는 "명단 밖 계정" 배지를 단다. 명단을 못 불러오면 예전처럼 모두 대상에 넣는다 | `responses-view.tsx:134,208-214,533-537` |
| **L9** 51명 이상이면 곧바로 오류 | 기본 선택을 "안 보낸 학생 중 앞 50명"으로 했다. 50명을 넘으면 오류 옆에 "앞 50명만 남기기" 버튼을 두고, 완료 학생이 50명을 넘으면 나눠 보내는 방법을 안내한다 | `praise-dialog.tsx:33-40,423-440` |
| **L10** qa 생성 예외 시 마침 버튼 잠김 | **처리 안 함(범위 밖).** 고치려면 `public/apps/sci-6-1-2-{3,4,5}/app.js` 또는 공통 틀을 수정해야 하는데, 지침상 금지다. 코드상 예외 경로는 발견되지 않았다(review와 같음). 다음에 앱을 고칠 때 `qa` 생성을 try/catch로 감쌀 것을 제안한다 | — |
| **L11** 질문 문구가 config와 글자 그대로 같지 않음 | 1-1-6 발표 대본 질문을 config `scriptPrompt` 전문으로 바꿨다. 1-1-2 분류 질문을 config 문장("위의 내 기록 표를 보고 여섯 가지 용액을 알맞은 곳으로 옮겨 보세요.")으로 바꿨다 | `sci-6-1-1-6.ts:91-97`, `sci-6-1-1-2.ts:56` |
| **L12** 기록이 빈 v3가 v4로 판정됨 | 기록이 비어 photo로 가를 수 없으면, 분석에서 고른 보기 문구의 시간 표기로 가른다("같은 5초"는 v2·v3, "같은 5.0초"는 v4). 표기가 없으면 예전처럼 v4로 본다 | `src/data/app-responses/sci-6-1-2-3.ts:32-46,52-61` |
| 참고(학생이 qa를 직접 넣을 수 있음) | 변경 없음(텍스트로만 표시, 본인 데이터라 위험 낮음) | — |

기타: 이전 버전 질문 안내 상자에는 정의된 색 토큰(`math-soft/strong`)을 쓴다. `groupByStage`는 제네릭으로 바꿨다(동작은 같음).

## 2. 검증

- `npx tsc --noEmit`, `npm run lint`(경고 0), `npm run build` 모두 통과. 빌드 후 개발 서버(:3000)도 200 응답 정상(dev는 `.next/dev`를 써서 충돌 없음).
- **가짜 DB e2e**(스크래치 전용 headless Chrome, 별도 프로필·포트, `*.supabase.co` 요청은 모두 CDP로 메모리 mock이 응답, 실시간 WebSocket 차단, 실제 서버로 나간 요청 0): **37/37 통과**
  - 질문별 보기: 지금 버전 묶음과 "이전 버전 질문 · 개정 전 버전(v1)", "… 개정 버전(v2·v3)" 묶음이 분리된다. 분석 2(지금 버전)는 답한 학생 6명(qa 5 + v4 1이 한 질문으로 묶임)이고, 다른 버전으로 답한 2명은 집계에서 빠진다. 선택 분포에 v1 보기(㉠~㉣)가 섞이지 않는다. v1 분석 2는 따로 "답한 학생 1명 · ㉡ 1명"으로 나온다. 예전 URL `q=analyze:q2`와 새 URL도 확인했다.
  - 이름 없는 학생(표시 이름 없음 1명, `s2024005` 1명): "이름 없음" 배지가 붙고, 미리보기에 이름 없는 문장이 따로 나온다. 확인 화면에서 학생별 실제 문장을 볼 수 있다. 저장된 본문은 "오늘 활동을 끝까지 잘 마쳤어요! …"이고 `{이름}`·아이디·"이름 없음"이 없다.
  - 중복 방지: 예전 결과로 칭찬 받은 뒤 다시 한 학생은 "이미 보냄"으로 기본 해제된다. 확인 화면을 연 뒤 다른 탭에서 보낸 학생이 생기면 경고가 뜨고 0건 전송된다.
  - 부분 실패와 재시도: 스레드 생성 실패 1명과 "저장됐지만 응답 유실" 1명이 "보내지 못한 학생"으로 나온다. "다시 보내기"를 누르면 복구된 학생 1명만 보내고, 응답이 유실됐던 학생은 "건너뜀(같은 문구를 이미 받음)"으로 처리된다. DB에는 1통만 있다. 다시 열면 모두 "이미 보냄"이다.
  - 관리자 계정 완료 기록: "명단 밖 계정" 배지가 붙고 칭찬 대상에서 빠진다(후보 7명).
  - qa 결과에 "열어 본 힌트 수"와 자기 기록 채점 주석이 보인다.
  - 1단원 실제 기록(M3) 2개가 매핑대로 표시된다.
  - 태블릿 768px, 375px(라이트·다크), 데스크톱 다크: 학생별·질문별·칭찬 확인 화면 모두 가로 넘침 0, 콘솔 오류 0.
- 추출 단위 검사(transpile한 실제 `app-responses.ts`): L12(기록이 빈 v3/v4 판정), L2(tries 0 → 채점 없음), L1(힌트·주석 보탬, 모르는 키는 extras), H1(qa≡v4, v1≠v4, 2-4/2-5는 qa와 최신 변형이 같은 질문으로 묶이고 이전 변형과는 섞이지 않음), `praiseNameOf`·`fillPraise` 입력별 결과를 확인했다.
- Build 단계 e2e(`harness/dash.mjs`) 재실행: 44/46. 실패한 2개는 질문 선택 값이 `analyze:q1` 형식일 것이라고 가정한 옛 검사다. H1 수정으로 `key~버전~해시` 형식이 된 것이 원인이고, 기능 회귀는 아니다.
- 스크린샷·스크립트는 세션 스크래치 `fx1/`에만 있다(`localStorage.clear()` 쓰지 않음, 자기 전용 headless만 사용).

## 3. 사용자가 할 일

1. **새 SQL 없음.** 아직 실행하지 않았다면 기존 `supabase/migrations/20260922020000_praise_presets.sql`만 실행하면 된다(Build 보고서 §5와 같음).
2. 배포 후 관리자 계정으로 `/admin/learning/?tab=responses`에서 실제 1단원 결과 몇 건을 확인한다. sci-6-1-1-6의 "참고 자료 카드" 항목은 대부분 "알 수 없음"으로 보이는 것이 정상이다(앱이 값을 남기지 않는 구조).
3. 다음에 2단원 탐구 3·4·5 앱을 고칠 일이 있으면 `buildDetail()`의 `qa` 생성을 try/catch로 감싼다(L10).
