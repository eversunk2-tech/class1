# Review — 홈 '선생님 글' 쪽 나누기 (커밋 `c370f6b`, 2026-09-26)

> 지침 `docs/classes/review-notice-pages-instructions.md`. 코드는 고치지 않았다(이 보고서만 새로 씀). 판정은 **커밋 내용**으로 했다: `git archive c370f6b` 사본을 스크래치에서 빌드해 시험(작업 트리·저장소 `out/` 안 씀). 참고: 지금 `main`의 `9a9909d`는 같은 커밋을 다른 커밋 위로 다시 만든 것이고, `src/`·이 작업의 4개 파일은 `c370f6b`와 한 글자도 다르지 않다(`git diff c370f6b 9a9909d -- src` 비어 있음).

## 0. 한 줄 판정
높음 0 · 중간 1 · 낮음 3 · 참고 6. **사용자 요청은 정확히 동작한다** — 좌우 반씩일 때 오른쪽(접은 제목 + 쪽 넘기기)이 왼쪽 '최근 과학 수업' 내용 높이(530px)를 넘지 않고, 다 들어가면 쪽 넘기기가 없고, 넘치면 들어가는 만큼 쪽으로 나뉜다. 쌓이면 5개씩. 글 빠짐·겹침·콘솔 오류·외부 요청 0, 첫 그림부터 나뉜 상태. 중간 1개는 **글을 펼친 채 쪽을 넘기면 화면이 튀는 것**(M1).

## 1. 발견

| # | 심각도 | 파일:행 | 무엇 | 재현 | 제안 |
|---|---|---|---|---|---|
| M1 | 중간 | `src/components/dashboard/teacher-posts.tsx:429-434`(`goTo`), `:476` | **글을 펼친 채 쪽을 넘기면 화면이 튄다.** 쪽을 넘기면 펼친 글이 접혀(지침대로) 그 본문 높이만큼 목록·쪽 넘기기가 위로 올라가는데, 스크롤은 그대로라 누른 버튼이 손가락 밑에서 사라진다. 본문 5줄 161px · 10줄 284px · 15줄 408px · 25줄 655px 위로 이동(쌓임 기준, 1440 좌우는 147·270·394px). 긴 글이면 새 쪽이 화면 밖으로 나간다 — 375 폰·1180×820(iPad 가로, 사이드바 펼침 = 쌓임)에서 25줄이면 새 쪽 글 5개 중 2개만 보이고, 40줄이면 **0개·쪽 넘기기도 화면 밖**, 화면에는 '최근 자유게시판'·'학습게임 미리보기'가 보인다(`shots/bodyjump-375-long.png`). 또 **빠르게 한 번 더 누르면**(쪽 넘길 때 흔한 동작) 그 자리에 다른 칸이 와 있다 — 본문 5줄에서 820×1180·1180×820은 '자유게시판 더 보기'(→ `/board/`), 1440은 '학습게임 더 보기' 링크. 글을 펼치지 않고 넘길 때는 튀지 않는다(아래 항목 3). | 글 12개, 첫 글 본문 N줄 → 제목 눌러 펼침 → 쪽 넘기기가 보이게 내림 → '다음'. 수치 `report-bodyjump2.json`, 두 번째 탭 자리 `doubletap.mjs`. | 쪽을 넘기기 직전에 쪽 넘기기(`nav`)의 화면 위치(`getBoundingClientRect().top`)를 기억하고, 새 쪽을 그린 직후(`useLayoutEffect`, 쪽 번호가 바뀔 때·펼친 글이 있었을 때만) 차이만큼 `window.scrollBy(0, 새 top − 옛 top)` — 쪽 넘기기가 손가락 밑에 그대로 남고 새 쪽 목록은 그 바로 위라 보인다(즉시 이동이라 움직임 줄이기와 충돌 없음). 몇 줄이면 된다. |
| L1 | 낮음 | `teacher-posts.tsx:22`(`MIN_PER_PAGE = 3`), `:137-145`(`listMinHeight`) | '한 쪽에 적어도 3개' 때문에 **제목 90~100자가 연달아 3개**면 좌우일 때 오른쪽이 왼쪽보다 **42px 길다**(칸 폭 404~420px: 1280 사이드바 펼침, 1080·1100 사이드바 접힘 — 100자 제목이 4줄 120px). `listMinHeight`가 가장 높은 쪽 높이라 모든 쪽이 572px(왼쪽 530). 보고서 §6-5의 "지금 홈에서는 생기지 않는다"는 이 경우 틀림. 70자 제목만이거나 100자 2개 + 보통 1개면 생기지 않음. | `titles.mjs` all100 1280 펼침(오른쪽 572 / 왼쪽 530), `longtitle-edge.mjs`. 스크린숏 `shots/titles-all100-12-1440.png`(1440은 3줄이라 괜찮음). | 드문 경우라 그대로 두어도 됨. 고친다면 최소 개수를 2로, 또는 예산을 넘긴 쪽만 길어지게(`listMinHeight`를 예산 이하로 제한). 보고서 문구 고치기. |
| L2 | 낮음 | `teacher-posts.tsx:109-120`(`pickPager`), 보고서 §3·§6-2 | 휴대폰 쪽 넘기기 모양이 **쪽 수에 따라 들쭉날쭉**: 390(칸 318px)에서 5쪽이면 "‹ 이전 1 / 5 다음 ›", 6쪽 이상이면 오히려 번호 "‹ 1 2 3 … 6 ›". 375(303px)는 2쪽 "‹ 이전 1 2 다음 ›", 3~4쪽 화살표만 "‹ 1 2 3 4 ›", 5쪽부터 "n / N". 보고서의 "4쪽 이상이면 '2 / 6'"은 실제와 다름. 쪽 수가 번호 칸 수 이하일 때 번호를 전부 보이는 모양만 시험하고, 더 좁은 "1 2 … 5" 모양은 시험하지 않아서다. | `shots/phone-390-25posts.png`("1 / 5") ↔ `shots/phone-390-30posts.png`("1 2 3 … 6"). | 두 모양 모두 쓰기에는 문제없다(아래 항목 3). 고친다면 전부-번호가 안 들어갈 때 줄인 번호 모양을 먼저 시험하고 그다음 "n / N". 보고서 문구를 "375에서 5쪽부터"로. |
| L3 | 낮음 | `teacher-posts.tsx:244`, `src/lib/pill.ts:8-12` | Windows 고대비(강제 색)에서 **지금 쪽이 구별되지 않고** 알약 테두리도 사라진다(지금 쪽 = 보라 배경만, 테두리 = box-shadow). 스크린리더는 `aria-current`로 알 수 있음. 사이트 알약 공통 방식. | 강제 색 에뮬레이션 `shots/forced-colors-pager.png`("‹ 이전 1 2 3 다음 ›"가 모두 같은 모양). | 지금 쪽 버튼에 `forced-colors:outline` 또는 `aria-[current=page]:underline` 한 단어. 학생 사용은 드묾. |

참고(발견 아님)
* R1 쌓일 때 목록 칸 최소 높이 = 가장 높은 쪽이라 **마지막 쪽이 짧으면 빈칸**이 남는다(375, 글 12개 3쪽: 글 2개 아래 약 200px, 아주 긴 제목이면 더 큼 — `shots/phone-375-12posts-page3.png`). 쪽을 넘겨도 쪽 넘기기가 제자리에 있게 하려는 설계(보고서 §3)라 그대로 두어도 됨.
* R2 홈 통계 '선생님 글 N개'는 전체 개수라 101개 이상이면 목록(최신 100개)보다 크다(보고서 §6-1과 같음).
* R3 옆 칸 자식은 효과가 돌 때의 목록으로 지켜본다(`:377-378`). 지금 '최근 과학 수업'은 고정 데이터라 괜찮지만, 나중에 그 칸이 불러오는 중 → 내용으로 **요소를 바꿔 끼우면** 그 뒤 새 요소의 크기 변화는 칸 전체 높이가 바뀔 때만 잡힌다(바꿔 끼우는 순간 한 번은 다시 잰다).
* R4 창 크기로 다시 나누면 '보던 쪽의 첫 글'을 기준으로 쪽을 고르므로, 펼쳐 둔 글이 그 쪽 뒤쪽이면 다른 쪽으로 옮겨 가 안 보일 수 있다(펼침 상태는 남음). 쪽 넘기기 버튼에 초점이 있는데 다시 나눠 한 쪽이 되면 버튼이 사라져 초점이 문서 처음으로 간다. 둘 다 드묾.
* R5 사이드바 접기 애니메이션(0.2초) 동안 1180에서 '쌓임 5개 → 좌우 5개 → 좌우 4개'로 한두 번 바뀐 뒤 멈춘다(반복 경고 0).
* R6 6개는 좌우일 때 528/530px로 2px 차이로 한 쪽에 다 들어간다. 실제 기기 글꼴 차이로 5+1이 될 수 있으나 그때그때 재므로 요청과 어긋나지 않는다.

## 2. 항목 1~6 판정과 수치

**1. 요청대로 — 통과(L1 제외).** 기본 표(126번 새로 열기: 글 0·1·3·5·6·7·12·30·101 × 1920×1080·1440×900·1280×800·1180×820·1024×768·768×1024 × 사이드바 펼침·접힘 + 390×844·375×812) 검사 1,239개 실패 0.
| 배치 | 해당 화면 | 왼쪽 내용 | 오른쪽(접은 상태, 가장 큰 쪽) · 쪽 |
|---|---|---|---|
| 좌우(칸 444px, 1280 펼침 420) | 1920·1440 펼침/접힘, 1280 펼침/접힘, 1180 접힘 | 530 | 1개 188 · 3개 324 · 5개 460 · **6개 528(한 쪽, 쪽 넘기기 없음)** · 7개 5·2 **516** · 12개 5·5·2 516 · 30개 5×6 516 · 101개 20쪽 516 |
| 쌓임 | 1180 펼침, 1024 펼침/접힘, 768 펼침/접힘, 390, 375 | — | 1·3·5개 한 쪽, 6개 5·1, 7개 5·2, 12개 5·5·2, 30개 5×6, 101개 20쪽(5개씩) |
* 모든 좌우 조합·모든 쪽: 오른쪽 ≤ 왼쪽(+0.5px), 두 칸 높이 = 왼쪽 내용 높이(오른쪽이 늘이지 않음), 다 들어가면 한 쪽, 아니면 쪽마다 다음 글이 안 들어갈 만큼 채움(실제 높이로 따로 계산해 확인). 좌우/쌓임 판단 = 그리드 열 수.
* 추가: iPad 크기 DPR 2(820×1180·810×1080·1080×810·1024×1366·1366×1024, 339개), DPR 1.25(1536×864 등, 285개), 윈도식 15px 스크롤바(1100×800 등, 420개), 작은 폰·폰 가로(360·320·667×375·844×390, 205개), 폰 10·15·20·25개(88개) — 실패 0. 1080×810·1100×800은 사이드바를 접으면 좌우(칸 404~414px).
* 제목(192개 검사): 1~5줄 섞임·100자·띄어쓰기 없는 100자 — 잘림 0, 가로 넘침 0, 좌우일 때 오른쪽 ≤ 왼쪽(1440 섞임 4·4·4 → 490/530, 1280 펼침 4·4·4 → 512/530). 실패 1 = L1.

**2. 빠지거나 겹치는 글 없음 — 통과.** 위 모든 조합에서 '다음'으로 끝까지 넘겨 모은 제목 = 최신순 전체(101개면 최신 100개, 요청 1번 `offset=0&limit=100`), 마지막 쪽까지. 한 창에서 1440 → 1180 → 1280 → 375 → 1920 → 사이드바 접기 → 글자 18px → 펼치기로 **다시 나눌 때마다** 1쪽부터 끝까지 다시 넘겨 봐도 23개가 한 번씩·최신순(9/9, 좌우 4·4·4·4·4·3 ↔ 쌓임 5·5·5·5·3).

**3. 쪽 넘기기 — 통과(M1 제외).** 36/36. 마우스로 '3' → 3쪽·`aria-current="page"`·초점 '3'에 남음·scrollY 그대로·쪽 넘기기 위치 그대로·알림 "선생님 글 3쪽 중 3쪽". 꺼진 '다음'은 마우스(pointer-events 없음)·Enter 모두 반응 없음. Enter·Space로 '이전', 첫 쪽에서 꺼져도 초점 유지, `:focus-visible` 3px 보라 테두리(밝음·어두움). Tab 순서 = 제목 5개 → 이전 → 번호 → 다음 → 다음 칸(사본에 초점 0). 손가락(터치 이벤트) 375·390. 모든 버튼 44×44px 이상(번호 사이 4px). 펼친 글은 쪽을 넘기면 접히고 돌아와도 접힘. 접근성 트리: `navigation "선생님 글 쪽 넘기기"`, 버튼 이름 "이전 쪽"·"선생님 글 2쪽"·"다음 쪽", 끝 버튼 disabled, live=polite. 모든 조합에서 쪽을 넘겨도 scrollY·쪽 넘기기 위치 그대로(펼친 글이 없을 때).
* **휴대폰 "2 / 6" 판정: 쓰기 좋음.** "‹ 이전 2 / 6 다음 ›" — 글자 이름 있는 44px 버튼, 보이는 "2 / 6"은 `aria-hidden`이고 스크린리더에는 "6쪽 중 2쪽"(+ 넘길 때 알림). 특정 쪽으로 바로 못 가지만 6쪽 안팎이면 충분. 다만 모양이 쪽 수에 따라 바뀌는 점은 L2(화살표만 보일 때도 버튼 이름 "이전 쪽"·"다음 쪽" 있음). 320px에서도 한 줄.

**4. 다시 재기 — 통과.** 26/26. 2쪽을 보다가 1180·1024·768·390·375·1920·1280·1440로 바꿔도 보던 글(6번)이 보이는 쪽, 좌우면 오른쪽 ≤ 왼쪽, 한 줄, 넘침 0. 사이드바 접기·펼치기(애니메이션 켬) 뒤 맞음(R5). 글자 18px: 좌우 유지(595/580), 20px: 쌓임으로 바뀌고 5개씩. 왼쪽 칩 5개 추가 → 530→614px·한 쪽 5→6개, 빼면 5개. **카드가 화면 위로 스크롤된 채**(top −177~−627px) 창·사이드바를 바꿔도 처음과 같게 나눔. 글꼴 2.5초 늦게 와도 끝 상태 맞음. **첫 그림 깜박임 0**: 목록이 있는 새로 열기 약 220번 모두 목록이 처음 보인 프레임부터 끝 상태 한 가지뿐. window 오류(ResizeObserver 반복 알림 포함)·콘솔 오류 0. **사본**: `visibility:hidden`·`inert`·`aria-hidden`·`pointer-events:none`·`overflow:hidden`, 칸과 같은 상자, 사본 목록 2,028px여도 페이지 높이 그대로(있을 때 = 없을 때 2,200px), 찾기(Ctrl+F)로 다른 쪽 제목 안 찾아짐, 접근성 트리·Tab·섹션 글자에 안 나옴, id·제목 태그 0, 중복 id 0. **성능**: 101개에서 창 폭 20번 바꾸기 — 스크립트 합계 25ms(12개 2ms), 50ms 넘는 작업 0, DOM 917개(12개 474개).

**5. 그대로여야 할 것 — 통과.** 15/15. 불러오는 중 뼈대 3줄(`aria-busy`), 오류 → "선생님 글을 불러오지 못했어요" + 다시 시도 → 쪽 나눈 목록, 표 없음(SQL 전) → "아직 선생님 글이 없어요", 잠금 켜짐(방문자) → 로그인 안내·목록 요청 0. 날짜·'더 보기' 없음. 본문 글자 그대로(`pre-wrap`) + `https://example.com/a`는 새 창 링크(`noopener noreferrer nofollow`). `<script>` 제목·본문은 글자로만(목록·사본 안 script·img 0, 실행 0). 방문자 = 총괄 글만(여우반 글 없음, 익명 키), 부엉이반 학생 = 자기 반(로그인 토큰, `class_id`로 거르지 않음), 여우반 학생 = 2개·쪽 넘기기 없음. 학생이 3쪽에서 글을 펼친 채 로그아웃 → 방문자 목록을 새로 불러 1쪽·접힘, 다른 탭에서 여우반 로그인 → '불러오는 중' 뒤 여우반 2개(이전 목록이 다시 그려지지 않음, 프레임 기록). 홈 다른 칸(바로가기·한눈에·자유게시판·학습게임) 그대로, `page.tsx`는 prop 한 개 + 주석.

**6. 어두움·움직임 줄이기·넘침·오류·외부 요청·lint·tsc — 통과(L3 제외).** 글자 대비: 지금 쪽 알약 밝음 5.59~6.0:1(흰 글자/보라 그라데이션 양끝), 어두움 8.09~8.65:1, 다른 버튼·"2 / 6" 19.8:1(밝음)·17.2:1(어두움), "…" 5.49:1(꺼진 버튼은 50% — 예외). 움직임 줄이기: 알약 올라감·전환 없음, 펼침 화살표 전환 없음. 가로 넘침 0(모든 조합), 콘솔 오류 0, 외부 요청 0. 커밋 사본: `npx tsc --noEmit` exit 0, `npm run lint` exit 0(출력 없음), `next build` 성공(25쪽).

## 3. showcase — `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-notice-pages/showcase/`
`wide-1440-12posts-page1.png`, `wide-1440-12posts-page2.png`, `stacked-768x1024-12posts.png`, `phone-375-12posts.png`.
그 밖(같은 폴더 위 `shots/`): `bodyjump-375-long.png`(M1), `phone-375-12posts-page3.png`(R1), `phone-375-30posts.png`("2 / 6"), `phone-390-25posts.png`·`phone-390-30posts.png`(L2), `titles-all100-12-1440.png`·`titles-mixed-12-1440.png`, `dark-1440-12posts.png`·`dark-375-30posts.png`, `stacked-1024-101posts-p5.png`("1 … 4 5 6 … 20"), `forced-colors-pager.png`(L3).

## 4. 시험 방법·차단
* 사본: `git archive c370f6b` → 스크래치 `review-notice-pages/src-c370f6b`, node_modules는 저장소 것에 링크, 사본 설정에만 `turbopack.root="/"`, 가짜 Supabase 주소·키(번들 속 Supabase 주소는 가짜 하나 — 실제 주소는 과학 앱 `config.js`에만 있고 그 앱은 열지 않음), `NEXT_TELEMETRY_DISABLED=1`, 구글 글꼴(Geist)은 앞선 links 검토의 로컬 사본을 `NEXT_FONT_GOOGLE_MOCKED_RESPONSES`로 자기 서버에서 공급.
* 자기 정적 서버 8786, 자기 headless Chrome(CDP 9360, 프로필 스크래치) `--host-resolver-rules="MAP * ~NOTFOUND, EXCLUDE 127.0.0.1"`, 탭마다 첫 이동 전 CDP `Fetch` 가짜 응답(Build 도구 `notice-pages/lib.mjs`·`backend.mjs`를 복사해 포트·주소·제목/본문 옵션만 바꿈), 캐시 끔. 시험 스크립트는 따로 씀(`matrix`·`interact`·`titles`·`dynamic`·`rewalk`·`viewers`·`bodyjump*`·`shots` 등), 결과 `report-*.json`·`*-run.log`.
* 검사 합계 약 2,850개 — 실패는 L1 1개(+ M1은 수치로 측정). 설치·내려받기·git commit/push·실 DB 쓰기·`localStorage.clear()` 0, 다른 포트·프로세스 손대지 않음, 서버·Chrome 종료. 끝나고 빌드 사본·Chrome 프로필은 지우고(약 176MB) 시험 도구·결과·스크린숏만 `scratchpad/review-notice-pages/`에 남김(2.4MB).

## 5. 확인하지 못한 것
* 실제 iPad Safari·휴대폰: 손가락으로 쪽 넘기기, VoiceOver 읽기("6쪽 중 2쪽"·알림), `inert`(iOS 15.5 미만), 실제 글꼴로 6개가 한 쪽에 드는지(R6).
* 실제 로그인·실제 DB의 글(모두 가짜 세션·가짜 응답), 실제 윈도 고대비(에뮬레이션만), 실제 교사가 쓴 긴 공지로 M1이 얼마나 자주 생기는지.

**배포해도 됨** — 높음 0: 요청(옆 칸 높이에 맞춘 쪽 나누기, 쌓이면 5개씩)이 약 2,850개 검사에서 정확했고 글 빠짐·겹침·깜박임·오류·외부 요청이 없다. 중간 M1(글을 펼친 채 쪽을 넘기면 화면이 튐)은 학생 눈에 보이지만 글이 6~7개 이상이고 글을 펼친 뒤 넘길 때만 생기며 글이 사라지지는 않아 배포를 막지는 않는다 — 다만 iPad에서 두 번째 탭이 '자유게시판 더 보기'를 누를 수 있으니 몇 줄로 고칠 수 있는 이번에 함께 고치기를 권한다.
