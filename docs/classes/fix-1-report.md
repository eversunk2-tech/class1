# 고친 것 보고(fix-1) — 반별 구분 + 학급별 선생님 글 Review 뒤 (2026-09-26, Claude)

기준: `review.md`(배포해도 됨 — 높음 0 · 중간 1 · 낮음 11).

## 고친 것
| 번호 | 무엇 | 어떻게 |
|---|---|---|
| L1 | 관리자 '선생님 글'을 쓰다가 관리자 메뉴 등을 누르면 쓰던 글이 묻지 않고 사라짐 | 쓰던 글이 있으면 사이트 안 링크를 누를 때 한 번 묻는다(문서 capture 단계 click — `next/link` 이동보다 먼저). 취소 = 그 화면에 머묾, 확인 = 이동. 탭 닫기·새로고침은 원래대로 `beforeunload` (`notices-view.tsx`) |
| L2 | 공용 확인 창 버튼 32px | 취소·확인 버튼 44px(`h-11 px-4`, `src/components/learning/confirm-dialog.tsx` — 관리자 확인 창 전체) |
| L3 | 개요 바로 가기 설명에 '우리 반' | "내 학급 학생에게 보일 글 쓰기"(`admin-overview.tsx`) |
| L4 | 총괄 화면 목록 머리 "○○반 학생에게만 보여요"가 사실과 다름 | 총괄에게는 "○○반 학생에게 보이고, 내가 쓴 글은 로그인하지 않은 방문자에게도 보여요." 담임은 그대로 |
| L5 | 잠금 켜짐 + 비로그인에서 통계 칸이 개수 요청 1건 | 방문자는 잠금 여부를 먼저 확인하고 켜져 있으면 세지 않음(홈 목록과 같은 방식, `stat-tile.tsx`) → 요청 0건 |
| L8 | 파일마다 단계 번호가 다름 | 모두 ① schema → ② setup-owl-class → ③ rls → ④ class_notices → ⑤ 함수 3개 재배포 → ⑥ 화면 push 로 통일(①·③·④ 머리말, setup SQL, 함수 README 3개) |
| L9 | setup-owl-class.sql 을 나중에 다시 돌리면 학급 없는 회원까지 부엉이반에·'부엉이반' 하나 더 | 총괄이 이미 있으면 아무것도 바꾸지 않고 멈추는 가드 + 머리말 "처음 설정할 때 한 번만". ③의 학급 없는 회원 알림도 "setup 을 다시 실행" → "이 메시지를 알려 주세요(학급을 정하는 SQL을 드립니다)" |
| L10 | ③ 뒤 옛 마이그레이션을 다시 돌리면 권한이 소리 없이 넓어짐 | ③ 머리말 경고 + CLAUDE.md "SQL 적용 절차"에 금지 한 줄(비상 되돌리기 `rollback-classes-rls.sql`) |
| L11 | 홈 '더 보기' 40px(예전부터) | '더 보기' 알약 버튼 4곳 44px(홈 선생님 글·자유게시판/학습게임 목록·검색·태그 글 목록) |

## 그대로 둔 것
* **M1(사용자 결정 필요)** — 과제·블로그 글은 교사 누구나 고치고 지울 수 있고(개정 1-5), 지우면 연쇄 삭제(RLS 밖)로 다른 반 제출물·읽음 기록까지 사라진다. 지금은 교사가 총괄 1명뿐이라 영향 없음. **두 번째 담임을 지정하기 전에** 사용자에게: 추천 = 과제·글 수정·삭제를 "쓴 교사 본인 또는 총괄"로 좁히는 새 SQL.
* L6(누구나 부를 수 있는 판별 함수 2개 — 드러나는 것이 "누가 총괄인가"와 학생 본인의 학급 uuid뿐), L7(같은 학급 보조 담임이 총괄 글을 고치면 방문자에게 보임 — 보조 담임이 생길 때 다시 봄).
* Review 제안(③ 되돌리기 SQL 미리 준비) → 별도 Build(`build-rollback-instructions.md` → `rollback-classes-rls.sql`).

## 확인
* `npm run lint`·`npx tsc --noEmit`·`npm run build` 통과, `git diff --check` 깨끗.
* 자기 정적 서버 + 자기 headless Chrome(Supabase 주소 차단 + CDP 가짜 응답, 실제 요청 0건):
  * 선생님 글 시험(Build 담당 도구) **117/117** — 총괄 머리 문구를 새 문구로 확인하도록 두 항목(H1·H4)을 고쳐서.
  * 쓰던 글 보호 **6/6**(묻기·취소하면 머묾+글 남음·확인하면 이동·쓰던 글 없으면 묻지 않음·콘솔 오류 0·외부 요청 0).
  * 잠금 중 비로그인 `class_notices` 요청 0건·통계 칸 "로그인 필요"·확인 창 버튼 44px **5/5**.
  * 반별 구분 관리자 화면 시험(화면 Build 도구) **109/109**.
