# Review 공통 지침: 6학년 2학기 2단원 시뮬레이션 앱 (2026-09-22)

Build와 **독립적으로** 검증한다. **코드는 수정하지 않는다.** 결과는 `public/apps/{앱}/review.md` 하나만 작성.

## 읽을 것
`CLAUDE.md`(과학 차시 앱 규칙, 측정 규칙 개정 포함), 앱 `spec.md`(끝 "개정 1" 우선 → "Claude 검토 메모"), `build-instructions.md`, `docs/science/6-2-unit2/build-common.md`, `build-report.md`, 앱 코드 전부.
지도서: `~/Library/CloudStorage/GoogleDrive-sunkyboy@pen.go.kr/내 드라이브/오션초2026/수업/교과서 usb자료/[과학]622_지도서 2.물질의 연소.pdf` (인쇄 쪽 = PDF 쪽 + 179, `pdftotext -layout -f N -l N`), 실험관찰 `실험관찰-6-2.pdf`.

## 검증 항목
1. **과학적 정확성(별도 절)**: 화면의 모든 수치·용어·현상을 지도서 쪽과 대조. 3D/2D 장면이 현상과 맞는지(불꽃 모양·색, 기포·풍선, 촛불 꺼지는 순서, 색 변화, 석회수 등), 안전 안내·불장난 부추김 여부. "모형"/"모형(추정)" 표시 누락, 다음 차시 스포일러(예: 탐구 1~3에서 이산화 탄소·연소 조건 선언), 오개념 유발 표현, 학생 화면의 "지도서" 문구.
2. **규칙 준수**: 예상·정리 질문 각 1개, 힌트가 답을 말하지 않음, 분석 보기 고르기 1~2개(자기 기록 기반), 발전 질문 없음, 궁금한 점 선택, 실험관찰 조건 모두 측정(개정 1), 값 타이핑·반올림 없음·소수 첫째 자리, 로그인 필수 가림막, `app_progress` 저장·복원, 로그아웃 시 `sci6…` 삭제, 완료 시 `detail.qa`, 저장 키 `sci622simN:v1`, 공통 틀 사본이 정본과 같은지(`diff -r scripts/templates/science-sim public/apps/{앱}/science-sim`, `class1-record.js`).
3. **기능·UI**: 전체 흐름 끝까지, 슬라이더/바(터치 드래그·키보드·끝값·눈금 스냅·기록 가능 조건), 3D 드래그와 슬라이더 간섭, 새로고침 복원, 앞 단계 이동·다시 하기, `?no3d=1`, 3D↔2D 반복 전환(WebGL 해제), 태블릿 가로 1024×768·세로 768×1024, 375px(가로 스크롤 없음), 다크모드, 콘솔 오류 0, 학생 입력 innerHTML 삽입 여부, 색만으로 정보 전달 여부, 접근성(aria).
4. **소요 시간**: 직접 진행해 단계별 시간 측정 + 보통/느린 학생 추정(7분 기준 대비).

## 작업 환경
브라우저는 **자기 전용 headless 브라우저**(겹치지 않는 포트)만 쓰고, 공유 브라우저·다른 탭·다른 프로세스를 건드리지 않는다. 로컬 정적 서버가 필요하면 자기 포트로 띄우고 끝나면 종료. 로그인은 가짜 세션/가짜 Supabase 응답. `localStorage.clear()` 금지. 실 DB 쓰기·git 금지. 임시 파일은 `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/f1b62131-752e-439d-a427-f0c4876f6afe/scratchpad/review-{앱}/`에만.

## review.md 형식
요약(심각도별 개수: 높음/중간/낮음/참고) / 과학적 정확성 표(항목, 화면 값, 근거 쪽, 판정) / 문제 표(심각도, 위치 파일:줄, 현상, 재현, 수정 제안) / 소요 시간 / 확인한 것 vs 못 한 것. 최종 응답은 10줄 이내 요약.
