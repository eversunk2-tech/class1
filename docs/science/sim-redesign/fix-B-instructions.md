# 단계 B 수정 지침(fix-B) — Review B 발견 고치기 (2026-09-25)

먼저 읽기: `CLAUDE.md`(과학 차시 앱 규칙 — 특히 장면 속 글자·시점 방향·직접 조작·답을 먼저 알려 주지 않기), `docs/science/sim-redesign/build-B-instructions.md`(맨 위 절 우선), **`review-B.md`(이 지침의 근거 — M1~M3, L1~L12)**, 맡은 앱의 Build 보고서 `build-B-<앱>-report.md`·`spec.md`(끝 개정 절)·`app.js`·`style.css`, 공통 틀 README("추가 기능 2026-09-25" 절 — Claude가 방금 더한 `v.draggable`의 `opts.pad`, 가로 화면 장면 높이 맞춤 포함).

담당: **fix-B1 = `sci-6-2-1-2` + `sci-6-2-1-3`**, **fix-B2 = `sci-6-2-1-4` + `sci-6-2-1-5`**. 자기 앱 두 개의 `app.js`·`style.css`·`data/lesson-config.js`·`index.html`과 그 앱 `spec.md` 끝의 개정 절(덧붙이기), 자기 보고서만 고친다. **공통 틀 정본·사본(`science-sim/`)·다른 앱·사이트 코드는 고치지 않는다**(필요하면 보고서에 적기). 측정 조건·측정값·문항·저장 키·`detail`/`detail.qa` 모양·과학적 의미가 있는 색은 그대로.

## 사용자 결정(2026-09-25)
* **2-1-2 태양 고도 호(M2)**: **"옆에서 본 모습" 작은 칸**을 둔다 — 처음 시점(북쪽에서 남쪽을 봄)에서는 호가 담긴 수직면을 거의 옆으로 봐서 태양 고도가 가장 높은 12:30·13:30에 화면 속 호가 가장 작아 보이는 문제(오개념 위험). 2-1-5 확대 칸처럼 장면 한쪽 구석의 작은 창에 **나무·땅·해에서 오는 빛줄기(나무 꼭대기 → 그림자 끝)·그림자·호를 늘 옆에서 본 모습(실제 각도)**으로 그린다.

## fix-B1: `sci-6-2-1-2`
1. **옆에서 본 모습 칸(M2)**: 해·빛줄기·그림자·3D 호와 **같은 태양 방향 상태값 하나**에서 계산(따로 계산 금지). 칸 안의 각 = 패널의 태양 고도(숫자는 칸에 쓰지 않는다 — 값은 패널에만, 칸에는 "옆에서 본 모습(모형)"과 "태양 고도" 같은 **안 바뀌는 이름**만). 빛이 오는 쪽은 3D 화면과 같게: 해가 남중 전(동쪽)이면 **왼쪽**에서, 남중 뒤(서쪽)면 **오른쪽**에서(북쪽에서 남쪽을 보는 처음 시점의 좌우와 같음). 해를 끄는 동안 칸도 함께 바뀐다. 자리: 토글(왼쪽 위)·시각 배지·막대·해의 길·나무·그림자·사람·백엽상을 가리지 않는 곳(재서 정한다), 기본 화면·크게 보기 두 모드와 가로·세로에서. 휴대폰처럼 장면이 너무 작으면 숨겨도 된다(2-1-5처럼, 보고서에 기록). 7개 시각 × 두 모드에서 칸 속 각(화면에서 잰 각) = 태양 고도(오차 1° 이내)를 숫자로 확인.
2. **2D 옆모습 방향(L5)**: 2D의 아래 옆모습이 오후에도 늘 왼쪽에서 빛이 오는 것 → 위 하늘 그림·3D와 같게 해가 서쪽이면 오른쪽에서 오도록.
3. **L11**: 3D 안내 글이 방위 이름표('북')를 가리는 것 정리.

## fix-B1: `sci-6-2-1-3`
1. **M3**: `activateExp()`에 넣은 "실험하기에 들어올 때 장면으로 스크롤"을 **뺀다**(공통 틀과 같게 — 들어올 때는 스크롤하지 않는다, Claude 결정: '실험 방법 알아 두기'를 먼저 읽게).
2. **L3**: "계절 대표 3개" 선을 굵고 진하게(예: 굵기 0.05·불투명 0.85 이상), 달별 선·하늘 색과 구별되는 색(색만으로 구분하지 않게 범례 이름 유지). 현재 달이 대표 달과 같아 대표 선을 안 그릴 때 범례가 헷갈리지 않게.
3. **L4**: 바뀌는 값 3D·2D 이름표("남중 고도 ○○.○°", `app.js:634`·`:810`)를 없앤다 — 값은 패널에만(CLAUDE.md).
4. **L5**: 3D 처음 시점과 2D의 좌우를 맞춘다 — 교과서처럼 **남쪽 하늘을 바라본 모습(동쪽이 왼쪽, 서쪽이 오른쪽)**으로 둘 다 통일("처음 방향으로"도 같은 시점).
5. **L8**: 1024×768 기본 화면에서 "이번 달만/계절 대표 3개" 버튼이 한 줄에 들어가게.
6. **L11·L12**: 크게 보기 막대가 너무 높아(186px) 3D가 작아지는 것 → 막대 안에서는 값 패널 제목을 줄이거나 빼기. 막대 속 값 패널 제목 번호 "②"가 조건 카드 "①"보다 위에 오는 것 → 크게 보기에서는 번호 대신 👀(공통 틀과 같게, `enlarge()`의 `onChange`).
7. **L2**: 2D + 크게 보기(가로·세로)에서 2D 그림이 너무 커져 막대까지 스크롤해야 하는 것 → 2-1-2처럼 `.is-full`일 때 2D 그림 최대 높이.

## fix-B2: `sci-6-2-1-4`
1. **M1 손전등 끌기**: ① **움직임 문턱**(예: 6px) — 누르기만 하고 안 움직이면 각도를 바꾸지 않는다(`onEnd`에서도), ② **상대 각도** — 잡은 순간의 각도 + 끈 만큼의 변화량(2-1-5처럼), ③ **잡는 영역**을 손전등 자체로 — 공통 틀의 새 옵션 `pad: 0`(레이가 맞을 때만) + 손전등 축을 따라 보이지 않는 굵은 손잡이 메시(보이는 상태로 두고 투명 재질 `opacity:0`)로, 옆의 빈 곳을 끌면 카메라가 돈다. 30° 눈금을 누른 뒤 손전등을 살짝 건드려도 30°가 유지되는지(기록하기가 풀리지 않는지). 끌기·슬라이더·눈금 버튼 동기화는 그대로.
2. **L4**: 3D 각도 숫자 이름표(`app.js:694`)를 없앤다 — 값은 패널에만(CLAUDE.md). 호는 남겨도 된다.
3. **L6**: 휴대폰(375×812)에서 "🔦 손전등(태양)" 이름표가 토글 밑에 가리는 것 → 토글 자리를 피해 옮기기, 두 줄이 된 안내 글이 센서·그림자 끝을 가리지 않게 짧게.
4. **L2**: 2D + 크게 보기에서 2D 그림 최대 높이(2-1-2처럼).

## fix-B2: `sci-6-2-1-5`
1. **L1**: 확대 칸은 **확대 화면일 때만** 보이게(처음 방향으로 돌아오면 숨김 — 가로 기본 화면에서 '라' 자리 지구본을 가리던 것).
2. **L6**: 확대 화면에서 '가' 이름표가 토글 밑에 가리는 것(가로 기본 화면·세로 끔) → 토글 자리를 피해.
3. **L2**: 2D + 크게 보기에서 2D 그림 최대 높이.

## 확인(반드시, 앱마다)
* 고친 발견마다 review-B의 같은 재현 방법으로 전·후 수치(예: M1 누르기만 했을 때 각도 변화 0, 빈 곳 끌기 → 카메라 회전; M2 7개 시각 칸 속 각 = 태양 고도; L1 확대 칸 숨김; L2 2D에서 막대가 화면 안).
* 두 모드(기본·크게 보기) × 768×1024·1024×768·1280×800·375×812, 어두움, 2D에서 겹침·잘림·가로 스크롤 없음, 콘솔 오류 0.
* 끝까지 흐름(예상 → 필수 칸 모두 기록 → 분석 → 정리 → 마치기)과 새로 고침 뒤 이어 하기, 저장 키·`detail`·`detail.qa` 모양 그대로(2-1-4의 예상 질문 id `q1b`는 B3 그대로), 공통 틀 사본 `diff -r` 불변.
* 앱 `spec.md` 끝 개정 절에 이번 수정 한 줄씩 덧붙이기.

## 테스트 규칙(반드시)
* **개발 서버 대신 자기 정적 서버로 저장소 `public/`을 직접 서빙**: `python3 -m http.server <포트> --bind 127.0.0.1 --directory /Users/sungchul/Desktop/classroom/public` → `http://127.0.0.1:<포트>/apps/<앱>/index.html`(fix-B1 **8787**, fix-B2 **8788**, 끝나면 종료).
* **자기 전용 headless Chrome**(fix-B1 포트 **9347**, fix-B2 **9349**, 프로필은 스크래치 `…/scratchpad/fix-B1/` 또는 `…/fix-B2/`), `--headless=new --use-angle=swiftshader --enable-unsafe-swiftshader --disable-web-security --host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`, Node 24 전역 WebSocket으로 CDP(설치 금지), CDP `Fetch.enable`(`*supabase.co*`) 가짜 응답만. 실제 Supabase·Gemini 요청 금지. **아무것도 내려받지 않는다**(CDN 파일을 읽으려고 받는 것도 금지 — 필요하면 브라우저 안에서 이미 불러온 모듈로 확인).
* `localStorage.clear()` 금지(그 앱 `sci6…`·`ssUiPref:` 키만), 다른 탭·프로세스·다른 Chrome 건드리지 않기, git commit/push·실 DB 쓰기·`npm run build` 금지. 끝나면 자기 Chrome·서버 종료, 스크린숏만 `…/scratchpad/fix-B1|fix-B2/shots/`에 남기고 정리(지우기가 거부되면 보고).

## 보고서 `docs/science/sim-redesign/fix-B1-report.md` / `fix-B2-report.md`
발견 번호별 바꾼 것(파일:행)과 전·후 수치, 화면 확인 결과, 끝까지 흐름·저장 불변, 안 고친 것과 까닭, 확인 못 한 것 — 한국어로 간결하게. 대표 스크린숏 4~6장 경로(2-1-2 옆에서 본 모습 칸은 오전·한낮·오후 3장 포함).
