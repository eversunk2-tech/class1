# Build 보고서(pan) — 확대한 3D 장면을 반투명 화살표로 옮기기 (2026-09-26, spec 개정 8)

지침: `pan-instructions.md`. 검토는 다른 에이전트(`review-pan-instructions.md`). 커밋·push·실 DB 쓰기 없음.

## 요약
* 3D를 쓰는 실험 앱 **20개 모두**(공통 틀 `sim3d.js`·`style-common.css`)에 이동 화살표 ▲▼◀▶를 붙였다. **앱 `app.js`·`style.css`는 하나도 고치지 않았다**(겹치는 앱이 없었다).
* 겉모양(더 반투명하게)과 확대 뒤 아래 이동 안내 줄은 사용자 피드백으로 바뀌었다 — **8절**(바꾼 뒤 행렬 100/100·세부 34/34·흐름 7/7 다시 통과, 스크린숏 `showcase2/`).
* 시험 결과(처음 구현): 행렬 **240/240**(20앱 × 6크기 × 기본 화면·크게 보기), 흐름 **19/19**, 세부 **96/96**(3앱 × 32항목), 물체 끌기 **6/6**, 배치 회귀 **240/240 + 확대 상태 40/40 같음**, 콘솔 오류 0, 실제 Supabase·Gemini 요청 0.

## 1. 바꾼 것
| 파일 | 요지 |
|---|---|
| `scripts/templates/science-sim/sim3d.js` | `create()`에 "확대한 장면 옮기기" 절 추가: 화살표 UI(`.ss-pan` 안 버튼 4개, SVG 아이콘), 화면 기준 이동(`panDelta`)·한도, 누르기·누르고 있기(`startPan`/`stepPan` — 렌더 루프에서), 보이기·흐림 판단(`controls`의 `change`), 겹치지 않는 자리 잡기(`layout`). `resetView`·`flyHome`·`focus`는 하던 이동을 멈추고, `resize`는 자리를 다시 잰다. 탭 숨김·창 떠남(`blur`)이면 멈춤, `dispose`에서 모두 뗀다. 선택 옵션 `groundY`(바닥 높이, 기본 0). 머리 주석에 설명 |
| `scripts/templates/science-sim/style-common.css` | `.ss-pan`·`.ss-pan-btn`(흰 반투명 원·굵은 화살표·44px·`touch-action:none`), 누름 `.is-pressed`, 한도 `.is-limit`, 어두움 모드 (+28줄) |
| `scripts/templates/science-sim/README.md` | 파일 표의 `sim3d.js` 줄 + "추가 기능(2026-09-26 spec 개정 8)" 절(보이는 때·이동 방식·한도·초기화·끼어들지 않음·자리·모양/접근성) |
| `public/apps/sci-*/science-sim/`(20개) | 정본 복사 — `diff -r` **20/20 같음** |

## 2. 정한 값과 까닭
| 항목 | 값 | 까닭(써 보고 정함) |
|---|---|---|
| 보이는 때 | 카메라~과녁 거리 < 화면 맞춤 거리 × **0.92** 또는 과녁이 처음 중심에서 맞춤 거리의 **1%** 넘게 옮겨짐 | 휠 두 번(0.90)·두 손가락 조금 확대부터 보이고, 처음 시점·아주 작은 흔들림에서는 숨는다. 앱이 다가간 시점(6-2-3-3 나침반, 6-2-1-5 확대 화면 등)도 확대라 보인다(지침대로) |
| 한 칸 | 지금 보이는 높이의 **12%**(3D 칸 높이 기준 — 아래 표) | 장면의 1/8쯤: 두세 번 누르면 옆 기구가 오고, 방금 보던 곳은 화면에 남는다. 0.2초 동안 부드럽게(움직임 줄이기면 바로) |
| 누르고 있기 | 초당 보이는 높이의 **60%**(한 칸의 5배), 누르는 즉시 시작·짧게 떼면 한 칸을 채우고 멈춤 | 1초에 보이는 높이의 60%. 틀 끝까지 ◀ 1.5~4.0초(행렬 측정). 장면이 멈칫해도 한 장면에 0.1초어치까지만 옮겨 튀지 않게 |
| 한도 | 과녁(화면 가운데)이 **처음 시점 카메라로 보았을 때 장면 틀 안**: 좌우 `width/2`, 위아래 `depth×0.4`(computeFit이 세로로 맞추는 depth×0.8의 절반) — 원근에 맞춰, 앞뒤(처음 시점 방향) `depth/2`. **바닥**(`groundY`=0 = 탁자 윗면, 중심이 더 낮으면 중심) 아래로는 안 가고 ▼는 **탁자 면을 따라 미끄러짐**. 끝에서는 멈추고 그쪽 화살표를 흐리게(`aria-disabled`) | 틀의 어느 부분이든 가운데로 가져올 수 있고 그 밖으로는 못 간다. 처음에는 축 상자(x·z ± 틀, 높이 바닥~중심+depth/2)로 했으나 내려다보는 장면(6-1-1-2)에서 ▲ 끝이 탁자 뒤 빈 하늘까지 가고, ▼→▲ 순서에 따라 닿는 곳이 달라져 바꿨다. 바닥 미끄러짐이 없으면 내려다보는 장면에서 ▼가 1칸 남짓에서 막히고, 바닥이 없으면 옆에서 보는 장면(6-2-2-2)에서 카메라가 탁자 아래로 간다 |
| 자리 | 가장자리 가운데(가장자리에서 8px)에서 시작 → 장면 칸(`.ss-exp-view`) 안의 보이는 다른 요소와 **4px 이상 떨어진 가장 가까운 자리**(▲는 아래로, ▼는 위로, ◀▶는 위아래로 번갈아 2px씩). 바탕 없는 글(드래그 안내)은 **글자 줄만** 피한다(… 줄임으로 잘린 부분 제외). 보이는 동안 0.25초마다 다시(누르는 동안은 그대로) | 앱마다 HUD·배지·확대 칸·실행 중 안내 글 자리가 달라 CSS 고정 자리로는 겹침(예: 6-2-3-3 HUD가 위 가운데까지, 6-2-1-5 확대 칸, 실행 중 아래 가운데 `ov-text`·`hud-ff`)을 피할 수 없었다 |
| 모양 | (처음) 흰 반투명 원(바탕 55%, 테두리 30%, 약한 그림자) + 굵은 V 화살표(3px 선) → **사용자 피드백으로 더 반투명하게 바꿈**(원 20%·어두움 10%, 그림자 없음, 꺾쇠에 빛 테, 한도 0.3 — 8절), 44×44px | 대비는 8절 표 |

**3D 칸 높이와 한 칸 크기**(행렬 측정, 앱마다 칸 높이가 다름)
| 크기 | 기본 화면: 칸 높이 → 한 칸 / 초당 | 크게 보기: 칸 높이 → 한 칸 / 초당 |
|---|---|---|
| 1024×768 | 305~474px → 37~57px / 183~284px | 422~493px → 51~59px / 253~296px |
| 768×1024 | 367~449 → 44~54 / 220~269 | 298~391 → 36~47 / 179~235 |
| 1180×820 | 357~507 → 43~61 / 214~304 | 474~545 → 57~65 / 284~327 |
| 1920×1080 | 538~558 → 65~67 / 323~335 | 712~805 → 85~97 / 427~483 |
| 812×375 | 298 → 36 / 179 | 170~245 → 20~29 / 102~147 |
| 375×812 | 208~298 → 25~36 / 125~179 | 216~345 → 26~41 / 130~207 |

## 3. 검증(자기 정적 서버 8788=작업 트리, 8789=`08f2fa8` 사본, 자기 headless Chrome 9353, 캐시 끔)
### 3-1. 행렬 — 20앱 × {1024×768, 768×1024, 1180×820, 1920×1080, 812×375, 375×812} × {기본 화면, 크게 보기} = **240/240**
한 번마다: 처음 화살표 없음 → 휠 4번(거리 0.815) 뒤 4개 보임·모두 44px → **겹침 0**(확대 직후·◀ 한도·▲ 한도에서 3번 = 720번, 토글·모형 배지·HUD·안내 글 글자 줄·막대·앱 표시·화살표끼리·칸 밖) → 방향마다 한 번 누름: 모두 **정확히 0.120**(보이는 높이 대비), 옆으로 샘 < 0.02, 카메라 방향 변화 < 1e-4(돌지 않음), 옛 화면 가운데 점이 반대쪽으로 움직임(◀ → 오른쪽, ▲ → 아래 = **좌우·위아래 뒤집힘 없음**) → 1초 누르고 있기 0.44~0.60 → ◀ 한도(흐림·`aria-disabled`·틀 안) → ▼ 한도(과녁이 바닥 아래로 안 감·카메라는 바닥 위·틀 안) → ▲ 한도(틀 안) → **두 번 탭 → 숨김·처음 시점**(거리 비 1.000, 옮김 0) → 다시 확대·이동 → **"🎥 처음 방향으로" → 숨김** → 크게 보기 페이지 스크롤 0 → 콘솔 오류 0. (headless SwiftShader fps 21~62 — 1920×1080 크게 보기가 가장 느림, 그래도 통과.)

**앱별**(모두 12/12). 가장자리 가운데에서 자리를 옮긴 곳(다른 요소를 피함, 켬=크게 보기·끔=기본 화면):
| 앱 | 자리를 옮긴 곳 |
|---|---|
| 6-1-1-1·-2·-3·-4, 6-1-2-1·-2·-5, 6-2-1-3·-5, 6-2-2-2, 6-2-3-1·-2 | 812×375 켬 ◀ 30px 아래(칸 높이 172px — 왼쪽 위 토글 피함) |
| 6-1-2-3 | 812×375 켬 ◀ +32, 375×812 켬 ◀ +8 |
| 6-1-2-4 | 812×375 켬 ◀ +30, ▶ +12(초시계 HUD) |
| 6-2-1-2 | ▶ +10~+70("옆에서 본 모습" 칸 — 1024×768 끔, 768×1024 끔·켬, 812×375 끔) |
| 6-2-1-4 | 812×375 켬 ◀ +30, ▶ +34(조도 HUD) |
| 6-2-2-1 | 812×375 켬 ◀ +32, 375×812 ▲ 72(앱의 오른쪽 위 배지 `ov-badge` 아래)·켬 ◀ +5 |
| 6-2-2-3 | 812×375 켬 ◀ +30 ▶ +10(상태 HUD), 375×812 끔 ◀ +12 |
| 6-2-2-4 | ▲ 50(1024×768 끔, 812×375 켬 — HUD 아래)·42(375×812), 812×375 켬 ◀ +30 |
| 6-2-3-3 | ▲ 50(1024×768 끔·켬, 1180×820 끔, 1920×1080 끔, 812×375 켬 — 스위치 HUD 아래)·42(375×812), 812×375 켬 ◀ +30 |
▼는 드래그 안내 글 줄과 겹치지 않는 곳(글이 가운데까지 오면 그 위 — 칸 아래에서 8~72px).

### 3-2. 흐름 — 확대·이동한 채 실행 → 관찰 → 기록 **19/19**
7앱(6-2-3-3 **나침반 다가가기**(실험 3 — 실험 1·2 기록을 미리 넣음), 6-2-1-5 **확대 화면**, 6-2-1-2(앱 자체 기록), 6-1-1-1(낮은 시점), 6-2-2-3(빨리 감기), 6-2-2-4, 6-1-2-3(따라가는 카메라)) × {1024×768 켬, 768×1024 켬} + 5앱 × 1024×768 끔.
* 휠 확대 → ▶·▲ 한 번씩(물체 고르기 상태 그대로 = 누름이 `onPick`으로 새지 않음) → 실행 버튼(막대) → **실행 중 ◀를 1.5초 누르고 있기** → 관찰 카드 → 보기·확인 → 기록하기 → **기록 +1**(19/19).
* 실행 뒤 앱이 다가간 시점(거리 비 6-2-3-3 0.46, 6-2-1-5 0.37, 6-1-1-1 0.30~0.42, 6-2-2-4 0.62, 6-1-2-3 0.42)에서 **화살표가 보이고 ◀ 한 칸**(0.116~0.120, 카메라 방향 그대로). 6-2-2-4는 앱이 틀 끝의 병으로 다가가 ◀가 한도(0~0.021). 기록 뒤 앱이 처음 시점으로 돌아가는 앱(6-2-1-5·6-1-1-1·6-2-2-4)은 화살표가 숨는다.
* 실행 중·실행 뒤 **겹침 0**(실행 중 0.4초마다 재고, 0.3초 넘게 남는 겹침만 셈 — 안내 글 `ov-text`·빨리 감기·확대 칸이 나타나도 0.25초 안에 자리를 옮김). 크게 보기 페이지 스크롤 0, 콘솔 오류 0.

### 3-3. 세부 — 6-2-3-3 1024×768, 6-2-1-4 768×1024, 6-1-1-1 375×812 각 **32/32**
* 터치: 0.8초 누르고 있기 → 0.48, 누른 채 화살표 밖으로 → 바로 멈춤(0), 누르는 동안 카메라 안 돎, **화살표 두 번 톡 = 두 칸**(0.240 — 처음 방향으로 안 감), 캔버스 두 번 톡 → 처음 시점·숨김, 캔버스 한 손가락 끌기 → 돌리기 그대로(44~77°), 터치 취소(`pointercancel`) → 바로 멈춤.
* 키보드·접근성: Tab 순서 ▲◀▶▼ → 토글 → 막대, `role="group"`·"확대한 화면 옮기기"·버튼 `type=button`·이름("화면을 오른쪽으로 옮기기" 등)·아이콘 `aria-hidden`, 초점 표시(3px 보라 테두리 `:focus-visible`), **Enter·Space = 한 칸**(0.120), **Enter 누르고 있기(반복 10번) = 한 칸**, 창 떠남(`blur`) → 멈춤, 마우스 누른 채 밖으로 → 멈춤(뗀 곳이 캔버스여도 돌리기·고르기 없음), 오른쪽 버튼 무시.
* 2D로 보기 → 화살표 없음, 3D로 돌아오면 숨김(처음). 움직임 줄이기: 짧게 누르면 70ms 안에 한 칸을 다 감(보통은 0.038 → 0.2초에 0.120), 누르고 있기는 그대로 계속(0.48). 어두움·밝음 색 확인.
* 앱 카메라 연출과: 누르고 있는 동안 `v.focus` → **연출 경로 그대로**(from→to 직선에서 벗어남 3e-15 이하, 끝 자리 정확) + 누르던 이동은 멈춤(끝난 뒤 저절로 안 감). 앱 자체 `v.tween`(카메라·과녁 lerp) → 경로 그대로(3e-15 이하), **트윈이 끝난 뒤 누르고 있던 이동을 잇는다**. `v.draggable` 물체를 끄는 중 다른 손가락으로 화살표 → 카메라 안 움직임.

### 3-4. 물체 끌기(기존 동작) — 6-2-1-2 태양·6-2-1-4 손전등·6-2-1-5 지구본 × {1024×768, 768×1024} 크게 보기 **6/6**
확대(거리 0.857) + ◀ 한 칸 옮긴 채 물체를 끌면 앱 값이 바뀐다(태양 9:30 → 11:30·고도 34.5° → 49.7°, 손전등 45° → 68°·빛의 세기 707 → 927, 지구본 위치 선택), 끄는 동안 카메라 회전 0.

### 3-5. 배치 회귀(`08f2fa8` 사본 ↔ 작업 트리, 머리말·실험하기 섹션·아래 막대의 모든 요소 `getBoundingClientRect`, 화살표 묶음은 빼고)
* 처음 화면: 20앱 × 6크기 × {끔, 켬} **240/240 같음**(요소 34,542개, 다른 자리 0, 한쪽에만 있는 요소 0, 페이지 높이·캔버스 크기·크게 보기 여부 같음, 콘솔 오류 0/0).
* 확대해서 화살표가 보이는 상태: 5앱(6-2-3-3·6-2-1-5·6-2-2-4·6-1-2-4·6-2-1-2) × {1024×768, 768×1024, 375×812, 812×375} × {끔, 켬} **40/40 같음**(요소 5,000여 개) — 화살표는 절대 위치라 다른 요소를 밀지 않는다.
* 행렬·흐름에서 크게 보기 페이지 스크롤 0.

### 3-6. 정적 검사
`npm run lint` 통과(공통 틀·앱은 eslint 대상 밖이라 아래로 확인), `node --check` 정본 15개 + 앱 `sim3d.js` 20개 오류 0, `git diff --check` 깨끗, 더한 줄에 `console.log`·`debugger`·`localStorage.clear()` 없음, `diff -r` 20/20.

## 4. 알려진 한계·확인 못 한 것
1. **6-2-2-3 "실험해요 2"**: 앱이 정한 처음 시점이 sim3d의 처음 시점보다 가까워(거리 비 0.72) **화살표가 처음부터 보이고**, "🎥 처음 방향으로"(앱이 그 시점으로 되돌림) 뒤에도 보인다(화살표는 정상 — ▶ 한 칸 0.120, 겹침 0). "실험해요 1"은 처음에 숨김. 없애려면 sim3d에 "앱이 정한 처음 시점" 알림(예: `v.setHome(pose)`)을 두고 앱이 부르게 해야 한다 — 앱 JS 수정이라 이번 범위 밖(결정 필요).
2. **Tab 순서**: 화살표 묶음이 3D 칸 안(캔버스 옆)이라 장면 위 토글보다 먼저 Tab을 받는다(▲◀▶▼ → 토글 → 막대).
3. **3D 이름표·물체**(캔버스에 그린 것)는 피하지 못한다 — 반투명 원 뒤로 비친다(예: 03 스크린숏의 "스위치" 이름표). DOM 표시는 모두 피함.
4. **휴대폰 가로 크게 보기(3D 칸 510×170)**: 칸이 낮아 ▼가 드래그 안내 줄 바로 위(칸 가운데쯤), ◀는 토글 아래로 30px — 겹침은 없지만 좁다. 한 칸이 20~29px로 작다.
5. **기본 화면 + 낮은 화면**(812×375 끔 등)은 장면이 머리말·아래 막대 사이보다 커서 페이지를 굴려야 보이는 화살표가 있다(기본 화면의 원래 한계 — 시험은 학생처럼 굴려서 눌렀다).
6. **한도 끝**에서는 과녁을 틀 끝까지 보낼 수 있어 화면 절반쯤이 틀 밖(빈 배경·탁자 밖)으로 보일 수 있다(지침의 "과녁이 틀 밖으로 나가지 않게" 기준).
7. 화살표 위에서 휠을 굴리면 확대되지 않는다(캔버스가 아니라서 — 기본 화면이면 페이지가 굴러감).
8. **실제 iPad Safari는 확인 못 함**(headless Chrome + 터치 흉내만): 길게 누르기·손가락이 화살표 밖으로 나갈 때(암묵적 포인터 붙잡기 — 자리로 판단하게 했음)·실제 fps·보이스오버. 화면 맨 위 두 줄 알림(토스트)이 접힌 머리말 아래 ▲와 잠깐 겹칠 수 있는지는 보지 않았다(알림은 누르기를 통과시킴).
9. 같은 시간에 다른 작업이 `public/apps/sci-6-2-1-3/`(fix-2, `app.js`)을 고치는 중이다 — 이 보고서의 6-2-1-3 결과는 그 변경 전 상태 기준.

## 5. 사용자에게 보여 줄 스크린숏(`/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/pan/showcase/`) — 처음 모양. 바뀐 모양은 8절 `showcase2/`
* `01BeforeZoom.png` — 전자석(6-2-3-3), 태블릿 가로 1024×768 크게 보기, 확대 전: 화살표 없음
* `02ZoomedArrows.png` — 휠로 확대한 뒤: 가장자리 가운데 반투명 ▲▼◀▶(▲는 스위치 HUD 아래, ▼는 드래그 안내 줄 위)
* `03MovedRight.png` — ▶를 0.7초 누른 뒤: 장면의 오른쪽(고리·스위치)이 보임, 방향 그대로
* `04PortraitTablet.png` — 태블릿 세로 768×1024 크게 보기, 확대 + ◀로 조금 옮긴 모습

## 6. Supabase·Gemini 차단 기록
* Chrome을 `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`로 띄우고, 페이지마다 첫 로드 전에 CDP `Fetch.enable(*supabase.co*)` 가짜 응답 + `Network.setCacheDisabled`.
* 끝까지 돈 시험 27번에서 가로챈 요청 **1,458건 — 모두 `GET /rest/v1/site_settings`(로그인 잠금 스위치, 가짜 `login_required:false` = 체험 모드)**, 가로채지 못하고 나간 요청 **0**, Gemini 요청 **0**. (중간에 멈춘 병렬 시험도 같은 설정이었다.)

## 7. 시험 도구(스크래치, 참고용)
`…/scratchpad/pan/lib/` — `cdp.mjs`(포트 9353·자기 창), `hook.js`(시험 전용: 만들어진 뷰·끌기 물체를 모음), `panhelp.js`(재기·겹침), `pan-matrix.mjs`, `pan-flows.mjs`, `pan-detail.mjs`, `pan-drag.mjs`, `pan-compat.mjs`, `e2-check.mjs`, `showcase.mjs`. 결과 JSON·로그는 `…/pan/out/`. 서버 2개와 Chrome은 종료했다. 크롬 프로필(`pan/profile`)·비교용 사본(`pan/base`)·확인용 스크린숏(`pan/shots`) 지우기는 권한 거부로 못 했다(지워도 됨).

## 8. 사용자 피드백 반영(겉모양·아래 안내 줄, 2026-09-26)
사용자 말: 처음 "반투명보다는 불투명에 가까워… 뒤의 이미지가 거의 그대로 비치는 반투명(연한 흰색 계통)… ▲▼◀▶ 모양" → 정정 "화살표 저 이미지 그대로 해도 될거 같아. 더 반투명으로만 바꿔줘. 그리고 확대후에는 아래쪽에 화살표로 화면 이동을 할 수 있다는 문구를 간단하게 추가해주면 될거 같아". 삼각형 안은 정정이 와서 넣지 않았다(모양은 원 + 꺾쇠 그대로).

**바꾼 것**(정본만 고친 뒤 20개 사본 복사, `diff -r` 20/20 같음)
* `style-common.css` — 원 바탕 흰 55% → **20%**(어두움: 어두운 55% → **흰 10%**), 테두리 30% → 16%(어두움 흰 24%), **그림자 없앰**. 꺾쇠(모양·3px 그대로): 밝음은 어두운 선 75% + 흰 빛 테(`filter: drop-shadow` 3겹), 어두움은 밝은 선 85% + 검은 빛 테 — 원이 옅어도 어두운 장면·밝은 물체 위에서 선이 보이게. 누름 `.is-pressed` 원 45%(어두움 26%)·선 진하게, 마우스 올림 34%(18%), 한도 `.is-limit` opacity 0.4 → **0.3**. 누르는 곳 44px·초점 표시·`aria-label`·자리 계산은 그대로.
* `sim3d.js` — `setTip()`: 화살표가 보이는 동안 장면 칸의 드래그 안내 줄(`.ss-view-tip`) 내용을 **"👆 화살표: 화면 옮기기 · 두 번 탭: 처음 방향"**(`span.ss-pan-tip`)으로 바꿔 두고, 화살표가 숨거나(처음 시점·`resetView`·`flyHome`) 3D를 뗄 때 원래 안내(앱별 끌기 안내·휴대폰 덧붙임 포함)로 되돌린다. 자리를 재기 전에 바꿔 화살표가 새 줄을 피한다. 화살표가 보이는 동안 앱이 안내 줄을 새로 쓰면 `tick()`이 0.25초 안에 그 새 글을 맡아 두고 다시 바꾼다(되돌릴 때 옛 글로 덮지 않음 — 6-2-2-3 실험해요 2처럼 처음부터 화살표가 보이는 시점에서도 이동 안내가 나옴). 이동·한도·자리 계산 코드는 그대로.
* README 7번(모양)·8번(아래 안내 줄), `sim3d.js` 머리 주석.
* 줄 뒤에 **붙이지 않고 바꾼** 까닭: 붙이면 휴대폰에서 2~3줄 → 3~4줄, 태블릿에서도 긴 안내(6-2-1-5 등)가 두 줄이 되어 장면 아래 44px에 뜨는 앱 안내 글(`ov-text`)·빨리 감기 표시(`hud-ff`)와 겹친다. 바꾸면 줄이 늘지 않는다(모든 크기 **1줄**, 폭 204~221px — 휴대폰 가로 크게 보기의 … 줄임에도 다 보임).

**확인**
* 행렬(바꾼 뒤 다시): 20앱 × {1024×768, 768×1024, 812×375 크게 보기} + {375×812, 1920×1080 기본 화면} = **100/100** — 확대 뒤 안내 줄 = 이동 안내 1줄(전 1~3줄 → 1줄), 안내 줄 ↔ 토글·배지·HUD·막대·화살표 겹침 0, 화살표 겹침 0, 두 번 탭·"처음 방향으로" 뒤 원래 안내 글자 그대로, 기존 항목(한 칸 0.120·누르고 있기·한도·방향 유지) 통과, 콘솔 오류 0.
* 세부 **34/34**(6-2-3-3 1024×768 — 새 색 값·누름·한도·안내 줄 포함), 흐름 **7/7**(6-2-3-3·6-2-1-5·6-1-1-1·6-2-2-1·6-2-3-1·6-2-2-4·6-2-3-2, 크게 보기 1024×768: 실행 중·뒤 화살표와 안내 줄 겹침 0, 기록 +1), 6-2-2-3 실험해요 2 안내 줄도 이동 안내(실험해요 1은 원래 안내).
* lint·`node --check`(정본 15 + 사본 20)·`git diff --check` 통과, 더한 줄에 디버그 코드 없음.

**대비**(꺾쇠가 알아보는 부분 — 화면 픽셀은 화살표 자리를 2배로 찍어 원 안 가운데값(비친 배경)과 어두운/밝은 2% 픽셀을 비교, 계산은 빛 테를 뺀 합성 색)
| 경우 | 화면 픽셀 | 계산(빛 테 뺌) |
|---|---|---|
| 밝은 장면(6-2-3-3·6-1-1-2·6-2-1-2, 휴대폰 포함) | 어두운 선 **3.0~6.1:1**(휴대폰 가로 ▲가 코일 위일 때 3.0) | 5.3~6.6:1 |
| 어두움 모드(6-2-3-3) | 밝은 선 **4.6~7.5:1** | 4.5~8.2:1 |
| 밝음 모드 + 어두운 장면(6-2-1-4 어두운 교실) | 흰 빛 테 **3.1~3.6:1**(선 자체 1.0~1.8) | 선 1.6~1.8:1 |
| 어두움 모드 + 흰 물체·불꽃 위 | 재지 못함 | 선 1.1~1.3:1 — 검은 빛 테에 기댐 |
| 원 자체 ↔ 장면 | — | 1.0~1.9:1(요청대로 거의 안 보이게) |
3:1 미만인 것: 원 자체(일부러), 밝음 모드의 어두운 장면·어두움 모드의 흰 물체 위에서 **선 자체**(빛 테로 보이게 함 — 어두운 교실에서 잰 빛 테는 3.1:1 이상). 어두움 모드에서 흰 물체 바로 위에 화살표가 오는 경우는 실제로 재지 못했다.

**한계**
* 확대한 동안에는 앱별 끌기 안내(예: 6-2-1-2 "해를 끌면 시각이 바뀌어요", 6-2-1-4 손전등, 휴대폰의 "페이지를 위아래로…" 덧붙임)가 이동 안내로 바뀌어 안 보인다 — 처음 시점으로 돌아가면 다시 보인다.
* 6-2-1-3 휴대폰(폭 640px 이하)은 앱 CSS가 3D 칸을 안내 줄 위에서 끝내서(`--tip-h`), 안내 줄이 2줄 → 1줄이 되면 3D 칸이 조금 커지고 카메라가 다시 맞춰진다(확대 비율은 그대로). 그 앱의 `app.js`·`style.css`는 다른 작업이 고치는 중이라 건드리지 않았다.
* 캔버스에 그린 3D 이름표는 안내 줄 글자 뒤에 비칠 수 있다(전과 같음).

**스크린숏**(`/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/pan/showcase2/`, 모두 확대 뒤 화살표 + 아래 이동 안내)
* `01ElectromagnetLandscape.png` — 전자석 6-2-3-3, 태블릿 가로 1024×768 크게 보기
* `02BrightScene.png` — 밝은 탁자 6-1-1-2(원 너머로 이름표 글자가 비침)
* `03DarkMode.png` — 6-2-3-3 어두움 모드
* `04PortraitTablet.png` — 6-2-1-2 태블릿 세로 768×1024 크게 보기
* `05PhoneLandscape.png` — 6-2-3-3 휴대폰 가로 812×375 크게 보기
* `06DarkSceneLightMode.png` — 6-2-1-4 어두운 교실(밝음 모드)
* `07PhonePortraitDefault.png` — 6-1-1-2 휴대폰 세로 375×812 기본 화면(안내 줄 3줄 → 1줄)
* `08ArrowCloseups.png` — 위 7장의 왼쪽 화살표 4배 모음(`*-left-arrow-4x.png`)

**Supabase·Gemini**: 이번 시험 6번(기록된 것)에서 가로챈 요청 129건 모두 `GET /rest/v1/site_settings`, 나간 요청 0, Gemini 0(전체 누계 33번·1,587건·0). 빛 테 비교·실험해요 2 확인 같은 짧은 도우미 스크립트도 같은 차단 설정(페이지마다 `Fetch.enable` + host-resolver-rules, 캐시 끔)으로 돌렸다.
