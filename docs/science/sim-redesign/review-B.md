# 검토 보고서(Review B) — 단계 B: 2학기 1단원 4개 앱 재설계 (2026-09-25)

지침: `review-B-instructions.md`. **코드는 고치지 않았다**(발견만). 읽은 것: CLAUDE.md, spec.md §4·개정 1~4, audit.md 2학기 1단원 절, build-B-instructions.md, Build 보고서 4개, 공통 틀 README "추가 기능 2026-09-25", 4개 앱 spec.md 개정 2, `git diff`(4개 앱·공통 틀 lesson.js).

**한 줄 요약**: 과학(수치·싱크·방향)·저장 구조는 모두 맞다(높음 0). 중간 3건 — ① 2-1-4 손전등을 **누르기만 하거나 옆 빈 곳을 끌어도 각도가 바뀜**, ② 2-1-2 처음 시점에서 **12:30·13:30의 호가 옆으로 서서 가장 작게 보임**, ③ 태블릿 세로 첫 진입 때 막대 위치가 앱마다 다름(2-1-3만 스스로 내려가 '실험 방법'을 건너뜀).

---

## 1. 발견(심각도별)

### 높음 — 없음

### 중간
| # | 앱·파일:행 | 무엇 | 재현 | 제안 |
|---|---|---|---|---|
| M1 | 2-1-4 `app.js:815-834`(끌기), 공통 `sim3d.js:364`(잡는 영역) | ① **손전등을 짧게 누르기만 해도 각도가 바뀐다**: `onEnd`가 움직임과 관계없이 놓은 점의 각도(절대값)로 `setAngle`. 30°에서 머리 위 가장자리 누름 → **36°**, 빨간 버튼 → 34°, 몸통 아래 가장자리 → 27°(터치도 같음, 36° 저장). ② **손전등 옆 빈 곳을 끌어도 손전등이 잡힌다**: 잡는 영역이 기울어진 손전등의 화면 **경계 상자+30px**라 3D 칸의 13~15%(45°·30°), 8~10%(80°). 45°에서 몸통 옆 5칸 떨어진 빈 하늘을 끌었더니 카메라는 안 돌고 각도가 **45°→72~73°**로 튐. 30° 눈금 버튼을 누른 뒤 손전등을 만지면 '30° 기록하기'가 풀린다(기록 값 자체는 화면 각도 그대로라 자료 오류는 아님) | 1024×768·768×1024, 30° 눈금 → 손전등 머리 위 모서리 탭 / 45°에서 손전등 오른쪽 위 빈 곳 끌기 | 2-1-5처럼 **상대 각도**(잡은 순간 각도 + 변화량)와 **움직임 문턱**(6px 또는 2°) — 안 움직였으면 `onEnd`에서 바꾸지 않기. 잡는 영역은 공통 틀에 `pad:0`/정밀 판정 옵션을 더하거나 손전등 축을 따라 가는 가는 손잡이만 등록(Claude 판단) |
| M2 | 2-1-2 `app.js:360`(처음 시점 `viewDir`), `:729-746, 836-841`(호) | 호의 **각 크기는 정확**(=태양 고도)하지만 처음 시점(북쪽에서 14°)에서는 호가 담긴 수직면을 거의 옆에서 봐서 **화면에 보이는 각**이 9:30 28° · 10:30 32° · 11:30 30° · **12:30 13° · 13:30 12°** · 14:30 23° · 15:30 22°(768×1024·1024×768 같음). 태양 고도가 가장 높은 12:30에 호가 가장 작아 보인다 — "선과 땅의 각 = 태양 고도"를 눈으로 비교하면 거꾸로 읽힐 위험(오개념). Build도 "가늘다"고 적었으나 각 크기 역전은 판단이 필요 | 크게 보기(768×1024) 12:30 눈금 → 그림자 끝의 빨간 호 | 사용자 확인 후: (가) 처음 시점을 동·서쪽으로 더 돌림(좌우 방향 유지 범위 안), (나) 2-1-5처럼 장면 안 작은 **"옆에서 본 모습" 칸**(나무·빛줄기·호를 늘 정면으로), (다) 그대로 두고 2D 옆모습을 안내 |
| M3 | 공통 `experiment.js:410`(`applyForOrientation`, 첫 진입 스크롤 없음 — review-A2 L1), 2-1-3 `app.js:999-1009` | 태블릿 세로(크게 보기 기본) **첫 진입**(알아 두기 펼침) 때 막대가: 2-1-2 **1018~1144px(아래 이동 막대 위 955px보다 완전히 아래)**, 2-1-5 935~1037(대부분 가림), 2-1-4 842~1008(기록 버튼 45px 가림). 2-1-2 Build 보고서의 "세로 태블릿 첫 화면 안"은 장면을 맞춘 뒤의 값이다. 2-1-3만 앱이 장면으로 스크롤(768×1024 306px, 1024×768 278px)해 막대는 보이지만 **펼쳐진 '🔎 실험 방법 알아 두기'와 안내 줄이 화면 위로 밀려 첫 진입에 안 보인다**(1024×768 기본 화면에서도). 4개 앱이 서로 다르게 움직임 | 앱 키만 지우고 실험하기로 처음 들어가기 | 앱마다 두지 말고 **공통 틀 한 규칙**으로(예: 알아 두기가 펼쳐진 첫 진입은 그대로 두고 막대 대신 "▼ 실험 화면으로" 한 줄, 또는 알아 두기를 접으면 맞춤). 정하기 전까지 2-1-3의 `activateExp` 스크롤은 빼는 편이 일관적 |

### 낮음
| # | 앱·파일:행 | 무엇 | 제안 |
|---|---|---|---|
| L1 | 2-1-5 `app.js:1045`(`bits.inset.show(st.shown)`), `:1206` | 확대 칸이 **전체 화면(처음 방향)에서도 남는다** → 마지막 칸(기울임·라)을 기록해 처음 방향으로 돌아오면 가로 기본 화면(1024×768·1280×800)에서 **라 자리 지구본이 확대 칸 밑에 가려짐**, 세로 크게 보기에서는 '라' 이름표 일부 가림(showcase 07·12). 확대 전 "처음 방향으로"를 눌러도 같음 | 확대 칸은 확대 화면일 때만(`st.shown && closeNow`) 보이기 |
| L2 | 2-1-3·2-1-4·2-1-5 `style.css`(2D 높이 제한 없음; 2-1-2만 `style.css:188-189`) | **2D + 크게 보기(가로)**: 2D 그림이 가로 전체로 커져(높이 769~938px) 막대가 아래 이동 막대보다 238~407px 아래 — 스크롤해야 기록. "2D로 보기" 버튼으로 누구나 들어갈 수 있다. 세로도 2-1-4 +159·2-1-5 +152px | 2-1-2처럼 `.is-full`일 때 2D 그림 `max-height` |
| L3 | 2-1-3 `app.js:652` | "계절 대표 3개" 선이 가늘고(0.035) 옅어(55%) 태블릿에서 잘 안 보임 — 특히 **여름(주황) 선이 파란 하늘 반구 위에서 흐림**, 10월 등 가을의 굵은 갈색 선과 색도 비슷(showcase 04) | 대표 선 굵기·불투명도 올리기(예: 0.05·0.85) |
| L4 | 2-1-3 `app.js:634`(3D), `:810`(2D); 2-1-4 `app.js:694`(각도 숫자) | 바뀌는 값 이름표가 장면에 남음: 2-1-3 "남중 고도 ○○.○°"(6월에 '태양 고도 측정기' 이름표·막대 끝을 25~54·231~256px² 가림), 2-1-4 각도 숫자. CLAUDE.md "바뀌는 값은 값 패널에"와 spec §4.6 "작은 호+캔버스 숫자 허용"이 엇갈림(둘 다 B 이전부터 있던 것) | 사용자에게 해석 확인. 남기면 2-1-3 이름표를 측정기와 겹치지 않는 자리로 |
| L5 | 2-1-2 `app.js:986-1099`(2D 옆모습), 2-1-3 `app.js:473`(3D 처음 시점) | 방향 불일치(모두 B 이전부터, 새 "방향 뒤집힘" 규칙과 관련): 2-1-2 **2D**는 오후에 위 하늘 그림의 해가 오른쪽(남서)인데 아래 옆모습은 늘 왼쪽에서 빛이 옴(14:30 확인). 2-1-3은 3D 처음 시점(남서쪽에서 봄: 동이 오른쪽)과 2D("남쪽 하늘을 바라본 모습": 동이 왼쪽)가 좌우 반대 | 단계 D 전에 사용자 확인(2-1-2 2D는 해가 서쪽이면 옆모습 좌우 반전 등) |
| L6 | 2-1-5 `app.js:910`, 2-1-4 `app.js:681` | 이름표↔토글: 2-1-5 확대 화면에서 '가' 이름표가 토글 밑(가로 기본 화면·세로 끔 825~1495px²). 휴대폰 375×812에서 2-1-4 "🔦 손전등(태양)"이 토글 밑(45° 852, 60~80° 574~771px², 크게 보기 700px²) — Build의 "살짝"보다 큼. 안내 글이 2줄이 되어(2-1-4 새 문구) 센서·그림자 끝을 가림 | 휴대폰은 보조 기기라 낮음. 토글 자리 피하기(오른쪽으로 이름표 이동)·안내 글 짧게 |
| L7 | 공통 `experiment.js:274, 340` | 812×375에서 학생이 크게 보기를 켜면 막대가 아래 이동 막대와 **25px**(기록 버튼 17px) 겹침(2-1-3·2-1-4·2-1-5, 2-1-2는 막대 없음). 머리말을 접어도 장면을 다시 안 맞춰 그대로 | 휴대폰 가로·기본 끔이라 낮음. 공통 틀 `fit()`을 낮은 가로 화면에도 |
| L8 | 2-1-3 `style.css:46` | 1024×768 기본 화면에서 "이번 달만/계절 대표 3개" 두 버튼이 세로로 쌓여 보기 도구 줄이 2줄(102px) → sticky 장면 칼럼이 띠보다 길어져 스크롤하면 장면 윗부분(모형 배지·토글)이 머리말 밑으로 | 한 줄 배치(글자 줄이기) |
| L9 | 2-1-4 `data/lesson-config.js:32` | 진행 중이던 학생(예전 `q1` 답 + 실험 기록)은 다시 열면 **예상하기로 돌아가 새 질문에 답해야** 한다(기록은 유지 — 확인함). 안내 문구 없음 | 설계대로. 사용자에게 알림만 |
| L10 | 2-1-2 `app.js:1557`, 2-1-3 `:1207`, 2-1-4·2-1-5(Curiosity 모듈) | `detail.qa`의 궁금한 점 질문 글은 예전 그대로("(선택)… 비워도 마칠 수 있어요.") — 화면은 다듬어졌지만 관리자 화면 질문 글과 다름(B4 보고와 같음, 설계상 의도) | 사용자·Claude 판단 |
| L11 | 2-1-2·2-1-3 | 3D 안내 글이 방위 이름표를 조금 가림(2-1-2 '북' 79px²(1024 기본), 2-1-3 '남' 84~168px²) · 2-1-3 크게 보기 막대(머리말+값+기록 버튼 186px)가 높아 1280×800에서도 3D 부분이 작음 | 모양 다듬기 |
| L12 | 2-1-3 크게 보기 | 막대 속 값 패널 제목이 "②"인데 조건 카드 "①"보다 위(공통 `create()`는 👀로 바꿈) | 번호 대신 👀 |

---

## 2. 항목별 판정

### 2.1 과학적 정확성 — 통과(앱별 숫자)
**2-1-2**(1024×768, 3D 물체 좌표를 시험 탭에서 읽음, 눈금 7곳 + 10:07·11:55·12:21·13:02·14:51)
| 확인 | 결과 |
|---|---|
| 해 − 나무 꼭대기 ∥ 태양 방향 d, 거리 | 12/12, 16.000 |
| 주황 빛줄기 끝 = 해 중심 / = 그림자 끝, 꼭대기-빛줄기 거리 | 0 / 0, 0 |
| 옅은 빛줄기 7개 ∥ d, 햇빛 조명 ∥ d | 최대 0.000001° |
| 호 각 = 태양 고도, 호 시작 = 땅(밑동 쪽)·끝 = 빛줄기, 호 위치 = 그림자 끝 | 34.5·43.5·49.7·51.7·48.3·42.2·32.8 = 패널 = 교과서, 0°·0°, 0 |
| 그린 그림자 = 10÷tan(고도) = 나무 그림자 가장 먼 점 | 14.550·10.538·8.481·7.898·8.910·11.028·15.517(세 값 같음) |
| 패널(교과서) − 그린 길이 | 눈금 ≤0.05 cm, 눈금 사이 ≤0.09 cm(10:07 11.7/11.79) — 눈금 사이는 "🏷 모형" 줄무늬·문구 |
| 그림자 방향·(해 쪽 수평) / 사람 그림자 | −1.000(12/12) / −0.998~−1 |
| 기온 | 눈금 7곳 교과서 그대로(21.1~27.0) |
| 해 끌어 놓은 곳 = 시간 바 = 패널 | 끄는 10단계 모두 같음, 12:31에서 놓으면 12:30에 맞춤 |

**2-1-3**: "이번 달만" = 고른 달 한 선만. "계절 대표 3개" = 가는 선 3개 남중 고도 **52.5°(봄·가을=3월, 청록) · 75.5°(여름=6월) · 29.0°(겨울=12월)**, 최고점 정남쪽. 고른 달이 6월이면 여름 선은 굵은 선과 같아 뺌(범례는 3개). 12칸 기록 = 교과서 12/12(52.5/12시간 10분 … 41.5/11시간 2분).
**2-1-4**: 각도 = 슬라이더 = 패널 = 3D 손전등 = 호 = HUD(끌기 8단계씩, 눈금·◀▶). 10·30·45·80·90°에서 빛의 세기 174·500·707·985·1000, 그림자 28.4·8.7·5.0·0.9·0.0 cm, 빛 자국 늘어남 5.7588·2·1.4142·1.0154·1(=1/sin), 밝기 0.305~0.9(=0.18+0.72 sin) — **HEAD와 모두 같음**.
**2-1-5**: 2.5절 표(32/32). 확대 칸 그림자 11.2 < 19.6 < 49.0 = 0.8 < 1.4 < 3.5 cm × 14 → 표 값 순서와 같음. 기록 8칸·`shadows` = 교과서 그대로.
**공통**: "모형" 배지·문구 있음, 학생 화면(숨은 단계 포함 DOM·aria-label·placeholder)에 "지도서" 0.

### 2.2 답을 먼저 알려 주기 — 통과
실험하기·기록 순간 글 전부 확인(코드 + 화면): 2-1-2 알아 두기 4문단(측정 방법 정의), 남중 정의 알림, 기록 알림 "📝 10:30 기록: 43.5° · 10.5 cm · 23.0 °C", 기록 안내·눈금 사이 안내, 3D 이름표(방위·시각·나무·백엽상) / 2-1-3 빨리 감기 배지 "…하루(낮 14시간 45분)를 약 1초로", 기록 알림(값만), 범례 / 2-1-4 기록 안내 "30°(그림자가 길 때)는 꼭 기록할 각도예요"(교과서 조건 이름), 알림(값만), 새 안내 "손전등을 끌면 각도가 바뀌어요" / 2-1-5 측정값 패널 "지구본: 라 위치 · 자전축 기울임(23.5°) · 태양이 남중할 때 · 그림자 길이 3.5 cm", 확대 칸 글 "우리나라 측정기(확대·모형) · 💡 전등 쪽 · 원 간격 1 cm", 화면 읽기 알림 "측정기가 전등을 정면으로 향해요. 그림자 3.5 cm". **"~할수록"·원인 단정·정답 문구 0**(유일한 "할수록"은 2-1-3 예상 칸 안내의 빈칸 틀 "나는 ○○할수록 ○○할 것 같아요").
2-1-4 새 예상 질문: 화면 글 = 설정 글 = 사용자 문구(코드포인트 50자 일치), 칸 안내 "계절과 그때 한낮의 태양 높이를 짧게 적어요", 힌트 2개 답(여름·높다) 없음.

### 2.3 두 모드 × 화면 크기 — 태블릿 통과(M3·L1·L2 제외), 휴대폰 낮음
| 크기 | 결과(4개 앱) |
|---|---|
| 768×1024(크게 보기 기본) | 첫 진입은 M3. 장면을 맞추면 막대가 아래 이동 막대보다 235~255px 위, 장면+막대 한 화면 |
| 1024×768·1180×820·1280×800 | 기본 끔(좌우). 켜면 장면 위 = 머리말+8px, **막대 아래 = 아래 이동 막대 −9px**(기록 버튼 −17px) — 4개 앱 모두, 머리말 접으면 장면 620·672·652px |
| 375×812 | 켜면 막대 대신/함께 장면 아래 카드, 겹침 없음(L6 이름표만) |
| 812×375 | L7(켰을 때 25px) |
| 어두움(768·1024) | 이상 없음(2-1-5 확대 칸 어두운 틀·밝은 판) |
| 2D(`?no3d=1`) | 2-1-2 통과, 나머지 L2 |
* 가로 스크롤 0, 콘솔 오류·경고 0(스윕 4개 × 약 30상태), 토글↔모형 배지 겹침 0, 장면 안 DOM(시계·빨리 감기·조도 HUD·확대 칸)↔토글·막대 겹침 0.
* 새로 연결한 토글(2-1-3·2-1-4)은 2-1-2·2-1-5와 같게 동작: 가로 끔/세로 켬 기본, 방향별 기억(`ssUiPref:sceneMode:*` 0/1), 끄면 값 노드·기록 버튼이 원래 자리, 켬·끔 4번 뒤에도 노드 1개.
* 3D 이름표 겹침(태블릿): 2-1-2 없음(12:30 해 빛무리만), 2-1-3 L4, 2-1-4 '센서'↔'스마트 기기 화면' 41~86px²·'센서'↔그림자 끝(60~80°) 75~181px²(전부터), 2-1-5 L6·'우리나라'↔막대 끝(다) 18~28px².

### 2.4 직접 조작 — 2-1-2·2-1-5 통과, 2-1-4 M1
* 2-1-2 해: 마우스(1024 기본)·터치(768 크게 보기) — 끄는 동안 시각·시간 바·해가 함께, 카메라 변화 0·카메라 조작 꺼짐 → 놓으면 켜짐, 3px 누름은 그대로, 빈 곳 끌기는 돌려 보기, 시각 점 → 14:30, 2D 해 끌기 10:00→14:30. 잡는 지름 약 96px(768)·78px(375). 두 손가락: 둘째 손가락을 더해도 첫 손가락만 따라가고 확대 안 됨(떼는 순서는 CDP 흉내 한계로 미확인).
* 2-1-5 지구본: 마우스 가→(10°) → **라에 붙고 라 선택·`globeAt` 저장**, 터치 라→195° → 나, 카메라 0 변화, 짧은 누름 무시, 빈 곳 끌기 돌려 보기.
* 2-1-4 손전등: 끌기 자체는 동기화 정확(45→30 마우스, 45→80 터치, 크게 보기 막대 값도 같이) — 그러나 M1.

### 2.5 시점 방향 — 통과(2-1-5 32/32)
2-1-5 8경우(1024×768; 768×1024·1180×820·375×812도 같은 결과). 전체 = 처음 방향 카메라에서 지구본→전등 화면 벡터, 확대 = 확대 카메라 축에 투영, 막대·그림자·확대 칸은 확대 화면 기준 코사인.
| 경우 | 높이각 | 전체 화면 전등 | 확대 화면 전등 | 전체·확대 cos | 막대·전등 | 그림자·전등 | 확대 칸 💡·전등 / 그림자·💡 | 확대 칸 그림자 |
|---|---|---|---|---|---|---|---|---|
| 수직·가 | 50° | 아래(0,−115.8) | 아래 | 1 | 0.84 | −1.00 | 1 / −1 | 19.6 |
| 수직·나 | 50° | 오른쪽 | 오른쪽 | 1 | 0.88 | −0.78 | 1 / −1 | 19.6 |
| 수직·다 | 70° | 위 | 위 | 1 | 1.00 | −0.84 | 1 / −1 | 19.6 |
| 수직·라 | 50° | 왼쪽(−191.8,0) | 왼쪽 | 1 | 0.91 | −0.67 | 1 / −1 | 19.6 |
| 기울임·가 | 50° | 아래 | 아래 | 1 | 0.91 | −0.95 | 1 / −1 | 19.6 |
| 기울임·나 | 56° | 오른쪽 | 오른쪽 | 1 | 0.99 | −0.46 | 1 / −1 | **11.2** |
| 기울임·다 | 68° | 위 | 위 | 1 | 0.98 | −0.72 | 1 / −1 | 19.6 |
| 기울임·라 | 50° | 왼쪽 | 왼쪽 | 1 | 0.72 | −0.92 | 1 / −1 | **49.0** |
* 카메라 방위 0°→0°(전등 쪽으로 돌지 않음), 측정기·막대 끝·그림자 끝이 확대 칸·토글·막대 밑에 있는 경우 0, 기록하면 처음 방향(거리 14.5~21.6). 공전 방향·자전축 방향(늘 +x로 기울어 나=여름·라=겨울)도 교과서와 맞음.
* 2-1-2(처음 방향으로·두 번 탭뿐)·2-1-3·2-1-4(처음 방향으로뿐)는 자동 카메라 이동 없음 → 뒤집힘 없음. 3D↔2D 사이 방향은 L5.

### 2.6 끝까지 흐름·시간 — 통과
* 4개 앱 × (가짜 로그인 1024×768 + 체험 768×1024 크게 보기): 예상(글 입력) → 실험(필수 칸 전부, **중간에 새로 고침 → 단계·기록·시각/달/각도/지구본 자리·선택 복원**) → 분석 2문항 → 결론·모범 답안 → 궁금한 점 **비우면 "'더 탐구하고 싶은 점'을 한 줄이라도 적어야 마칠 수 있어요."** → 적고 마치기. 로그인: `check-answer`·`app_results` POST·`app_progress` PATCH, "제출 완료". 체험: 서버 요청 0, "제출 안 됨".
* `app_results` 본문 `details`: 2-1-2 `predict, hintsOpened, records(7), analysis, conclusion, curiosity, qa(6)` / 2-1-3 `…source, records(12)…` / 2-1-4 `predict{q1b}, records(30·45·80), analysis, conclusion, curiosity, qa` — **qa 첫 줄 = predict·q1b·새 질문 글** / 2-1-5 `…records(8), shadows…`. **4개 앱 `buildDetail()`이 HEAD와 글자까지 같음**, 저장 키 4개 그대로, 저장 항목은 2-1-3 `ghosts`→`pathMode`(UI 설정)만 바뀜.
* 시간(추정, 실제 학생 미측정): 바뀐 글 양은 2-1-2 실험 안내 +88자(약 +20초), 2-1-4 예상 질문 −14자, 나머지 같음 → 각 앱 spec 시간표(2-1-2 6:05~6:30, 2-1-3 6:00~6:30, 2-1-4 5:05, 2-1-5 6:55~7:30 — 느린 학생 7분 넘을 수 있음, 전과 같음) 유지로 봄. 자동 조작 시간은 38·50·29·59초.

### 2.7 규칙·정리 — 통과
공통 틀 사본 `diff -r`: science-sim 20·science-guide 3·class1-record 23 **모두 정본과 같음**(공통 틀 변경은 Claude의 lesson.js 궁금한 점 문구 정리뿐). 새 `console.log`·`debugger` 0(있는 `console.warn`은 전부터 있던 오류 처리), `localStorage.clear()` 0, 앱 안 경로 상대, 새 외부 자원 0(2-1-4 `index.html`에 로컬 `./science-sim/experiment.js`만), `npm run lint` 통과(단, eslint 설정이 `public/apps/**`를 뺌 → 4개 앱 `app.js`·`lesson-config.js` `node --check` 통과).

### 2.8 성능 느낌 — 통과(상대 비교, headless swiftshader 1024×768)
| | HEAD | 지금 |
|---|---|---|
| 가만히 / 카메라 끌기 | 60 / 59.7 fps | 60 / 60 fps |
| 시간 바 끌기 | 31 fps(p95 67ms) | 36 fps(p95 67ms) |
| 해 끌기 | — | 35 fps(p95 83ms) |
* 3D↔2D 30번: 캔버스 늘 1, 이벤트 수신기 그대로(149→149), 힙 +4.2MB(HEAD)/+5.7MB(지금)·노드 +360/+270 — **HEAD에도 있던 느린 증가**(2-1-5도 같은 모양), 단계 B에서 생긴 누수 아님.

---

## 3. "Build 뒤 알려진 것" 판정
1. **궁금한 점 문구 정리**: 화면 글 자연스러움 — 2-1-2 "하루 동안의 태양 고도, 그림자 길이, 기온에 대해 더 알아보고 싶은 점이 있으면 한 줄로 적어 보세요." · 2-1-3 "더 알아보고 싶은 점이 있다면 한 줄로 적어 보세요." · 2-1-4 "이번 실험을 하면서 더 알아보고 싶은 점이나 궁금한 점이 있으면 한 줄로 적어요." · 2-1-5 "계절의 변화에 대해 더 알아보고 싶은 점이 있으면 한 줄로 적어요." + 꼬리표 "궁금한 점" + "한 줄이라도 적어야 '학습 마치기'를 할 수 있어요." 칸 안내(예: …)는 그대로. `detail.qa` 글은 L10.
2. **B2 첫 진입 스크롤**: 앱마다 다르게 움직이는 것은 바람직하지 않고, 2-1-3은 '실험 방법'을 건너뛰게 된다 → M3.
3. **B4 가로 크게 보기 관찰 카드 숨김**: 기록 흐름 문제 없음(실행 뒤 스크롤 이동 0, 장면·확대 칸·막대 한 화면, 기록 버튼·값은 막대, 화면 읽기는 장면 안 `aria-live` 알림과 알림창으로 값 전달). 잃는 것은 이 모드에서 "이 칸은 이미 기록했어요…" 안내뿐(낮음). **OrbitControls 내려받기**: 저장소·스크래치에 파일 없음(확인).
4. **B3 `setAngle` 별칭**: 끌기·슬라이더·눈금·◀▶가 늘 같은 값(슬라이더·패널·3D·호·HUD·저장). 끌기의 다른 문제는 M1.
5. **812×375 막대 겹침 17~25px**: 3개 앱 25px(버튼 17px), 기본 끔·휴대폰 가로 → 낮음(L7). **좁은 화면 이름표↔토글**: "살짝"보다 큰 곳이 있음(2-1-4 휴대폰 최대 852px², 2-1-5 태블릿 확대 화면 '가') → 낮음(L6).

## 4. 사용자에게 보여 줄 스크린숏 — `…/scratchpad/review-B/showcase/`
01 2-1-2 태블릿세로 크게보기(기본) 해를 끄는 중 13:46(모형 값·기록 꺼짐) · 02 2-1-2 태블릿가로 기본화면 10:30 · 03 2-1-3 이번 달만(10월) · 04 2-1-3 계절 대표 3개(10월) · 05 2-1-4 예상하기 새 질문과 힌트 · 06 2-1-4 크게보기 손전등 끌기(터치) 80° · 07 2-1-5 라·기울임 전체 화면 · 08 2-1-5 라·기울임 확대 화면 · 09 2-1-5 확대 화면 3곳(나 0.8·가 1.4·라 3.5)과 확대 칸 · 10 2-1-5 전체↔확대 나란히(좌우 같음) · 11 [발견 M1] 2-1-4 손전등 옆 빈 곳 끌기 45°→73° · 12 [발견 L1] 2-1-5 기록 뒤 전체 화면에서 확대 칸이 라 자리 지구본을 가림. (스윕·방향·끌기 스크린숏 전체는 `review-B/shots/`)

## 5. Supabase 차단 기록
자기 전용 headless Chrome(9351, 프로필 스크래치) `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"` + 모든 탭 첫 로드 전 CDP `Fetch.enable(*supabase.co*)` 가짜 응답(site_settings → `login_required:false`, GET → `[]`, POST/PATCH → 201, OPTIONS → 204, check-answer → `{ok:true, verdict:"ok"}`). 기록된 스크립트 요청 349건(조작용 탭 제외) 모두 가로챔, **새어 나간 요청 0·실패 0·Gemini 0**. 앱은 자기 정적 서버(`python3 -m http.server 8783`, 작업 트리 `public/` 읽기 전용), 비교용 HEAD 사본은 8781. 솔직히 적을 것: ① 도중에 끝난 시험 스크립트 탭 4개가 한동안 내 Chrome에 남아 같은 앱의 `meta`(학습 시간)를 다시 썼다 — 찾아 닫고 영향받은 흐름 시험을 모두 다시 했다(보고서 수치는 다시 한 것). ② 처음 쓴 가짜 check-answer 응답에 `ok:true`가 없어 로그인 시험에서 되짚기 카드가 떴다 — 고쳐 다시 했다(앱 문제 아님). `localStorage.clear()` 안 씀(각 앱 `sci621sim*`·`ssUiPref:`·시험용 가짜 세션 키만). git commit/push·실 DB 쓰기·내려받기·`npm run build` 없음.

## 6. 확인하지 못한 것
실제 iPad·안드로이드(해·손전등·지구본 손가락 끌기 감도, 두 손가락 떼는 순서 — CDP 흉내 한계, iOS Safari 2D 해 끌기 `touch-action`, 실제 GPU 프레임), 실제 로그인·Supabase·Gemini, 실제 학생 소요 시간, 소리(해당 없음).

---
**판단: 사용자에게 보여 주고 커밋해도 된다(과학 수치·싱크·확대 방향 32/32·저장 구조·흐름 모두 통과, 높음 0). 배포는 M1(2-1-4 손전등 누르기·빈 곳 끌기로 각도가 튐 — 작은 수정)을 고친 뒤를 권하고, M2(12:30 호가 가장 작게 보임)·M3(첫 진입 동작 통일)는 사용자에게 보여 주며 방향을 정한 뒤 처리한다.**
