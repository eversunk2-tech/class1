# 단계 C Build 보고서 — C2: `sci-6-1-1-3`, `sci-6-1-1-4` 간략화 (2026-09-25~26)

지침: `docs/science/sim-redesign/build-C-instructions.md`(C2 줄). 공통 틀 사본·다른 앱·`src/lib/`·관리자 화면은 고치지 않았다. git commit/push·실 DB 쓰기·`npm run build`·내려받기 없음.

## 1. 바꾼 파일
| 파일 | 요지 |
|---|---|
| `public/apps/sci-6-1-1-3/data/lesson-config.js` | 저장 키 `:v2`, 단계 4개, 예상 q2만·힌트 2개, 분석 q3·q4만, 결론만, '더 탐구하고 싶은 점' 문구·한 줄(최대 200자), 실험하기 모형 안내(`modelNote`)에서 관찰 결과를 먼저 말하던 문장 뺌 |
| `public/apps/sci-6-1-1-3/app.js` | `buildDetail()`(`questionSet: 2`, `extension` 없음), 결론 제출 뒤 `finish-wrap` 보이기(`showFinish`), 한 줄 입력(rows 1·Enter 막음), `lesson.finish({ stage: "conclude" })`·`canFinish`=결론 제출, `gates`·`done`에서 `curiosity` 삭제, 예상 게이트 문구 |
| `public/apps/sci-6-1-1-3/index.html` | 5단계 섹션 삭제 → 정리하기 안 `finish-wrap`(더 탐구하고 싶은 점 + 학습 마치기), 그 아래 `done-card`·다시 하기(`sci-6-2-1-4`와 같은 구조) |
| `public/apps/sci-6-1-1-3/style.css` | `.ss-textarea.one-line`, '⏩ 시간 빨리 감기' 표시를 오른쪽 위로·휴대폰에서 작게 |
| `public/apps/sci-6-1-1-4/data/lesson-config.js` | 저장 키 `:v3`, 단계 4개, 예상 q2만·힌트 2개, 분석 inferA·q1만, 결론만, '더 탐구하고 싶은 점' 문구·한 줄 |
| `public/apps/sci-6-1-1-4/app.js` | 위와 같은 정리하기 구조·`buildDetail()`·게이트, 관찰 카드 '색 구별' 도우미를 색 이름으로만(`chartNode({ ends: "color" })`) |
| `public/apps/sci-6-1-1-4/index.html`·`style.css` | 위와 같은 정리하기 구조, 한 줄 입력, 방울 수 세기 표시를 휴대폰에서 오른쪽 위로 |
| 두 앱 `spec.md` | 끝에 "개정 (2026-09-25) 간략화" 절 덧붙임(앞 내용 그대로) — 문항 전·후, 저장 키, detail, 매핑, 시간표 |
| `src/data/app-responses/sci-6-1-1-3.ts`·`sci-6-1-1-4.ts` | v2 변형 맨 앞 추가, v1은 label만 "간략화 전(~2026-09-25)", `stages` 4단계, `standard: "slim"`, 머리 주석에 v1·v2 detail |

## 2. 앱별 문항 · 저장 키 · detail
### sci-6-1-1-3 (산성·염기성 성질 비교)
* 예상하기: q1·q2 → **q2**("식초에 달걀을 일주일…", 문구 그대로). 힌트 3 → 2(1번·3번 그대로, 조개껍데기 비교 2번 뺌).
* 분석: q1·q2·q3·q4 → **q3(녹는 까닭)·q4(대리암)** — 문구·보기·피드백 그대로(화면 번호 분석 1·2).
* 정리하기: 결론 + 발전 1·2 → **결론만**. '더 탐구하고 싶은 점'은 정리하기 안 한 줄(필수): "이번 실험을 하고 나서 더 탐구하고 싶은 점이나 궁금한 점을 한 줄로 적어 보세요."
* 저장 키 `sci611sim3:v1` → **`sci611sim3:v2`**.
* detail 전 `{predict{q1,q2}, records, analysis{q1..q4}, conclusion, extension{q1,q2}, curiosity}` → 후 **`{questionSet:2, predict{q2}, records, analysis{q3,q4}, conclusion, curiosity}`**(가로챈 실제 본문, 1,209자).

### sci-6-1-1-4 (산+염기 섞기)
* 예상하기: q1·q2 → **q2**("묽은 염산에 … 계속 넣으면…", 문구 그대로). 힌트 3 → 2(1번·3번, "무엇으로 알아보았는지" 2번 뺌).
* 분석: readA·inferA·q1·q2·q3 → **inferA(성질 변화 추리)·q1(실험 B 방향)** — 그대로(화면 번호 분석 1·2).
* 정리하기: 결론 + 발전 1·2 → **결론만** + 정리하기 안 '더 탐구하고 싶은 점'(같은 문구).
* 저장 키 `sci6114sim:v2` → **`sci6114sim:v3`**.
* detail 후 **`{questionSet:2, predict{q2}, records[{phase,drops,colorFamily,recordedAt}], analysis{inferA,q1}, conclusion, curiosity}`**(1,558자). 뺀 키(`predict.q1`, `analysis.readA/q2/q3`, `extension`)는 없음.

관찰 칸(8칸·12칸)·결과 표·관찰 보기·칸 색·과학 내용은 바꾸지 않았다. 남긴 문항의 id·키 이름은 예전과 같다.

## 3. 응답 매핑 확인(jiti 스크립트, 스크래치 — 저장소에 넣지 않음)
HEAD(`9d2dc16`)의 `src/`를 스크래치에 꺼내 **HEAD 매핑과 지금 매핑**에 같은 detail을 넣어 비교했다. 출력 요약:
```
[sci-6-1-1-3] variants = v2:간략화 후(2026-09-25~) / v1:간략화 전(~2026-09-25), stages = predict,experiment,analyze,conclude
■ 예전 모양(모든 키 채움)      → 변형 v1, 항목 11개, extras=[]  ✔ HEAD 결과와 key·stage·label·질문·답·version 같음(11=11)
   groupByStage: 예상하기(2) → 실험하기(1) → 기록·분석하기(4) → 정리하기(3) → 궁금한 점(1)   ✔ 예전 궁금한 점은 맨 뒤
■ 예전 모양(일부 빈 문자열)    → 변형 v1, 11개, extras=[]  ✔ HEAD와 같음
■ 새 앱 끝까지 가로챈 detail   → 변형 v2, 항목 6개(예상·관찰 기록·분석 1·분석 2·결론·더 탐구하고 싶은 점) 모두 답 있음, extras=[]
   groupByStage: 예상하기(1) → 실험하기(1) → 기록·분석하기(2) → 정리하기(2)   ✔ 앱 단계 순서
[sci-6-1-1-4] 같은 결과 — 예전 모양 v1 12개(HEAD와 같음), 새 detail v2 6개·extras 없음
progressInfo(예전 진행 중 step="curiosity") → {"stageLabel":"궁금한 점","index":null,"total":4}
(참고) 새 detail을 HEAD 매핑에 넣으면 extras=["questionSet"] — 매핑과 앱을 같은 커밋으로 배포하면 문제없음
결과: 모두 통과
```
* 매핑 v2의 질문 문구·보기 = 앱 `lesson-config.js` 문구(스크립트로 대조, 5항목 × 2앱 모두 같음).
* v2 항목 label은 학생 화면 번호에 맞춰 "예상 / 분석 1 / 분석 2 / 결론 / 더 탐구하고 싶은 점"(v1 label은 그대로).

## 4. 동작 확인(자기 정적 서버 8785 + 자기 headless Chrome 9353)
* **가짜 로그인 끝까지**(1024×768): 예상 → 실험(8칸/12칸) → 분석 2개 → 결론 제출 → 빈 '더 탐구하고 싶은 점'으로 마치기 → "'더 탐구하고 싶은 점'을 한 줄이라도 적어야 마칠 수 있어요." → 적고 마치기 → `app_results` POST 1번(위 detail), `app_progress` 저장 prefix는 새 키뿐, 새로 고침 뒤 정리하기·제출 완료 그대로. 콘솔 오류 0.
* **체험 모드 끝까지**(768×1024, 크게 보기 기본): 같은 흐름, 마치면 "체험 모드라서 결과는 저장되지 않았어요", Supabase 요청은 `site_settings` GET뿐. 결론 제출 전에는 `finish-wrap`이 숨음, Enter로 줄바꿈 안 됨(rows 1, 최대 200자), 새로 고침 뒤 이어서, "처음부터 다시 하기" 뒤 예상하기·빈 칸.
* 머리말 접기: 161px → 82px(계정 줄·단계 메뉴 숨김, "▼ 펼치기"), 단계 메뉴 4칸, 이전/다음 단계 이동, 예상 게이트 문구 "예상하기 질문에 내 생각을 5글자 이상 먼저 적어 주세요."
* **예전 진행 기록**: 가짜 `app_progress` GET이 예전 prefix(`sci611sim3:v1`/`sci6114sim:v2`, step은 curiosity/conclude, 예전 답 포함) 스냅샷을 주고 이 기기에도 예전 키 8개가 있을 때 → 예상하기 빈 칸에서 시작, 입력하면 `on_conflict=user_id,app_id` 덮어쓰기로 새 prefix 저장, 예전 로컬 키는 섞이지 않음(그대로 남았다가 로그아웃 때 `sci6…` 정리로 지워짐), 새로 고침 뒤 새 판 이어서.

## 5. 실험하기 화면 점검(지침 §4)
두 모드 × 1024×768·768×1024·375×812·812×375(크게 보기)·어두움·`?no3d=1`(1024·768·375)에서 실행 뒤 화면까지 — 6-1-1-3은 달걀 껍데기+염산(기포·빨리 감기)·달걀흰자+수산화 나트륨(흐물흐물·여러 날)·조개껍데기+수산화 나트륨(변화 없음), 6-1-1-4는 첫째 줄 25방울(홈 준비 포함)·15방울. 콘솔 오류 0, 가로 스크롤 없음.

**고친 것**
1. (6-1-1-3) '⏩ 시간 빨리 감기 (모형)' 표시가 휴대폰(375)에서 "⛶ 전체 화면 보기" 버튼과 **반응 중인 홈을 덮음**(top 44px·폭 260px) → 오른쪽 위(top 10px)로, 폭 520px 이하·높이 480px 이하에서는 작게(폭 ≤200px). 뒤: 겹침 0(모든 크기).
2. (6-1-1-4) 방울 수 세기 표시가 휴대폰(3D·2D)에서 토글 버튼과 겹침 → 폭 520px 이하에서 오른쪽 위(2D는 홈판 위 빈 자리). 태블릿은 그대로. 뒤: 겹침 0.
3. (6-1-1-3, 답 먼저 알려 주기) 실험하기 모형 안내 전: "기포는 넣자마자 생기지만, 물질이 녹아 작아지는 데에는 시간이 걸려요 … 삶은 달걀흰자와 삶은 닭 가슴살이 흐물흐물해지는 변화는 … 오래 걸려요(여러 날)" → 후: "실제로는 물질이 변하는 데 수업 시간 안에는 보이지 않을 만큼 오래(여러 날) 걸리기도 해요. 모형에서는 그런 부분을 빨리 감기로 보여 주고, 화면에 '⏩ 시간 빨리 감기 (모형)'이 떠요." 물질별 안내는 분석 단계 `meltNote`에 그대로(과학 내용 불변).
4. (6-1-1-4, 답 먼저 알려 주기) 기록하는 순간 관찰 카드의 '색 구별' 도우미가 "◀ 산성이 강함 / 염기성이 강함 ▶", 화면 읽기 "넣은 뒤는 염기성이 강한 쪽" → 분석 1(inferA)의 답. → 관찰 카드에서는 "색 띠 (모형)", "◀ 붉은색 쪽 / 노란색 쪽 ▶", "처음은 붉은색 쪽, 넣은 뒤는 노란색 쪽"(띠 색·▲ 자리 그대로). 산성·염기성 이름의 색깔 변화표는 분석 단계에 그대로.

**확인만 한 것**
* 3D 탭 고르기: 6-1-1-3 접시→물질, 병·홈판→용액, 홈→둘 다 / 6-1-1-4 홈→실험 줄+방울 수, 잠긴 실험 B 홈→"실험 A(6칸)을 모두 기록하면 실험 B가 열려요." 알림. 모든 조건이 이미 탭으로 골라져 더한 것 없음(스포이트 탭으로 한 방울씩 넣기는 조작 단계가 늘어 넣지 않음).
* 시점 방향: 두 앱 모두 실행 때 `v.flyHome()` 뒤 `v.focus()` — `focus`는 지금 카메라 방향을 그대로 두고 거리만 줄인다(`sim3d.js`). 전·후 스크린숏에서 좌우 유지(6-1-1-3 염산 홈판·염산용 핀셋 왼쪽, 6-1-1-4 0→25방울 왼쪽→오른쪽).
* `scenePanel`: 넘기지 않음 — 두 앱 모두 계속 보여 줄 "지금 값"이 없다(관찰은 보기 고르기, 빨리 감기·방울 수는 실행 중에만 뜨는 장면 안 표시, 기록 버튼 글자에 값 없음).
* 기록 순간 글: 알림 "📝 기록했어요: 묽은 염산 + 달걀 껍데기 → 기포가 발생하며 녹는다"(학생이 고른 관찰), 관찰 카드 안내(빨리 감기 안내·"색연필로 칠하듯…")는 사실·방법만. 2D 홈 화면 읽기 글은 실행 뒤 관찰 사실.
* 앱은 기록 버튼을 찾거나 스크롤하지 않는다(`ss-observe`·스크롤 코드 없음).

**공통 틀 쪽 관찰(고치지 않음, Claude 참고)**
* 휴대폰(375) 크게 보기에서 드래그 안내(`.ss-view-tip`)가 3줄이 되어 3D 장면 아래쪽 이름표를 덮는다(모든 앱 공통).
* 가로 크게 보기(1024×768)에서 실행 뒤 관찰 카드를 보이려고 장면이 대부분 화면 위로 스크롤된다(공통 틀 설계대로).
* 확대 시점에서 왼쪽 위 토글이 안 바뀌는 3D 이름표를 조금 가릴 때가 있다(6-1-1-4 "둘째·셋째 줄: 이번 실험에서 쓰지 않음", 크게 보기 1024×768).
* 이전부터: 6-1-1-3 처음 시점에서 홈판 위 홈 이름표가 매우 작음(확대 시점에서는 읽힘), 6-1-1-4 휴대폰 2D는 20·25방울 칸을 가로로 밀어 봐야 함.

## 6. 시간표(추정, 두 앱 `spec.md` 끝에 자세히)
방식: 화면에서 센 한글 음절 수 + 질문 다시 읽기 × 70%, 보통 450음절/분·60자/분, 느린 300음절/분·40자/분, 실험 연출은 자동 진행 실측(6-1-1-3 칸당 약 6초, 6-1-1-4 약 5.4초).

| 앱 | 예상 | 실험 | 분석 | 정리 | **합계 보통 / 느린** | 간략화로 준 시간(보통/느린) |
|---|---|---|---|---|---|---|
| 6-1-1-3 | 1:09/1:50 | 2:49/4:00 | 1:19/2:06 | 1:58/2:55 | **약 7:15 / 10:50** | 약 3:20 / 5:10 (§1.5 "약 3분"과 맞음) |
| 6-1-1-4 | 1:13/1:58 | 3:45/5:21 | 1:24/2:14 | 2:02/3:01 | **약 8:25 / 12:35** | 약 3:45 / 5:45 (§1.5 "약 3~4분"과 맞음) |

7분을 넘는 까닭: 실험관찰 조건 전부(8칸·12칸)를 관찰·기록(칸마다 연출 5~6초 + 고르기·기록), 실험 전 알아 두기(219~238음절)·분석 표 읽기, 결론과 '더 탐구하고 싶은 점' 두 번 입력. 측정 규칙은 줄이지 않았다.

## 7. 규칙·정리
* `diff -r scripts/templates/science-sim public/apps/<앱>/science-sim` 두 앱 모두 같음, `class1-record.js`도 정본과 같음.
* `npm run lint` 통과, `node_modules/.bin/tsc --noEmit -p tsconfig.json` 통과, 앱 JS `node --check` 통과(ESLint는 `public/apps/**` 제외).
* **Supabase 차단**: Chrome `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"` + `--disable-web-security` + CDP `Fetch.enable(*supabase.co*)` 가짜 응답(`site_settings`→`login_required:false`, `app_progress` GET→[] 또는 가짜 한 줄, POST/PATCH→201, `app_results`→201, `functions/v1/check-answer`→`{ok:true, verdict:"ok"}`). 브라우저가 보낸 supabase.co 요청 수 = 가로채 답한 수(로그인 3=3, 체험 1=1), 네트워크 실패 0. 가짜 세션은 127.0.0.1 출처 localStorage에만. 외부로 나간 요청은 `cdn.jsdelivr.net`(페이지가 원래 불러오는 three.js·supabase-js·글꼴)뿐, 실제 Supabase·Gemini 요청 0.
* `localStorage.clear()` 안 씀(그 앱 `sci611sim3`/`sci6114sim` 키·`ssUiPref:` 키만 지움). 끝나고 자기 Chrome(9353)·정적 서버(8785) 종료 확인.
* **스크래치 정리 미완료**: `build-C2/profile/`(Chrome 프로필, 118MB — 가짜 세션만)과 `build-C2/work/`(확인 스크립트 `check-mapping.mjs`·`check-texts.mjs`·CDP 도우미, 로그, 가로챈 detail JSON, HEAD `src` 사본, 1.8MB) 지우기(`rm -rf`)가 **권한으로 거부**되어 남아 있다. 실제 비밀값은 없다 — Claude가 지우거나 Review가 스크립트를 다시 쓸 수 있다. `shots/`(309장)은 남김.

## 8. 대표 스크린숏(스크래치 `…/scratchpad/build-C2/shots/`)
* 정리하기 안 '더 탐구하고 싶은 점' + 학습 마치기: `showcase-sci-6-1-1-3-3-정리하기-더탐구-제출완료.png`, `showcase-sci-6-1-1-4-3-정리하기-더탐구-제출완료.png`, `sci-6-1-1-3-trial-conclude-finishwrap-768x1024.png`, `sci-6-1-1-4-trial-done-768x1024.png`, 어두움 `sci-6-1-1-3-dark-conclude-finishwrap-1024x768.png`
* 결론 제출 전(마치기 칸 없음): `sci-6-1-1-4-trial-conclude-before-submit-768x1024.png`
* 예상 1개·분석 2개: `showcase-sci-6-1-1-4-1-예상하기-질문1개.png`, `showcase-sci-6-1-1-3-2-분석-2개.png`
* 실험 화면(고친 뒤): `sci-6-1-1-3-exp-375x812-on-3-fizz-mid.png`, `sci-6-1-1-4-exp-375x812-on-3-A25-drops.png`, `sci-6-1-1-4-exp-375x812-off-2d-3-A25-drops.png`, `sci-6-1-1-4-observe-colorband-1024x768.png`
* 시점 방향 전·후: `sci-6-1-1-3-exp-1024x768-on-1-init.png` ↔ `…-on-3-fizz-mid.png`, `sci-6-1-1-4-exp-1024x768-on-2-A25-selected.png` ↔ `…-on-3-A25-drops.png`

## 9. 확인하지 못한 것
* 실제 iPad·휴대폰 터치(탭 고르기·드래그), 실제 로그인·실제 DB(`app_progress`·`app_results`)·실제 Gemini 판정 — 모두 가짜 세션·가짜 응답으로만 확인.
* 관리자 "학생 응답" 화면에서의 표시(매핑 로직은 jiti로 확인, 화면은 Review 몫).
* 실제 학생 소요 시간(시간표는 글자 수·자동 진행 연출 시간으로 낸 추정).
* 지금 이 두 앱을 진행 중인 학생이 있는지(있으면 저장 키 버전을 올려 처음부터 다시 — 완료 결과는 그대로).
