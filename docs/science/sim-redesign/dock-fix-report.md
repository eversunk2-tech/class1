# 크게 보기(한 화면 실험실) 검토 뒤 수정 — Claude (2026-09-26)

검토 `review-dock.md`(배포해도 됨, 높음 0)의 **M1**(처음 들어오면 조작 칸이 펼쳐진 '알아 두기'로 차서 고르는 버튼이 칸을 밀어야 보임 — 세로 768×1024 0/20, 가로 1024×768 6/20)과 **L5**(주석)를 고쳤다. 사용자 요청("스크롤 없이 한 눈에 실험 조작")과 직접 어긋나는 문제라 배포 전에 고쳤다.

## 바꾼 것 (정본 `scripts/templates/science-sim/` → 실험 앱 20개 사본, `diff -r` 20/20 같음)
* `experiment.js`
  * `enlarge()`의 `placeDock()`이 조작 칸 맨 위로 옮긴 `details.ss-intro`('알아 두기')를 **제목 줄만** 남긴다(`foldIntro`). 학생 선택으로 **저장하지 않고**(`setOpenQuietly` + `isQuietToggle` — 프로그램이 접고 편 toggle 이벤트는 저장 리스너가 건너뜀), 학생이 크게 보기에서 직접 펴거나 접지 않았으면 끌 때 원래대로 편다(`unfoldIntros`). 한 번 켠 동안 같은 카드는 한 번만 접는다(학생이 편 것을 화면 변화로 다시 접지 않음).
  * `create()`의 '알아 두기' 저장 리스너가 `isQuietToggle(det)`이면 저장하지 않음. `SciSim.Experiment.isQuietToggle` 내보냄.
  * 머리·`enlarge()` 주석 갱신.
* `style-common.css`(크게 보기 조작 칸에서만 — 기본 화면은 그대로): 칸 안쪽 여백 10→8px, 카드 16→12px·아래 틈 14→10px, 접힌 '알아 두기' 카드는 제목 줄 높이만, 안내 줄·진행 막대 틈 줄임, 진행 막대 단계 이름 칸 `fit-content(55%)`(이름이 두세 줄로 쪼개지지 않게). 누르는 곳 44px 그대로. L5 주석("장면 바로 아래 카드" → "조작 칸 맨 위 카드").
* `README.md` "추가 기능(Phase A)" 절에 '알아 두기' 접기와 `isQuietToggle` 사용법.
* 앱 2개(`enlarge()` 직접 사용): `sci-6-2-1-3`·`sci-6-2-1-4` `app.js`의 '알아 두기' 저장 리스너에 `S.Experiment.isQuietToggle(det)` 확인 한 줄.

## 확인 (자기 정적 서버 8797 + 자기 headless Chrome 9345, Supabase는 DNS 차단 + CDP 가짜 응답 — 가로챈 요청 400여 건, 새어 나간 요청 0, Gemini 0)
* 조작 칸 첫 화면(칸 맨 위)에서 첫 고르는 버튼이 **통째로** 보이는 앱: **세로 768×1024 19/20**(전 0/20 — 남은 6-2-2-3도 버튼 윗부분 33px 보임), **가로 1024×768 20/20**(전 6/20).
* 크게 보기에서 '알아 두기' 접힘·저장 안 됨(`<key>:intro` 없음 20/20), 끄면 다시 펼침 20/20, 크게 보기에서 학생이 펴면 저장되고 끈 뒤에도 펼침 20/20, 다시 켜면 다시 접힘. 처음부터 접어 두는 앱(6-2-2-3)은 그대로.
* 20개 앱 × 6크기(1024×768·1180×820·768×1024·810×1080·1920×1080·812×375) 크게 보기 120판: 페이지 스크롤 0, 페이지·조작 칸 가로 넘침 0, 콘솔 오류 0.
* L1(1024×768 조작 칸의 '기록한 칸 한눈에 보기' 표 끝 열): 여백을 줄여 +48/+58 → +36/+46px(6-1-1-1·-2·-4, 1180×820은 0) — 남음(낮음, 옆으로 밀면 보임).
* `npm run lint`·`npm run build` 통과, `node --check` 통과.

## 남긴 것(사용자 결정·단계 D)
* **M2**(흔한 태블릿에서 크게 보기의 3D 장면이 기본 화면보다 크지 않음, 세로는 약 −24%) — 사용자에게 물음.
* L1(위), L2(키보드가 창을 줄이는 옛 안드로이드 브라우저의 방향 판정), L3(2D 대체 화면 16개 앱 칸 안 스크롤), L4(휴대폰에서 크게 보기를 켜면 장면이 좁음 — 기본 끔) → 단계 D.
* R3: `scripts/templates/science-guide/persist.js`에 science-sim판의 가짜 충돌 거르기(`sentFp`·`isExtensionOf`)와 숫자 조사 개선이 빠져 있음(조사 앱 3개) — 사용자 확인 뒤 옮김.
