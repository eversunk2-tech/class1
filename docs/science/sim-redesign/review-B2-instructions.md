# 재검토 지침(Review B2) — 단계 B 수정(fix-B1·fix-B2) 확인, 배포 전 마지막 검토 (2026-09-25)

당신은 **Review** 담당이다. **코드를 고치지 않는다**(발견만 기록). 이 검토가 끝나면 Claude가 사용자 확인 없이 **단계 A+B를 커밋·배포**한다(사용자 결정) — 그러니 **학생에게 바로 보일 문제**(기록이 잘못 저장·막힘·과학적으로 틀림·답을 먼저 알려 줌·겹쳐서 못 누름·콘솔 오류)를 가장 먼저 찾는다.

먼저 읽기: `CLAUDE.md`(과학 차시 앱 규칙), `docs/science/sim-redesign/spec.md` 끝의 **개정 5**(단계 B 검토 뒤 결정), `review-B.md`(지난 발견 M1~M3·L1~L12와 재현 방법), `fix-B-instructions.md`, `fix-B1-report.md`, `fix-B2-report.md`, 공통 틀 README(끌기 도우미 `opts.pad`, 가로 화면 장면 높이 맞춤 — Claude가 바꿈), `git diff -- scripts/templates/ public/apps/sci-6-2-1-2 public/apps/sci-6-2-1-3 public/apps/sci-6-2-1-4 public/apps/sci-6-2-1-5`(단계 A 커밋 `f93f716` 이후 전부).

## 확인할 것
1. **지난 발견이 고쳐졌는지(review-B와 같은 재현)**: M1(2-1-4 누르기만·빈 곳 끌기·30° 뒤 살짝 건드림), M2(2-1-2 **"옆에서 본 모습" 칸** — 7개 시각과 눈금 사이 몇 곳에서 칸 속 각 = 패널 태양 고도 = 3D 호(오차 1° 이내), 남중 전 왼쪽·뒤 오른쪽에서 빛, 해를 끄는 동안 함께 바뀜, 숫자는 칸에 없음, 토글·시각 배지·막대·해의 길·나무·그림자·사람·백엽상을 가리지 않음(두 모드·가로·세로·휴대폰)), M3(2-1-3 첫 진입 자동 스크롤 없음 — 네 앱이 같게), L1(2-1-5 확대 칸은 확대 화면일 때만), L2(2D + 크게 보기에서 막대가 화면 안), L3(2-1-3 대표 선이 잘 보임), L4(바뀌는 값 이름표 없음), L5(2-1-3 3D 처음 시점·2D 모두 남쪽 하늘을 바라본 모습 — 동쪽 왼쪽, 2-1-2 2D 옆모습이 오후엔 오른쪽에서 빛), L6(이름표↔토글), L7(812×375 크게 보기 막대 겹침), L8, L11, L12.
2. **회귀**: 4개 앱 × {768×1024 크게 보기 기본, 1024×768 기본(끔)·켬, 1280×800 켬, 375×812, 812×375 켬} + 어두움 + 2D — 겹침·잘림·가로 스크롤·콘솔 오류. 끝까지 흐름(예상 → 필수 칸 모두 기록 → 분석 → 정리 → 더 탐구하고 싶은 점 → 학습 마치기, 가짜 로그인으로 `app_results` POST 본문 확인) + 새로 고침 뒤 이어 하기. 저장 키·`detail`·`detail.qa` 모양이 단계 A 커밋(`f93f716`)의 앱과 같은지(2-1-4는 예상 질문 id `q1b`만 다름). 과학 값(측정값·교과서 표) 그대로.
3. **공통 틀 변경의 영향(다른 앱)**: 가로 화면 장면 높이 맞춤이 휴대폰 가로에도 적용됨 — 다른 실험 앱 최소 4개(`sci-6-1-1-2`, `sci-6-1-2-3`, `sci-6-2-2-3`, `sci-6-2-3-3`)를 812×375·1024×768에서 크게 보기 켬·끔으로 열어 막대·장면이 괜찮은지, 콘솔 오류 0. 23개 사본 `diff -r` 일치, `npm run lint`.
4. **답을 먼저 알려 주지 않기**: 4개 앱의 실험·기록 순간 글(새 옆모습 칸 이름 포함)에 결론·관계 단정이 없는지.
5. 판단: **배포해도 되는지**. 막아야 할 문제가 있으면 "높음"으로 분명히(파일:행, 재현, 고칠 방법).

## 사용자에게 보여 줄 대표 스크린숏(8장) — `review-B2/showcase/`
2-1-2 옆에서 본 모습 칸 오전·한낮·오후(태블릿 세로 크게 보기) 3장, 2-1-3 남쪽 하늘(3D)·계절 대표 3개 1장, 2-1-4 손전등 끌기 1장, 2-1-5 확대 화면+확대 칸 1장, 2-1-5 기록 뒤 전체 화면(확대 칸 없음) 1장, 휴대폰 가로 크게 보기 1장. 파일 이름에 앱·크기·모드를 한국어로.

## 테스트 규칙(반드시)
* **자기 정적 서버로 저장소 `public/`을 직접 서빙**: `python3 -m http.server 8789 --bind 127.0.0.1 --directory /Users/sungchul/Desktop/classroom/public` → `http://127.0.0.1:8789/apps/<앱>/index.html`. 단계 A 커밋과 비교가 필요하면 `git archive f93f716 public`으로 꺼낸 사본을 포트 8781로. 끝나면 종료.
* 자기 전용 headless Chrome만(포트 **9351**, 프로필은 스크래치 `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-B2/` 안), `--headless=new --use-angle=swiftshader --enable-unsafe-swiftshader --disable-web-security --host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`, Node 24 전역 WebSocket으로 CDP(설치 금지), CDP `Fetch.enable`(`*supabase.co*`) 가짜 응답만. 실제 Supabase·Gemini 요청 금지, **아무것도 내려받지 않기**(CDN 파일 포함).
* `localStorage.clear()` 금지(그 앱 `sci6…`·`ssUiPref:` 키만), 다른 탭·프로세스·다른 Chrome 건드리지 않기, git commit/push·실 DB 쓰기·`npm run build` 금지. 끝나면 자기 Chrome·서버 종료, 스크린숏만 남기고 정리(지우기가 거부되면 보고).

## 보고서 `docs/science/sim-redesign/review-B2.md`
지난 발견별 "고쳐짐/덜 고쳐짐/안 고쳐짐"(수치), 새 발견(심각도·파일:행·재현·제안), 항목 2~4 판정, showcase 목록, Supabase 차단 기록, 확인 못 한 것 — 한국어로 간결하게. 마지막 줄에 **"배포해도 됨 / 고친 뒤 배포"** 판단과 까닭.
