# fix-B1 보고서 — `sci-6-2-1-2` + `sci-6-2-1-3` (Review B 발견 수정, 2026-09-25)

지침: `fix-B-instructions.md`("사용자 결정"·"fix-B1"·확인·테스트 규칙). 고친 파일은 두 앱의 `app.js`·`style.css`와 `spec.md` 끝 "개정 3" 절, 이 보고서뿐이다. **`index.html`·`data/lesson-config.js`는 그대로**, 공통 틀 사본 `science-sim/`·`class1-record.js`는 정본과 `diff -r` 같음(두 앱 모두). git commit/push·실 DB 쓰기·내려받기·패키지 설치·`npm run build`는 하지 않았다.

전·후 수치는 review-B와 같은 방법으로 쟀다. "전"은 작업 전 파일을 스크래치에 복사해 두고, 시험 탭에서만 그 앱의 `app.js`·`style.css` 요청을 CDP로 원본 사본으로 바꿔 끼워 잰 값이다(저장소 파일은 그대로).

## 1. 요약
| 앱 | 발견 | 결과 |
|---|---|---|
| 2-1-2 | **M2** 호가 12:30에 가장 작아 보임 | **"옆에서 본 모습" 칸**을 새로 넣었다. 칸 속 각은 태양 고도와 같다(7개 시각 × 기본·크게 보기, 오차 **≤ 0.01°**). 빛이 오는 쪽도 맞다(남중 전 왼쪽, 남중 뒤 오른쪽). 14가지 화면에서 해 길·나무·그림자·사람·백엽상·토글·배지·막대와 **겹침 0** |
| 2-1-2 | L5 2D 옆모습이 늘 왼쪽에서 빛 | 해가 서쪽이면 오른쪽에서 빛이 온다. 위 하늘 그림과 7/7 일치 |
| 2-1-2 | L11 안내 글 ↔ '북' | 79px²(1024×768 기본) → **0**. 휴대폰 가로 155 → 0 |
| 2-1-3 | **M3** 들어올 때 스크롤 | 306·278·370px → **0**, '실험 방법 알아 두기'가 보인다 |
| 2-1-3 | L3 대표 선이 흐림 | 굵기 0.035 → 0.05, 불투명 0.55 → 0.9, **점선**, 구별되는 3색. 3월에 대표 선 2개 → 3개(범례와 늘 일치) |
| 2-1-3 | L4 바뀌는 값 이름표 | "남중 고도 ○○.○°" 3D 1 → 0, 2D 1 → 0 |
| 2-1-3 | L5 3D·2D 좌우가 반대 | 3D 처음 시점도 **동쪽이 왼쪽**(남쪽 하늘을 바라본 모습). "처음 방향으로"도 같다 |
| 2-1-3 | L8 도구 줄 2줄 | 102px → **48px 한 줄**(1024×768·1280×800 기본 화면) |
| 2-1-3 | L11·L12 막대 높이·번호 | 막대 186 → **126px**(범례가 있으면 204 → 148) → 3D가 59px 커짐. 크게 보기 번호 ② → **👀** |
| 2-1-3 | L2 2D + 크게 보기 막대 | 아래 이동 막대보다 383~444px 아래 → **19px 위**(가로 태블릿) |

## 2. 발견별 바꾼 것과 전·후 수치

### 2-1-2 M2 — 옆에서 본 모습 칸(사용자 결정)
* **코드**
  * `app.js:340-405` `makeSideView()`: SVG 그림이다. 하늘·땅, 나무(3D와 같은 비율), 그림자, 해 → 나무 꼭대기 → 그림자 끝을 잇는 빛줄기, 그림자 끝의 빨간 호, 해를 그린다.
  * `set(d)`는 **3D와 같은 태양 방향 벡터 d**만 쓴다.
    * 각의 cos·sin = d의 수평 길이·d.y
    * 그림자 길이 = 10 × 수평 길이 ÷ d.y
    * 빛이 오는 쪽 = d.x의 부호
  * `applySun(d)`의 마지막 줄 `side.set(d)`(`:1005`)에서 부르므로 해를 끄는 동안에도 함께 바뀐다.
  * 칸의 글자는 "옆에서 본 모습(모형)"과 "태양 고도"뿐이고 숫자는 없다. 화면 읽기 설명에는 "지금 햇빛은 왼쪽(동쪽)에서 와요"를 쓴다.
* **자리**
  * 3D 칸 오른쪽 위: 위에서 46px(토글과 같은 줄), 빨리 감기 배지 아래. `style.css:37-61`
  * 폭은 `placeSide()`(`app.js:881-955`)가 **재서 정한다**. 처음 시점에서 칸 높이 띠 안에 있는 해 길(해 원판)·이름표의 가장 오른쪽을 재고, 칸이 그보다 오른쪽에 오게 한다(28%, 132~240px). 학생이 시점을 돌려 두었어도 잠깐 처음 시점으로 재고 곧바로 되돌린다(돌린 방향 유지 확인).
  * 폭을 재는 까닭: 장면 칸이 가로에 비해 높을수록(머리말 접기, 1366×1024·1920×1080 기본 화면) 15:30 쪽 해 길이 오른쪽으로 온다. 28%로 고정했을 때 1024×768 머리말 접기에서 해와 306px², 시각 이름표와 39px² 겹쳤다. 잰 폭에서는 0.
  * 3D 칸이 폭 480px·높이 250px 미만이면 숨긴다. 375×812(두 모드), 812×375 크게 보기가 여기에 해당한다(2-1-5와 같은 방식). 휴대폰에서는 2D 화면 아래쪽이 같은 옆모습이다.
* **칸 크기(폭×높이 px)**

  | 화면 | 기본 | 크게 보기 |
  |---|---|---|
  | 1024×768 | 153×110 (머리말 접으면 138) | 240×161 |
  | 768×1024 | 206×141 | 206×141 |
  | 1280×800 | 181 (접으면 173) | 240 |
  | 1180×820 | 181 (접으면 168) | 240 |
  | 1366×1024 | 151 | 240 |
  | 812×375 | 213 | 숨김 |

* **각(화면에서 잰 값, 칸 SVG의 실제 화면 좌표로 계산)** — 1024×768·768×1024·1280×800 × 기본·크게 보기가 모두 같다.

  | 시각 | 패널 | 칸 빛줄기 | 칸 호 | 빛이 오는 쪽 |
  |---|---|---|---|---|
  | 9:30 | 34.5 | 34.50 | 34.49 | 왼쪽 |
  | 10:30 | 43.5 | 43.50 | 43.51 | 왼쪽 |
  | 11:30 | 49.7 | 49.70 | 49.70 | 왼쪽 |
  | 12:30 | 51.7 | 51.70 | 51.69 | 오른쪽 |
  | 13:30 | 48.3 | 48.30 | 48.31 | 오른쪽 |
  | 14:30 | 42.2 | 42.20 | 42.20 | 오른쪽 |
  | 15:30 | 32.8 | 32.80 | 32.80 | 오른쪽 |

  * 12:30이 오른쪽인 까닭: 12:30의 방위는 182.2°(남중 12:25, 방위 180.1°)로 남중 뒤이기 때문이다.
  * 눈금 사이 18곳(20분 간격)에서도 칸 속 각 = 3D 해의 고도(차 0.00°).
  * 3D에서 처음 시점에 보이는 호는 그대로다(28·32·30·13·12·23·22°). 칸이 대신 실제 각을 보여 준다.
* **끌기**
  * 1024×768 마우스, 768×1024 터치로 9:30 해를 12:31까지 끌었다. 7단계마다 칸 속 각 = 3D 고도(39.26 … 51.68)였다.
  * 빛은 12:00까지 왼쪽, 12:25부터 오른쪽이었다(d.x −0.1096 → +0.0011).
  * 놓으면 12:30에 맞춰지고 기록하기가 켜진다. 끄는 동안 카메라 변화는 0이다.
  * 칸은 누르기를 통과시킨다(`pointer-events:none`).
* **겹침(0이 목표)**
  * 잰 화면: 12가지 화면 + 1366×1024 두 모드 × (7개 눈금 + 눈금 사이 18곳) + 머리말 접기 7곳, 그리고 어두움 2가지.
  * 잰 것: 칸 ↔ 토글·시각·빨리 감기 배지(빨리 감는 중에도)·모형 배지·막대·안내 글·해·해 길 점·시각/물체/방위 이름표·나무·사람·백엽상·모든 그림자·그림자 끝·3D 호·주황 빛줄기.
  * 결과: **모두 0**. 가로 스크롤 0, 콘솔 오류 0.
* **그림 색**: "태양 고도" 글자 대비 4.89:1이다. 어두움에서는 틀만 어둡고 그림 색은 그대로다.

### 2-1-2 L5 — 2D 옆모습 빛 방향
* **바꾼 것**
  * `app.js:1153-1230`(자리 맞추기)·`:1262-1301`(그리기): 나무·사람을 x=0 둘레에 그리고, 해가 서쪽이면(`sunVec(az,alt).x > 0`, 3D와 같은 식, `:1264`) 땅 폭 안에서 좌우 자리를 바꾼다.
  * 그림자·빛줄기·옅은 빛줄기·치수선·호(sweep 방향 포함)를 같은 부호로 그린다. 글자는 뒤집지 않고 자리·정렬만 바꾼다.
* **확인**: 1024×768 기본·크게 보기, 768×1024, 375×812 × 7개 시각에서 다음을 모두 확인했다.
  * 위 하늘의 해 쪽 = 옆모습의 빛 쪽 = 그림자 반대쪽 → 7/7
  * 빛줄기 각 = 패널(51.69/51.7처럼 반올림 차만)
  * 2D 글자끼리·사람과 겹침 0

### 2-1-2 L11 — 안내 글 ↔ '북'
* **바꾼 것**: '북' 이름표만 높이 2 → 3.1, 크기 3 → 2.7(`app.js:501`).
* **자리를 고른 방법**: 이 탭에서 이름표를 옮겨 가며 7개 화면 × 15개 시각을 재서 골랐다. 예를 들어 3.2/3.0은 휴대폰 11:30에서 그림자 끝과 6px² 겹쳐서 뺐다.
* **처음에 시도했다가 되돌린 것**: 안내 글을 왼쪽 절반(두 줄)에 두는 방법. 1024×768 15:30에 그림자 끝·그림자와 겹쳐서 되돌렸다.
* **결과(전 → 후)**

  | 화면 | 안내 글 상자 ∩ '북' | 실제 글자 ∩ '북' |
  |---|---|---|
  | 1024×768 기본 | 79 → 0 | 21 → 0 |
  | 1280×800 기본 | 11 → 0 | — |
  | 812×375 기본 | 155 → 0 | — |
  | 812×375 크게 보기 | 213 → 65 | 0 → 0 |

  * 812×375 크게 보기에서 상자가 65px² 겹치지만, 글자는 x≈402에서 끝나고 '북'은 x≈426부터라 실제로는 가리지 않는다.
  * '북' ↔ 그림자 끝·호·그림자 길이 선·나무 그림자: 모든 화면·시각에서 0.

### 2-1-3 M3 — 들어올 때 스크롤 뺌
* **바꾼 것**: `activateExp()`의 `scrollIntoView` 삭제(`app.js:1046`). 달을 누르면 장면을 맞추는 동작은 그대로다.
* **첫 진입 scrollY(전 → 후)**

  | 화면 | 전 | 후 |
  |---|---|---|
  | 768×1024 | 306 | 0 |
  | 1024×768·1280×800·1180×820 | 278 | 0 |
  | 375×812 | 370 | 0 |

  * 알아 두기 위 끝이 보이는지: 전 false → 후 true(8가지 화면 모두).
  * 세로 태블릿 첫 진입 때 막대는 아래 이동 막대보다 51px 아래에 있다. 공통 틀의 다른 앱과 같은 동작이다(Claude 결정).

### 2-1-3 L3 — 계절 대표 3개
* **바꾼 것**
  * 색: 봄·가을 `#7b3fb8`, 여름 `#d81b60`, 겨울 `#00838f`(`app.js:124`). 어두운 3D에서는 `#c3a3ff`·`#ff7aa8`·`#45d3e0`을 쓴다.
  * 3D(`:671-700`): 굵기 0.05·불투명 0.9의 점선(표본 4칸 그리고 2칸 비움). 하늘 반구보다 2% 큰 구면에 그린다(같은 하늘 방향).
  * 2D(`:815`, `:842`): 4.5px·0.95의 점선을 지금 달의 실선 **위에** 그린다.
  * 지금 달이 대표 달과 같아도(3월) 셋 다 그린다(`pathList`, `:382`).
  * 범례: **"3월(지금)" 실선 + "봄·가을/여름/겨울 대표" 점선**(`:253`, `style.css:55`).
* **대비(비텍스트 기준 3:1)**
  * 밝은 3D 배경: 5.80 / 4.43 / 4.05
  * 2D 낮 하늘: 4.71 / 3.60 / 3.29
  * 어두운 3D 배경: 7.59 / 6.53 / 8.84
* **전 → 후(3월을 보는 중)**
  * 그려진 대표 선: 2개 → 3개
  * 굵기: 0.035 → 0.05
  * 불투명: 0.55 → 0.9
  * 범례: 3개(이 중 한 선은 안 보임) → 4개 모두 화면에 있음

### 2-1-3 L4 — 값 이름표 삭제
3D `makeNoon`의 이름표(`:663`)와 2D `drawNoon`의 상자·글자(`:850`)를 뺐다. 남중 고도 주황 선·호·정남쪽 점선은 남겼다. 쓰지 않게 된 CSS 두 줄도 지웠다. 전 1·1 → 후 0·0.

### 2-1-3 L5 — 처음 시점 좌우
* **바꾼 것**: `viewDir` [0.45, 0.5, −0.74](남서쪽에서 봄) → **[−0.34, 0.5, 0.8]**(`:497`, 북쪽에서 동쪽으로 약 23°, 높이 30°). 교과서·2D처럼 남쪽 하늘을 바라본 모습이다.
* **동쪽이 왼쪽인지(1024×768 기본에서 잰 x좌표)**: 동x 448 > 서x 95 → 동x 88 < 서x 465. 8가지 화면 모두 동쪽이 왼쪽이다.
* **"처음 방향으로"**: 돌린 뒤 누르면 같은 방향([−0.339, 0.499, 0.798])으로 돌아온다.
* **정북으로 두지 않은 까닭**: 정북에서 보면 자오선 위의 남중 고도 선·호가 옆으로 선다. 23° 돌렸을 때 화면에 보이는 호는 12월 14.9° < 10월 19.3° < 3월 23° < 6월 30.8°로, 실제 고도와 순서가 같다.
* **방위·보조선 이름표를 선 위에 그림**(`:542`, `:564`, depthTest 끔): 새 시점에서는 여름 길이 '서' 앞을 지나가서 추가했다. 선이 이름표 상자를 지나는 달은 전(옛 시점) '동' 2·3·9·10월, 후 '서' 4~8월이었다. 이름표를 밖으로 옮겨도 없어지지 않아서 이 방법을 썼다. 이제 글자가 가리지 않고, 이름표 밑 짧은 선 한 토막만 가린다.

### 2-1-3 L8 — 보기 도구 한 줄
* **바꾼 것**: 그 줄의 버튼 좌우 여백만 18 → 11px, `nowrap`, 궤적 두 버튼 묶음의 기준 폭 220px → auto(`style.css:46-50`). 높이 48px(44px 이상)는 그대로다.
* **결과**: 1024×768·1280×800 기본 화면 102 → 48px(한 줄). 휴대폰은 여러 줄 그대로(164px, 가로 스크롤 0).

### 2-1-3 L11·L12 — 크게 보기 막대
* **L11 막대 높이**
  * 막대 윗줄을 "👀 3월 21일 · 🌱 봄" + 값 두 칸 한 줄로 바꾸고, 범례는 그 아래에 둔다(`style.css:72-81`).
  * 막대: 186 → **126px**, 범례가 있으면 204 → 148px.
  * 3D 칸 높이: 1024×768 334 → 393, 1280×800 366 → 425, 768×1024 344 → 403px.
* **L12 번호**: `enlarge()`의 `onChange`로 크게 보기에서는 ② → 👀, 기본 화면에서는 ②(`app.js:266`, `:296`).
* **휴대폰 안내 글(L11 겹침 부분)**: 폭 480px 이하에서는 3D 칸이 두 줄 안내 글 위에서 끝나게 했다(`--tip-h`를 app.js가 잰다, `:187-191`, `style.css:103-106`).
  * 전: 375×812 기본에서 안내 글자가 '남'을 230px² 가림, 크게 보기에서 '서' 10px².
  * 후: 0.
  * 대신 휴대폰의 3D 칸이 53px 낮아진다(298 → 245, 크게 보기 209 → 156).

### 2-1-3 L2 — 2D + 크게 보기
* **바꾼 것**: `.ss-exp-layout.is-full .sky2d`에 화면 띠에 맞춘 최대 높이를 준다(가로·세로, `style.css:108-114`).
* **막대 아래 끝 − 아래 이동 막대 위 끝(px, 음수 = 화면 안)**

  | 화면 | 전 | 후 |
  |---|---|---|
  | 1024×768 | +383 | −19 |
  | 1280×800 | +444 | −19 |
  | 1180×820 | +424 | −19 |
  | 768×1024 | −25 | −85 |
  | 812×375 크게 보기 | +500 | +225 |

  * 812×375 크게 보기는 아래 "안 고친 것" 참고.

## 3. 화면 확인(두 모드 × 크기, 어두움, 2D)
* **2-1-2**
  * 확인한 화면: 1024×768·768×1024·1280×800·1180×820·1366×1024·375×812·812×375 × 기본·크게 보기, 머리말 접기, 어두움(768 크게 보기·1024 기본), 2D(4가지).
  * 결과: 겹침·잘림·가로 스크롤 0, 예외·콘솔 오류 0.
* **2-1-3**
  * 확인한 화면: 1024×768·768×1024·1280×800·1180×820·375×812·812×375 × 기본·크게 보기 × 3·6·12월 + 계절 대표(3·10월) + 2D, 어두움(2가지).
  * 결과: 안내 글자 ↔ 방위 이름표, 토글 ↔ 배지·빨리 감기·방위 이름표, 안내 글 ↔ 막대 모두 0. 가로 스크롤 0, 예외·콘솔 오류 0.

## 4. 끝까지 흐름·저장 불변
* **가짜 로그인, 1024×768**: 예상 → 실험 → 분석 → 정리 → 마치기를 진행했다.
  * 2-1-2: 7칸을 기록하되 4칸 뒤 새로 고침. 단계·기록 4개·12:30이 되살아났다.
  * 2-1-3: 12칸을 기록하되 5칸 뒤 새로 고침. 단계·5/12·고른 달이 되살아났다.
  * 궁금한 점을 비우면 "한 줄이라도 적어야" 안내가 뜨고, 적고 마치면 `app_results` POST 1건이 나간다.
  * **가로챈 `app_results`의 `details`를 원본 코드로 같은 입력을 넣어 진행한 것과 비교했더니 글자까지 같았다**(2-1-2 2,559자, 2-1-3 3,535자, 2-1-3은 크게 보기에서 한 번 더 진행해도 같음).
    * 2-1-2 `qa`: predict:q1 · experiment:records:table · analyze:q1·q2 · conclude:conclusion · curiosity
    * 2-1-3 `qa`: … conclude:curiosity
  * 로컬 저장 키 목록도 전·후가 같다(2-1-2 11종, 2-1-3 13종). 저장 키는 `sci621sim2:v1`·`sci621sim3:v1` 그대로다.
* **체험 모드**(768×1024 크게 보기, 375×812): 두 앱 모두 끝까지 진행했고, Supabase 쓰기·함수 요청 0이었다(잠금 확인용 `site_settings` 읽기만). "체험 모드라서 결과는 저장되지 않았어요"가 떴다.
* **시간표**: 두 앱 모두 조작 단계·읽을 글이 늘지 않아 spec 시간표를 그대로 둔다. 2-1-2 칸은 보기만 하는 그림이다. 실제 학생 시간은 재지 않았다.

## 5. 안 고친 것·한계·공통 틀 제안
1. **812×375(휴대폰 가로) + 크게 보기 + 2D(2-1-3)**: 화면 띠가 약 177px이라 2D 위 여백 98px(공통 규칙)·그림(최소 180px)·막대 73px이 한 화면에 들어갈 수 없다. 전 +500 → 후 +225px로 줄었다. 기본 끔·보조 기기라 그대로 두었다. 공통 틀에서 낮은 화면의 2D 위 여백을 줄이는 것을 검토할 만하다.
2. **2-1-3 휴대폰 3D 칸**: 안내 글 자리를 비운 만큼 53px 낮아졌다. 공통 안내 글이 좁은 화면에서 두 줄(" · 페이지를 …밀어요")이 되는 문제는 다른 앱에도 있을 수 있다. 공통 틀에 "좁은 화면은 3D 칸을 안내 글 위에서 끝내기" 옵션을 두면 좋겠다.
3. **2-1-2 칸 폭 재기**: `v.resetView()`로 잠깐 처음 시점을 잡고 `v.setCameraPose()`로 되돌린다(같은 작업 안이라 화면에 안 보인다). 공통 `sim3d.js`에 `homePose()`(처음 시점 자세만 돌려주기)가 있으면 더 깔끔하다.
4. **2-1-2 12:30의 빛 방향**: 12:30은 방위 182.2°(남중 12:25 뒤)라 칸에서 **오른쪽**에서 빛이 온다. 규칙("남중 뒤면 오른쪽")대로이고 과학적으로 맞다. 다만 교과서의 "12시 30분 무렵 남중"과 나란히 보면 학생이 물을 수 있어 알려 둔다.
5. **2-1-3 새 시점에서 화면 속 남중 고도 호**: 비스듬히 보여 실제보다 작다(6월 30.8° 등). 순서는 맞다. 숫자는 값 패널, 참모습은 2D에 있다. 2-1-2처럼 옆에서 본 칸이 필요한지는 사용자 판단이다.

## 6. 확인하지 못한 것
* 실제 iPad·휴대폰: 해 끌기 손맛, 칸 글자 선명도, 실제 GPU(모두 headless swiftshader).
* 실제 로그인·Supabase·Gemini(가짜 응답만).
* 실제 학생 소요 시간.
* 두 손가락 조작과 칸 폭 다시 재기가 겹치는 경우(CDP 흉내 한계).

## 7. 시험 환경·Supabase 차단 기록
* **서버·브라우저**
  * 자기 정적 서버: `python3 -m http.server 8787 --bind 127.0.0.1 --directory public`.
  * 자기 전용 headless Chrome 9347(프로필은 스크래치 `fix-B1/profile`). `--headless=new --use-angle=swiftshader --enable-unsafe-swiftshader --disable-web-security --host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`, Node 24 전역 WebSocket으로 CDP.
* **Supabase 차단**
  * 모든 시험 탭에서 첫 로드 전에 `Fetch.enable(*supabase.co*)`로 가짜 응답만 주었다(site_settings → 잠금 꺼짐, GET → [], POST/PATCH → 201, check-answer → `{ok:true, verdict:"ok"}`).
  * 실행마다 `leaked []`·`failed 0`이었다. **실제 Supabase·Gemini 요청 0.**
  * 3D 확인을 위해 이 탭에서만 `SciSim.Sim3D.create`를 감싸 뷰어를 읽었다(앱 코드 무변경).
* **localStorage**
  * `localStorage.clear()`는 쓰지 않았다. 지운 것은 두 앱 `sci621sim2·3`, `ssUiPref:`, 시험용 가짜 세션 키뿐이다.
  * 솔직히 적을 것: 처음에 앱 화면이 열린 채 키를 지웠더니 앱이 페이지를 떠날 때 메모리의 `meta`(`finishedAt`)를 다시 써서, 몇몇 시험 화면의 단계 메뉴에 "정리하기 ✓"가 남았다. 이후 같은 출처의 빈 목록 페이지에서 지우도록 고쳤고, 대표 스크린숏을 다시 찍었다.
  * 이 일은 측정값에 영향이 없었다(`detail` 비교는 이 상태가 서로 달랐던 두 번의 실행에서도 같았다). 끝난 뒤 남은 키는 0개다.
* **정리**: Chrome·서버를 끄고, 스크래치의 스크립트·결과·원본 사본·프로필을 지웠다. 스크린숏 24장만 `…/scratchpad/fix-B1/shots/`에 남겼다.

## 8. 대표 스크린숏(`/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/fix-B1/shots/`)
* **2-1-2 옆에서 본 모습 칸**
  * `fixB1-212-옆에서본모습-오전한낮오후.png`: 칸만 2배, 9:30 왼쪽 빛 · 12:30 · 14:30 오른쪽 빛
  * `fixB1-212-768x1024-크게보기-930.png`, `…-1230.png`, `…-1430.png`: 태블릿 세로 기본(크게 보기), 오전·한낮·오후 전체 화면
* **2-1-2 그 밖**
  * `fixB1-212-1024x768-기본화면-1530.png`: '북' 이름표·안내 글·칸
  * `fixB1-212-2D-크게보기-1430.png`: 2D 오후, 오른쪽에서 빛
* **2-1-3**
  * `fixB1-213-768x1024-첫진입.png`: M3, 알아 두기가 보임
  * `fixB1-213-1024x768-기본화면-6월.png`: 동쪽 왼쪽, 도구 한 줄, 이름표 없음
  * `fixB1-213-768x1024-크게보기-계절대표3개-3월.png`: 점선 3개 + 범례, 막대 126px, 👀
  * `fixB1-213-1024x768-2D-크게보기-6월.png`: 막대가 화면 안
* **비교용**
  * 전: `전-212-768x1024-크게보기-1230-호가작게보임.png`, `전-213-1024x768-기본화면-6월-동쪽이오른쪽.png`, `전-213-1024x768-2D-크게보기-막대화면밖.png`, `전-213-768x1024-계절대표3개-3월.png` 등
  * 후: `후-212-1024x768-머리말접기-1530.png`, `후-212-2D-0930-왼쪽에서빛.png`, `후-213-375x812-6월.png` 등
