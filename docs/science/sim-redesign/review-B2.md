# 재검토 보고서(Review B2) — 단계 B 수정(fix-B1·fix-B2) 확인, 배포 전 마지막 검토 (2026-09-25)

지침: `review-B2-instructions.md`. **코드는 고치지 않았다**(발견만). 읽은 것: CLAUDE.md, spec.md 개정 5, review-B.md, fix-B-instructions.md, fix-B1·fix-B2 보고서, 공통 틀 README(끌기 `opts.pad`, 가로 화면 장면 높이), `git diff f93f716`(공통 틀·4개 앱).

**한 줄 요약**: 지난 발견 13개 중 11개 고쳐짐, L4·L7 덜 고쳐짐. 높음 0. 새 발견은 중간 1개와 낮음 5개다.
* **중간 N1**: 2-1-2 "옆에서 본 모습" 칸의 좌우가 12:25~12:59에 3D 처음 시점과 반대다(12:30 눈금 포함).
* **낮음 N2**: 공통 틀 변경으로, 휴대폰 가로 크게 보기에서 머리말을 펼친 채면 기록 버튼이 더 많이 가려진다.

기록 저장·`detail`·과학 값은 단계 A 커밋과 같다(2-1-4 `q1b`만 다름). 콘솔 오류 0.

---

## 1. 지난 발견별 결과

| # | 결과 | 확인한 수치(재현은 review-B와 같게) |
|---|---|---|
| M1 | **고쳐짐** | 2-1-4 30°에서 누르기만 한 경우: 머리 위 가장자리·머리 가운데·빨간 버튼·몸통 아래·몸통 끝·렌즈 6곳 × (마우스 1024×768, 터치 768×1024 크게 보기, 터치 1024×768 크게 보기) = **18/18번 30°→30°**. 기록 버튼은 "📝 30° 기록하기" 그대로. 3·2px 살짝 끌기 3/3 유지. 손잡이 밖 빈 곳(손전등 축에서 42~45px, 67~72px) 끌기 6/6: 각도 그대로, 카메라는 돎. 진짜 끌기(머리 30°→50° 자리) 3/3: 슬라이더·패널·3D 모두 50°. 보이지 않는 손잡이만 맞는 곳(몸통 밖 3.3) 끌기 50°→70°. 새 탭에서 끄는 동안 카메라 변화 0(마우스·터치). CDP 실제 입력으로 콘솔 오류 0(fix-B2가 적은 NotFoundError는 합성 이벤트 탓으로 보임) |
| M2 | **고쳐짐**(각·끌기·겹침). 좌우는 N1 | 14개 화면 × 13시각(눈금 7 + 사이 6) = 182번 측정. 칸 속 빛줄기 각·호 각 = 3D 호(최대 차 **0.025°**) = 패널(반올림 차 ≤0.055°). 표는 아래. 숫자 0. 칸은 3D 칸 안에 있음. 해를 끄는 동안(마우스 1024 기본, 터치 768 크게 보기, 9:30→12:31 12단계) 칸 각 = 패널 = 3D였고, 놓으면 12:30에 맞춰지고 기록하기가 켜졌다. 카메라 변화 0. 겹침은 아래 "M2 겹침" |
| M3 | **고쳐짐** | 예상하기 → 다음 단계로 처음 들어왔을 때 4개 앱 × 768×1024·1024×768·375×812 모두 **scrollY 0**. 스크롤 명령은 단계 이동의 "맨 위로" 1번뿐이고, '실험 방법 알아 두기'가 펼쳐진 채 보인다. 공통 틀 앱(sci-6-2-2-3)과 같다 |
| L1 | **고쳐짐** | 2-1-5 8개 화면(어두움 포함) × (라·기울임 실행 → 처음 방향으로 → 다시 실행 → 기록): 확대 칸 **보임 → 숨김 → 보임 → 숨김**. 기록 뒤 '라' 지구본이 가리지 않는다(showcase 07) |
| L2 | **고쳐짐**(태블릿) | 2D + 크게 보기에서 (막대 아래 끝 − 아래 이동 막대 위 끝): 2-1-3 −19(대표 3개 범례가 있으면 +3, 기록 버튼은 −6), 2-1-4 −23~−60, 2-1-5 −18~−54(실행 뒤 +3, 버튼 −6), 세로 −42~−94. 휴대폰 가로 812×375 2D는 +225~+520 그대로(fix-B1이 적은 한계) |
| L3 | **고쳐짐** | 대표 선은 굵기 0.05·불투명 0.9 점선(색마다 16조각 × 3색). 범례 4개("3월(지금)" + 대표 3개). 3월에도 대표 선 3개를 다 그린다(showcase 04) |
| L4 | **덜 고쳐짐** | 2-1-3: 달을 골라도 3D 이름표 수 8→8, 2D 글자에 '남중 고도' 없음. 2-1-4: 3D 숫자는 없어졌지만 **2D 그림에 각도 숫자가 남음**(N3) |
| L5 | **고쳐짐** | 2-1-3 3D 처음 시점에서 동쪽이 왼쪽: 7개 화면 7/7. '처음 방향으로'를 누르면 처음 시점으로 돌아옴(차 <0.01). 2-1-2 2D는 위 하늘의 해 쪽 = 아래 옆모습의 빛 쪽 7/7(12:30 오른쪽/오른쪽) |
| L6 | **고쳐짐** | 2-1-5 확대 화면: 이름표 8→3개(가~라·우리나라 숨김, 휴대폰 7→1), 돌아오면 다시 8개, 토글 밑 이름표 0(8개 화면). 2-1-4 375×812: 손전등 이름표 숨김(6각도 × 켬·끔), 안내 글 한 줄(19px) |
| L7 | **덜 고쳐짐**(머리말을 펼치면 나빠짐) | 머리말 접음: −9px(고쳐짐). 머리말 펼침(처음 상태): **+65px(기록 버튼 +57)**. 단계 A 커밋은 +25/+17이었다 → N2 |
| L8 | **고쳐짐** | 도구 줄 48px 한 줄(1024×768·1280×800 기본) |
| L11 | **고쳐짐** | 안내 글자와 방위 이름표 겹침 0 — 2-1-2 '북' 4개 화면, 2-1-3 방위 4개 × 7개 화면(글자 상자로 잼). 2-1-3 크게 보기 막대 126px, 3D 393·403px |
| L12 | **고쳐짐** | 크게 보기 👀, 기본 화면 ② |

**M2 각(1024×768 기본; 다른 화면도 같음)**

| 시각 | 패널 | 3D 호 | 칸 빛줄기 | 칸 호 | 칸 빛 | 3D 화면의 해(나무 꼭대기 기준) |
|---|---|---|---|---|---|---|
| 9:30 | 34.5 | 34.5 | 34.500 | 34.493 | 왼쪽 | 왼쪽 −108px |
| 10:30 | 43.5 | 43.5 | 43.500 | 43.507 | 왼쪽 | 왼쪽 −81px |
| 11:30 | 49.7 | 49.7 | 49.697 | 49.696 | 왼쪽 | 왼쪽 −50px |
| **12:30** | 51.7 | 51.7 | 51.700 | 51.689 | **오른쪽** | **왼쪽 −17px** |
| 13:30 | 48.3 | 48.3 | 48.303 | 48.306 | 오른쪽 | 오른쪽 +17px |
| 14:30 | 42.2 | 42.2 | 42.198 | 42.205 | 오른쪽 | 오른쪽 +48px |
| 15:30 | 32.8 | 32.8 | 32.799 | 32.795 | 오른쪽 | 오른쪽 +76px |

**M2 겹침**
* 칸 크기: 1024 기본 153×110(머리말 접으면 138), 1024·1180·1280 크게 보기 240, 768×1024 206×141, 1280·1180 기본 181, 1366×1024 기본 151, 812×375 기본 213. 812×375 크게 보기와 375×812는 숨김.
* 칸이 가리는 DOM(토글·시각·빨리 감기·모형 배지·막대·안내 글): 0.
* 칸이 가리는 3D(나무·그림자·사람·백엽상·해 원판·시각 점·이름표): 0. 해 원판과의 틈 ≥10px.
* 조금 겹치는 것
  * 옅은 빛줄기: 20~120px².
  * 15:30 해 빛무리 사각형: 1024 기본 머리말 접기 1,656px², 1366×1024 기본 2,256px². 가장자리가 투명한 부분이라 눈으로는 거의 안 보인다(확인함).
* 812×375 기본 화면: 장면(300px)이 화면 띠(182px)보다 커서, 장면을 맨 위에 맞추면 칸 아래쪽이 아래 이동 막대 밑에 온다. 스크롤하면 보인다.

## 2. 새 발견

### 중간
**N1. 2-1-2 옆에서 본 모습 칸과 3D 처음 시점의 좌우가 12:25~12:59에 반대(12:30 눈금 포함)**
* **파일**
  * 칸의 좌우: `app.js:380` `var sg = d.x > 0 ? -1 : 1;`. 정남(방위 180°)을 지나면 뒤집는다.
  * 3D 처음 시점: `app.js:444` `viewDir: [-0.217, 0.438, 0.872]`. 북쪽에서 동쪽으로 14° 돌린 시점이다.
  * 그래서 3D 화면에서는 해가 방위 약 194°(13:00)가 되어야 나무 꼭대기 오른쪽으로 넘어간다.
* **재현**: 아무 화면에서 12:30 눈금을 누른다(showcase 02).
  * 3D: 해가 나무 꼭대기보다 **17~21px 왼쪽**에 있고, 그림자 끝은 화면 오른쪽 아래로 뻗는다.
  * 칸: 해·빛이 **오른쪽**에서 오고, 그림자는 왼쪽으로 뻗는다.
  * 1분 간격으로 재면 칸은 12:25에 뒤집히고, 3D는 13:00에 넘어간다. 1024×768 기본·768×1024 크게 보기·1280×800 크게 보기 모두 같다(35분).
  * 해를 끌며 보면 12:25에 칸만 먼저 좌우가 바뀐다.
* **영향**
  * 지시의 "빛이 오는 쪽은 3D 화면과 같게"를 칸의 목적 시각인 12:30에 지키지 못한다.
  * 나란히 놓인 두 그림의 좌우가 거울처럼 다르다(CLAUDE.md "시점 방향" 규칙).
  * 각 크기(51.7°, 하루 중 가장 큼)는 바르게 보인다. 저장·과학 값과는 무관하다.
* **제안**
  * 좌우를 3D 처음 시점 기준으로 정한다. `sg`를 처음 시점 카메라의 오른쪽 벡터(약 (0.970, 0, 0.242))와 d의 내적 부호로 정한다. 더 정확하게는 `v.project`로 해와 나무 꼭대기를 처음 시점에서 투영해 비교한다.
  * aria-label의 "(동쪽)/(서쪽)"은 12:25~13:00에 맞지 않게 되므로 방위를 따로 말한다.
  * 2D(정남을 바라본 그림)는 지금대로 맞다.
  * 학생이 3D를 돌린 뒤에도 맞추려면 지금 카메라 기준으로 할 수도 있다(Claude 판단). 처음 시점을 정북으로 돌리는 방법은 겹침을 다시 재야 한다.

### 낮음
**N2. 공통 틀 회귀: 휴대폰 가로(812×375) 크게 보기 + 머리말 펼침(처음 상태)에서 기록 버튼이 더 가려짐**
* **파일**: `scripts/templates/science-sim/experiment.js:275, 350`. 가로 화면이면 `Math.max(MIN_SCENE_H(240), 띠 − 16)`으로 잰다. 그런데 띠가 182px이라 장면이 240px이 된다.
* **수치**: 막대가 아래 이동 막대 밑으로 **+65px**, 기록 버튼은 +57px. 단계 A 커밋은 +25/+17이었다.
  * 같은 조건을 잰 앱: 2-1-3·2-1-4·2-1-5 + sci-6-1-1-2·6-1-2-3·6-2-2-3·6-2-3-3(2-1-2는 이 화면에서 막대가 없다).
  * 공통 틀을 쓰는 실험 앱 20개가 모두 같은 규칙을 따른다.
  * 머리말을 접으면 −9px로 고쳐진다(A 커밋 −52).
* **실제 영향**
  * create() 앱: 실행 뒤 막대가 보이게 스크롤해 준다(기록 버튼이 화면 안).
  * 2-1-3: 달 재생 뒤 스크롤해 준다.
  * 2-1-4: 학생이 손으로 내려야 한다(showcase 08).
* 가로 화면은 크게 보기가 꺼져 있는 게 기본이고 휴대폰은 보조 기기라 낮음으로 본다.
* **제안**: 띠가 240px보다 낮으면 띠에 맞추거나(최소 160 등) 낮은 화면은 예전 CSS 값을 유지한다.

**N3. 2-1-4 2D 그림에 각도 숫자가 남음**
* 파일: `app.js:892, 991`(`angT` → "30°").
* 개정 5-3은 "2-1-4 각도 숫자는 장면에서 뺀다"고 했다. 2-1-3은 3D·2D 모두 뺐다.
* 값은 패널·조도 HUD에 있다.
* 제안: `angT`를 빼고 호는 남긴다.

**N4. 2-1-5 확대 화면에서 확대 칸이 전등·이름표 일부를 가림**
* 768×1024 크게 보기
  * 나 위치: 전등 공 2,988~3,912px²와 빛무리(스크린숏 `shots/inset-768x1024-크게-자전축 기울임-나.png`).
  * 다 위치: 이름표 5,104~5,544px².
* 1024 기본 기울임·라: '자전축' 이름표 952px²('축' 글자가 칸 밑).
* 빛 방향은 화살표와 칸의 💡로 보인다. 단계 B부터 있던 배치다.
* 제안: 확대 화면에서 '자전축' 이름표를 칸 반대쪽에 둔다. 전등이 칸 밑이면 바라보는 점을 조금 더 옮긴다.

**N5. 휴대폰에서만 보이는 겹침**
* 375×812 2-1-3 기본 화면: 토글이 30°·60° 보조선 이름표(각 108px²)와 길 일부를 가린다.
* 375×812 2-1-4 크게 보기: 토글 밑에 손전등 뒷부분(396px²)이 온다. 머리는 보여서 끌 수 있다.
* 812×375 안내 글 ↔ '스마트 기기 화면' 이름표: 560px²(2-1-4).

**N6. 2-1-2 옆모습 칸 글자가 작음**
* 가로 기본 화면에서 칸 폭이 138~153px라 칸 속 "태양 고도" 글자가 약 **8px**다(1024×768·1366×1024).
* 크게 보기(240px)·세로(206px)는 11~13px.
* 제안: 칸 폭 하한을 올리거나 칸 속 글자를 키운다.

(참고) 2-1-4 2D의 "넓게 퍼져 닿아요/좁게 모여 닿아요"는 빛 자국 모양이라는 관찰 사실이다. 전부터 있던 글이라 문제로 보지 않았다.

## 3. 항목 2~4 판정

**2. 회귀 — 통과(N2 제외)**
* **스윕**: 4개 앱 × 17개 화면·모드(768×1024 크게 보기 기본, 1024×768 끔·켬, 1280×800 켬, 375×812 끔·켬, 812×375 켬·머리말 접기, 어두움 2, 2D 7). 여기에 2-1-3 대표 3개, 2-1-5 실행 상태를 더해 약 100가지 상태를 봤다.
  * 가로 스크롤 0, 콘솔 오류 0, 장면 안 DOM끼리 겹침 0.
  * 막대 − 아래 이동 막대: 태블릿 가로 켬 −9(버튼 −17), 세로 −235~−255.
  * 3D↔2D + 토글을 4번씩 반복한 뒤에도 캔버스·칸·막대·토글은 1개씩이다.
  * 학생이 돌린 카메라 방향은 칸 다시 재기(크게 보기·머리말 접기) 뒤에도 그대로다.
* **끝까지 흐름**: 가짜 로그인 1024×768 4/4, 체험 768×1024 크게 보기 4/4.
  * 중간에 새로 고치면 단계·기록 수·시각/달/각도/조건 선택이 되살아났다.
  * 궁금한 점을 비우면 "'더 탐구하고 싶은 점'을 한 줄이라도 적어야 마칠 수 있어요."가 뜨고 막힌다.
  * 마치면 로그인은 "제출 완료", 체험은 "제출 안 됨"이 뜬다.
  * 체험 모드의 쓰기 요청은 0이다.
* **`app_results` 본문**: 같은 입력으로 단계 A 커밋(`f93f716`)과 비교했다.
  * 2-1-2·2-1-3·2-1-5: `details`가 **글자까지 같음**(시간 값만 다름).
  * 2-1-4: `predict.q1`→`q1b`와 `qa` 첫 줄의 질문 글만 다름.
  * 저장 키 목록이 같다. `detail`을 만드는 코드는 4개 앱 모두 diff 0이다.
* **과학 값**: lesson-config를 비교했더니 결과 표는 그대로다. 바뀐 것은 2-1-2 intro·modelNote, 2-1-4 predict뿐이다. sun-table도 같다.

**3. 공통 틀 변경의 영향 — 통과(N2 제외)**
* 다른 앱 4개와 2-1-5를 1024×768 켬·끔, 768×1024 기본에서 열었다. 막대·장면 수치가 A 커밋과 같고 콘솔 오류는 0이다.
* 812×375에서는 N2다.
* 사본 `diff -r`: science-sim 20·science-guide 3·class1-record 23개 모두 정본과 같다.
* `npm run lint` 통과. 4개 앱 JS와 공통 틀 `node --check` 통과. 새 `console.log`·`debugger`·`localStorage.clear()` 0. 404는 favicon뿐이다(시험용 목록 페이지).

**4. 답을 먼저 알려 주기 — 통과**
* 4개 앱 실험 화면 글 79~107개(숨은 요소·aria-label·title·placeholder 포함)와 기록 알림에 "~할수록·그래서·때문·비례·원인"이 0이다.
* 새 칸의 글자는 안 바뀌는 이름("옆에서 본 모습(모형)", "태양 고도")뿐이다. aria-label은 그림 설명이다.
* 알림은 값만 말한다(예: "📝 12:30 기록: 51.7° · 7.9 cm · 26.1 °C").

## 4. 사용자에게 보여 줄 스크린숏 — `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-B2/showcase/`
1. `01_탐구2_옆에서본모습_태블릿세로768x1024_크게보기_0930오전.png`
2. `02_탐구2_옆에서본모습_태블릿세로768x1024_크게보기_1230한낮_3D와좌우다름.png` (N1)
3. `03_탐구2_옆에서본모습_태블릿세로768x1024_크게보기_1430오후.png`
4. `04_탐구3_남쪽하늘3D_계절대표3개_태블릿세로768x1024_크게보기_3월.png`
5. `05_탐구4_손전등끌기_태블릿세로_크게보기_30도에서62도로끄는중.png`
6. `06_탐구5_확대화면과확대칸_라기울임_태블릿세로768x1024_크게보기.png`
7. `07_탐구5_기록뒤전체화면_확대칸없음_라기울임_태블릿가로1024x768_기본화면.png`
8. `08_탐구4_휴대폰가로812x375_크게보기_머리말펼침과접음.png` (N2: 왼쪽 펼침 — 기록 막대 가림, 오른쪽 접음 — 보임)

전체 스크린숏은 같은 폴더의 `../shots/`(스윕·M1·M2·L1 등).

## 5. Supabase 차단 기록
* **서버·브라우저**
  * 자기 정적 서버: `python3 -m http.server 8789`(작업 트리 `public/`), 8781(`git archive f93f716 public`).
  * 자기 전용 headless Chrome 9351(프로필은 스크래치 `review-B2/profile`), `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`.
* **가짜 응답**: 모든 탭에서 첫 로드 전에 `Fetch.enable(*supabase.co*)`로 가짜 응답만 주었다.
  * site_settings → 잠금 꺼짐, GET → `[]`, POST/PATCH → 201(본문 기록), OPTIONS → 204.
  * check-answer → `{ok:true, verdict:"ok"}`.
* **결과**: 기록을 남긴 실행 17번에서 가로챈 요청 436건. **새어 나간 요청 0·실패 0·Gemini 0**. 기록을 남기지 않은 짧은 시험 7번도 같은 가로채기·DNS 차단 아래서 했다.
* **지킨 것**
  * `localStorage.clear()` 안 씀. 지운 것은 각 앱 `sci621sim*`·`ssUiPref:`·시험용 가짜 세션 키뿐이다. 앱 키는 앱이 열리지 않은 같은 출처 페이지에서 지웠다.
  * 내려받기·git commit/push·실 DB 쓰기·`npm run build` 없음. 다른 탭·Chrome 안 건드림.
  * CDN(three.js·글꼴)은 페이지가 스스로 불렀고, 따로 받은 파일은 없다.
* **정리**: 자기 Chrome(9351)·정적 서버(8789·8781)는 끝냈다. 스크래치 정리(`review-B2/`의 `lib/` 시험 스크립트·`out/` 결과 JSON·`base/` A 커밋 사본 16MB·`profile/` Chrome 프로필 149MB·`chrome.pid` 삭제)는 **`rm -rf` 권한이 거부되어 하지 못했다**. 남긴 것은 `shots/`·`showcase/` 외에 위 다섯 가지다(지워도 된다).

## 6. 확인하지 못한 것
* 실제 기기: iPad·안드로이드의 손가락 끌기 감도(6px 문턱), 두 손가락 조작, Safari. 실제 GPU(모두 swiftshader).
* 실제 로그인·Supabase·Gemini.
* 실제 학생 소요 시간(조작 단계는 늘지 않았다).
* 실제 휴대폰 브라우저의 주소 막대 높이(N2는 더 나빠질 수 있다).

---
**판단: 배포해도 됨.**
* **까닭**: 높음 0이다. 기록 저장(`app_results`·저장 키)·과학 값·흐름·새로 고침 이어 하기가 A 커밋과 같다. 콘솔 오류 0, 가로 스크롤 0이다. M1·M2(각)·M3·L1이 모두 수치로 고쳐졌다.
* **N1(중간)**: 12:30 좌우 불일치다. 각 크기·저장에는 영향이 없어 배포를 막을 정도는 아니다. 다만 한 줄 수정이라 가능하면 배포 전에, 아니면 곧바로 이어서 고치기를 권한다.
* **N2(휴대폰 가로 회귀)**: 함께 고치면 좋다.
