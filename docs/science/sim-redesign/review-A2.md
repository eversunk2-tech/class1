# 단계 A 재검토 보고서(Review A2) — fix-A 확인 (2026-09-25)

지침: `review-A2-instructions.md`. 코드는 고치지 않았다(이 파일만 새로 씀). 스크린숏은 스크래치 `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-A2/` 아래 `shots/`(약 330장)·`showcase/`(10장).
환경: 개발 서버(`localhost:3000`)는 검토 내내 켜져 있었다(작업 트리 그대로 사용, 끄거나 재시작 안 함). 자기 전용 headless Chrome 9351, 자기 정적 서버 8781(`git archive HEAD` 사본 + fix 전(Build A) 사본 `fix-A/pre`를 복사한 것)·8782(시험 사본). 검토 시작(11:23) 뒤 저장소 파일 변경 없음(`find -newer`).

## 0. 결론

* **높음 2건(H1·H2)은 재현되지 않는다.** 중간 7건 중 M3·M4·M5·M6·M7은 고쳐졌고, M1은 머리말을 펼친 상태에서 겹침 0(머리말을 접었을 때 두 줄 알림만 4~33px 내려옴), M2는 태블릿 세로(크게 보기 기본)에서 왕복이 없어졌고 가로 크게 보기(학생이 켠 경우)에만 왕복 1번이 남는다(L9 때문, 지침상 안 고침).
* **끔(기본 화면) 상태 회귀 없음**: 배치는 제목 폭 −76px(접기 버튼)뿐, 기록 버튼 켜짐/꺼짐 순서 같음, 자동 스크롤은 HEAD와 같거나 조금 적다.
* 새로 찾은 것: **중간 1건(N1 — 수업 중 `sci-6-2-1-2`가 세로 태블릿 기본 크게 보기에서 '기록하기'가 첫 화면 밖으로 밀림)**, 낮음~중간 1건(N2 — 창 높이만 바뀌는 resize에 장면으로 끌려감, 실기기 확인 필요), 낮음 4건.
* `diff -r` 23/23 일치, `npm run lint` 오류 0, 임시 코드·`localStorage.clear()` 없음, 20개 실험 앱 × 3설정 콘솔 오류 0.

## 1. 지난 발견별 결과

| # | 판정 | 재현 결과·수치 |
|---|---|---|
| H1 | **고쳐짐** | `sci-6-2-1-2` 768×1024(기본 켬)·1024×768 켬·끔: 막대 숨김(버튼 0), 공통 실행·기록 버튼은 안 보이고 초점도 못 받음, 보이는 '기록하기'는 앱 버튼 1개. 시간 바 눈금 7곳 기록 → 7/7, 저장된 기록 7개, 가짜 `{"phase":"M"}`·`scene.undefined` 0(3설정 모두) |
| H2 | **고쳐짐** | `sci-6-1-2-4` 초시계 4.2초에 5.5 입력 → 막대의 기록하기 꺼짐 + "초시계에 나온 숫자를 다시 확인해 보세요." → 눌러도 0/2 → 4.2 입력 → 켜짐 → `time 4.2, reading 4.2`(1/2). 1024×768 켬·768×1024 기본 켬·1024×768 끔·"틀린 값 입력 뒤 토글로 끔"·"Enter 키" 5경우 모두 같음 |
| M1 | **고쳐짐(머리말 펼침)** / 접힘 일부 남음 | 알림↔보이는 장면 겹침: 머리말 펼침 12경우(2앱 × 기본·크게 × 가로·세로 + 375×812·812×375) **모두 0**. 머리말 접힘: 두 줄 알림이 머리말 아래로 4~33px 나와 장면 1,656~8,976px²(`6-1-1-2` 크게 세로 8,976, `6-2-1-2` 기본 가로 5,396·세로 1,656) 또는 패널 글 4,098~13,550px²(812×375)와 겹침. 한 줄 알림은 0. `pointer-events:none` 유지(머리말의 접기·로그인 버튼 위에 떠도 눌림) |
| M2 | **대부분 고쳐짐** | 아래 §6 표. 보기를 고른 뒤 '확인하기'가 화면 안: fix 전 0/6 → **6/6**, 확인 뒤 '기록하기'(막대) 화면 안 6/6, 실행 뒤 막대 화면 안: 1/10 → **10/10**. 크게 보기 합계(5앱 × 2크기): 움직임 62→48번(손 24→16), 거리 37,600→17,857px. 태블릿 세로(기본 켬)는 왕복 없음. **가로 크게 보기는 왕복 1번 남음**: 실행 뒤 막대를 화면에 남기느라 관찰 카드의 보기·확인하기가 첫 화면 밖 → 손으로 내림 → 확인 뒤 자동으로 올라옴(`6-2-3-3` 286px, `6-1-2-3` 315px, `6-2-2-3` 332px, `6-2-2-4` 488px). 원인은 L9(장면과 패널 사이 보기 도구·모형 설명) |
| M3 | **고쳐짐** | 4앱 × 1024×768·1180×820·1280×800·1366×768·1920×1080: 토글 직후·**접은 직후**·편 직후 **20/20 들어감**(접음: 장면 위 71, 막대 아래 = 아래 이동 막대 위 −9px). 조건 카드를 보다가 접기·펴기: 보던 카드 이동 0px(20/20), 장면으로 끌어올림 없음 |
| M4·L6 | **고쳐짐**(`6-2-2-3` 배지만 남음) | 8앱 × 768×1024·1024×768 실행 중 0.2초마다: 3D 칸이 막대 밑으로 들어간 높이 0(16/16), 막대와 겹친 글 0 — 예외 `6-2-2-3` `.hud-ff` "⏩ 빨리 감기(모형)" 4,450px²(알려진 것, 앱 CSS, 단계 D). 드래그 안내는 막대 위 캔버스 안. 다만 실행 중 카메라가 다가가면 안내 문구가 캔버스 아래쪽 3D 이름표와 겹칠 수 있음(`6-2-3-1` "전구", 앱별 연출 — 단계 B/D 점검 항목) |
| M5 | **고쳐짐(API 준비)** | 아래 §3 "enlarge()". 두 앱은 지침대로 저장소에서 안 고침(토글 없음, 기본 화면 정상) |
| M6 | **고쳐짐** | 켜진 토글: 밝음 흰 글자/#6c4dd6 5.72:1·/#9033bd 6.19:1, **어두움 #110d26/#aa9dff 8.09:1·/#d698f1 8.67:1**(전 2.34/2.18). 꺼진 토글 13.1(밝음)·15.0(어두움). 막대 실행 버튼 어두움 8.09/8.67 |
| M7 | **고쳐짐**(시험 사본) | 8782 사본만: `sci-6-2-1-2`(조건 없음, `vp` 4칸) 막대 = 값 한 줄(88px, 970/714px 폭), `sci-6-2-2-2`(조건 있음 + 값 4칸) 막대 152px = 값 윗줄 970/714px + 버튼 아랫줄 481×2/353×2px. **잘린 글자 0**, 15.2px, 캔버스가 막대에 들어간 높이 0, 기록 흐름 정상(1/6). 375×812·812×375는 값이 장면 아래 카드. 노드 늘 1개(켜기·끄기·다시 켜기, 원래 자리 있음/없음 두 경우) |
| L1 | 안 고침(지침) | 가로 크게 보기를 기억한 채 들어오면 y=0(자동 스크롤 없음) → 막대가 첫 화면 밖. 세로 기본 켬도 '알아 두기'가 펼쳐져 있으면 장면이 553px부터 |
| L2 | **고쳐짐** | `?no3d=1` 7앱 × 1024×768·768×1024·375×812 × 끔·켬 42경우: 토글을 잠깐 숨기고 그 자리를 찍어 보니 바탕과 다른 픽셀 **0%**(42/42), 2D 내용 시작 99px > 토글 아래 91px |
| L3 | **고쳐짐** | 강제 색 모드: 켬·끔 모두 1px 테두리 알약 모양이 보임(`shots/a11y-forced-colors-*.png`) |
| L4 | 일부(문서만) | 머리말 +3~19px 그대로(768 폭 긴 제목 앱은 제목이 두 줄 — `6-2-1-2` +19px 등) — 끔 상태 스크롤 거리 차이(`6-1-2-3`·`6-1-2-4` 768 +19~37px)의 원인 |
| L5 | **고쳐짐** | 접근성 트리: role button, 이름 "전체 화면 보기" ↔ "기본 화면", pressed 없음. Tab: 토글 → 실행 → (켜졌으면) 기록하기 → 보기 도구. 초점 3px 테두리 |
| L7 | **고쳐짐** | `sci-6-2-3-3` 3D에 시험 공을 이 탭에만 붙여 확인: 왼쪽 끌기 start→drag×4→end, 끄는 동안 카메라 꺼짐·위치 변화 0, 끝나면 켜짐 / 오른쪽 버튼 반응 0 / 카메라를 잠가 둔 앱은 끝나도 잠김 / 합성 pointerId 41로 잡고 42가 닿았다 떨어져도 안 끝남, 41을 떼야 끝 / 숨긴 물체·숨긴 조상 안 잡힘 / 작은 공 경계 상자 +20px 잡힘·+45px 안 잡힘 / 끄는 중 `discard`·3D→2D(`dispose`) → `onEnd(cancelled:true, point 있음)` + 카메라 되돌림 |
| L8 | 일부 | `6-2-2-3`: 틀린 보기로 확인 → 실제 상태 글 숨김 유지, 맞는 보기 → 드러남(두 크기 확인). 나머지 앱(`6-2-3-1`·`6-2-3-2`·`6-2-2-1`·`6-2-2-2`·`6-1-1-1`)은 단계 D |
| L9 | 안 고침(지침) | M2의 가로 왕복 원인 |
| L10 | **고쳐짐** | 켠 채 1024→1280→1024: 4/4 들어감 |
| L11 | **고쳐짐** | 주석 "왼쪽 위", 접기 버튼 위치, 30px 기준(화면 경계 상자) 문서 반영. 작은 잔여: README "꺼짐은 픽셀 단위로 그대로"(실제 제목 폭 −76px), "원래 자리(부모·다음 형제 기준)"(구현은 앞 형제 우선) |

## 2. 새로 찾은 문제

| # | 심각도 | 위치 | 무엇·재현 | 제안 |
|---|---|---|---|---|
| N1 | **중간** | `experiment.js:634`(`useBar` false면 막대 숨김, `:318`), `style-common.css:384`(세로 켬 장면 `min(52vh,620px)`), `fit()`(가로) | **수업 중 `sci-6-2-1-2`**: 조건이 없는 앱은(H1 수정으로) 크게 보기에서 장면 안에 넣을 조작이 없어, 장면만 커지고 앱의 시간 바·값 카드·'기록하기'가 아래로 밀린다. 장면을 머리말 아래에 맞췄을 때 **768×1024(세로 기본 켬) 기록하기가 아래 이동 막대 밑으로 96px, 810×1080은 50px 숨음**(HEAD·끔은 0 — 장면 410/432px). 가로 켬(1024×768)은 시간 바 126px·기록하기 527px 아래. 7곳 기록마다 장면(해)과 기록하기 사이를 스크롤해야 함. `showcase/09-…` | 단계 A만 따로 배포한다면: 막대에 넣을 것이 없는 앱(`!useBar && !scenePanel`)은 크게 보기에서 장면 높이를 키우지 않거나(또는 세로 기본값을 끔으로), 앱이 `--ss-scene-h`를 줄 수 있게. 단계 B에서 `6-2-1-2`를 다시 만들 때 기록 버튼·값을 막대에 넣는 설계(예: `create()`에 앱 기록 버튼을 막대로 넘기는 옵션) 포함 |
| N2 | 낮음~중간(실기기 확인 필요) | `experiment.js:356-358`(모든 `resize` → `fit(true)`), `:345·:350` | 창 **높이만** 바뀌는 resize(모바일 브라우저 주소창이 스크롤로 숨거나 나타날 때 등)에도 "학생이 창 크기를 바꿨다"로 보고, 장면이 띠의 절반 이상 보이면 장면으로 다시 맞춤. 재현: 가로 크게 보기에서 조건 카드 쪽으로 180px 내린 뒤 창 높이 +56px → **180px 위로 되돌아감(4앱 × 5크기 20/20)**. 주소창이 숨는 기기(안드로이드 크롬 등)에서 학생의 스크롤과 싸울 수 있음. (패널을 보고 있을 때는 제자리 유지 — 높이 −300px 키보드 흉내에서 입력 칸 이동 −31px) | 너비·방향이 바뀔 때만 장면 맞춤(높이만 바뀌면 `"keep"`처럼 높이만 다시 재고 보던 곳 유지) |
| N3 | 낮음 | 앱 CSS `sci-6-2-2-3/style.css:199`(폭 520px 이하 `.hud-ff{top:40px;left:10px}`), `sci-6-1-2-2/style.css:136`(640px 이하 초 카운터), 토글 `style-common.css:406` | 좁은 화면에서 토글이 앱의 왼쪽 위 표시를 덮음: `6-2-2-3` "⏩ 빨리 감기(모형)" 64~80% 가림(375·390·412 폭, 끔·켬 모두 — '모형' 표시), `6-1-2-2`(수업 미사용) "⏱ N초" 87%(600×960 포함). Build A부터(토글 추가) | 단계 D에서 두 앱의 좁은 화면 규칙을 토글 아래(top ≥ 96px)나 오른쪽으로 |
| N4 | 낮음(단계 B 참고) | `sci-6-2-1-3/style.css:49`(`.ov-values` top 42px left 10px) | 시험 사본에 `enlarge()`를 붙이면 토글이 앱의 값 표시("3월 · 남중 고도 52.5° · 낮의 길이 …")와 3,167~4,001px² 겹침(토글은 끔 상태에도 있음). `shots/sp-enl-sci-6-2-1-3-1024x768-on-after-run.png` | 단계 B에서 `.ov-values`를 옮기거나 없앰(바뀌는 값은 scenePanel 원칙) |
| N5 | 낮음 | F6(`style-common.css:438`)로 3D 칸이 73px 줄며 카메라가 다시 맞춰짐 | `6-1-2-5` 1024×768 크게 보기: "빨간색 자동차" 이름표 윗부분이 토글에 살짝 닿음(fix 전엔 안 닿음), "노란색 자동차"는 캔버스 왼쪽 끝에서 잘림(fix 전과 같음) | 단계 D의 이름표·카메라 점검 목록에 |
| N6 | 낮음(설계 메모) | 관찰 카드 번호 | 크게 보기에서 관찰 카드 ③이 조건 카드 ①② **위**에 보임(번호는 단계 순서로 맞음 — 끔 ④/켬 ③, README에 의도로 적힘) | 사용자 확인 때 한 줄 알리기 |

## 3. 항목 2 — 회귀 판정

| 항목 | 판정 | 요지 |
|---|---|---|
| 끔 하위 호환(5앱 × 1024×768·1366×768·768×1024) | **합격** | HEAD ↔ 지금 `getBoundingClientRect` 약 30개 요소: 14/15는 `.ss-title` 폭 −76px만, `6-2-1-2` 768은 머리말 +19px(L4). 실행 뒤(관찰 카드) 자리도 같음. 픽셀 차이 0.5~2.6%(토글·접기 버튼·4단계 메뉴 칸) |
| 끔 기록 흐름(보기형·보기+확인형·측정하기형·값만·숫자 입력형 × 2크기) | **합격** | 켜짐/꺼짐 순서가 보이는 모든 단계에서 HEAD와 같음. 차이는 기록 직후 **숨은** 기록 버튼이 꺼져 있는 것뿐 |
| 끔 자동 스크롤(9앱 × 2크기, HEAD ↔ 지금) | **합격** | 18칸 중 11칸 같음(±2px). `6-2-2-2` 가로 자3→4(거리 1,339 같음 — 확인 뒤 83px를 27+56px로), `6-2-2-1` 자5→4(−33/−34px, 앱 도우미 중복 이동이 하나로), `6-2-2-3` +28px×2(맞히면 드러나는 상태 글 한 줄), `6-1-2-3`·`6-1-2-4` 768 +19~37px(머리말 높이 차이, L4). 늘어난 손 스크롤 0 |
| 크게 보기 관찰 카드 옮김·번호 | **합격** | 4경우 × 토글 6번: 켬 = 패널 맨 앞·③(조건 1개 앱은 ②)·기록/실행 버튼 막대·실행 카드 숨김, 끔 = 원래 자리(실행 안내 카드 뒤)·④·기록 버튼은 안내 글 앞 — 매번 같음 |
| 조건을 바꿀 때 누른 버튼 튐 | **합격** | `6-1-1-2`·`6-2-1-5`·`6-2-3-2` 가로·세로, 위 카드·아래 카드 번갈아 4번: 누른 버튼 이동 −0.5~+0.3px(관찰 카드 314~489px가 사라지며 스크롤 −328~−503px 한 번). **스크롤 앵커링을 끈 경우(`overflow-anchor:none`, iPad Safari 흉내)도 같음** — 두 번 보정·반대 튐 없음 |
| 3D 칸이 막대 위에서 끝난 뒤 카메라·이름표·안내 | **합격(낮음 N5)** | 20앱 1024×768 켬·768×1024 기본 켬 스크린숏(`shots/sheet-all20-*.png`): 막대가 있는 17앱 × 2 = 34/34 캔버스가 막대 밑으로 들어간 높이 0, DOM 겹침 0. 어색한 곳은 N5·M4 비고 정도 |
| 3D↔2D·토글 반복 | **합격** | 토글 10번 + 3D↔2D 6번(사이사이 토글): 캔버스 1→1, window 수신기 13→13, document 4→4, 토글 1, 막대 1, 경고·오류 0 |
| 새 공개 기능 `enlarge()` | **합격(API)** | 코드 읽기: `create()`의 켜기/끄기(Build A `setEnlarged`)는 모두 `place()`·`fit()`·`set()`으로 옮겨졌고 달라진 것은 F1~F8(+F4의 관찰 카드 옮김·스크롤)뿐. 스크래치 사본 `sci-6-2-1-3`·`sci-6-2-1-4`에 README 예시대로 연결(experiment.js 추가, `{layout, viewBox, panel, record, scenePanel, onChange}`, is-2d 뒤 `fit()`): 가로 기본 끔 → 켜면 막대(값 윗줄 + 기록하기), 장면 522px 들어감, **막대 기록하기로 기록됨**(3월 / 30°), 토글 4번 왕복마다 원래 자리(부모·앞 형제 동일), 기록 버튼 늘 1개, 세로 처음 켬 → 세로에서 끔 → 가로 켬(기억) → 세로 끔(기억), 375 폭은 값이 장면 아래 카드, 2D는 막대가 2D 내용 아래(static)·위 여백 98px, 오류 0. 단 N4 |
| fix-A가 지침과 다르게 판단한 곳 ① 기록 버튼이 꺼져 있을 때 '확인하기' 줄만 보이게 | **타당** | 다음에 누를 것이 '확인하기'이므로 맞다. 보기 고른 뒤 확인하기 화면 안 6/6(두 모드), 끔 상태에서 손 스크롤 증가 0, 자동 움직임 ±1번 |
| ② 장면 다시 맞추기는 학생이 접기·창 크기를 바꿀 때만 | **대체로 타당** | 불러오는 중 머리말 높이 변화(체험 → 가짜 로그인 계정 줄, 161→165px)로 자동 스크롤 0(y=0, 3앱). 다만 "창 크기"를 모든 `resize`로 잡아 높이만 바뀌는 경우까지 끌어올림 → N2 |
| 20개 실험 앱 × {1024×768 켬, 768×1024 기본 켬, 1024×768 끔} | **합격** | 60/60 예외 0·콘솔 오류/경고 0. 토글 있음 18(`6-2-1-3`·`-4` 없음), `6-2-1-2`는 막대 숨김 |
| 조사 앱 3개 | **합격** | 3앱 × 1024×768·768×1024·375×812: 접기 161→63(세로 긴 제목 180→82, 휴대폰 279→136), 단계 메뉴 숨김·`aria-expanded`, 새로 고침 뒤 접힘 기억, 다시 펴기, 알림은 예전처럼 화면 아래, 오류 0 |

## 4. 항목 3 — 남은 것 평가(사용자에게 보여 주기 전에 꼭 고칠 것?)

| 남은 것 | 지금 | 판단 |
|---|---|---|
| L1 크게 보기로 들어올 때 막대가 첫 화면 밖 | 가로(학생이 켠 경우)·세로(알아 두기 펼침) | 보여 주기 전 필수 아님 — 기본 화면도 실행 카드가 첫 화면 밖이라 예전보다 나빠지지 않음. 단계 B/D 뒤 한 번에 |
| L9 장면과 패널 사이 보기 도구·모형 설명 | 가로 크게 보기 왕복 1번의 원인 | 필수 아님(가로는 기본 끔). 단계 B/D에서 크게 보기일 때 보기 도구를 관찰 카드 아래로 옮기는 안 검토 |
| L4 머리말 +3~19px | 끔 상태 스크롤 +19~37px | 필수 아님 |
| `6-2-2-3` "빨리 감기(모형)" 배지 ↔ 막대 | 4,450px²(배지가 막대 버튼 위에 뜸, 끄는 동안만) | 필수 아님 — 단계 D 앱 CSS 한 줄(README에 방법). 휴대폰은 토글에 가림(N3) |
| 머리말 접힘 + 두 줄 알림 | 4~33px 겹침, 3.8초 이하 | 필수 아님 |
| L8 나머지 "본 모습" 글 | 5개 앱 | 필수 아님(단계 D) |
| **N1 `6-2-1-2` 세로 기본 켬** | 기록하기 96px 밑 | **보여 줄 때 함께 알리고, 단계 A만 따로 push하지 말 것**(단계 B에서 해결하거나 A에 작은 수정) |

## 5. 항목 4 — 정적 검사

* `diff -r` 정본 ↔ 사본: `science-sim` 20/20, `science-guide` 3/3 **일치**. `class1-record.js` 사본도 정본과 같음.
* `npm run lint`: 출력 없음(오류 0). `node --check`: 정본 JS 전부 + 앱 app.js 5개 통과.
* 정본·앱(app.js·data·index.html·style.css)에 `fullScene`, `debugger`, `console.log`, `localStorage.clear()`, 앱의 `.draggable(` 호출 **없음**.

## 6. M2 스크롤 표(한 칸 기록, 자=앱이 움직인 횟수, 손=학생이 밀어야 한 횟수, px=합계 거리)

크게 보기(fix 전 `pre` → 지금):

| 앱 | 1024×768 켬 | 768×1024 켬 |
|---|---|---|
| sci-6-1-1-2(보기) | 자3·손2·2,911 → 자2·손2·1,886 | 자3·손1·2,324 → 자2·손1·1,535 |
| sci-6-2-2-2(보기+확인, 첫 보기 틀림) | 자6·손4·8,234 → 자5·손1·2,023 | 자6·손5·7,118 → 자5·손2·2,101 |
| sci-6-2-3-3(보기+확인) | 자4·손3·4,590 → 자3·손3·2,409 | 자5·손2·3,427 → 자4·손1·1,535 |
| sci-6-2-1-5(값만) | 자2·손3·3,363 → 자2·손2·1,919 | 자2·손2·2,669 → 자2·손1·1,581 |
| sci-6-1-2-3(측정하기) | 자4·손1·1,745 → 자3·손2·1,775 | 자3·손1·1,219 → 자4·손1·1,093 |

* 지금 크게 보기의 추가 4앱: `6-1-2-4` 자2·손1(1,663/1,308px, 입력 뒤 막대 기록하기 화면 안), `6-2-2-1` 자3·손1, `6-2-2-3` 가로 자4·손3·세로 자3·손2, `6-2-2-4` 가로 자4·손2·세로 자6·손1.
* 기본 화면(끔)은 §3 표 — HEAD와 같거나 적음. 크게 보기 ↔ 기본 화면(지금, 5앱 합): 48(손 16) ↔ 45(손 13).

## 7. showcase(사용자용, fix 뒤 최신 — `…/scratchpad/review-A2/showcase/`)

1. `01-768x1024-태블릿세로-기본값(크게보기켬)-실행뒤-관찰카드.png` (sci-6-2-3-3, 막대 + 바로 아래 관찰 카드)
2. `02-1024x768-태블릿가로-기본값(끔).png`
3. `03-1024x768-태블릿가로-크게보기켬.png`
4. `04-1024x768-크게보기켬-머리말접음.png`
5. `05-1920x1080-PC-크게보기켬.png`
6. `06-375x812-휴대폰세로-기본값(끔).png`
7. `07-768x1024-어두움-크게보기켬.png`
8. `08-1024x768-기본(끔)-기록알림이-머리말위에.png`
9. `09-768x1024-sci-6-2-1-2-크게보기-가짜버튼없음(H1고침).png` (N1도 보임 — 기록하기가 화면 밖)
10. `10-768x1024-sci-6-1-2-4-초시계와다른값-기록하기꺼짐(H2고침).png`

## 8. Supabase 차단 기록

* 자기 전용 headless Chrome(9351, 프로필 스크래치)에 `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"` + `--disable-web-security`, 모든 탭에서 첫 로드 전에 CDP `Fetch.enable(*supabase.co*)` 가짜 응답(site_settings→`login_required:false`, app_progress GET→`[]`·POST/PATCH→201, OPTIONS→204, auth 가짜 사용자, check-answer→통과 모양).
* 기록을 남긴 시험 실행 36번에서 가로챈 요청 1,058건(거의 모두 체험 모드 `GET site_settings`, 불러오기 시험의 가짜 로그인 요청 포함), **새어 나간 요청 0, 실패 0, Gemini 요청 0**. 기록을 남기지 않은 짧은 디버그 실행도 같은 차단(Fetch 가로채기 + 주소를 127.0.0.1:9로)으로만 돌렸다. 가짜 세션은 localhost 출처 localStorage에만 넣었다.
* `localStorage.clear()` 안 씀(그 앱 `sci6…`·`ssUiPref:` 키만). 다른 탭·Chrome·개발 서버는 건드리지 않았다(끝까지 200 응답). `npm run build`·git commit/push·실 DB 쓰기·내려받기 안 함.
* 끝나고 자기 Chrome(9351)·정적 서버(8781·8782)는 종료했다(프로세스 0, 포트 닫힘). **스크래치 정리(`rm -rf`)는 거부됐다** — `…/scratchpad/review-A2/`에 스크린숏(`shots/` 50MB, `showcase/`) 말고도 `profile/`(Chrome 프로필 148MB), `head/`·`pre/`(비교용 사본 14·15MB), `sp/`(시험 사본 3.4MB), `lib/`(시험 스크립트), `out/`(측정 JSON·로그), `prefix-exp.diff`, `*.pid`가 남아 있다. 저장소와 무관하니 지워도 된다.

## 9. 확인하지 못한 것

* 실제 iPad/안드로이드 태블릿: Safari에는 스크롤 앵커링이 없어 `overflow-anchor:none`으로 흉내만 냄. **N2(주소창·키보드로 창 높이가 바뀌는 기기에서의 끌려감)**는 창 높이를 바꿔 흉내 낸 것 — 실제 기기의 resize 발생 여부는 못 봄. 회전·안전 영역(`env(safe-area-inset-top)`) 알림 자리.
* 실제 두 손가락 터치의 떼는 순서: CDP 터치 이벤트로는 둘째 손가락만 떼는 동작이 만들어지지 않아 합성 PointerEvent(pointerId 41·42)로 논리만 확인.
* 실제 로그인·Supabase·Gemini(가짜 응답만), 실제 학생 소요 시간.
* 20개 앱 전부의 실행 결과 화면을 두 모드에서 끝까지 본 것은 아님(9개 앱 흐름 + 20개 앱 첫 화면 + 8개 앱 실행 중).

**판단: 사용자에게 보여 주고 커밋해도 된다(높음 0, 지난 높음·중간은 모두 고쳐졌거나 의도된 범위만 남음). 단 push(배포) 전에 N1(`sci-6-2-1-2` 세로 기본 크게 보기에서 기록하기가 첫 화면 밖)을 사용자에게 알리고 — 단계 B와 함께 배포하거나 A에 작은 수정 — N2는 실기기 확인이나 "너비가 바뀔 때만 맞춤" 한 줄 수정을 권한다.**
