# 검토 보고서(Review pan) — 확대한 3D 장면 이동 화살표 (2026-09-26, spec 개정 8)

검토: Review 담당(코드는 고치지 않았다). 지침 `review-pan-instructions.md`. 대상: 정본 `scripts/templates/science-sim/{sim3d.js,style-common.css,README.md}`와 실험 앱 20개 사본(`public/apps/sci-*/science-sim/`). Build 보고 `pan-report.md`(8절 "사용자 피드백 반영" 포함)를 먼저 읽었다.

**검토 중에 파일이 바뀌었다**: Build가 13:15·13:21·13:32에 겉모양(원 바탕 20%·빛 테)과 아래 안내 줄(`setTip`, `tick()`의 안내 줄 다시 바꾸기)을 고쳤다. 이동·한도·자리 계산 코드는 바뀌지 않았음을 diff로 확인했고, 행렬·회귀·흐름 등 수치 시험은 13:18 이후 파일로 **처음부터 다시** 돌렸다(13:32 변경은 안내 줄 다시 바꾸기뿐). 조정자 지시대로 **불투명도·대비·아이콘 모양과 새 안내 문구 자체는 판정하지 않았다**. 최종 파일: `sim3d.js` fd481b3…, `style-common.css` 5f9d3ad…, `README.md` ffd822d…, `diff -r` 정본↔20개 사본 20/20 같음.

## 한눈에
* **높음 0 · 중간 1 · 낮음 9.** 배포를 막는 문제는 없다.
* 행렬 **280/280**(20앱 × 7크기 × 기본 화면·크게 보기): 처음 숨김 280, 확대 뒤 4개 보임 280(모두 44×44), 한 칸 1,103/1,120 = 보이는 높이의 **0.120**(나머지 17은 부하로 느린 headless 장면에서 재기 전에 덜 끝났거나 떼기가 늦게 처리된 것·▼ 바닥 미끄러짐 — 같은 경우를 따로 돌리면 0.120), **좌우·위아래 뒤집힘 0, 방향 변화 0, 카메라·과녁 따로 움직임 0, 뗀 뒤 계속 흐름 0**, DOM 겹침 0(확대 직후 280·한도 1,120), 고르기·끌기로 샘 0, 크게 보기 페이지 스크롤 0/140, 콘솔 오류 0.
* 흐름 **27/27**(9앱 × 3), 기록 +1 58/59(1건은 시험 도구 문제), 회귀 **36/36** 요소 10,728개 자리 같음·처음 3D 그림 같음.

## 발견(심각도별)
| # | 심각도 | 앱 · 파일:행 | 무엇 | 재현 | 제안 |
|---|---|---|---|---|---|
| M1 | 중간 | 공통 틀 `sim3d.js:841` `layout()` — 특히 `sci-6-2-1-5` | 화살표 자리 잡기가 DOM만 피하고 **3D 속 누를 수 있는 물체·끌 수 있는 물체는 피하지 않아**, 그 자리를 누르면 물체 대신 화면이 옮겨진다. 6-2-1-5는 확대하면 **지구본(가 위치, 끌어서 위치를 고르는 주 조작 물체)의 한가운데를 ▲가 덮는다** — 1024×768 거리 비 0.815 이하, 768×1024 0.903 이하, 1180×820 0.774 이하의 모든 확대에서(휠 1번씩 8단계 측정). 휠 4번(0.815) 직후 화살표 밑(가운데·둘레 8점)에 고를 수 있는 물체가 있는 앱 **8/20**: 6-1-2-4 자동차(◀ 9/9점), 6-1-2-5 자동차(◀▶), 6-2-2-3 아크릴 통(◀▶), 6-2-3-2 회로판(◀▶), 6-1-1-2·6-1-1-3·6-2-1-2(일부 점). 한 번 누르면 물체가 화살표에서 비켜나고, 조작 칸 버튼으로도 고를 수 있어 막히지는 않는다 | 6-2-1-5 크게 보기 1024×768 → 장면 가운데에서 휠 4번 → 지구본 가운데(342,122)의 맨 위 요소 = `.ss-pan-up` → 지구본을 끌면 화면이 위로 한 칸 옮겨짐 | `layout()` 장애물에 sim3d가 이미 가진 `draggables`·`pickables`(보이는 것)의 화면 상자(`screenBox()` 재사용)를 넣고, ▲▼는 좌우로도 비켜 갈 수 있게. 자리를 못 찾으면 지금처럼 가장자리 가운데. 최소한 `draggables`(6-2-1-5 지구본·6-2-1-2 태양·6-2-1-4 손전등) |
| L1 | 낮음 | `sim3d.js:618-622` `startPan` | 빠르게 연달아 누르면 앞 칸이 덜 간다 — 새 누름이 새 칸(`want`=한 칸, `done`=0)을 시작해 앞 칸의 남은 거리를 버린다. 마우스 60ms 간격 2번 → **1.34칸**, 3번 → **1.66칸**(300ms 간격이면 2칸), 터치 두세 번 톡 1.94·2.87칸 | 확대 → ▶를 60ms 간격으로 두 번 클릭 → 0.16(=1.34칸) | 같은 방향이면 남은 거리(`want - done`)를 새 칸에 더하기 |
| L2 | 낮음 | `sim3d.js:588`(바닥) + `614` `canPan` | **느린 기기(30fps 미만)**에서 ◀/▶ 한도에 붙은 채 ▼를 누르고 있으면 바닥 조금 위(보이는 높이의 1~4%)에서 멈추는데 **▼가 흐려지지 않고**, 다시 눌러도 움직이지 않는다. 한 장면 이동이 바닥에 걸려 수평 미끄러짐이 되면 원근 좁아짐 때문에 좌우 한도에 막혀 **이동 전체(세로 부분까지)가 0**이 되고, `canPan`은 더 짧은(보이는 높이 2%) 시험 이동으로 재서 "움직일 수 있음"으로 본다. 60fps에서는 `canPan`이 먼저 막혀 흐려지므로 생기지 않음(계산·실측) | 매 장면 150ms가 걸리게 해(약 7fps) 6-2-1-4 1180×820 크게 보기: 확대 → ▶·▲·◀ 끝까지 → ▼ 누르고 있기 → 과녁 높이 1.1에서 멈춤, `is-limit` 없음, 다시 눌러도 0(6-2-1-3 768×1024도 같음). 부하로 5~13fps였던 행렬에서 8번 | 바닥에 걸려 미끄러짐이 막히면 세로 부분만(바닥까지) 옮기기, `canPan`을 다음 실제 한 장면 길이(`max(0.02, PAN_SPEED×dt)`)로 재기 |
| L3 | 낮음 | 3D 이름표(스프라이트) — Build 한계 3 | 확대·이동한 162자리(20앱, 확대 직후 + ◀2·▶4·▲2·▼4·◀2) 중 **74자리(46%)**에서 화살표가 3D 이름표 일부를 덮었다(17/20앱). 이름표 면적의 50% 넘게 덮은 것 15번 — 예: 6-1-2-2 "↑ 북쪽" 75~93%(▲), 6-1-2-4 "노란색 자동차" 앞글자(◀), 6-2-1-3·6-2-1-5 작은 이름표 100%. 원이 옅어 글자는 비치지만 꺾쇠가 글자 위에 그려진다 | 6-1-2-2 크게 보기 1024×768 → 휠 4번 → ▲가 "↑ 북쪽" 위(`shots/label-sci-6-1-2-2-1024x768-zoom.png`) | M1과 같이 스프라이트 상자를 약한 장애물로(피할 자리가 있으면 피함). 또는 그대로(사용자가 이름표가 비치는 겉모양을 보고 받아들임) |
| L4 | 낮음 | 6-2-2-3 실험해요 2, 6-2-3-1 — Build 한계 1 | 앱이 정한 처음 시점이 더 가까워 **화살표가 처음부터 보인다**(6-2-2-3 실험해요 2: 거리 비 0.72~0.76, "🎥 처음 방향으로" 뒤에도). 두 번 탭은 공통 틀의 처음 시점(실험해요 1 틀, 거리 1.0 — 화살표 숨음)으로 가서 두 초기화가 서로 다른 화면을 준다(두 번 탭 쪽은 전부터 그랬음). 6-2-3-1은 실행 연출(거리 0.95·과녁 1.8% 이동)만으로 화살표·이동 안내가 나타나 기록 뒤에도 남는다 — 학생은 확대한 적이 없다 | 6-2-2-3: 실험해요 1 두 칸 기록 → "성냥의 머리 부분" → 화살표 4개(겹침 0, ▶ 0.120). 6-2-3-1: 회로 (가) 실행 → 화살표 보임 | 앱이 처음 시점을 알리는 `v.setHome(pose)`(앱 수정 필요) 또는 `PAN_MOVED`(1%)를 키우기(예 5%). 결정 필요 |
| L5 | 낮음 | 기본 화면(크게 보기 끔) — Build 한계 5 | 기본 화면에서 처음 열린 스크롤 자리에서는 4개가 다 보이는 경우가 적다(1024×768 2/20, 1180×820 2/20, 768×1024 11/20, 375×812 7/20) — 장면이 페이지 아래쪽이라 원래 굴려야 보인다. **휴대폰 가로 812×375는 3D 칸(298px)이 보이는 띠(머리말~아래 막대 182px)보다 커서 ▲▼를 함께 볼 수 없고** 처음에 ▲는 머리말 밑(0% 보임) — ▲·▼를 누르려면 굴려야 함(시험 4번). 막대 밑으로 반쯤 가린 화살표의 가린 부분을 누르면 그 막대가 눌린다(가운데라 버튼 사이 빈 곳) | 812×375 기본 화면 → 장면을 가운데로 굴림 → 휠 4번 → ▲ 0%·▼ 62~64%만 보임 | 기본 화면의 원래 한계. 태블릿·PC는 크게 보기가 기본이라 그대로 두어도 됨 |
| L6 | 낮음 | 휴대폰 가로 크게 보기 — Build 한계 4 | 3D 칸 510×170~245px에서 ▼가 22~24px 위(드래그 안내 줄 피함), ◀가 30~32px 아래(토글 피함) — 네 화살표가 칸 높이의 약 1/4씩 차지, 한 칸 20~29px. 겹침은 0 | 812×375 크게 보기 → 휠 4번 | 휴대폰은 크게 보기가 기본 끔이라 그대로 두어도 됨 |
| L7 | 낮음 | `sim3d.js:740`대 버튼 — Build 한계 6 | 화살표 위에서 휠을 굴리면 확대되지 않는다(크게 보기에서는 페이지도 안 굴러감 — 아무 일 없음). 화살표 8px 옆에서는 정상 확대 | 확대 → ▶ 위에서 휠 → 거리 비 그대로 | 버튼의 `wheel`을 캔버스로 넘기기(선택) |
| L8 | 낮음 | 알림(`.ss-toast`) — Build 한계 7 | 두 줄 알림이 뜨는 3~4초 동안 ▲ 위쪽이 가려진다: 세로 태블릿 768×1024·810×1080 **14%**(6px), 휴대폰 가로 812×375 **42%**. 알림은 누르기를 통과시켜 ▲ 가운데를 누르면 화살표가 눌린다. 가로 태블릿·PC(1024×768·1180×820·1920×1080)는 한 줄이라 0% | 크게 보기 → 휠 4번 → 기록 알림 "🎉 스위치 실험을 모두 기록했어요! 이제 전지 개수 실험이 열렸어요." | 그대로 두어도 됨 |
| L9 | 낮음 | Tab 순서 — Build 한계 2 / README | 화살표(▲◀▶▼)가 장면 위 토글보다 먼저 Tab을 받는다(`experiment.js:343` 주석 "Tab 순서 = 보이는 순서(왼쪽 위 토글 → …)"와 다름). README "개정 8" 절 번호가 6 → **8 → 7** 순서 | 확대 → Tab: 화면을 위로/왼쪽/오른쪽/아래로 옮기기 → "↙ 기본 화면" | 묶음을 토글 뒤(`.ss-exp-view` 끝, 3D 칸과 같은 inset)로 옮기거나 그대로. README 7·8 순서 정리 |

## 항목 1~8 판정과 수치
1. **보이는 때 — 통과.** 처음 시점 숨김 280/280, 확대(휠 4번, 거리 비 0.815) 뒤 4개 보임 280/280. 2D 대체 화면(`?no3d=1`) 20/20 화살표·이동 안내 없음, "2D로 보기" → 없음, "3D로 보기" → 숨김·원래 안내 줄(5앱). 두 번 탭 → 숨김·처음 시점 276/280(나머지 4는 기본 화면 세로에서 부하로 두 탭 간격이 320ms를 넘은 시험 문제 — 같은 경우 따로 2번 통과), "🎥 처음 방향으로" → 숨김 280/280(6-2-2-3 실험해요 2는 L4). 앱 연출 뒤: 6-2-3-3 나침반 다가가기(0.40~0.46)·6-2-1-5 확대 화면(0.37)·6-1-1-1 낮은 시점(0.30~0.42)·6-2-2-4(0.55~0.62)·6-2-2-1(0.72)·6-2-2-3(0.80)·6-2-3-1(0.95, L4)에서 나타나고, 앱이 처음 시점으로 돌아가면(기록 뒤 6-2-3-3·6-2-1-5·6-1-1-1·6-2-2-4 실험 1) 숨는다. 연출 뒤·연출 중 DOM 겹침 0(0.3초 넘게 남는 것). 6-2-1-3 남쪽 하늘 시점은 처음에 숨김(앱 처음 시점 = 공통 틀 처음 시점). 6-1-2-4는 실행하면 앱이 처음 시점으로 가서 숨는다(앱 동작).
2. **움직임 — 통과(L1·L2).** 한 칸 1,103/1,120 = 0.120(범위 0.105~0.120), 1,120번 모두 카메라·과녁이 같이 움직이고(차이 0) 방향 변화 0, 옛 화면 가운데가 반대쪽으로(◀ → 오른쪽, ▲ → 아래) — **뒤집힘 0**. 1초 누르고 있기 0.43~0.60(부하가 적을 때), **뗀 뒤 이동 0**(부하로 1건 0.04 — 따로 돌리면 0). 터치(`Input.dispatchTouchEvent`): 0.8초 누르고 있기 0.44~0.58, 떼기·밖으로 60px·`touchcancel` 뒤 0, 화살표 두 번 톡 = 두 칸(0.24, 처음 방향으로 안 감), 창 떠남(`blur`) 뒤 0. 키보드 Enter·Space 0.120, Enter 누르고 있기(반복 10번) 한 칸, 초점은 전역 `:focus-visible`(보라 3px + 2px, 스크린숏 확인). 돌려 둔 상태(101~147°)에서 ◀ = 화면 기준 −0.12(옆으로 0.001). 움직임 줄이기: 짧게 누르면 한 장면에 한 칸, 누르고 있기는 계속(0.54/초). 한도: 1,111/1,120 흐려짐(`is-limit`·`aria-disabled`), 흐린 화살표를 눌러도 0. 나머지 9는 L2(8)·부하(1). **장면을 잃은 경우 없음** — 한도 끝에서 작은 물체가 0개가 된 곳은 6-2-1-3 오른쪽 위 구석 2번(반구 구석·막대·방위 글자는 남음, 화면의 약 80%가 빈 배경), 그 밖 최소 3%(6-1-2-5 구석: 탁자 끝 절반). 구석 한도에서 화면의 절반~80%가 빈 배경인 것은 Build 한계 6 그대로(틀 끝까지 가져올 수 있게 한 설계). 앱 연출과: 누르는 중 `v.focus` → 연출 끝 자리 정확(과녁 오차 0)·누르던 이동 멈춤.
3. **새는 입력 없음 — 통과.** 행렬 280번의 누름·누르고 있기 동안 `onPick`·끌기 시작 0, 흐름 27번에서 화살표 누름 뒤 고르기 0. 마우스로 누른 채 캔버스로 나가 떼기 → 돌리기 0·고르기 0, 오른쪽 버튼 0, 화살표 두 번 클릭 → 처음 방향으로 안 감. 끌기 물체(6-2-1-2 태양·6-2-1-4 손전등·6-2-1-5 지구본 × 1024×768·768×1024) 6/6: 화살표 누름이 끌기를 시작하지 않고, 확대 + ◀ 한 칸 옮긴 채 끌면 끌기 시작·앱 값 바뀜·카메라 회전 0. 화살표 8px 옆: 끌기 → 돌리기 40~65°, 휠 → 확대, 30~70px 옆 물체 톡 → 고르기(6-1-1-2·6-2-2-1). 반대로 화살표 **위**의 3D 물체는 M1.
4. **가림·겹침 — DOM은 통과, 3D 물체·이름표는 M1·L3.** 화살표 ↔ 토글·"모형" 배지·HUD·드래그 안내 글(글자 줄)·장면 안 막대·앱 DOM 이름표·확대 칸: 확대 직후 280·한도 1,120·흐름 실행 중 0.3초 넘게 남는 것 **모두 0**, 크게 보기 켜고 끄기·가로↔세로·창 크기 바꾸기 뒤 0(81번), 3D 칸 밖 0, 화살표끼리 0. 크게 보기에서 화살표 가운데의 맨 위 요소는 늘 화살표(가로막는 겹 없음). 가장자리 가운데에서 비켜 간 자리: ▼ 22~24px 위(휴대폰 드래그 안내 줄), 6-2-3-3·6-2-2-4 ▲ 34~42px 아래(HUD), 6-2-1-2 ▶ 6~70px 아래("옆에서 본 모습" 칸), 6-2-2-1 휴대폰 세로 ▲ 64px 아래(배지) 등. 누르는 곳 44×44 280/280, `aria-label` 4개·묶음 `role="group"`·"확대한 화면 옮기기"·아이콘 `aria-hidden`. 기본 화면의 머리말·아래 막대 밑은 L5, 알림은 L8. (불투명도·대비는 판정하지 않음.)
5. **흐름 — 통과.** 9앱(6-2-3-3·6-2-1-5·6-2-1-2·6-1-1-1·6-2-2-3·6-1-2-4·6-2-2-4·6-2-2-1·6-2-3-1) × {1024×768 크게 보기, 768×1024 크게 보기, 1024×768 기본 화면}: 휠 4번 → ▶·▲ 한 번씩 → 고르기 → 실행(실행 중 ◀ 1.2초 누르고 있기) → 관찰 → 기록하기 → **기록 +1 58/59**(6-2-3-3 실험 3 나침반 다가가기, 6-2-1-5 확대 화면, 6-2-2-3 시간 바 끝까지 포함. 1건은 6-2-1-2 기본 화면에서 시험 도구가 시간 바 눈금을 못 찾은 것 — 크게 보기 2건은 +1). 실행 중·뒤 겹침 0, 크게 보기 두 번 켜고 끄기·방향 바꾸기·창 크기 바꾸기(3번) 뒤 화살표 보임이 카메라 상태(거리 < 0.92 또는 과녁 > 1%)와 맞음 81/81, `.ss-pan` 1개, 콘솔 오류 0.
6. **회귀 없음 — 통과.** `08f2fa8` 사본(8797) ↔ 작업 트리(8796), 9앱(6-2-3-3·6-2-1-5·6-2-1-2·6-1-1-1·6-2-2-3·6-1-2-4·6-2-2-4·6-1-1-2·6-2-1-3) × {1024×768, 768×1024} × {기본 화면, 크게 보기} = **36/36 요소 위치 같음**(`getBoundingClientRect` 10,728개, 화살표 묶음·알림 뺌, 페이지 높이·크게 보기 여부 같음, 콘솔 오류 0/0). 처음 시점 3D 그림(3D 칸 스크린숏, 움직임 줄이기): 6앱 16/16 픽셀 같음, 6-2-2-3·6-2-2-4는 0.01~0.05% 다름 — 시간 따라 움직이는 기포·촛불(같은 판끼리 다시 찍으면 0%)이라 회귀 아님. 6-2-1-3은 그림 비교에서 뺐다(fix-2 작업 중 — 요소 위치는 같음).
7. **성능·정리 — 통과.** 화살표는 자기 `requestAnimationFrame` 반복을 만들지 않고 기존 렌더 루프에 얹힌다. 렌더 루프는 전과 같이 3D 칸이 보이는 동안 매 장면 돌고(08f2fa8도 같음), **실험하기 단계를 떠나면 0**(현재·08f2fa8 모두, 확대해 화살표가 보이던 상태에서도). 확대해 화살표가 보이는 동안 가만히 있을 때 스크립트 시간 12/17/25 ms/초 vs 08f2fa8 13/17/26(6-2-3-3·6-1-1-2·6-2-1-2) — 0.25초마다 자리 재기의 비용이 보이지 않고 레이아웃·스타일 재계산 0회/초. 크게 보기 10번 켜고 끄기 + 화살표 10번 누른 뒤 창 이벤트 처리기 수 그대로(`pointerup/move/cancel`은 누를 때만 붙고 떼면 뗀다), `.ss-pan` 1개, `controls` change 1→1, 뷰 1개. 누른 채 다른 단계로 가면 누름이 풀리고 한 칸만 끝낸 뒤 0, 그 뒤 저절로 움직이지 않음.
8. **정적 검사 — 통과.** `diff -r` 정본↔20 사본 20/20, `node --check` 정본·20 사본, `git diff --check` 깨끗, 더한 457줄에 `console.log`·`debugger`·`localStorage.clear()` 없음, 조사 앱 3개·`science-guide` 안 바뀜, 앱 `app.js`·`style.css` 안 바뀜(6-2-1-3은 fix-2가 고치는 중), `npm run lint` 오류 0(경고 4는 다른 작업 중인 `src/app/admin/(dashboard)/learning/assignment-manager.tsx` — 이 변경과 무관, 공통 틀·앱은 eslint 대상 밖). README "개정 8" 절 설명은 실제 동작(보이는 조건·한 칸 12%·초당 60%·한도·초기화·끼어들지 않음·자리·움직임 줄이기)과 맞다(번호 순서만 L9).

## Build가 적은 한계 판정
| # | 한계 | 판정 |
|---|---|---|
| 1 | 6-2-2-3 실험해요 2 처음부터 화살표 | L4(낮음) — 겹침 0·이동 정상이라 해롭지 않으나 합격 기준 1("처음 시점에서는 숨김")에서 벗어남. `setHome` 여부는 결정 필요 |
| 2 | Tab 순서가 토글보다 앞 | L9(낮음) |
| 3 | 3D 이름표를 피하지 못함 | L3(낮음) — 162자리 중 46%. 같은 원인이 누를 수 있는 물체에도 미쳐 M1(중간) |
| 4 | 휴대폰 가로 크게 보기가 좁음 | L6(낮음) |
| 5 | 기본 화면이 짧으면 굴려야 함 | L5(낮음) — 휴대폰 가로는 ▲▼를 함께 볼 수 없음 |
| 6 | 화살표 위 휠 | L7(낮음) |
| 7 | 두 줄 알림이 ▲를 가리는지 | L8(낮음) — 세로 태블릿 14%, 휴대폰 가로 42%, 누르기는 됨 |

## 사용자에게 보여 줄 스크린숏(`/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-pan/showcase/`, 13:32 이후 겉모양)
* `01-before-zoom-1024x768.png` — 전자석 6-2-3-3, 태블릿 가로 크게 보기, 확대 전: 화살표 없음
* `02-zoomed-arrows-1024x768.png` — 휠 4번 뒤 화살표 4개(▲는 스위치 HUD 아래, ▼는 안내 줄 위)
* `03-moved-left2-up2-1024x768.png` — 같은 앱에서 ◀ 2번·▲ 2번: 장면의 왼쪽 위(전자석 위 빈 곳)가 보이고 방향 그대로
* `04-portrait-tablet-810x1080.png` — 태블릿 세로 810×1080 크게 보기, 확대 + ▶ 한 칸
* `05-dark-1024x768.png` — 어두움 모드
* 참고(같은 폴더의 `../shots/`): `label-sci-6-1-2-2-1024x768-zoom.png`(L3 "북쪽"), `label-sci-6-2-1-5-1024x768-zoom.png`(M1 지구본 위 ▲), `zoom-614-1024.png`(M1·L3 자동차), `limit-sci-6-2-1-3-1024x768-on-right-up.png`(구석 한도), `downlimit-sci-6-2-1-3-768x1024-on.png`(L2), `sheet-1024x768on.png`(20앱 확대 모음)

## Supabase 차단 기록
* 자기 headless Chrome(9356)을 `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`로 띄우고, 모든 시험 페이지에서 첫 로드 전에 CDP `Fetch.enable(*supabase.co*)` 가짜 응답 + `Network.setCacheDisabled`, 시험마다 따로 된 브라우저 컨텍스트(저장소가 섞이지 않게 — `localStorage.clear()`는 쓰지 않고 그 앱 키·`ssUiPref:` 키만). 서버는 자기 정적 서버 8796(작업 트리 `public/`)·8797(`git archive 08f2fa8 public`).
* 끝까지 기록이 남은 실행 27번에서 가로챈 요청 **350건**(`GET /rest/v1/site_settings` 347 — 가짜 `login_required:false` 체험 모드, `POST /functions/v1/check-answer` 3 — 가짜 응답), **밖으로 나간 요청 0, Gemini 요청 0**. 중간에 멈춘 실행도 같은 설정이었다. 아무것도 내려받거나 설치하지 않았다(three.js·글꼴은 페이지가 CDN에서 불러옴).

## 확인하지 못한 것
* 실제 iPad Safari·손가락: 누른 손가락이 화살표 밖으로 나갈 때(암묵적 포인터 붙잡기 풀기), 길게 누르기 메뉴·확대 제스처 막힘, 보이스오버·스위치 제어의 누르기(`click` detail 값 — detail이 0이 아니면 포인터 이벤트 없이 누른 경우 안 움직일 수 있음), 실제 fps(L2는 60fps에서는 안 생기는 것으로 계산·실측), 느린 학교 태블릿.
* 부하가 큰 headless SwiftShader(다른 작업의 Chrome 동시 실행, 1~61fps)라 시간에 기대는 수치(누르고 있기 거리·연달아 누르기)는 흔들렸다 — 흔들린 경우는 따로 다시 돌려 확인했다.
* 새 안내 줄 문구·겉모양(조정자 지시로 제외 — Claude가 따로 확인), 6-2-1-3의 3D 그림(fix-2 중), 실제 로그인·DB 저장.
* 스크래치 정리: Chrome·서버는 종료했다. 크롬 프로필(`review-pan/profile`)·비교 사본(`review-pan/base`)·시험 도구(`review-pan/lib`)·결과(`review-pan/out`)·pid 파일 지우기(`rm -rf`)는 **권한 거부**로 못 했다(지워도 됨, 스크린숏은 `shots/`·`showcase/`).

**판단: 배포해도 됨** — 높음 0. 사용자 합격 기준(확대하면 가장자리 가운데에 반투명 화살표, 누르는 만큼 한 칸·누르고 있으면 계속, 좌우·위아래 방향 유지, 한도에서 멈춤, 처음 방향으로 되돌림, 2D에는 없음)을 280개 조합·27개 흐름에서 충족했고, DOM 겹침·새는 입력·콘솔 오류·배치 회귀가 모두 0이다. 중간 M1(확대했을 때 6-2-1-5 지구본 등 누를 수 있는 3D 물체 위의 화살표)은 한 번 누르면 물체가 비켜나고 버튼으로도 고를 수 있어 막히지 않으므로 배포를 막지 않지만, `layout()`이 끌기·고르기 물체를 피하게 하는 수정을 권장한다(L1·L2는 작은 계산 수정).
