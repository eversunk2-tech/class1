# 단계 A 수정 보고서(fix-A) — Review A 발견 고치기 (2026-09-25)

지침: `fix-A-instructions.md`(근거 `review-A.md` H1·H2·M1~M7·L). 정본 `scripts/templates/science-sim/`을 고친 뒤 실험 앱 20개 `science-sim/`에 복사, 앱 파일은 지침의 5곳만 고쳤다. 저장 키·`detail`·문항·측정값·과학 내용·3D 색은 그대로다. git commit/push·실 DB 쓰기·내려받기는 하지 않았다.

## 0. 요약

* **F1~F11 모두 반영.** 높음 2건(H1 가짜 기록, H2 초시계와 다른 값 기록)은 재현되지 않는다.
* **끔(기본 화면) 상태는 예전과 같다**(허용 차이: 토글·접기 버튼, 알림 위치, 단계 메뉴 칸 수 — review-A와 같은 결과).
* 크게 보기의 스크롤이 크게 줄었다(5개 앱×2크기 합계: 움직임 63→48번, 그중 손 25→16번, 거리 38,078→17,857px). 보기를 고른 뒤 '확인하기'가 화면 밖으로 밀리는 일 0건(전 6건).
* 남은 것: 가로 크게 보기에서는 장면이 화면 띠를 꽉 채우고 장면과 패널 사이에 보기 도구·모형 설명(L9, 이번에 안 고침)이 있어, 조건 카드·관찰 보기까지 손 스크롤 1~2번이 남는다. `sci-6-2-2-3`의 "⏩ 빨리 감기(모형)" 배지는 앱이 장면 칸에 직접 붙인 요소라 막대와 겹친다(전과 같음, 단계 D에서 앱 CSS 한 줄).

## 1. 바꾼 파일

| 파일 | 내용 |
|---|---|
| `scripts/templates/science-sim/experiment.js` | 크게 보기 도우미 `enlarge()` 분리(F9)와 F1·F2·F4·F5·F7 |
| `scripts/templates/science-sim/style-common.css` | F3 알림, F6 막대·3D 칸, F7 토글, F8 값 칸, F11 2D 여백, 주석 |
| `scripts/templates/science-sim/sim3d.js` | F10 끌기 도우미 |
| `scripts/templates/science-sim/README.md` | 새 동작·사용법·오류 고침(L11) |
| `scripts/templates/science-guide/README.md` | 머리말 접기 버튼 위치 문구(L11)만 |
| 실험 앱 20개 `science-sim/`(위 4개 파일), 조사 앱 3개 `science-guide/README.md` | 정본 복사 |
| `public/apps/sci-6-1-2-4/app.js:258` | 기록 버튼을 `exp.recordButton`으로(H2) |
| `sci-6-2-2-1/app.js:212-216`, `sci-6-2-2-2/app.js:1271-1278`, `sci-6-2-2-3/app.js:1423-1431`, `sci-6-2-2-4/app.js:243-251` | '가리면 스크롤' 도우미를 `exp.revealRecord()` 호출로 |
| `sci-6-2-2-3/app.js:1157-1159, 1181-1186` | 실제 상태 글(`chip-reveal`)은 맞는 보기로 확인했을 때만 |

## 2. 발견별 바꾼 것과 확인

### F1 (H1) 막대의 가짜 실행·기록 버튼
* `experiment.js:634` `useBar = o.sceneBar !== false && factors.length > 0` — 아니면 실행·기록 버튼·관찰 카드를 옮기지 않고 실행 카드도 건드리지 않는다. 막대에 넣을 것이 없으면 막대를 숨긴다(`:318`). 새 옵션 `sceneBar`(`:77`). 기록 버튼은 처음부터 `disabled`(`:603`).
* 확인(`sci-6-2-1-2`, 768×1024 기본 켬·1024×768 켬·1024×768 끔): 막대 숨김·막대 안 버튼 0, 공통 실행·기록 버튼은 어디에도 안 보임, 보이는 '기록하기'는 앱 버튼 1개뿐. 시간 바 눈금 7곳 기록 → 진행률 7/7, 기록 7개, 가짜 `{"phase":"M"}` 0 (세 경우 모두).

### F2 (H2) 기록 버튼 고정 훅
* `ss-run-btn`·`ss-record-btn` 클래스(`experiment.js:594, 603`), `exp.runButton`·`exp.recordButton`·`exp.revealRecord()`(`:1413-1415`), README에 "관찰 카드 안 선택자로 찾지 말 것".
* `sci-6-1-2-4/app.js:258` → `exp.recordButton`. 확인(1024×768 켬·768×1024 기본 켬): 초시계 4.2초, 5.5 입력 → 막대의 기록하기 꺼짐 + "초시계에 나온 숫자를 다시 확인해 보세요." → 눌러도 기록 0(0/2) → 4.2 입력 → 켜짐 → `time 4.2, reading 4.2`로 기록(1/2).
* 4개 앱 도우미는 `exp.revealRecord()`만 부른다(꺼져 있으면 움직이지 않음, 크게 보기 규칙은 F4 — 규칙이 한 곳).
* `sci-6-2-2-3`: 틀린 보기("계속 탄다")로 확인 → 상태 글 숨김 유지, 맞는 보기로 확인 → 드러남.

### F3 (M1) 알림 위치
* `style-common.css:559-564` `.ss-toast { top: calc(env(safe-area-inset-top, 0px) + 8px) }`(머리말 위, `pointer-events:none` 유지), 낮은 화면 주석(`:220`)·README 갱신. 조사 앱(science-guide)은 그대로.
* 알림과 **보이는 장면**의 겹침 넓이(px², 전 = 머리말 바로 아래였던 단계 A 첫 구현):

| 앱·알림 | 화면 | 머리말 펼침 전 → 후 | 머리말 접힘 전 → 후 |
|---|---|---|---|
| sci-6-1-1-2 "📝 기록했어요…"(세로 폭에서 두 줄) | 기본 가로 | 14,395 → **0** | 14,395 → **0** |
| 〃 | 기본 세로 | 0 → 0 | 0 → 0 |
| 〃 | 크게 가로 | 22,169 → **0** | 22,169 → **0** |
| 〃 | 크게 세로 | 24,300 → **0** | 24,300 → **8,976** |
| sci-6-2-1-2 "📝 기록했어요: 9:30 — …"(두 줄) | 기본 가로 | 25,133 → **0** | 25,133 → **5,396** |
| 〃 | 기본 세로 | 30,096 → **0** | 30,096 → **1,656** |
| 〃 | 크게 가로 | 0 → 0 | 0 → 0 |
| 〃 | 크게 세로 | 30,096 → **0** | 30,096 → **1,656** |

* 머리말 펼침에서는 모두 0. 머리말을 접으면(63px, 세로 긴 제목 앱 82px) **두 줄 알림**(8~86px)이 머리말 아래로 최대 23px 내려와 위 넓이만큼 겹친다(한 줄 알림은 8~59px라 0). 가장 긴 문구 모의("🎉 모든 실험을 기록했어요! …")도 같은 결과.

### F4 (M2) 크게 보기 스크롤 왕복
* 관찰 카드를 켜면 패널 맨 앞(scenePanel 카드가 있으면 그 다음)으로, 끄면 원래 자리로(`experiment.js:311-316`, `goHome` `:203-213`). 번호: 켬 ③/끔 ④(조건 2개 앱).
* `revealRecord()`(`:1088-1110`): 꺼져 있으면 아무것도 안 함. 크게 보기는 막대부터 관찰 카드의 방금 바뀐 곳까지 최소 이동(이미 보이면 가만히, 안 들어가면 막대 우선, `revealRange` `:164-176`). 기본 화면은 예전 방식.
* 보태어 `revealCheck()`(`:1113-1126`): 기록 버튼이 꺼져 있고 '확인하기'가 필요하면 '확인하기' 줄·피드백이 가릴 때만 최소로 보이게(예전 기본 화면은 꺼진 기록 버튼까지 스크롤하며 이것을 함께 보여 주었다 — 그 몫을 대신한다).
* `showObserve()`(`:1017-1026`): 크게 보기는 막대가 화면에 남는 데까지만 최소로 내려 관찰 카드를 보인다.
* `afterSelect()`(`:709-729`): 크게 보기에서 관찰 카드가 숨을 때 방금 누른 카드의 자리를 숨기기 전·후로 재서 되돌린다. 확인: 카드 463px/323px가 숨을 때 누른 조건 버튼 이동 **−0.9~+0.2px**(스크롤 −476/−337px 한 번만 — Chrome 스크롤 앵커링과 두 번 보정되지 않음). 3D에서 고르거나 막대에서 누른 경우는 움직이지 않는다.
* 스크롤 표(한 칸 기록, 자동 = 앱이 움직인 횟수, 손 = 학생이 밀어야 한 횟수, 같은 방법으로 전·후 모두 다시 잼):

| 앱(관찰 유형) | 1024×768 켬 전 → 후 | 768×1024 켬 전 → 후 | 1024×768 끔 전 → 후 | 768×1024 끔 전 → 후 |
|---|---|---|---|---|
| sci-6-1-1-2(보기) | 자3·손2·2,911 → 자2·손2·1,886 | 자3·손1·2,324 → 자2·손1·1,535 | 자1·손1·913 → 같음 | 자2·손2·2,901 → 같음 |
| sci-6-2-2-2(보기+확인, 첫 보기 틀림) | 자6·손4·8,234 → 자5·손1·2,023 | 자6·손5·7,118 → 자5·손2·2,101 | 자3·손2·1,339 → 자4·손2·1,339 | 자4·손1·2,249 → 같음 |
| sci-6-2-3-3(보기+확인) | 자4·손4·5,068 → 자3·손3·2,409 | 자5·손2·3,427 → 자4·손1·1,535 | 자4·손2·2,529 → 같음 | 자4·손1·2,447 → 같음 |
| sci-6-2-1-5(값만) | 자2·손3·3,363 → 자2·손2·1,919 | 자2·손2·2,669 → 자2·손1·1,581 | 자2·손1·936 → 같음 | 자3·손1·2,129 → 같음 |
| sci-6-1-2-3(측정하기) | 자4·손1·1,745 → 자3·손2·1,775 | 자3·손1·1,219 → 자4·손1·1,093 | 자4·손1·916 → 같음 | 자4·손1·1,999 → 같음 |

* 보기를 고른 뒤 '확인하기'가 화면 띠 안: 전 0/6 → **후 6/6**. 실행 뒤 막대(기록하기)가 화면 띠 안: 크게 보기 전 1/10 → **후 10/10**. 값만 앱(6-2-1-5)의 "기록하려면 손으로 649~834px 올라가기"가 없어졌다.
* 크게 보기 ↔ 기본 화면(후): 합계 움직임 48(손 16) ↔ 45(손 13), 거리 17,857 ↔ 18,358px. 10칸 중 7칸은 크게 보기가 같거나 적고, 3칸이 1~2번 많다(6-1-1-2 가로, 6-2-1-5 가로 — 장면이 띠를 꽉 채워 조건 카드·막대 사이를 손으로 오감, 6-2-2-2 세로 — 앱의 시간 바 카드·보기 도구·모형 설명 316px이 막대와 관찰 카드 사이에 있어 확인 줄로 49·58px 내렸다가 기록 때 107px 올라감). 남은 원인은 L9(이번에 안 고침).
* 기본 화면 차이: 6-2-2-2 가로 끔만 자동 3→4(거리 같음) — 틀린 확인 뒤 예전엔 꺼진 기록 버튼까지 +83px 한 번, 지금은 피드백까지 +27px, 맞힌 뒤 기록 버튼까지 +56px.

### F5 (M3·L10) 접기·창 크기 변경 뒤 장면 맞춤
* `enlarge()`의 `fit(mode)`(`:339-357`): 학생이 머리말을 접거나 폈을 때(`.is-collapsed`가 바뀜, `:376`)와 창 크기·방향이 바뀔 때, 장면이 화면 띠의 절반 이상 보이고 있었으면 `scrollIntoViewSafe(장면)`(움직임 줄이기면 즉시). 조건 카드 쪽을 보고 있었으면 끌어올리지 않고, 장면 높이가 바뀌어도 보던 카드 자리를 되돌린다(`:348-356`).
* 불러오는 중 머리말 높이 변화(계정 줄·글꼴)로는 맞춤 이동을 하지 않는다 — 처음엔 이것까지 맞췄더니 `sci-6-2-2-3`만 불러오자마자 저절로 246px 스크롤돼(L1의 일부만 들쑥날쑥 고치는 셈) 뺐다. 지금은 불러올 때 전과 똑같다(3개 앱 모두 y=0).
* 확인(4개 앱 × 1024×768·1180×820·1280×800·1366×768·1920×1080): **접은 직후 전 20칸 모두 89px 넘침 → 후 20칸 모두 들어감**(장면 위 71, 막대 아래 = 아래 이동 막대 위 − 9px), 다시 편 직후 20/20, **1024→1280 창 변경 직후 전 3/4 어긋남(장면 위 142 < 머리말 161) → 후 4/4 들어감**(되돌려도 4/4). 조건 카드를 보다가 접기·펴기: 장면으로 끌어올림 0, 보던 카드 이동 0px(전: Chrome에서 98px 튐).

### F6 (M4·L6) 막대가 장면 아래쪽을 가림
* `style-common.css:438` `.ss-exp-view.has-bar:not(.is-2d) > .ss-view3d { bottom: var(--ss-exp-overlay-h, 64px) }`(막대가 없으면 안 올림), 막대 바탕 불투명 96%(`:433, 437`), 드래그 안내는 막대 위 캔버스 안(`:454`, `:has()` 대신 `.has-bar`).
* 확인(8개 앱 × 768×1024·1024×768, 실행 중 0.2초마다): 3D 칸이 막대 밑으로 들어간 높이 **전 73px(16/16) → 후 0(16/16)**. 막대와 겹친 글: `ov-text`(6-2-3-1·6-2-2-1·6-2-3-2) 전 3,848~6,633px² → **0**, `hud-ff`(6-2-1-5·6-2-2-4) 전 6,118~7,295px² → **0**, 아래 3D 이름표(6-2-2-3 "크기가 같은 아크릴 통"·6-2-3-3)는 캔버스가 막대 위에서 끝나 가려질 수 없음, 6-1-1-3 드래그 안내는 막대 위.
* **남은 것**: `sci-6-2-2-3`의 `.hud-ff` "⏩ 빨리 감기(모형)"는 앱이 3D 칸이 아니라 장면 칸에 붙인(`bottom:40px`) 요소라 막대와 4,450px² 겹친다(전과 같음, 2D에서도 보여야 해서 3D 칸으로 옮길 수 없음). 앱 CSS 한 줄(`.ss-exp-view.has-bar:not(.is-2d) .hud-ff { bottom: calc(40px + var(--ss-exp-overlay-h, 0px)) }`)로 풀리며 README에 방법을 적었다(단계 D, 이번 범위 밖 파일).

### F7 (M6·L3·L5) 토글 접근성
* 켜짐 `.is-on`: 글자 `var(--ss-on-primary)`, `border: 1px solid transparent`(`style-common.css:402-414`), `aria-pressed` 삭제, 기호는 `aria-hidden`, 토글을 막대보다 앞(`experiment.js:250-253`, 글자 `:277-282`).
* 대비: 밝음 흰 글자/#6c4dd6 5.72:1, /#9033bd 6.19:1 · **어두움 #110d26/#aa9dff 8.09:1, /#d698f1 8.67:1**(전 2.34/2.18). 강제 색 모드에서 테두리 `1px solid`(검정)로 버튼 모양이 보인다. Tab: 토글 다음이 막대의 실행 버튼.

### F8 (M7) 막대 안 측정값 칸
* 값 칸은 막대 첫 칸·윗줄 전체 폭(`flex: 1 1 100%`, 0.95rem = 15.2px), 실행·기록은 아랫줄(`style-common.css:441`, DOM 순서 = 보이는 순서).
* 스크래치 사본으로만 확인(저장소 앱에 scenePanel 안 켬): `sci-6-2-2-2` 사본(조건 있는 앱 + 값 4칸) 1024×768 값 줄 970px·버튼 481+481px, 768×1024 값 줄 714px·버튼 353+353px, **잘린 글자 0**, 캔버스가 막대에 들어간 높이 0. 375×812·812×375는 값이 장면 아래 카드로, 막대는 버튼 둘. `sci-6-2-1-2` 사본(조건 없음): 막대에 값 한 줄만, 끄면 원래 자리 있음 → 원래 자리 / 없음 → 장면 아래 카드. 노드는 늘 1개.

### F9 (M5) `SciSim.Experiment.enlarge(opts)`
* `experiment.js:218-428`(토글·기본값·방향별 기억·장면 높이·막대·scenePanel·관찰 카드 옮기기), `create()`가 이것을 쓴다(`:634-651`). 돌려주는 값 `{ isOn, set, refresh, fit, bar, toggle }`. README에 사용 예.
* `sci-6-2-1-3`·`sci-6-2-1-4`는 고치지 않았다. 스크래치 사본 `sci-6-2-1-4`(experiment.js 추가 + `enlarge({ layout, viewBox, panel, record, scenePanel: .val-grid })`)로 확인: 768×1024 기본 켬 / 1024×768 기본 끔, 켜면 기록 버튼·값 3칸이 막대(값 윗줄), 끄면 원래 자리(기록 버튼 앞 형제가 원래의 알림 줄 그대로), 가로 켬 장면 522px 들어감, 막대의 기록하기로 30° 기록됨, 방향별 기억 키 저장, 오류 0.

### F10 (L7) 끌기 도우미
* `sim3d.js:34-43`(주석), `:360-506`(끌기), `:533`(discard), `:898`(dispose): 시작한 `pointerId`만, 마우스 주 버튼만, 끌기 전 `controls.enabled`로 되돌림, 끄는 중 `discard`·`dispose`면 마지막 point로 `onEnd`(`cancelled: true`), 숨긴 물체(자신·조상) 안 잡힘, 30px 보정은 **보이는 부분의 화면 경계 상자** 기준(주석·README).
* 확인(`sci-6-2-2-2` 3D에 시험 물체를 이 탭에만 붙임): 왼쪽 끌기 start→drag×4→end·중간 카메라 꺼짐·끝나면 켜짐 / 오른쪽 버튼 0 / 카메라를 잠가 둔 상태는 끝나도 잠김 / 두 손가락(CDP 터치·합성 pointerId 41·42) 둘째 손가락을 떼도 끌기 계속, 첫 손가락으로 끝 / 숨긴 물체 0(보이게 하면 잡힘) / 작은 공 경계 상자 +20px 잡힘·+45px 안 잡힘, 긴 막대 끝 10px 밖(중심에서 51px) 잡힘 / 포인터취소 onEnd / 끄는 중 discard·dispose → onEnd(point 있음, cancelled) + 카메라 복구. 앱에서 쓰는 곳은 아직 없다.

### F11 (L2·L11·L4) 작은 것
* 2D: `#experiment-root .ss-exp-view.has-toggle.is-2d > .ss-view2d { padding-top: 98px }`(`style-common.css:395-397`, 앱의 2D 위 여백 규칙보다 우선). 확인(`?no3d=1`, 5개 앱 × 2크기 × 끔/켬): 토글과 2D 내용 겹침 **전 1,801~3,445px²(6-1-1-2 "지시약 \ 용액"·6-1-1-4·6-2-2-3) → 후 0(20/20)**.
* 주석·README: "장면 오른쪽 위" → "왼쪽 위"(`experiment.js:93`), 머리말 접기 버튼은 "제목 뒤, 저장 상태 배지는 그 뒤(줄 맨 끝)"(CSS `:159-160`, 두 README), 30px 기준(sim3d 주석·README).

## 3. 끔 상태 하위 호환
* review-A 항목 1의 5개 앱 × 1024×768·1366×768·768×1024(켬→토글로 끔), HEAD(8781)↔지금 `getBoundingClientRect` 23~25개 요소: 14/15는 **`.ss-title` 폭 −76px만**(Build A의 접기 버튼 자리), `sci-6-2-1-2` 768×1024는 제목이 두 줄이 되어 머리말 +19px(L4, Build A, 안 고침). 픽셀 차이 0.5~0.67%(5단계 앱)·2.1~2.6%(4단계 앱) — 토글·접기 버튼·단계 칸 자리뿐(review-A와 같음, `sci-6-2-1-2` 768×1024만 19px 밀림 때문에 23.6%).
* 끔 상태 기록 흐름(보기형 6-1-1-2, 보기+확인 6-2-2-2, 측정하기 6-1-2-3, 값만 6-2-1-5, 숫자 입력 6-1-2-4 × 2크기) HEAD↔지금: 기록 버튼 켜짐/꺼짐 순서 같음, 모두 기록 — 차이는 기록 직후 **숨은** 기록 버튼이 꺼져 있는 것뿐(Build A, 안 보임).
* 2D 화면 위 여백만 전보다 넓어졌다(F11, 의도).

## 4. 크게 보기 기록 흐름 · 20개 앱
* 8개 앱(6-1-1-2·6-1-1-4·6-2-2-2·6-1-2-3·6-1-2-4·6-2-1-5·6-2-3-3·6-2-2-4) × {켬 가로, 켬 세로, 켬→실행 뒤 끔→기록, 끔→실행 뒤 켬→기록} **32/32 기록·진행률 +1**, 실행·기록 버튼 늘 1개(복제·유실 0), 켬이면 막대 버튼 2개·관찰 카드 패널 맨 앞, 끔이면 막대 버튼 0. 마지막 코드로 다시 돌려 같은 결과.
* 20개 실험 앱 × 1024×768(켬)·768×1024(기본 켬) 실험하기까지: **예외 0, 콘솔 오류 0, 경고 0**(40/40). 6-2-1-3·6-2-1-4는 토글 없음(기본 화면), 6-2-1-2는 막대 숨김.

## 5. 정적 검사
* `node --check`: experiment.js·sim3d.js·lesson.js·persist.js(정본) + 앱 app.js 5개 통과.
* `diff -r` 정본↔사본: science-sim 20개 + science-guide 3개 **23/23 일치**.
* `npm run lint`: 오류 0(앱·정본 폴더는 ESLint 제외 대상).
* 바뀐 파일 범위: 정본 5개 파일 + 사본 83개 + 앱 app.js 5개(고치기 전 작업 트리 사본과 `diff -rq`로 확인).

## 6. Supabase 차단 기록
* 자기 전용 headless Chrome(포트 9346, 프로필 스크래치 `fix-A/profile`), `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"` + `--disable-web-security`, 모든 탭에서 첫 로드 전에 CDP `Fetch.enable(*supabase.co*)` 가짜 응답(site_settings→`login_required:false`, GET→`[]`, POST/PATCH→201, OPTIONS→204, check-answer→통과 모양).
* 기록을 남긴 시험 실행 32번에서 가로챈 요청 1,052건(모두 체험 모드라 `GET site_settings`, 디버그용 실행 몇 번도 같은 차단으로 돌림), **새어 나간 요청 0, 실패 0**, Gemini 요청 0. 가짜 로그인은 쓰지 않았다.
* `localStorage.clear()` 안 씀(그 앱 `sci6…` 키와 `ssUiPref:` 키만). 다른 탭·Chrome·개발 서버(3000)는 건드리지 않았다(끝까지 200 응답). 정적 서버 8781(head/pre/sp 사본)과 Chrome은 끝나고 종료했다(프로세스 0, 포트 닫힘).
* **스크래치 정리는 거부됐다**(`rm -rf` 권한 거부). `…/scratchpad/fix-A/`에 스크린숏(`shots/`) 말고도 `profile/`(Chrome 프로필 147MB), `head/`·`pre/`(비교용 사본 14·15MB), `sp/`(시험 사본 2.8MB), `lib/`(시험 스크립트), `out/`(측정 JSON·서버 로그), `chrome.pid`·`serve.pid`가 남아 있다 — 지워도 된다(저장소와 무관).

## 7. 안 고친 것
* **L1**(크게 보기를 기억한 채 실험하기로 들어오면 막대가 첫 화면 밖), **L9**(장면과 패널 사이의 보기 도구·모형 설명·앱 카드) — 지침대로 안 고침. F4의 남은 손 스크롤과 6-2-2-2 세로의 작은 왕복은 L9 때문이다.
* **L4** 중 머리말 높이(+3~19px) — 이번엔 문서 오류만 고침.
* `sci-6-2-2-3` `.hud-ff` 막대 겹침(F6 참고, 앱 CSS — 단계 D).
* 머리말을 접은 상태의 두 줄 알림 겹침(F3 표) — 지침의 위치(top 8px)를 그대로 따랐다.
* L8 중 6-2-2-3을 뺀 나머지 앱의 "본 모습" 글(단계 D).

## 8. 확인하지 못한 것
* 실제 iPad/태블릿(모두 headless Chrome): iPad Safari에는 스크롤 앵커링이 없을 수 있어 F4의 조건 버튼 제자리 유지·F5의 카드 제자리 유지는 코드가 직접 재서 맞추게 했지만 실제 Safari에서는 보지 못했다. 손가락 끌기 감도, 회전 때 전환, 안전 영역(`env(safe-area-inset-top)`)이 있는 기기의 알림 자리.
* 실제 로그인·실제 Supabase·Gemini(가짜 응답만), 실제 학생 소요 시간.
* 20개 앱 전부의 실행 결과 화면을 두 모드에서 끝까지 본 것은 아니다(8개 앱 흐름 + 20개 앱 첫 화면 + 8개 앱 실행 중 막대 가림).

## 9. 스크린숏(스크래치 `…/scratchpad/fix-A/shots/`)
* 전·후 나란히: `showcase-F3-toast-768x1024-on.png`, `showcase-F3-toast-6212-1024x768-off.png`, `showcase-F5-collapsed-6233-1024x768.png`, `showcase-F6-ovtext-6231-768x1024.png`, `showcase-F6-hudff-6224-768x1024.png`, `showcase-F11-2d-6112-768x1024.png`, `compat-pair-6222-1024.png`, `f11-pair-*.png`.
* 항목별: `f3-*`(알림 32), `f5-*`(접은 직후·창 변경), `f6-*`(실행 중·끝 64), `f7-*`(토글 대비·강제 색), `f8-sp-*`·`f9-sp-*`(시험 사본), `h1-*`·`h2-*`(높음 2건), `compat-*`(하위 호환 30), `all20-*`(20개 앱 40), `smoke-*`.
