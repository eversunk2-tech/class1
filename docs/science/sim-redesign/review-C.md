# 검토 보고서(Review C) — 단계 C: 1학기 1단원 6개 앱 간략화 (2026-09-26)

지침: `review-C-instructions.md`. **코드는 고치지 않았다**(발견만 — 저장소에는 이 보고서 하나만 씀). 읽은 것: CLAUDE.md, `spec.md` §1·§6·§7.4~7.5와 끝의 개정 2(5·6)·개정 5·개정 6, `build-C-instructions.md`, `build-C1/C2/C3-report.md`, 6개 앱 `spec.md` 개정 절, `docs/admin/responses-spec.md`, `src/data/app-responses/{types,helpers,index}.ts`·`sci-6-1-1-{1..6}.ts`, `src/lib/app-responses.ts`·`learning.ts`·`science-progress.ts`·`auth.ts`·`local-data.ts`, 관리자 `responses-view.tsx`·`response-panel.tsx`, 공통 틀 `lesson.js`·`persist.js`·`answer-check.js`·`class1-record.js`, `git diff 9d2dc16`(6개 앱·매핑).

**한 줄 요약**: 높음 0 · 중간 2 · 낮음 7. **예전 완료 기록은 선생님 화면에 전과 똑같이 보인다** — HEAD(`9d2dc16`) 매핑과 지금 매핑에 같은 예전 detail을 넣으면 항목(key·stage·label·질문·답·주석·버전)·extras·단계 묶음 순서·질문별 묶음 키가 모두 같고, 변형 이름만 "현재 버전" → "간략화 전(~2026-09-25)"이다(6개 앱 × 3가지 모양, 144/144). 새 기록은 6개 앱 모두 `questionSet: 2`, 뺀 문항 키 없음, 매핑 v2로 빠짐없이 보인다. 예전 진행 중 기록은 섞이지 않고 새로 시작한다(54/54). 스냅샷 뒤 저장소에 들어간 09-26 변경 3건도 보충 확인했다(§3, 문제 없음).

---

## 0. 무엇을 어떻게 봤나
* **스냅샷**: `scratchpad/review-C-site/class1/`(00:57 `npm run build`). 검토를 시작할 때 스냅샷의 6개 앱 폴더 = 저장소 작업 트리(`diff -rq` 차이 없음). 자기 정적 서버 8791 + 자기 headless Chrome 9355(프로필 `scratchpad/review-C/profile`), CDP `Fetch.enable(*supabase.co*)` 가짜 응답.
* **스냅샷 뒤 저장소 변경(01:20~01:22, Claude — STATUS의 "사용자 결정(09-26)")**: `sci-6-1-1-1` app.js·lesson-config.js(거품 5초 → 빨리 감기), `sci-6-1-1-5` lesson-config.js(정리 틀 3줄 → 2줄), `sci-6-1-1-6` app.js·lesson-config.js(출처 고르기) + `src/data/app-responses/sci-6-1-1-6.ts`(v2 항목 문구 1곳) + 세 앱 spec.md. 공통 틀 `science-sim/experiment.js·style-common.css`(개정 6 dock 작업)는 지침대로 제외. → 항목 1~9는 **스냅샷** 기준, 09-26 변경은 **§3 보충 확인**(스냅샷의 공통 틀 사본 + 저장소의 앱 파일로 만든 사본)으로 따로 봤다. 매핑 비교·lint·tsc는 지금 저장소(09-26 매핑 포함) 기준.
* 포트 8781은 **다른 프로세스**(내 것이 아님, 이미 떠 있던 http.server)가 쓰고 있어 쓰지 않았다(건드리지 않음). 보충 사본은 8791 한 서버에서 스냅샷(`/class1/`)과 함께 `/alt/class1/`로 서빙했다(스냅샷 파일은 그대로).

## 1. 발견

| # | 심각도 | 어디(파일) | 무엇 | 재현 | 제안 |
|---|---|---|---|---|---|
| **M1** | 중간 | 6개 앱 공통 — 원인은 로그아웃 올리기 `class1-record.js` `flushLocalProgress`(앱 머리말 로그아웃)·`src/lib/science-progress.ts` `flushScienceProgress`(블로그 로그아웃) | 예전 판(v1) 로컬 사본이 `dirty`로 남은 기기에서, 그 학생이 새 판(v2)을 한 뒤 로그아웃하면 **"'…' 기록이 다른 기기에서 저장한 기록과 달라요 [확인] 이 기기의 기록으로 저장하기 [취소] …" 창**이 뜬다(같은 기기인데 "다른 기기"). **확인을 누르면 v1 스냅샷이 v2 진행 기록(`app_progress`)을 덮어** 다음에 열 때 처음부터 시작한다. 완료 기록(`app_results`)·선생님의 완료 표시는 영향 없음. 전제 조건은 흔하다: 앱에서 다른 화면으로 나갈 때(pagehide) keepalive 저장의 응답을 받기 전에 페이지가 사라져 **`__meta.dirty = true`가 남는 것을 확인**(학습 시간이 10초마다 meta를 바꿈 — 저장 뒤 12초 머물다 나가면 dirty·sentFp가 남음). 배포 전에 v1을 쓰고 로그아웃하지 않은 기기면 v1 사본이 dirty로 남아 있을 수 있다 | 6-1-1-1: 로컬에 v1 키(`dirty:true`) → v2에서 예상 입력(DB 행 v2) → ① 앱 머리말 [로그아웃]: 창 1개, **확인 → DB 행 prefix `sci611sim1:v1` → 다시 열면 빈 예상하기**, 취소 → v2 유지. ② 블로그 로그아웃: 창이 v1, 이어서 v2로 2번 — 확인·확인이면 v2가 남음(시험), 첫 창 취소면 창 1번·v2 유지(시험), 확인 뒤 둘째 창에서 취소하면 v1이 남는다(코드로 확인) | (a) 이번 C에서 할 수 있는 최소 조치: v2 앱이 시작할 때 **자기 앱의 예전 판 로컬 키만** 지운다(`sci611sim1:v1:`·`sci611sim2:v1:`·`sci611sim3:v1:`·`sci6114sim:v2:`·`sci6115guide:v1:`·`sci611guide6:v1:` — 그 앱 키만이라 규칙 안). 같은 기기에서 v2를 연 적이 있으면 창이 생기지 않는다. (b) 근본(공통 틀·블로그): 두 flush에서 서버 행 `state.prefix`가 로컬 스냅샷 prefix와 다르면 묻지 않고 로컬 사본을 버린다(다른 판은 새 앱이 어차피 쓰지 않음, `persist.js` `usable()`과 같은 기준) |
| **M2** | 중간(이미 알림·사용자 결정) | 6개 앱 `spec.md` 끝 시간표 | 간략화로 2~4분 줄었지만 **6개 모두 7분을 넘는다**(보통 7:15~12:44, 느린 학생 10:50~19:38 — 추정). 까닭: 실험관찰 조건 전부(8~24칸)와 바꾸지 않은 조사 활동. CLAUDE.md는 이때 사용자에게 알리게 한다(Build·STATUS에 적혀 있고 09-26 결정으로 일부 더 줄임 — §3) | 항목 8 | 수업에서 실제 시간 확인. 더 줄일지는 사용자 결정 |
| L1 | 낮음 | 공통 틀 `lesson.js` finish() onStage | 체험 모드에서 마친 뒤 새로 고침하면 마치기 칸 안내가 "로그인이 풀렸어요. 다시 로그인하면…"(체험 모드인데). 마치기 전에는 "지금은 체험 모드예요"로 맞게 나옴. `isTrial()`을 site_settings 확인 전에 읽음 | 6-1-1-5·6 체험 → 학습 마치기 → 새로 고침(Build C1이 알린 것과 같음) | 공통 틀에서 `getUser()` 뒤에 `isTrial()` 다시 읽기 |
| L2 | 낮음 | 공통 틀 `lesson.js` checkCuriosity | '더 탐구하고 싶은 점'을 비우고 마치기 → "…한 줄이라도 적어야 마칠 수 있어요." 안내가 한 줄을 적은 뒤에도 다음 클릭까지 남음 | 6개 앱 모두(스크린숏 `shots/sci-6-1-1-3-768x1024-08-…`) | 입력 때 `finish-msg` 지우기(공통 틀) |
| L3 | 낮음(사용자 안내) | 저장 키 버전 | "진행 중이던 학생"뿐 아니라 **이미 마친 학생도** 앱을 다시 열면 빈 첫 단계에서 시작한다(예전 판 진행 기록을 쓰지 않으므로 완료 카드·"제출 완료"도 안 보임). 완료 기록·선생님 화면·내 학습 활동은 그대로 | 예전 prefix 행(step `conclude`·`curiosity`, `finishedAt` 있음)으로 열기 | 사용자에게 알릴 문구에 포함 |
| L4 | 낮음(설계대로·안내) | 관리자 "학생 응답 > 질문별" | 문구·key가 같은 질문(예: 결론)도 판마다 나뉜다. 새 기록이 하나라도 생기면 기본 목록은 "간략화 후" 질문이고, 그 표에서 예전 학생은 "(다른 버전 질문에 답함 · 간략화 전(~2026-09-25))", 예전 답은 드롭다운의 "이전 버전 질문 · 간략화 전" 묶음에서 본다(showcase 09). 새 기록이 생기기 전에는 전과 같은 화면 | 6개 앱 관리자 화면 | 선생님께 안내. 원하면 나중에 "key·문구가 같으면 판을 넘어 묶기"(`questionIdentity`) |
| L5 | 낮음 | 6-1-1-4 `style.css` `.drop-counter` · 6-1-1-2·6-1-1-3 3D 이름표 | ① 6-1-1-4 방울 수 표시("💧 묽은 염산 10방울씩 넣기 (⏩ 빨리 감기)", 장면 가운데 위)가 **세로 태블릿 768×1024**(기본·크게 보기 모두)에서 실행하는 약 11초 동안 뒤쪽 병 이름표(묽은 수산화 나트륨 용액·붉은 양배추 용액)를 가린다. 1024×768·휴대폰은 안 가림(C2가 휴대폰만 오른쪽 위로 옮김). ② (기존) 실행 뒤 다가간 시점에서 고정 이름표(리트머스·홈판 이름)가 장면 가장자리에서 잘리고, 휴대폰에서는 3줄 드래그 안내가 장면 아래쪽 이름표를 덮는다(모든 앱 공통). 모두 값이 아닌 고정 이름표 | ① `shots/ff/sci-6-1-1-4-P-표시중.png` ② `shots/exp/sci-6-1-1-2-L-base-2-실행뒤.png`, `…-1-1-3-M-base-2-실행중.png` | ① 6-1-1-3처럼 오른쪽 위로(태블릿도) ② 단계 D·크게 보기 개편 때 |
| L6 | 낮음(모양) | 6-1-1-3·6-1-1-4 `style.css` | '학습 마치기' 버튼 폭이 1·2·5·6은 전체 폭(`.finish-card .wide`), 3·4는 기본 폭(본보기 6-2-1-4와 같음) | showcase 01~06 | 통일하려면 한 줄 |
| L7 | 낮음 | 매핑 `notes` | 새 기록 카드에도 "예상 질문 2·… 간략화 전 기록만 해당해요" 안내 줄이 함께 보인다(틀린 말은 아님) | 관리자 학생별 | 그대로 둬도 됨 |

## 2. 항목별 판정

### 항목 1 — 예전 완료 기록이 그대로 보이는지: **통과**
방법: HEAD 앱 코드(`git archive 9d2dc16`)의 `buildDetail()` 모양 그대로 예전 detail을 앱마다 3가지 만들었다 — ⓐ 모든 키 채움, ⓑ 일부 빈 문자열(예상 q2·발전 질문·궁금한 점 등), ⓒ 또 다른 빈 모양(확인 0번 퀴즈 `tries:0`, 빈 분류·빈 기록 표, 6-1-1-6은 고친 생각 있음/없음·`openedFacts` true/false). 앱별 detail 모양은 첫 커밋 뒤로 바뀐 적이 없어(git 이력 확인) 더 오래된 변형은 없다. HEAD의 `src/`를 스크래치에 꺼내 `node_modules/jiti`로 **HEAD 매핑**과 **지금 매핑**(09-26 수정 포함)에 각각 `extractResponses`·`groupByStage`·`questionIdentity`·`progressInfo`를 돌려 비교했다.

| 앱 | v1 항목 정의(HEAD와 같음) | 예전 detail 3모양: 항목 key·stage·label·질문·답·주석·버전 | extras | 단계 묶음(HEAD = 지금) | 지금 변형 | v2.match(예전) |
|---|---|---|---|---|---|---|
| 6-1-1-1 | 13개 같음, ignore `skipped` 같음 | 3/3 같음 | 없음 | 예상하기(2) > 실험하기(1) > 기록·분석하기(6) > 정리하기(3) > **궁금한 점(1)** | 간략화 전 | false |
| 6-1-1-2 | 13개 같음 | 3/3 같음 | 없음 | 2 > 1 > 6 > 3 > 궁금한 점(1) | 간략화 전 | false |
| 6-1-1-3 | 11개 같음 | 3/3 같음 | 없음 | 2 > 1 > 4 > 3 > 궁금한 점(1) | 간략화 전 | false |
| 6-1-1-4 | 12개 같음 | 3/3 같음 | 없음 | 2 > 1 > 5 > 3 > 궁금한 점(1) | 간략화 전 | false |
| 6-1-1-5 | 16개 같음, ignore `kind` 같음 | 3/3 같음 | 없음 | 조사 시작하기(3) > 조사하기(3) > 발표 준비하기(2) > 정리 질문(7) > 궁금한 점(1) | 간략화 전 | false |
| 6-1-1-6 | 18개 같음, ignore `kind` 같음 | 3/3 같음(고친 생각 있음 18개·없음 16개) | 없음 | 조사 준비(6/4) > 조사하기(2) > 공유 준비하기(3) > 정리하기(6) > 궁금한 점(1) | 간략화 전 | false |

* 예전 '궁금한 점' 항목은 `stages`에서 빠졌지만 `DEFAULT_STAGE_LABELS`로 같은 제목 "궁금한 점"을 받아 **맨 뒤**에 모인다(HEAD와 같은 순서·제목). 매핑 `notes`만 앱 4개(1·2·5·6)에서 한 줄씩 늘었다(L7).
* `progressInfo`(진행 중 행): 예전 step `curiosity` → HEAD "궁금한 점 (5/5단계)" → 지금 "궁금한 점"(번호 없음, 오류 없음). 다른 step은 "n/5" → "n/4". 관리자 화면에 "진행 중 — 궁금한 점 · 마지막 저장 …"으로 보임(showcase 08).
* (참고) 새 detail을 HEAD 매핑에 넣으면 v1 "현재 버전"으로 잡히고 extras에 `questionSet`이 뜬다 → 매핑과 앱을 같은 커밋으로 배포해야 한다.

### 항목 2 — 새 완료 기록: **통과**
6개 앱을 가짜 로그인(768×1024)으로 처음부터 끝까지 진행(실험관찰 조건 전부 기록, 분류·보기 고르기 정답까지, 결론 제출, 빈 '더 탐구하고 싶은 점'으로 마치기 거부 확인 뒤 한 줄 적고 마침)해 `app_results` POST 본문을 가로챘다. 앱마다 POST 1건, `app_progress` 저장 prefix는 새 키뿐.

| 앱 | detail 최상위(가로챈 실제 본문) | 크기 | 변형 | v2 항목 → 답 있음 | extras | 질문 문구 = 앱 config |
|---|---|---|---|---|---|---|
| 6-1-1-1 | `questionSet:2, predict{q1}, records[23], skipped[1], myCriterion{criterion,result}, analysis{criteria}, classify{color,transparent,foam}, conclusion, curiosity` | 2,685자 | 간략화 후 | 10/10 | 없음 | 6/6 |
| 6-1-1-2 | `questionSet:2, predict{q1}, records[24], classify{groups,correct,tries}, analysis{q1,q4}, conclusion, curiosity` | 2,931자 | 간략화 후 | 7/7 | 없음 | 5/5 |
| 6-1-1-3 | `questionSet:2, predict{q2}, records[8], analysis{q3,q4}, conclusion, curiosity` | 1,148자 | 간략화 후 | 6/6 | 없음 | 5/5 |
| 6-1-1-4 | `questionSet:2, predict{q2}, records[12], analysis{inferA,q1}, conclusion, curiosity` | 1,484자 | 간략화 후 | 6/6 | 없음 | 5/5 |
| 6-1-1-5 | `kind, questionSet:2, intro{answer,hintsOpened}, worksheet[3], propertyDiffers[0], comparedWithModel, share{script,reflect}, quiz{q1,q3}, conclusion, curiosity` | 720자 | 간략화 후 | 11/11(성질이 다른 줄이 없으면 표 "(기록 없음)" — 예전과 같은 표시) | 없음 | 5/5 |
| 6-1-1-6 | `kind, questionSet:2, topic, intro{q2,hintsOpened,revised{q2}}, research{cause,damage,solution,source}, openedFacts, share{script,checked,reflect}, quiz{q1,q2}, conclusion, curiosity` | 890자 | 간략화 후 | 13/13 | 없음 | 5/5 |

* 뺀 문항 키(`predict.q2`·`intro.experience`·`intro.q1`·`revised.q1`·`analysis.q2/q3/q5/q1/readA`·`quiz.q2/q4`·`extension`·`fill`·`why`)는 **없다**. 빈 값·`null`·`undefined`도 없다. 16,000자 제한 안.
* 단계 묶음 = 앱 단계 순서(예: 6-1-1-1 예상하기(1) > 실험하기(1) > 기록·분석하기(6) > 정리하기(2)). '더 탐구하고 싶은 점'은 정리하기(조사 앱은 정리 질문/정리하기) 안.
* check-answer 본문 키는 `appId, stage, question, answer, priorBlocks`(정리하기는 `model, hint` 더) — 이름·이메일·user id 없음.

### 항목 3 — 선생님 화면(스냅샷의 관리자 "학생 응답"): **통과**
가짜 관리자 세션 + 가짜 응답(`rpc/is_admin`→true, `profiles`→admin, `member_directory` 학생 5명, 앱마다 `app_results` 예전 1·새 1, `app_progress` 예전 판 진행 중 1·새 판 진행 중 1)으로 6개 앱 모두 열었다. 화면이 부른 것: site_settings, rpc/unread_feedback_count, rpc/is_admin, profiles, rpc/my_must_change_password, app_results, app_progress, member_directory, feedback_threads — 모두 가짜 응답, 콘솔 오류 0.
* **학생별**: 예전 학생 카드 "정리 방식: 앱별 응답 매핑 · 간략화 전(~2026-09-25)" + 단계 묶음 5개(끝이 '궁금한 점'), 새 학생 카드 "… · 간략화 후(2026-09-25~)" + 단계 4개(정리하기 안에 '더 탐구하고 싶은 점'), 두 카드 모두 "그 밖의 저장 값" 없음. 진행 중: 예전 판 "진행 중 — 궁금한 점 · 마지막 저장 …", 새 판 "정리하기 (4/4단계)"(조사 앱 "정리 질문/정리하기 (4/4단계)"). 완료 2 · 진행 중 2 · 시작 안 함 1(showcase 07·08).
* **질문별**: 지금 판 묶음 = 간략화 후 질문(앱마다 6~13개), 드롭다운 끝에 "이전 버전 질문 · 간략화 전(~2026-09-25)" 묶음(11~18개). 결론 질문: 지금 판 표 = 새 학생 답 + 예전 학생 "(다른 버전 질문에 답함 · 간략화 전)", 이전 판 표 = 예전 학생 답(showcase 09, L4). 6개 앱 모두 같은 모양.

### 항목 4 — 진행 중 기록(저장 키 버전): **통과 54/54**
앱마다: 가짜 `app_progress` GET이 **예전 prefix**(`sci611sim1:v1`·`sci611sim2:v1`·`sci611sim3:v1`·`sci6114sim:v2`·`sci6115guide:v1`·`sci611guide6:v1`, step `conclude`/`curiosity`/`wrapup`, 예전 답·6-1-1-6은 예전 `fill:` 하위 키 포함) 스냅샷을 돌려주고, 이 기기 localStorage에도 예전 키 5~6개(+`__meta` 주인 = 같은 학생)를 넣어 둠 →
* 첫 단계·빈 칸에서 시작(복원·충돌 창·새로 고침 연쇄 없음), 열 때 저장 요청 0.
* 첫 입력 뒤 첫 저장 = `POST ?on_conflict=user_id,app_id`(덮어쓰기) · prefix **새 키**, 스냅샷 키는 `step·predict·meta·intro`뿐(예전 값·`fill` 없음).
* 예전 로컬 키는 읽지도 고치지도 않음(값·`__meta` 그대로 — 로그아웃 때 `sci6…` 정리로 지워짐, 단 M1).
* 새로 고침 뒤 새 판 이어 하기, 이후 저장은 `PATCH`·새 prefix. 콘솔 오류 0.

### 항목 5 — 문항 규칙: **통과**
| 앱 | 예상(도입) | 분석(보기) · 그대로 둔 활동 | 정리 | '더 탐구하고 싶은 점' |
|---|---|---|---|---|
| 6-1-1-1 | 1(q1), 힌트 2 | 1(criteria) · 내 분류 기준 2칸·분류 3기준 그대로 | 결론 1 | 정리하기 안 한 줄 |
| 6-1-1-2 | 1(q1), 힌트 3 | 2(분석 1·2 = q1·q4) · 분류하기 그대로 | 결론 1 | 〃 |
| 6-1-1-3 | 1(q2), 힌트 2 | 2(q3·q4) | 결론 1 | 〃 |
| 6-1-1-4 | 1(q2), 힌트 2 | 2(inferA·q1) | 결론 1 | 〃 |
| 6-1-1-5 | 1(answer), 힌트 3 | 정리 문제 2(q1·q3) · 정리 틀·발표 준비 그대로 | 결론 1 | 정리 질문 안 한 줄 |
| 6-1-1-6 | 1(q2), 힌트 3 | 정리 문제 2(q1·q2) · 개념 카드·주제·정리 틀·공유 준비 그대로 | 결론 1 | 정리하기 안 한 줄 |
* 6개 모두: 발전 질문·빈칸 채우기·서술형 칸 없음, `data-stage="curiosity"` 섹션 없음, 단계 메뉴 4칸(`--ss-steps: 4`), 제목 "1.~4.", '더 탐구하고 싶은 점'은 rows 1·최대 200자·Enter 줄바꿈 없음·`finish-wrap` 안, 결론 제출 전(조사 앱은 문제 확인·결론 제출 전)에는 `finish-wrap` 숨김, **비우면 "…한 줄이라도 적어야 마칠 수 있어요."로 마치지 못함**, "(선택)"·"비워도" 없음(앱 파일 grep 0), 버튼 위 "'🎉 학습 마치기'를 눌러야 선생님에게 제출돼요." 늘 보임.
* 뺀 문항을 가리키는 글 없음(grep·화면): 게이트 "예상하기 질문에…"·"조사 시작하기의 질문에…"·"조사 준비의 질문에…", 결론 잠김 "🔒 위의 문제 2개를…", 완료 카드 "분석 질문: 2/2 맞힘"·"정리 문제 2개". 학생 화면 문자열에 "지도서" 0.
* 머리말 접기(계정 줄·단계 메뉴 숨김, "▼ 펼치기", 높이 63px) → 접은 채 다음/이전 이동, 잠긴 4단계를 누르면 까닭 안내, "처음부터 다시 하기" = 확인 창 → `app_progress` DELETE → 첫 단계·빈 칸: 4개 앱(1-1-2·1-1-3·1-1-5·1-1-6) 24/24. 새로 고침 뒤 정리하기·완료 카드·"제출 완료" 복원: 6/6. 체험 모드 끝까지(1-1-5·1-1-6): 서버 호출은 site_settings뿐, 로컬 키는 모두 새 키.

### 항목 6 — 내용 불변: **통과**(바뀐 글은 모두 타당)
HEAD와 지금 `lesson-config.js`를 값으로 비교: **남긴 문항은 id별로 글자까지 같다**(문구·보기·정답·피드백·모범 답안·compareTip), 문항 밖 키(용액·방법·지시약·색·결과 표·관찰 보기·분류 기준·정답·방울 수·참고 자료·조사 팁·정리 틀·공유 준비)는 **하나도 안 바뀜**. 바뀐 것:
* 모든 앱: `storageKey`, `stages`(curiosity 뺌), 뺀 문항·힌트, `curiosity`(prompt "…한 줄로 적어 보세요.", `minLength` → `maxLength: 200`; 6-1-1-3·4는 예전의 앱 맞춤 문구 "산성 용액과 염기성 용액에 대해/…섞는 것에 대해"가 새 기준 공통 문구 "이번 실험을 하고 나서…"로, placeholder 앞부분만), 6-1-1-5 `intro.question2/placeholder2` 삭제.
* 6-1-1-3 `modelNote`: 관찰 전에 결과를 말하던 문장("기포는 넣자마자 생기지만…", "삶은 달걀흰자와 삶은 닭 가슴살이 흐물흐물해지는 변화는…")을 "실제로는 물질이 변하는 데 수업 시간 안에는 보이지 않을 만큼 오래(여러 날) 걸리기도 해요 …"로 — CLAUDE.md "답을 먼저 알려 주지 않는다"에 맞고, 물질별 안내는 분석 단계 `meltNote`에 그대로.
* 6-1-1-4 관찰 카드 '색 구별' 도우미: "◀ 산성이 강함 / 염기성이 강함 ▶"(분석 1의 답) → "색 띠 (모형)" "◀ 붉은색 쪽 / 노란색 쪽 ▶", 화면 읽기 "처음은 붉은색 쪽, 넣은 뒤는 노란색 쪽". 띠의 색·▲ 자리는 그대로이고 변화표 오른쪽 끝이 실제로 노란색(#e7c52c)이라 사실과 맞다. 산성·염기성 이름의 변화표는 분석 단계에 그대로.
* 앱 코드: 6-1-1-1·2 결과를 보던 시점에서 다른 칸을 고르면 `flyHome`(방향 그대로), 6-1-1-1 안내 문장 자리, 6-1-1-3 빨리 감기 표시 자리, 6-1-1-4 방울 수 표시 자리(휴대폰) — 표시만.

### 항목 7 — 실험 앱 4개 실험 화면: **통과**
4개 앱 × 10가지(기본·크게 보기 × 1024×768·768×1024·375×812, 어두움 1024·768, `?no3d=1` 1024·768) = 40회, 앱마다 대표 칸 2~3개를 실행하고 실행 중·실행 뒤 화면을 찍고 기록까지 했다(6-1-1-1 빨랫비누 물×거품·식초×색깔·묽은 염산×냄새(안전 칸), 6-1-1-2 식초×푸른색 리트머스·빨랫비누 물×페놀프탈레인, 6-1-1-3 달걀 껍데기×묽은 염산·삶은 달걀흰자×묽은 수산화 나트륨 용액, 6-1-1-4 실험 A 25방울·0방울).
* **기록 흐름 40/40**(크게 보기에서는 기록하기가 장면 안 막대에 있고, 누를 때 화면 안). 기본값: 세로 태블릿만 크게 보기 켬, 가로·휴대폰 끔(40/40 맞음). 가로 스크롤 0, 말 줄임 0, 콘솔 오류 0.
* 장면 안 요소(배지·토글·드래그 안내·안내 문장·초시계·빨리 감기·방울 수·막대) 겹침: **기본 화면 0**(6-1-1-3 빨리 감기 표시·6-1-1-4 방울 수 표시는 떠 있는 동안 0.35초마다 따로 재도 1024×768·768×1024·375×812 모두 0 — 빨리 감기 표시는 오른쪽 위라 관찰하는 홈을 안 가림). 크게 보기 휴대폰에서만 3건(6-1-1-1 안내 문장×드래그 안내 175×33px, 6-1-1-2·3 토글×드래그 안내 96×4px) — 지침대로 크게 보기 모양은 적지 않음(개정 6으로 바뀜). 3D 이름표를 가리는 것은 L5(6-1-1-4 세로 태블릿 방울 수 표시).
* **3D 탭 고르기**(1024×768 기본): 6-1-1-1 병 5개·흰 종이·글씨 종이 → 용액·관찰 방법(격자 54점 중 9번 바뀜), 6-1-1-2 빨랫비누 물 병·붉은색 리트머스 통·페놀프탈레인 점적병·홈 2곳 → 맞게 골라짐, 붉은 양배추(잠김) → "실험 A(18칸)을 모두 기록하면 실험 B가 열려요.", 6-1-1-3 접시 2·병·홈판·홈 → 맞게, 6-1-1-4 첫째 줄 25·10·0방울 홈 → 맞게, 넷째 줄(잠김) → 안내. 버튼은 그대로.
* **시점 방향**: 실행 때 다가간 시점(6-1-1-2 홈, 6-1-1-3 염산 홈판, 6-1-1-1 종이 앞)이 처음 시점과 같은 앞쪽에서 봐 좌우 유지(식초 병·염산용 핀셋 왼쪽, 0→25방울 왼쪽→오른쪽 — `shots/exp/*-L-base-0-처음` ↔ `*-실행중/실행뒤`).
* **실험·기록 순간 글**: 알림 "📝 기록했어요: 식초 · 색깔 → 노란색", "…묽은 염산 + 달걀 껍데기 → 기포가 발생하며 녹는다", 관찰 카드 질문·색 견본·"⏩ 화면의 모형은 실제보다 시간을 빨리 감아…", "⏱ 5초가 지난 뒤에도 거품이 남아 있어요.", 2D 화면 읽기 "물질 표면에서 기포가 생기고, 시간이 지난 뒤 조각이 작아짐(빨리 감기 모형)" — 측정값·관찰 사실·방법뿐, "~할수록"·원인 단정·산성/염기성 결론 없음.

### 항목 8 — 시간: 추정 방식은 타당, **6개 모두 7분 초과(M2)**
각 앱 spec.md 끝 시간표(새로 보이는 글 음절 × 70%, 보통 450음절·60자/분, 느린 300음절·40자/분, 실험 칸은 실측 연출 시간):

| 앱 | 보통 / 느린(간략화 후) | 줄인 시간 | 7분을 넘는 까닭 |
|---|---|---|---|
| 6-1-1-1 | 12:44 / 19:38 | 2:35 / 4:03 | 실험관찰 23칸(거품 실제 5초 ×6), 분류 활동 그대로 |
| 6-1-1-2 | 11:47 / 18:02 | 3:51 / 5:52 | 24칸 |
| 6-1-1-3 | 7:15 / 10:50 | 3:20 / 5:10 | 8칸 + 알아 두기·분석 표 |
| 6-1-1-4 | 8:25 / 12:35 | 3:45 / 5:45 | 12칸 |
| 6-1-1-5 | 7:46 / 12:22 | 2:51 / 4:16 | 정리 틀·발표 준비(바꾸지 않은 조사 활동) |
| 6-1-1-6 | 7:53 / 12:13 | 2:23 / 3:33 | 정리 틀·공유 준비 |
* 뺀 글의 음절을 HEAD config에서 따로 세어 봤다(6-1-1-1 318, -2 1,005, -3 620, -4 858, -5 542, -6 400음절 + 서술형 칸 3~4개) — 줄인 시간 추정과 크기가 맞는다. 자동 진행 시간은 학생 시간이 아니라 따로 적지 않음. 09-26 결정(§3)으로 6-1-1-1 약 0:30, 6-1-1-5 한 줄, 6-1-1-6 출처 타자만큼 더 줄지만 여전히 7분 넘음.

### 항목 9 — 규칙·정리: **통과**
`npm run lint` 통과, `node_modules/.bin/tsc --noEmit -p tsconfig.json` 통과(09-26 매핑 수정 뒤 다시 돌려도 통과), 바뀐 앱 JS `node --check` 통과. 6개 앱·매핑 diff에 `console.log`·`debugger`·`localStorage.clear()`·임시 코드 없음. `src/` 변경은 `src/data/app-responses/sci-6-1-1-{1..6}.ts` 6개뿐. (공통 틀 사본 `diff -r`은 지침대로 Claude 몫 — 확인 안 함.)

## 3. 스냅샷 뒤 저장소 변경(09-26 사용자 결정) 보충 확인 — 문제 없음
저장소의 앱 파일 4개(app.js·lesson-config.js·index.html·style.css, 세 앱)를 **스냅샷의 공통 틀 사본 위에** 올린 사본(`/alt/class1/apps/`)으로 봤다(개정 6 dock 공통 틀은 넣지 않음).
* **6-1-1-1 거품 빨리 감기**: 가짜 로그인 끝까지 23/23칸·POST 1건·v2 매핑 10/10·extras 0. 화면 "⏩ 2" + "⏩ 5초 빨리 감기(모형) — 거품을 지켜봐요"(휴대폰에서 두 줄이어도 드래그 안내와 안 겹침), 모형 안내에 "흔든 뒤 5초 기다리기는 빨리 감기로 짧게 보여 줘요(실제 실험에서는 5초를 기다려요)" — 모형임을 밝혀 규칙에 맞음. 실행 버튼 글은 "▶ 빨랫비누 물을 흔들고 5초 기다리기" 그대로(실제 절차 설명이라 괜찮음). detail 모양 불변.
* **6-1-1-5 정리 틀 2줄**: 산성 1줄 + 염기성 1줄이면 비교 버튼이 켜지고 발표 준비로 넘어감, 안내 "…2줄 이상 채우면…". 가짜 로그인 끝까지·v2 11/11. detail 모양 불변.
* **6-1-1-6 출처 고르기**: 보기 6개(교과서·실험관찰, 사이언스올, 한국해양과학기술원, 에듀넷, 국가환경교육통합플랫폼, 기타(누리집·책)). 세 칸만 적으면 참고 자료 잠김 유지·다음 단계 막힘("…출처는 하나 골라 주세요."), 고르면 열림. `research.source`에 **보기 글자 그대로** 저장("교과서·실험관찰") → 관리자 표 "어디서 찾았나요?(출처)" 칸에 그대로 보임(v2 13/13, extras 0). v2 매핑의 참고 자료 카드 문구도 새 동작("세 칸을 적고 출처를 고르면")과 맞음. (낮음·참고) 공유 초안이 "이 내용은 기타(누리집·책)에서 찾았습니다."처럼 되어 '기타'를 고르면 어색하고, 보기 "국가환경교육통합플랫폼"은 조사 팁 이름("국가환경교육 통합플랫폼")과 띄어쓰기가 다름(보기 글자에 빈칸을 못 넣는 제약) — 학생이 초안을 고칠 수 있어 그대로 둬도 됨.
* 지금 저장소 매핑으로 예전 detail 비교(항목 1)를 다시 돌려도 144/144 같음.

## 4. 사용자용 대표 스크린숏 — `scratchpad/review-C/showcase/`(10장)
01~06 `…-탐구1~6-…-정리하기(조사 앱은 정리 질문)-결론·더탐구하고싶은점·학습마치기.png`(가짜 로그인, 768×1024, 결론 제출 뒤 '더 탐구하고 싶은 점' 한 줄 + 제출 안내 + 학습 마치기), 07 `관리자-학생응답-학생별-예전기록(간략화전)-탐구3.png`, 08 `…-새기록(간략화후)·진행중학생-탐구3.png`, 09 `…-질문별-이전버전질문(결론)-탐구3.png`, 10 `실험-크게보기-탐구4-세로태블릿-관찰카드와기록하기.png`. 나머지 약 410장은 `scratchpad/review-C/shots/`(실험 화면 40회 `shots/exp/`, 빨리 감기·방울 수 표시 `shots/ff/`, 09-26 사본 `shots/alt/`, 관리자 `shots/admin-*`, 로그아웃 창 `shots/logout-*`).

## 5. Supabase 차단 기록
Chrome `--host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"` + `--disable-web-security` + 모든 탭 첫 로드 전 CDP `Fetch.enable(*supabase.co*)`. supabase.co 요청은 전부 가로채 가짜로 응답(site_settings GET 136, app_progress GET 71·POST 34·PATCH 201·DELETE, app_results POST 15·GET 2, functions/v1/check-answer POST 34 → `{ok:true, verdict:"ok"}`, 관리자 rpc·profiles·member_directory·feedback_threads, 로그아웃 auth/v1/logout 6). 블로그 로그아웃 시험에서 블로그 홈이 부른 posts·community_posts 등은 가짜 404로 응답(실제로 나가지 않음). 실제 Supabase·Gemini 요청 0. 가짜 세션은 127.0.0.1 출처 localStorage에만(ref 값은 적지 않음). 내려받은 파일 없음(CDN 스크립트·글꼴은 페이지가 스스로 불러옴). `localStorage.clear()` 안 씀(그 앱 `sci6…` 키·`ssUiPref:`·가짜 세션 키만 지움).

**정리**: 내 headless Chrome(9355)은 `Browser.close`로 닫았고 정적 서버(8791)도 껐다(둘 다 포트 닫힘 확인). 스크래치 지우기(`rm -rf`)는 **권한으로 거부**되어 `scratchpad/review-C/` 안에 `shots/`·`showcase/` 말고도 `work/`(시험 스크립트·로그·가짜 detail JSON, 1.7MB), `profile/`(내 Chrome 프로필 — 가짜 세션·앱 키만, 75MB), `head/`(HEAD `src`·6개 앱 사본), `alt/`(09-26 앱 사본), `serve/`(심볼릭 링크)가 남아 있다. 실제 비밀값은 없다 — 지워도 된다.

## 6. 확인하지 못한 것
* 실제 iPad·휴대폰 손가락 조작(합성 마우스 이벤트로만), 실제 로그인·실 DB·실제 Gemini 판정, 실제 학생 소요 시간(시간표는 추정).
* 관리자 화면에서 학생이 많을 때(30명 × 두 판)의 모양 — 5명 가짜 명단으로만 봄.
* 개정 6(크게 보기 새 배치)과 합친 뒤의 실험 화면 — 스냅샷의 단계 A·B 공통 틀로만 봄. 공통 틀 사본 `diff -r`은 Claude 몫.
* 배포 당시 이 6개 앱을 진행 중인 학생·v1 사본이 남은 기기가 있는지(M1·L3의 영향 범위).

## 판단
높음 0. 예전 완료 기록·선생님 화면·새 기록 저장이 모두 맞고, 09-26 변경도 문제없다. M1(예전 판 로컬 사본 → 로그아웃 충돌 창)은 완료 기록을 건드리지 않고 조건이 맞을 때 진행 중 기록만 잃을 수 있어, 배포 전에 (a)를 넣거나 배포 뒤 곧 (b)로 고치기를 권한다. M2는 사용자 결정 사항.

**커밋·배포해도 됨**(권장: M1 (a)를 함께 넣으면 더 안전 — 넣으면 그 부분만 다시 확인).
