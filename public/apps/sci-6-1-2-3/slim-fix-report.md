# slim-fix-report — sci-6-1-2-3 slim-review 반영 (2026-09-22)

수정 파일: `app.js`, `data/lesson-config.js`, `index.html`, `spec.md`(끝에 "개정 2" 절 추가만). `science-sim/`(정본과 diff 없음), `class1-record.js`, `config.js`, `style.css`는 그대로. 과학 수치 불변(27.0 / 24.4 cm/s, 5초 135 / 122 cm, ±1 cm). 커밋·실 DB 쓰기 없음.

## 항목별 처리
| 항목 | 처리 |
|---|---|
| M1 분석 화면 반복 설명 | 안내 문단 → "내 기록으로 만든 표와 그래프를 보고 분석 질문 2개에 답해요." 그래프 아래 설명 문단 삭제(범례로 충분). 표·그래프 캡션 → "내 기록 표" / "내 기록 그래프". 분석 1 문장 짧게("내 기록에서 5초 동안 더 빠른 자동차는 어느 것인가요?"). 분석 화면 한글 약 505 → 393음절 |
| m1 실험 안내와 lead 중복 | lead → "▶ 버튼을 눌러 두 자동차를 한 번 달려요." 관찰 시간 설명 → "두 자동차는 언제나 동시에 출발해요." 모형 안내 소폭 축약(내용 유지). 실험 화면 약 596 → 525음절 |
| m2 확대 사진 aria-label에 답 | "확대한 사진: {자동차} 앞부분 근처의 줄자 눈금"으로 바꿈(쓰지 않게 된 변수 `fl` 삭제) |
| m3 spec 시간표에 느린 학생 없음 | spec "개정 2" 절에 보통/느린 학생 시간표 추가 |
| 저장 키 | `sci612sim3:v2` → `sci612sim3:v3` |

## 검증
자기 전용 headless Chrome(CDP, 스크래치 프로필) + 가짜 세션(localStorage) + 가짜 Supabase(CDP Fetch 가로채기, 실제 네트워크 요청 0건). 끝까지 자동 진행 4회.

| 화면 | 결과 |
|---|---|
| 1024×768 3D(가로) / 800×1100 3D(세로) / 375×812 다크·터치 3D / `?no3d=1` 2D | 모두 완료. 콘솔 오류 0, 가로 스크롤 0, 3D 캔버스 1개(2D는 0개), 다크 배경 rgb(17,22,31) |
| 로그인·저장 | 머리말 "fixtest 계정으로 로그인 중 · ✅ 저장됨". `app_progress` GET → POST → PATCH, 마치면 `app_results` POST(completed: true) |
| 흐름 | 분석 2 '멈춘 곳' 오답 피드백 확인 → 정답. 결론 전 마치기는 "결론을 먼저 적고…"로 막힘 |
| 새로고침 복원 | 마친 뒤 정리하기 단계·완료 카드·결론 입력 복원. 로컬 키는 `sci612sim3:v3:*` |

## 새 시간표
| 단계 | 기계 시간(측정) | 보통 학생 | 느린 학생 |
|---|---|---|---|
| 예상하기 | 0.3초 | 0:50 | 1:14 |
| 실험하기 | 10.3~10.9초(달리기 연출 약 8초) | 1:23 | 2:00 |
| 기록·분석하기 | 1.0초 | 1:06 | 1:50 |
| 정리하기(+마치기) | 2.0초 | 1:16 | 1:53 |
| **합계** | **약 14초** | **약 4:35** (전 4:53) | **약 6:57** (전 7:23) |

사람 시간은 slim-review와 같은 가정(읽기 450/300음절/분, 화면 글 70% 읽음)으로 줄어든 글자 수만큼 다시 계산한 추정이다.

## 못 한 것
실제 학생 스톱워치 측정, 실제 Supabase(RLS), 실제 태블릿 터치, 실험 도중 새로고침 복원(이번 변경과 관계없는 부분이라 다시 하지 않음).
