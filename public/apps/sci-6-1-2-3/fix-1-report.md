# fix-1-report — sci-6-1-2-3 (review 1차 수정)

수정 범위: `public/apps/sci-6-1-2-3/`의 `app.js`, `data/lesson-config.js`, `index.html`, `style.css`만. 공통 틀(`scripts/templates/science-sim/`, 앱의 `science-sim/`)은 건드리지 않았다. 과학 사실은 지도서 206~212쪽(PDF 0-index 26~32)을 `fitz`로 다시 뽑아 대조했다.

## 항목별 처리

| # | 처리 | 위치(파일:줄) |
|---|---|---|
| D1 힌트 3이 방법을 알려 줌 | 줄자(도구)·거리 재기(방법)를 뺀 지도서 207쪽 도입 발문 3을 힌트로 바꿈: "두 친구가 동시에 출발해 같은 시간 동안 달린다면, 그 시간이 되었을 때 두 친구가 있는 위치는 같을까요, 다를까요?" | data/lesson-config.js:45-46 |
| D2 '멈춘' 표현·연출 | ① 힌트 2를 "정한 시간이 된 바로 그 순간에도 두 자동차는 아직 달리고 있어요…"로 바꿈. ② 사진 aria-label과 캡션을 "출발하고 N초가 된 순간 … 이 순간에도 달리고 있었어요"로 바꿈. 2D aria-label에서 "멈춘 곳"을 없앰. ③ 연출: 셔터 순간 자동차가 서지 않고 OVERRUN(0.8초) 동안 같은 빠르기로 계속 달리고, 사진 순간의 앞부분 위치는 📸 표시(2D 점선과 📸, 3D 검은 선·기둥과 '📸 사진 속 위치' 라벨)로 남김. 타이머 문구는 "📸 찰칵! N초가 된 순간을 찍었어요 (자동차는 계속 달려요)". 새로고침 복원(showInstant) 때도 같은 모습으로 보임. ④ 절차 ③과 모형 안내에 "달리는 자동차를 그 순간 찍는다", "멈춘 곳이 아니라 📸 사진을 찍은 순간의 위치까지 잰다"를 넣음. 2D 도움말에 📸 표시 설명을 넣음 | lesson-config.js:43-44, 66, 71; app.js:8-10, 69-74, 199-238(표시), 264-265, 377-460(2D), 610-634, 700-760(3D), 868; style.css:31-32 |
| D3 발전 1이 분석 2·3과 겹침 | 교과서 활동(표에서 가장 빠른·느린 물체, 208쪽 정리 1·2)은 ①로 짧게 남기고, ②에 "송골매는 언제나 사람보다 약 10배 빠르게 움직여"가 옳은지 표 아래 안내(208쪽 지도 길잡이 6: 평균값 아님)로 판단하게 함. 표 안내에 "가장 빠르게 움직였을 때의" 기록이라는 말을 보탬(208쪽 "최고 속력을 바탕으로"와 맞음). 108 ÷ 10 = 10.8이라 "약 10배"는 맞음 | lesson-config.js:175, 190-201 |
| m1 발전 2 모범 답안 중복·결승 기록 누락 | 두 번째 예를 '12분 달리기'(12분 동안 달린 거리를 잼)로 바꿈. 단체 추발 설명에 "따라잡지 못하면 결승선까지 달린 기록으로 겨뤄요"를 덧붙이고, 따라잡는 장면으로 범위를 좁혔음을 밝힘(210쪽) | lesson-config.js:203-216 |
| m2 클라이밍 리드 | 질문 예에서 뺌(선수마다 떨어지는 때가 달라 같은 시간 비교가 아님). compareTip의 "(높이)"도 지움 | lesson-config.js:204-216 |
| m3 두더지·개미 굴 길이 정보 없음 | 질문 1에 "나와서 보니 두더지가 판 굴이 개미가 판 굴보다 훨씬 길었어요."를 넣음. 결론은 여전히 학생이 씀 | lesson-config.js:32 |
| m4 절차 ⑤ 내 값으로 쓰기 없음 | 기록·분석하기의 그래프 아래에 '내 기록으로 빠르기 비교하기' 서술 칸을 새로 둠. 학생이 5초 평균값을 넣어 더 빠른 자동차와 까닭을 쓰고 제출하면, 내 평균값이 들어간 예시 답과 비교할 수 있음. 제출해야 정리하기로 넘어가고 단계 ✓가 붙음. 결과 detail에 `myCompare`로 저장됨 | lesson-config.js:76-86; index.html:77-78; app.js:1003-1028, 1055, 1096, 1104-1106 |
| m5 다시 재기 회차·문구 겹침 | 앱에서만 기록 저장소를 감싸(`expRecords`) 세 번 모두 잰 칸을 다시 재면 **사진 눈금과 2.5 cm 넘게 다른 회차를 먼저** 바꾸고, 그런 회차가 없을 때만 가장 먼저 잰 회차를 바꿈. 모두 잰 칸의 관찰 카드에서는 "N번째 측정이에요" 줄을 지우고 "이번에 기록하면 N회 기록이 새 기록으로 바뀌어요(…회차예요)" 한 문구만 보임. 분석 표 알림 문구와 doneLead도 이 규칙에 맞춤 | app.js:96-116, 857, 875-890, 965 |
| i1 확대 사진 aria-label | 유지(접근성상 필요) | — |
| i2 사진이 위에서 본 그림 | 캡션과 aria-label에 "(보기 쉽게) 위에서 본 그림"을 넣음 | app.js:264-265 |
| i3 3D 첫 프레임 멈춤 | 코드 수정 없음. headless Chrome 3D 실행 3번에서 타이머가 2.8~2.9초에 정상 진행 확인. 실제 태블릿 첫 실행 확인은 여전히 권장 | — |
| i4 3·8초 확장값 | 유지. 8초 사진 뒤 더 달린 위치도 216 + 21.6 = 237.6 cm로 줄자(250 cm) 안(안전하게 246 cm에서 자름) | app.js:72-74 |

## 제안과 다르게 한 것
- D1: 리뷰 예시 문구 대신 지도서 207쪽 도입 발문 3을 힌트로 썼다(출처가 지도서이고 방법·도구를 말하지 않음).
- D2 ③: '반투명 잔상' 대신 '사진 뒤에도 0.8초 더 달리기 + 📸 위치 표시'로 했다. 0.8초 뒤 장면이 멈추는 것은 화면 정리용이라고 모형 안내에 밝혔다.
- D3: 표를 쓰는 교과서 활동을 없애지 않으려고 ①(찾기)을 짧게 남기고 ②(평균값 아님 적용)를 더했다. 212쪽 문항 3(서로 다른 출발선)은 쓰지 않았다.
- m4: 분석 1(교과서 고정값 선택형)은 그대로 두고, 따로 서술 칸을 더했다(학생이 직접 쓰는 활동).
- m5: 공통 틀 대신 앱에서 처리했다. 관찰 카드 문구는 틀이 그린 DOM을 한 박자 뒤(microtask)에 고쳐 쓴다.

## 틀 반영 제안 (코디네이터용)
1. `record-store.js` `nextTrial(k, max, prefer)`: 바꿀 회차를 앱이 고를 수 있게 prefer 콜백을 받기(이 앱의 `expRecords` 대신).
2. `experiment.js:589, 608-613`: 모두 잰 칸이면 "N번째 측정이에요"를 빼고 "이번에 기록하면 N회 기록이 바뀌어요" 한 문구만 보이게(이 앱의 DOM 후처리 대신).
3. `conclude.js`: 저장 키를 옵션으로 받기(이 앱은 `mine-` 접두사 store 래퍼로 두 번째 Conclude를 씀).

## 검증 (개발 서버, 자기 headless Chrome 포트 9347/9348, 공유 탭은 잠깐 연 자기 탭만 쓰고 닫음)
- 전체 흐름(1024×768, `?no3d=1`): 예상(힌트 3개 문구 확인) → 18회 측정(빨강 5초 2회를 일부러 141 cm) → 분석 표 알림 "빨간색 자동차 5초 2회(141 cm)" → 같은 칸 다시 재기: 관찰 카드에 "이번에 기록하면 2회 기록이…(차이가 커 보였던 회차예요)" 한 문구, 기록 뒤 [[1,136],[2,136],[3,136]]이 되고 알림이 사라짐 → 내 기록 쓰기 없이 다음 단계: 막힘 안내 → 제출 뒤 예시 답에 내 평균(136 / 122.3 cm) → 분석 6/6 → 결론·발전 1·2 → 궁금한 점 → 마치기(비로그인 안내, 18번, 6/6).
- 2D 실제 시간: 3초 달리기에서 0.3초마다 약 8.1 cm(27 cm/s)씩 늘고, 셔터 뒤에도 같은 빠르기로 +21.6 cm까지 계속 감. 📸 표시는 81.6 / 72.9 cm에 고정.
- 3D(태블릿 세로 800×1100, 가로 1024×768, 휴대폰 375×812 다크): 달리는 동안 캔버스가 화면 안(128-610 / 128-602 / 167-465 px)에 보임. 셔터 뒤 두 자동차가 계속 달리고 '📸 사진 속 위치' 표시가 남는 것을 스크린샷으로 확인.
- 375px 다크: 모든 단계 scrollWidth = 375, 배경 rgb(17,22,31). 새로고침 복원(궁금한 점 단계, ✓ 5개, 완료 카드, 내 기록 쓰기 제출 상태) 확인. 3D↔2D 6번 전환 시 캔버스 0/1개 반복. 콘솔 오류 0.
- 저장 크기: 기존 최악 13,607자 + myCompare(따옴표 1000자 → 약 2,020자) ≈ 15,630자 < 16,000. 여유가 작다.
- 못 한 것: 실제 태블릿 터치, 로그인 상태 저장.
