# Build(수정) 서브에이전트 지침: sci-6-1-2-3 slim-review 반영

## 목표
`public/apps/sci-6-1-2-3/slim-review.md`의 문제와 제안을 모두 반영한다. 목표 시간: **보통 학생 6분 이하, 느린 학생(읽기·타자 느림)도 7분 안팎**. 화면 글은 짧고 쉽게(한 화면 안내문은 2~3문장 이내), 같은 설명 반복 제거.


* sci-6-1-2-3: 분석 화면의 반복 설명 제거 등 Review 제안 반영.

## 규칙
`CLAUDE.md` 과학 차시 앱 규칙(새 기준) 준수. 과학 수치 불변(탐구 3·4·5 일관성). 공통 틀 사본·`class1-record.js`·`config.js` 수정 금지. 저장 키는 현재 `:v2`에서 한 번 더 올린다(`:v3`, `sci6` 접두사 유지). `spec.md` 개정 절에 변경 사항을 덧붙인다(기존 본문 삭제 금지).
수정 범위: `public/apps/sci-6-1-2-3/**`(위 제외 파일 빼고). git commit/push·실 DB 쓰기 금지.

## 검증
가짜 로그인/가짜 Supabase로 전체 흐름. 태블릿 가로·세로, 375px 다크, `?no3d=1`, 새로고침 복원, 콘솔 오류 0. 단계별 시간표(기계 시간 측정 + 보통/느린 학생 추정)를 다시 계산. 브라우저는 자기 탭 또는 자기 전용 headless만, `localStorage.clear()` 금지, 임시 파일은 스크래치에만.

## 완료 보고
`public/apps/sci-6-1-2-3/slim-fix-report.md`: 항목별 처리, 새 시간표.
