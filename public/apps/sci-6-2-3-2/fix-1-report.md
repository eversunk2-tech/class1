# 수정 1 보고서 — sci-6-2-3-2 (전기의 이용 · 탐구 2) 2026-09-23

`review.md`의 지적에 따라 **이 앱 폴더 안만** 고쳤다. `science-sim/**`·`class1-record.js`(공통 틀 사본)와 `spec.md`·`review.md`·지침 파일은 건드리지 않았다(`diff -r`로 정본과 차이 0 재확인). git 명령 0건, 실제 DB 요청 0건, `localStorage.clear()` 미사용.

## 1. 고친 것

| # | 심각도 | 파일·위치 | 고친 내용 |
|---|---|---|---|
| 1 | **중간** | `data/lesson-config.js` `extra.paragraphs[2]` | "대신 전지 두 개가 **번갈아** 힘을 보태 주어서 전구를 더 오래 켤 수 있어요." → **"대신 두 전지가 힘을 나누어 내기 때문에 전구를 더 오래 켤 수 있어요. (두 전지가 번갈아 일하는 것이 아니라 나란히 함께 일해요.)"** 지도서 278쪽(펌프 두 개를 나란히 놓은 비유)에 맞추고, "교대로 켜진다"는 오개념을 문장으로 직접 막았다. 팝업은 기록·채점이 없어 저장 구조·키와 무관하다. |
| 2 | 낮음(#2) | `app.js` 팝업 열기 | 포커스를 맨 아래 '닫기' 대신 **팝업 제목**(`.px-title`, `tabindex="-1"`)으로 옮기고 `.px-back`·`.px-box`의 `scrollTop`을 0으로 맞췄다. 375×812에서 제목(top 48px)과 "교과서에서 배우는 내용은 아니고…" 안내가 **처음부터 화면 안에** 보인다. 제목에만 포커스 테두리가 보이도록 `style.css`에 `.px-title:focus-visible` / `.px-box:focus { outline: none }` 추가. |
| 3 | 낮음(#6) | `app.js` 팝업 열기·닫기 | 팝업이 떠 있는 동안 `.ss-header`·`.ss-main`·`.ss-footer-nav`에 **`inert` + `aria-hidden="true"`**를 주고 닫을 때 되돌린다(화면 읽기 프로그램이 뒤 내용을 읽지 않게). Tab 가두기·Esc·바깥 누르기·포커스 되돌리기는 그대로 동작. |
| 4 | 낮음(#5) | `app.js` `levelGauge` | 3D 장면의 "밝기 / 빠르기 / 소리 크기 ▮▯▯" 표시를 **높이 0.3 → 0.38 + 굵게**로 키웠다(태블릿에서 읽기 쉽게). |
| 5 | 참고 C | `app.js` 팝업 · `style.css` | 팝업 그림 아래에 **"🏷 위 그림은 회로를 간단히 나타낸 모형이에요."** 캡션을 넣었다(`aria-label`에만 있던 "모형"을 화면 글자로도 표시). SVG 안 캡션은 그대로 두어 폭이 넘치지 않게 했다(측정: 글자 폭 207 < viewBox 260). |

## 2. 고치지 않은 것(까닭)

* **문제 3(공통 틀 `.ss-toast` pointer-events)** — 지침대로 **앱에서 우회하지 않았다.** `app.js`·`style.css`에 토스트 관련 코드가 없음을 확인했다(`pointer-events`는 3D 위 안내 오버레이 `.ov3d`에만 있고 예전부터 있던 것). 정본 수정은 Claude가 처리한다.
* **문제 4(소리 설정이 `app_progress`에도 올라감)** — 그대로 두었다. `CLAUDE.md`가 "앱에서 localStorage를 직접 쓰지 말고 store만 쓴다"고 정하고 있어, 기기별 키를 직접 쓰면 그 규칙과 충돌한다. store가 곧 이 앱의 localStorage이므로 spec 개정 1의 "앱의 localStorage에 기억"은 지켜지며, 리뷰도 "그대로 두어도 동작에는 문제 없음"으로 적었다. 저장 키·구조는 그대로다(`sci623sim2:v1`, `:vN` 유지).
* **참고 A(첫 진입 시 안내 카드가 펼쳐져 3D가 아래로 밀림)** — 공통 틀 기본값이라 앱에서 바꾸지 않았다.
* **참고 B(분석 표의 "다시 관찰해 볼까요?" 죽은 코드)** — 해롭지 않아 그대로 두었다(관찰 확인 규칙이 바뀌면 다시 쓰인다).
* **참고 D(spec 인용 쪽수 276 → 277)** — `spec.md`는 수정 금지 파일이라 건드리지 않았다.

## 3. 다시 확인한 것

자기 전용 정적 서버(포트 8942, 스크래치의 앱 사본)와 자기 전용 headless Chrome(CDP 9942, 임시 프로필, `--autoplay-policy=document-user-activation-required`). 가짜 로그인 세션 + 가짜 Supabase 응답만 사용.

| 항목 | 결과 |
|---|---|
| 팝업 문구 | `대신 두 전지가 힘을 나누어 내기 때문에 …` 로 바뀐 것을 화면에서 확인 |
| 팝업 포커스(375×812) | 포커스 = 제목, `scrollTop` 0/0, 제목 top 48px·안내 문단 모두 화면 안 |
| 뒤 화면 `inert`/`aria-hidden` | 열 때 머리말·본문·이동 막대 모두 `inert=true`, `aria-hidden="true"` → 닫으면 모두 해제 |
| Esc·닫기·포커스 되돌리기·본문 스크롤 잠금 | 모두 정상 |
| 모형 캡션·SVG 캡션 폭 | 캡션 표시됨, 글자 폭 207 < 260(넘침 없음) |
| 3D 세기 표시 | 1024×768에서 "소리 크기 ▮▯▯ / ▮▮▮" 또렷하게 읽힘(스크린숏 확인) |
| 흐름 회귀(6칸 → 분석 2문항 → 정리 → 궁금한 점 → 마치기) | 통과, `completed: true`, `details.qa` **6개** 그대로 |
| 콘솔 오류 | **0건** |

확인하지 못한 것은 `build-report.md` §5와 같다(실제 소리 크기 느낌, 실제 태블릿 터치·GPU, 실 DB, `npm run build`). 스크래치 폴더는 삭제 명령이 권한 거부되어 남아 있고, 띄운 서버와 headless 브라우저는 종료했다.
