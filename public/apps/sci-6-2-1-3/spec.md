# spec.md — sci-6-2-1-3 (계절별 태양의 남중 고도와 낮의 길이의 관계는?)

작성: Plan 서브에이전트(구현하지 않음). `public/apps/sci-6-2-1-3/spec.md`만 작성했다.

근거 자료: `[과학]621_지도서 1.계절의 변화.pdf`를 `pdftotext -layout`으로 추출해 대조했다. **쪽수 대응 확인**: PDF 33쪽 하단 각주가 "136", PDF 34쪽 각주가 "137"이므로 **인쇄 쪽 = PDF 쪽 + 103**이다(plan-instructions.md 안내와 일치). 이 차시(5~6/13차시, 지도서 136~143쪽)는 PDF 33~40쪽에 있다. 학생용 실험관찰 원본(빈 칸)은 `실험관찰-6-2.pdf` 12~13쪽에서 대조했고, 값이 채워진 예시표는 지도서 142쪽(실험관찰 12쪽을 그대로 실은 지면)에서 확인했다. 아래 쪽수는 모두 인쇄 쪽 기준.

## 1. 지도서 분석

### 1.1 차시 기본 정보
* 단원 1. 계절의 변화, **탐구 3(교과서 표기), 지도서 5~6/13차시**. `science-curriculum.ts` term "6-2", unit 1, lesson id **"5"**.
* 제목: "계절별 태양의 남중 고도와 낮의 길이의 관계는?"
* 쪽수: 『과학』 20~21쪽, 『실험관찰』 12~13쪽, 지도서 136~143쪽.
* 성취기준: `[6과13-02] 계절에 따른 태양의 남중 고도와 낮의 길이 사이의 관계를 자료에 근거하여 추론할 수 있다.` (139쪽)
* 학습 목표(136쪽): ① 계절별 태양의 남중 고도와 낮의 길이를 조사할 수 있다. ② 계절별 태양의 남중 고도와 낮의 길이를 그래프로 나타내고, 서로의 관계를 설명할 수 있다.

### 1.2 지도서상 활동의 성격(136~137, 141쪽)
* 실물 실험 기구를 쓰는 손조작 실험이 아니라, **스마트 기기로 한국천문연구원 '생활 천문관' 웹사이트에 접속해 매월 21일 오후 12시 30분의 태양 남중 고도·낮의 길이를 검색해 표·그래프로 만드는 조사 활동**이다(136쪽 활동해요 1·2, 138쪽 지도 길잡이 2 "필요한 자료를 스스로 검색해서 찾을 수 있게 지도").
* 준비물(136쪽): 개인별 스마트 기기, 종이 그래프(『실험관찰』73쪽), 색깔이 다른 유성펜 2개, 자.
* 안전 유의점(137쪽): "스마트 기기로 정해진 주제 이외의 것을 조사하지 않도록 안내한다." 그 외 손조작 안전 수칙은 없음(칼·약품 등 사용 안 함).
* 참고 자료(140쪽, 보충)에는 천체 관측 프로그램으로 6월 21일·12월 21일 태양 위치를 직접 관찰하는 대안 활동도 있다.

### 1.3 실험관찰 예시 자료표(142쪽, 지도서에 실린 실험관찰 12쪽 채움 예시)
> 조사한 지역: 서울특별시 · 조사한 연도: **2024년**

| 계절 | 월 | 태양의 남중 고도(°) | 낮의 길이 |
|---|---|---|---|
| 봄 | 3월 | 52.47 | 12시간 10분 |
| 봄 | 4월 | 64.27 | 13시간 23분 |
| 봄 | 5월 | 72.42 | 14시간 21분 |
| 여름 | 6월 | 75.52 | 14시간 45분 |
| 여름 | 7월 | 72.44 | 14시간 22분 |
| 여름 | 8월 | 64.23 | 13시간 24분 |
| 가을 | 9월 | 52.58 | 12시간 12분 |
| 가을 | 10월 | 41.29 | 11시간 1분 |
| 가을 | 11월 | 32.22 | 9시간 59분 |
| 겨울 | 12월 | 29.00 | 9시간 33분 |
| 겨울 | 1월 | 32.30 | 10시간 0분 |
| 겨울 | 2월 | 41.48 | 11시간 2분 |

* 142쪽 분석: "가장 높은 달: 6월, 가장 높은 계절: 여름 / 가장 낮은 달: 12월, 가장 낮은 계절: 겨울 / 가장 긴 달: 6월, 가장 긴 계절: 여름 / 가장 짧은 달: 12월, 가장 짧은 계절: 겨울."
* 정리해요(137, 142쪽): "여름에는 태양의 남중 고도가 높고, 낮의 길이가 길다. 겨울에는 태양의 남중 고도가 낮고, 낮의 길이가 짧다." + 137쪽 정리 문장: **"태양의 남중 고도가 높아질수록 태양이 지평선 위로 떠 있는 시간이 길어져 낮의 길이가 길어집니다. 따라서 태양의 남중 고도가 높은 여름에는 낮의 길이가 깁니다. 반대로 태양의 남중 고도가 낮은 겨울에는 낮의 길이가 짧습니다."**

### 1.4 지도상 유의점(137~138쪽)
* 137쪽 지도 길잡이 1: "본 학습에 들어가기 전, 태양이 정남쪽에 있을 때 태양 고도가 가장 높다는 것을 숙지하도록 안내한다." → 남중 개념은 **탐구 2(sci-6-2-1-2)에서 이미 배움**. 이 앱은 간단히 한 줄로만 되짚는다.
* 137쪽 지도 길잡이 3: "그래프를 해석하는 과정에서 같은 계절이라도 태양의 남중 고도와 낮의 길이가 달라질 수 있음을 지도한다." → 3월(52.47°)과 9월(52.58°)처럼 비슷하지만 똑같지 않은 값을 표·그래프에 그대로 보여줘 자연스럽게 전달한다(값 왜곡 없음).
* 138쪽 지도 길잡이 4: "태양의 남중 고도는 동지에서 하지로 갈수록 점점 높아지고, 하지에서 동지로 갈수록 점점 낮아진다. 학생들에게는 여름에 태양의 남중 고도가 가장 높고, 겨울에 가장 낮음을 자료 해석의 결과로 파악하도록 지도한다."
* 137쪽 **오개념**: "낮의 길이는 월별로 달라진다." **바로잡기**: "낮의 길이는 월별로 달라지는 것이 아니며 연속적으로 달라진다. 하지에 가장 길었다가 점점 짧아지며 동지에 가장 짧고 다시 길어지는 것을 반복한다."
* 137쪽 지도 길잡이 5: "『실험관찰』13쪽에 제시한 계절별 태양의 남중 고도, 낮의 길이 그래프는 **2024년 서울특별시 자료**로, 지역마다 차이가 있음을 지도한다."

### 1.5 확인 문제(138쪽)
1. "여름에서 겨울로 갈수록 태양의 남중 고도는 (낮아지고, 높아지고), 낮의 길이는 (길어집니다, 짧아집니다)." → 정답: **낮아지고 / 짧아집니다**.
2. "계절별로 같은 시각에 나무의 그림자 길이는 어떻게 달라질지 이야기해 봅시다." → 답: "겨울에서 여름으로 갈수록 태양의 남중 고도가 점점 높아져 나무의 그림자 길이가 점점 짧아진다. 반대로 여름에서 겨울로 갈수록 남중 고도가 낮아져 그림자 길이가 점점 길어진다."

### 1.6 평가 문항(138~139쪽)
* "태양의 남중 고도가 가장 낮다 / 낮의 길이가 가장 짧다" → 정답 **겨울**.
* 서술형(같은 시각의 여름·겨울 그림 ㉠·㉡ 중 여름 고르기) → 정답 ㉡, 까닭 "여름일 때 태양의 남중 고도가 겨울보다 더 높기 때문".

### 1.7 이 단원에서 다루지 않는 개념(스포일러 금지 — plan-instructions.md 메모 반영)
* **계절 변화의 원인(지구 자전축이 약 23.5° 기울어진 채 공전하기 때문)은 지도서 140쪽 "보충 자료"에 있지만, 이는 다음 차시 탐구 5(150~163쪽, sci-6-2-1-5)에서 다룰 내용**이다. 이 앱에서는 "왜 계절별로 남중 고도가 달라지는지"는 설명하지 않고, "계절별로 남중 고도·낮의 길이가 어떻게 달라지고 서로 어떤 관계인지"만 다룬다. 화면 어디에도 "자전축", "23.5°", "기울어져서" 같은 원인 설명을 넣지 않는다.
* "절기"(141쪽 보충 자료)도 이 차시 범위 밖이라 다루지 않는다.

## 2. 앱 종류 판단

**sim(실험 시뮬레이션)**, 사용자 결정(`plan-common.md`)에 따름. 다만 근거를 밝힌다: 지도서상 이 차시는 손조작 실험이 아니라 **스마트 기기로 온라인 천문 자료를 검색하는 조사 활동**이다(1.2절). `plan-instructions.md` 메모대로, 이를 그대로 "웹사이트 검색" UI로 옮기지 않고 **3D 모형(하늘에서 태양이 움직이는 경로)에서 학생이 매달의 남중 고도·낮의 길이를 '관찰·측정'하는 방식**으로 옮긴다. 값은 실험관찰 예시 자료(142쪽, 서울특별시·2024년)를 그대로 쓰고 화면에 지역·연도를 명시한다.

## 3. 화면 설계

4단계: **예상하기 → 실험하기 → 기록·분석하기 → 정리하기**(궁금한 점은 정리하기 안 선택 입력 한 줄).

### 3.1 예상하기 (탐구형 질문 1개)
* Q1: "우리나라에서는 여름과 겨울에 오후 6시의 하늘 모습이 달라요. 여름의 오후 6시에는 해가 아직 떠 있지만, 겨울의 오후 6시에는 이미 어두워요. 계절에 따라 태양이 하늘 가장 높은 곳까지 떠오르는 정도(남중 고도)와 낮의 길이는 서로 어떤 관계가 있을지 예상해 보고, 그렇게 생각한 까닭도 함께 써 보세요." (136쪽 도입 대화 인용, 최소 10자)
* 힌트(방향만 제시, 답 금지):
  1. "여름과 겨울 중, 낮에 해가 하늘에 더 오래 떠 있는 계절은 언제인지 먼저 생각해 보세요."
  2. "태양이 하늘에서 더 높이 뜨면, 해가 뜨는 곳부터 지는 곳까지 지나가는 길이 어떻게 달라질지 떠올려 보세요."
  3. "탐구 2에서 배운 것처럼, 태양이 정남쪽에 있을 때가 그날 고도가 가장 높은 순간(남중)이라는 점을 기억해 보세요."
* "왜 그런지(원인)"는 절대 묻지 않는다(1.7절 스포일러 금지).

### 3.2 실험하기
* **3D 장면**: 학교 운동장 위 반구형 하늘, 지평선 원에 동·서·남·북 표시, 가운데에 탐구 2에서 썼던 것과 같은 "태양 고도 측정기"(막대) 재등장(짧게 "탐구 2에서 쓴 것과 같아요" 정도만 안내). 드래그로 시점 회전 가능.
* **조건(다각적 실험)**: 달 고르기 factor 1개 — **3월(봄) / 6월(여름) / 9월(가을) / 12월(겨울)**, 4개(측정 월 선택 근거는 8절 열린 질문 1 참고). 모두 "매월 21일 오후 12시 30분" 기준(136, 142쪽과 동일).
* 실행 흐름(달마다 **측정 1회**): 달 고르기 → "▶ 이 달 태양 관찰하기" → 3D에서 태양이 동쪽 지평선에서 떠올라 남쪽 하늘의 가장 높은 지점(남중)을 지나 서쪽으로 져서 사라지는 하루를 **빨리 감기(모형, 약 4~5초로 압축, 실제 걸린 시간이 아님을 배지로 표시)**로 보여준다. 해가 떠 있는 동안 지평선 띠가 밝게 빛나 낮 시간대를 표현. 태양 호의 **최고 높이는 그 달의 실제 남중 고도에, 호의 폭(뜨고 지는 지점 사이 넓이)은 그 달의 실제 낮의 길이에 비례**하게 그린다(연출은 압축하되 수치는 왜곡하지 않음).
* 관찰·기록 카드: 연출이 끝나면 "🔭 측정하기" 버튼(입력 칸 없음, `check` 훅으로 값 공개) → 그 달의 **남중 고도(소수 첫째 자리, °)**와 **낮의 길이(○시간 ○분)**를 함께 보여줌 → "📝 기록하기"로 저장. 4개월을 모두 기록해야 다음 단계로 진행.
* 미니 표: 달(3·6·9·12월) × 기록 여부(✔)로 진행 상황 표시.
* 안전 안내: 특별한 손조작 안전 수칙 없음(137쪽 "스마트 기기로 정해진 주제 이외의 것을 조사하지 않는다"만 한 줄로 안내, 실제로는 조사 UI가 없으므로 문구만 형식적으로 남김) — 8절 열린 질문 참고.

### 3.3 기록·분석하기
* **표**: 계절·월·남중 고도(°)·낮의 길이(○시간 ○분), 내가 기록한 4행.
* **꺾은선그래프 2개**(교과서 142쪽처럼 별도 그래프, 하나로 합치지 않음):
  1. "월별 태양의 남중 고도" — x축 월(1~12), y축 고도(°). 내가 기록한 4점(3·6·9·12월)을 굵게 표시 + **얇은 참고선**으로 12개월 전체(1.3절 표 값, 소수 첫째 자리로 반올림)를 함께 그려 "달이 바뀔 때 갑자기 뛰는 게 아니라 계속 이어진다"는 것을 보여준다(137쪽 오개념 바로잡기 반영).
  2. "월별 낮의 길이" — 같은 방식(y축은 분 단위로 그리되 눈금 라벨은 ○시간으로 표시).
  * 그래프 아래 안내: "가는 참고선은 매달 어떻게 이어지는지 보여 주는 2024년 서울특별시 자료예요(실험관찰 12~13쪽). 낮의 길이는 달이 바뀔 때가 아니라 날마다 조금씩 계속 달라져요."
* **보기 고르기 2개**(자신의 기록으로 답함):
  1. "내가 기록한 자료에서, 태양의 남중 고도가 가장 높은 달은 언제인가요?" 보기: 3월/6월/9월/12월 → 정답 6월. 오답 피드백: "내 기록 표에서 고도 숫자가 가장 큰 달을 다시 찾아보세요."
  2. "태양의 남중 고도가 가장 높았던 그 달(6월)의 낮의 길이는 내가 기록한 네 달 중 어땠나요?" 보기: 가장 길었다 / 가장 짧았다 / 다른 달과 같았다 → 정답 "가장 길었다". 정답 피드백: "맞아요! 태양의 남중 고도가 높을수록 낮의 길이도 길었어요."

### 3.4 정리하기 (탐구형 질문 1개 + 선택 궁금한 점)
* 결론: "지금까지 관찰한 자료를 바탕으로, 계절별 태양의 남중 고도와 낮의 길이는 서로 어떤 관계가 있는지 정리해 써 보세요."
* 모범 답안(137쪽 정리 문장 그대로): "태양의 남중 고도가 높아질수록 태양이 지평선 위로 떠 있는 시간이 길어져 낮의 길이가 길어집니다. 따라서 태양의 남중 고도가 높은 여름에는 낮의 길이가 깁니다. 반대로 태양의 남중 고도가 낮은 겨울에는 낮의 길이가 짧습니다."
* 발전 질문 없음(새 기준).
* 궁금한 점: 선택 입력 한 줄(표준 문구, 비워도 마칠 수 있음).

## 4. 단계별 예상 소요 시간표 (합계 ≤ 7분)

| 단계 | 할 일 | 보통 학생 |
|---|---|---|
| 예상하기 | 질문 읽기 15초 + 힌트 1개 10초 + 답 입력 45초 | 약 1:10 |
| 실험하기 | 안내 읽기 15초 + (달 고르기 5초 + 태양 관찰 연출 5초 + 측정하기·결과 보기 5초 + 기록하기 5초) × 4달 = 80초 + 진행 표 확인 10초 | 약 1:45 |
| 기록·분석하기 | 표·그래프 2개 보기 25초 + 보기 고르기 2문항(응답+피드백) 35초 | 약 1:00 |
| 정리하기 | 결론 쓰기 50초 + 모범 답안 비교 15초 + 궁금한 점 한 줄(선택) 10초 + 마치기 5초 | 약 1:20 |
| 단계 이동·스크롤 | 3회 | 약 15초 |
| **합계** | | **약 5:30** |

느린 학생(읽기·입력이 느리고 힌트를 모두 열어 보는 경우 약 +1분) → **약 6:30**, 7분 이내. Review 단계에서 실제 진행으로 재확인한다.

## 5. 과학적 정확성 체크리스트

| 항목 | 앱 표현 | 지도서 근거 |
|---|---|---|
| 측정 기준 시각 | 매월 21일 오후 12시 30분(남중 무렵) | 136, 142쪽 |
| 자료 지역·연도 | 서울특별시, 2024년(화면에 명시) | 142쪽, 137쪽 지도 길잡이 5 |
| 측정 월·값 | 3월 52.5°/12시간10분, 6월 75.5°/14시간45분, 9월 52.6°/12시간12분, 12월 29.0°/9시간33분(원자료 142쪽을 소수 첫째 자리로 반올림해 표시) | 142쪽 |
| 참고선(12개월) | 그래프에 얇은 선으로 표시, 값은 142쪽 표 그대로(소수 첫째 자리) | 142쪽 |
| 남중 고도가 가장 높은/낮은 달 | 6월 / 12월 | 138, 142쪽 |
| 낮의 길이가 가장 긴/짧은 달 | 6월 / 12월 | 138, 142쪽 |
| 관계 결론 문장 | "남중 고도가 높아질수록 낮의 길이가 길어진다. 여름에 길고 겨울에 짧다." | 137쪽 정리해요 |
| 오개념 지도 | 그래프 참고선 + 안내 문구로 "낮의 길이는 매일 조금씩 계속 달라진다"를 전달(월별로 갑자기 바뀌는 것 아님) | 137쪽 오개념·바로잡기 |
| 같은 계절이라도 값이 다를 수 있음 | 3월(52.5°)과 9월(52.6°)처럼 비슷하되 다른 값을 그대로 표시 | 137쪽 지도 길잡이 3 |
| 남중 개념 | "탐구 2에서 배운 것처럼"으로만 되짚고 새로 설명하지 않음 | 137쪽 지도 길잡이 1 |
| 계절 변화의 원인 | 화면에 없음(스포일러 금지) | 140쪽은 다음 차시(탐구 5)에서 다룸 — plan-instructions.md 메모 |
| 안전 | 특별한 손조작 안전 수칙 없음(조사 활동) | 137쪽 |
| 출처 표기 | "실험관찰 12~13쪽 참고 자료" (지도서라는 말 사용 안 함) | CLAUDE.md 공통 규칙 |
| 측정값 표시 | 반올림 없이 소수 첫째 자리(고도), 시간·분(낮의 길이) | CLAUDE.md 공통 규칙 |

## 6. config 초안 (`data/lesson-config.js`)

```js
window.LessonConfig = {
  appId: "sci-6-2-1-3",
  storageKey: "sci621sim3:v1",
  stages: [ /* 예상하기·실험하기·기록·분석하기·정리하기(궁금한 점 포함), 4단계 */ ],

  source: { area: "서울특별시", year: 2024, note: "실험관찰 12~13쪽 참고 자료" },

  predict: {
    minLength: 10,
    questions: [
      { id: "q1", text: "우리나라에서는 여름과 겨울에 오후 6시의 하늘 모습이 달라요. 여름의 오후 6시에는 해가 아직 떠 있지만, 겨울의 오후 6시에는 이미 어두워요. 계절에 따라 태양이 하늘 가장 높은 곳까지 떠오르는 정도(남중 고도)와 낮의 길이는 서로 어떤 관계가 있을지 예상해 보고, 그렇게 생각한 까닭도 함께 써 보세요." },
    ],
    hints: [
      "여름과 겨울 중, 낮에 해가 하늘에 더 오래 떠 있는 계절은 언제인지 먼저 생각해 보세요.",
      "태양이 하늘에서 더 높이 뜨면, 해가 뜨는 곳부터 지는 곳까지 지나가는 길이 어떻게 달라질지 떠올려 보세요.",
      "탐구 2에서 배운 것처럼, 태양이 정남쪽에 있을 때가 그날 고도가 가장 높은 순간(남중)이라는 점을 기억해 보세요.",
    ],
  },

  // 실험하기: 측정 월(조건) — 소수 첫째 자리로 반올림한 값(원자료: 3월 52.47/12h10m, 6월 75.52/14h45m, 9월 52.58/12h12m, 12월 29.00/9h33m)
  months: [
    { id: "3", label: "3월(봄)", altitude: 52.5, dayMinutes: 730, dayLabel: "12시간 10분" },
    { id: "6", label: "6월(여름)", altitude: 75.5, dayMinutes: 885, dayLabel: "14시간 45분" },
    { id: "9", label: "9월(가을)", altitude: 52.6, dayMinutes: 732, dayLabel: "12시간 12분" },
    { id: "12", label: "12월(겨울)", altitude: 29.0, dayMinutes: 573, dayLabel: "9시간 33분" },
  ],

  // 그래프 참고선(12개월 전체, 142쪽 표를 소수 첫째 자리로 반올림)
  refCurve: [
    { month: 3, altitude: 52.5, dayMinutes: 730 }, { month: 4, altitude: 64.3, dayMinutes: 803 },
    { month: 5, altitude: 72.4, dayMinutes: 861 }, { month: 6, altitude: 75.5, dayMinutes: 885 },
    { month: 7, altitude: 72.4, dayMinutes: 862 }, { month: 8, altitude: 64.2, dayMinutes: 804 },
    { month: 9, altitude: 52.6, dayMinutes: 732 }, { month: 10, altitude: 41.3, dayMinutes: 661 },
    { month: 11, altitude: 32.2, dayMinutes: 599 }, { month: 12, altitude: 29.0, dayMinutes: 573 },
    { month: 1, altitude: 32.3, dayMinutes: 600 }, { month: 2, altitude: 41.5, dayMinutes: 662 },
  ],

  quiz: [
    { id: "q1", text: "내가 기록한 자료에서, 태양의 남중 고도가 가장 높은 달은 언제인가요?",
      options: [{ id: "3", label: "3월" }, { id: "6", label: "6월" }, { id: "9", label: "9월" }, { id: "12", label: "12월" }],
      answer: ["6"], correct: "맞아요! 6월에 태양이 가장 높이 떠올랐어요.",
      wrong: "내 기록 표에서 고도 숫자가 가장 큰 달을 다시 찾아보세요." },
    { id: "q2", text: "태양의 남중 고도가 가장 높았던 그 달(6월)의 낮의 길이는 내가 기록한 네 달 중 어땠나요?",
      options: [{ id: "long", label: "가장 길었다" }, { id: "short", label: "가장 짧았다" }, { id: "same", label: "다른 달과 같았다" }],
      answer: ["long"], correct: "맞아요! 태양의 남중 고도가 높을수록 낮의 길이도 길었어요.",
      wrong: "내 기록 표에서 낮의 길이 숫자도 함께 비교해 보세요." },
  ],

  conclude: [
    { id: "conclusion", kind: "결론",
      prompt: "지금까지 관찰한 자료를 바탕으로, 계절별 태양의 남중 고도와 낮의 길이는 서로 어떤 관계가 있는지 정리해 써 보세요.",
      model: "태양의 남중 고도가 높아질수록 태양이 지평선 위로 떠 있는 시간이 길어져 낮의 길이가 길어집니다. 따라서 태양의 남중 고도가 높은 여름에는 낮의 길이가 깁니다. 반대로 태양의 남중 고도가 낮은 겨울에는 낮의 길이가 짧습니다." },
  ],

  curiosity: { prompt: "더 알아보고 싶은 점이 있다면 자유롭게 적어 보세요(적지 않아도 괜찮아요).", minLength: 0 },
};
```

`app.js`의 `SciSim.Experiment.create` 핵심(요지):
```js
factors: [{ id: "month", title: "관찰할 달 고르기", options: C.months.map(m => ({ id: m.id, label: m.label })) }],
phases: [{ id: "M", name: "관찰", lead: "달을 고르고 태양이 하루 동안 움직이는 모습을 관찰해요(빨리 감기 모형).", trials: 1,
           cells: C.months.map(m => ({ month: m.id })) }],
cellKey: sel => sel.month,
runLabel: sel => "▶ " + C.months.find(m => m.id === sel.month).label + " 태양 관찰하기",
observe: function (sel) {
  var m = C.months.find(x => x.id === sel.month);
  return {
    question: "🔭 측정하기를 눌러 이 달의 태양 남중 고도와 낮의 길이를 확인해요.",
    body: /* 3D 정지 장면(남중 순간) 요약 카드 */,
    type: "numeric", fields: [],
    checkLabel: "🔭 측정하기",
    check: function () {
      return { ok: true, message: m.label + " 남중 고도 " + m.altitude.toFixed(1) + "°, 낮의 길이 " + m.dayLabel };
    },
  };
},
makeRecord: (sel, v) => { var m = C.months.find(x => x.id === sel.month); return { month: Number(sel.month), altitude: m.altitude, dayMinutes: m.dayMinutes, dayLabel: m.dayLabel }; },
miniTable: { rows: C.months, cols: [{ id: "done", label: "기록" }], sel: r => ({ month: r.id }) },
```
(sci-6-1-2-3 개정3의 `fields: [] + checkLabel + check` 패턴을 그대로 재사용 — 8절 참고.)

## 7. 3D 장면 스케치

* 바닥: 학교 운동장(밝은 회색·옅은 녹색), 지평선을 원으로 표시하고 동/서/남/북 라벨.
* 가운데: 탐구 2와 동일한 "태양 고도 측정기"(막대) — 이번 차시의 주인공은 아니므로 작게 배치, 재등장 안내만.
* 하늘: 반구 돔, 은은한 그라데이션(낮/밤 전환).
* 태양: 노란 구, 선택한 달의 궤적(동쪽 지평선 → 남쪽 하늘 정점 → 서쪽 지평선)을 따라 이동. 궤적의 **최고 높이는 그 달의 남중 고도**, **호의 좌우 폭(뜨고 지는 지점 간격)은 그 달의 낮의 길이**에 비례하게 그린다(겨울은 낮고 좁은 호, 여름은 높고 넓은 호).
* 재생: 실제 하루를 약 4~5초로 압축(항상 "빨리 감기(모형)" 배지 표시), 해가 떠 있는 동안 지평선 띠가 밝게 빛나고 태양이 지면 어두워짐.
* 정점(남중) 통과 시 살짝 정지 강조(카메라가 남쪽을 향해 자동 정렬) 후 계속 진행.
* 종료 후 "🔭 측정하기"를 누르면 고도·낮의 길이 결과 카드가 뜸.
* 2D 대체 화면(`?no3d=1`): 옆에서 본 반원 궤적 SVG(동일한 비례 규칙)로 대체.

## 8. `detail.qa` 질문 목록

| stage | id | question | kind |
|---|---|---|---|
| predict | q1 | 계절에 따른 태양의 남중 고도·낮의 길이 관계 예상과 까닭 | text |
| experiment | months(3,6,9,12) | (관찰·기록만, 별도 서술형 질문 없음 — 공통 틀이 records를 detail에 포함) | record |
| analyze | q1 | 내 기록에서 남중 고도가 가장 높은 달 | choice |
| analyze | q2 | 그 달의 낮의 길이가 네 달 중 어땠는지 | choice |
| conclude | conclusion | 계절별 태양의 남중 고도와 낮의 길이의 관계 정리 | text |
| conclude | curiosity | 더 알아보고 싶은 점(선택) | text(optional) |

## 9. 공통 틀 확장 필요

**없음.** `scripts/templates/science-sim/`만으로 구현 가능하다.
* `observe.type: "numeric", fields: [], checkLabel, check`(입력 칸 없이 버튼으로 결과 공개) 패턴은 `sci-6-1-2-3` 개정 3에서 이미 쓴 것과 동일하다.
* 두 개의 독립된 꺾은선그래프(수치 x축, 여러 계열)는 `table-chart.js`의 `renderLine`(README 3절)이 그대로 지원한다. "참고선(12개월)" + "내 기록(4점)"을 서로 다른 `series`로 넣으면 자동으로 다른 색·마커로 그려진다(점선 스타일까지는 없지만, 계열 이름과 범례로 구분 가능하므로 충분).
* 보기 고르기 2개, 결론 1개, 궁금한 점은 `quiz.js`/`conclude.js`/`curiosity.js` 표준 인터페이스 그대로 쓴다.

## 10. 열린 질문(추천안 포함)

1. **측정 월을 3·6·9·12월 4개로 제한한 것이 적절한지.** 지도서는 12개월 전체를 조사한다. 추천: **4개 유지**(7분 예산, 학습 목표는 "계절별" 비교이지 매달의 정밀 비교가 아님). 대신 그래프의 얇은 참고선으로 12개월 흐름을 함께 보여줘 137쪽 오개념(월별로 갑자기 바뀜)을 방지한다. 사용자 승인 시 6개(짝수 달)로 늘리는 대안도 가능하나 실험하기 단계가 약 40초 늘어난다.
2. **"낮의 길이" 표시 방식.** 추천: 화면·기록·그래프 캡션은 실험관찰 표기 그대로 "○시간 ○분"을 쓰고, 그래프 내부 계산에만 분 단위 숫자를 쓴다(축 눈금 라벨은 시:분으로 변환 표시). CLAUDE.md의 "소수 첫째 자리" 규칙은 각도(고도)에는 그대로 적용하되, 낮의 길이는 원자료 표기 형식을 지키는 예외로 둔다 — 사용자 확인 필요.
3. **3D 태양 궤적을 실제 비율(고도·낮의 길이)에 비례하게 그릴지.** 추천: 비례하게 그린다(학습 목표와 직결되고 구현 난도가 낮음: 호의 높이=고도, 폭=낮의 길이). Build 단계에서 구현이 어려우면 "고도만 비례, 낮의 길이는 고정된 큰/중간/작은 호 3단계"로 단순화하는 절충안도 가능.
4. **탐구 2(sci-6-2-1-2)와 3D 장면(태양 고도 측정기 막대)을 재사용할지, 이 앱만의 새 장면으로 만들지.** 추천: 막대는 배경 소품으로만 재사용해 두 앱의 연속성을 주되, 이번 차시의 핵심(하루 전체 궤적·월별 비교)은 새로 만든다. 두 앱을 만드는 서브에이전트가 다르므로 Build 단계에서 소품 모델(간단한 지오메트리)만 서로 맞추면 된다.

---

## Claude 검토 메모 (2026-09-22, 사용자 승인 전)
* Plan 서브에이전트가 읽기 전용이라 spec 본문은 Claude가 응답에서 옮겨 저장했다.
* 1단원 4개 앱 공통 결정 후보(사용자 확인 대상): **측정값 입력 방식** — "측정하기" 버튼으로 값을 공개하고 기록(타이핑 없음, sci-6-1-2-3 개정 3 방식) vs 눈금을 읽어 직접 타이핑. 추천: 버튼 공개로 통일(7분 기준, 반올림 없음 규칙과도 맞음).
* 저장 키 형식은 기존 앱 관례 `sci621simN:v1`로 맞춘다.
* 로그인 필수(CLAUDE.md) — 미로그인 이용 문구는 삭제함.
* 이 앱 특이사항: 낮의 길이는 실험관찰 표기 "○시간 ○분"을 그대로 쓴다(소수 첫째 자리 규칙은 각도에만 적용) — 추천안.

---

## 개정 2 (2026-09-25, 개편 단계 B) — 궤적 보기 재설계 + 전체 화면 보기. **이 절이 위 본문과 개정 1보다 우선한다.**

지침: `docs/science/sim-redesign/build-B-instructions.md`("모든 앱 공통" + "### B2"), 근거: `docs/science/sim-redesign/spec.md` §4.2·개정 1·개정 4, `docs/science/sim-redesign/audit.md`(N4 — `.ov-values`가 토글과 겹침).

* **"⛶ 전체 화면 보기" 토글 연결**: 이 앱은 공통 `SciSim.Experiment.create()`를 쓰지 않고 `.ss-exp-layout` 등을 직접 만들어 토글이 없었다. `SciSim.Experiment.enlarge({ layout, viewBox, panel, record, scenePanel })`를 연결해 기본 화면·크게 보기 두 모드를 갖췄다(`index.html`은 이미 `science-sim/experiment.js`를 불러오고 있어 추가 스크립트 태그는 필요 없었다). 기록 버튼(`R.record`)은 `record`로, 값 패널(`R.valuePanel` — "②" 머리말 + 남중 고도·낮의 길이 값 + 궤적 범례)은 `scenePanel`로 넘긴다. 켜짐(태블릿 세로 기본값)이면 장면 안 막대(윗줄 값, 아랫줄 기록 버튼)로, 좁은/낮은 화면은 장면 바로 아래 카드로, 꺼짐이면 원래 자리(관찰할 달 고르기 카드 안)로 돌아간다.
* **값 겹침 수정(audit.md N4)**: 장면 위에 겹쳐 띄우던 값 배지(`.ov-values`, 왼쪽 위 top:42px)를 없앴다 — 이제 그 자리에 토글이 온다(top:46px). 바뀌는 값(남중 고도·낮의 길이)은 3D 이름표 대신 값 패널로만 보여준다는 공통 원칙과도 맞다.
* **실험하기 첫 진입 스크롤 보정**: 태블릿 세로에서 크게 보기가 기본으로 켜진 채 실험하기에 처음 들어오면 장면 안 막대(기록 버튼)가 첫 화면 아래로 밀려날 수 있었다(공통 틀의 알려진 제약, review-A2 L1과 같은 종류). `activateExp()`에 `SciSim.Experiment.scrollIntoView(R.viewBox)` 호출을 추가해 실험하기에 들어올 때마다 장면이 보이는 위치로 스크롤한다(이미 보이면 아무 일도 하지 않는다).
* **궤적 보기 재설계(사용자: "지난 경로가 계속 남아 헷갈린다")**: 기록한 달을 전부 남기던 "👣 지난 경로: 보임/숨김" 토글을 없애고 **"🌤️ 이번 달만"(기본)** · **"📊 계절 대표 3개"** 두 가지로 바꿨다.
  * "계절 대표 3개"는 **기록 여부와 무관하게** 3월(봄·가을 대표) · 6월(여름) · 12월(겨울) 값으로 **계산**해서 그린다(교과서 142쪽 표 값 그대로, `pathSamples()`가 이미 기록 여부와 무관한 천문 계산이라 그대로 재사용). 지금 보고 있는 달이 대표 달과 정확히 같으면(예: 3월을 보는 중 "봄·가을" 대표) 굵은 현재 경로와 겹치므로 그 항목만 뺀다(`pathList()`).
  * 이름표는 3D/2D 장면 안에 띄우지 않는다(겹침이 audit.md가 지적한 원래 문제였다) — 대신 값 패널 옆에 **색 점 + 이름 범례**(`R.pathLegend`, "🟢 봄·가을 🟠 여름 🔵 겨울")를 둔다. 범례는 "계절 대표 3개"일 때만 보인다.
  * 저장: 새 키 `pathMode`("current" | "compare3", 기본 "current")를 쓰고 예전 `ghosts`(boolean) 키는 더 이상 읽지 않는다(무해하게 버려둠). **저장 키 버전(`sci621sim3:v1`)·`records`/`detail`/`detail.qa` 구조는 바꾸지 않았다** — 12개월 기록·조사 기간 표기 생략은 그대로.
* **직접 조작(태양 끌기)은 넣지 않음**: B2 지침이 "필요 없다고 판단하면 생략 가능"으로 열어 둔 항목이다. 이 앱은 이미 "달 바 버튼 → 재생(약 1.2초) → 측정하기 → 기록"이 12번 반복되는 짧고 명확한 흐름이고, 하늘 반구 위에서 태양을 끌어 "그날의 해 움직임"을 살펴보는 조작을 추가해도 **기록되는 값(남중 고도·낮의 길이)은 항상 남중 순간 값 하나뿐이라 학습 목표에 새 정보를 주지 않는다**(이미 재생 애니메이션이 뜨고 지는 전 과정을 보여준다). 반면 하늘 반구는 카메라 자유 회전(드래그)이 이미 있는 자리라 태양을 끄는 제스처가 "장면을 돌려보기" 제스처와 혼동될 위험이 있고, 12번 반복되는 짧은 흐름에 새 조작을 얹으면 시간 예산에 여유가 없다. 그래서 이번엔 생략했다 — 달 바 버튼 고르기(이미 탭 1번)로 충분하다고 판단.
* **카메라 방향 유지(개정 3 확인 항목)**: 이 앱은 `v.moveTo`/`v.focus`/`v.setCameraPose` 같은 스크립트 카메라 이동이 코드에 전혀 없다(자유 회전 드래그와 "처음 방향으로" 리셋만 있음) — 따라서 "확대·시점 이동 때 좌우가 뒤집히는" 위험 자체가 없다(코드 확인, 화면 확인 병행).
* **바뀌지 않은 것**: 측정 조건(1~12월×21일), 측정값(교과서 142쪽 표), 예상·정리 질문 문구, 분석 보기 2개, 저장 키·`detail`/`detail.qa` 모양, 3D/2D 궤적·태양의 색(과학적 의미가 있는 색이라 디자인 개편 대상 아님).
* 고친 파일: `app.js`, `style.css`(`.ov-values` 삭제, `.path-mode-group`·`.path-legend*` 추가). `data/lesson-config.js`·`index.html`은 고치지 않았다(`index.html`은 이미 `experiment.js`를 불러오고 있었다).
* Build 보고서: `docs/science/sim-redesign/build-B-sci-6-2-1-3-report.md`.

---

## 개정 1 (2026-09-22, 사용자 지시) — 12개월 모두 측정. **이 절이 위 본문보다 우선한다.**
* 측정: **1월~12월 모든 달의 21일**(오후 12시 30분 기준) — 필수 기록 12칸. 값은 실험관찰 예시표(지도서 142쪽, 서울 2024년)를 소수 첫째 자리로:
  3월 52.5°/12시간 10분 · 4월 64.3°/13시간 23분 · 5월 72.4°/14시간 21분 · 6월 75.5°/14시간 45분 · 7월 72.4°/14시간 22분 · 8월 64.2°/13시간 24분 · 9월 52.6°/12시간 12분 · 10월 41.3°/11시간 1분 · 11월 32.2°/9시간 59분 · 12월 29.0°/9시간 33분 · 1월 32.3°/10시간 0분 · 2월 41.5°/11시간 2분.
* 달 고르기: 가로로 긴 달 바(실험관찰 순서 3월→2월, 계절 색 구분). 누르면 그 달 태양의 하루 경로를 짧게(약 2초, "빨리 감기(모형)") 보여 주고 값 패널에 남중 고도·낮의 길이가 바로 나타난다 → **기록하기**. 타이핑 없음.
* 이전 달의 경로를 흐린 선으로 남겨 두어 계절에 따라 경로가 바뀌는 모습을 비교할 수 있게 한다(선택 토글).
* 본문의 "4개월 측정 + 12개월 참고선"은 없앤다(학생이 12개월을 모두 기록하므로 그래프 = 내 기록). "낮의 길이는 날마다 조금씩 계속 달라진다"는 오개념 안내 문구는 그래프 아래에 유지.
* 분석 보기 2개는 본문 그대로(보기 q1은 3·6·9·12월 대신 12개월 중 고르기가 아니라, "가장 높은 **계절**"(봄/여름/가을/겨울)로 바꿔도 됨 — Build 판단, 정답은 6월/여름).
* 시간표(개정): 예상 1:10 · 실험 2:30(안내 15초 + 12칸 × 약 10초 + 비교 15초) · 분석 1:00 · 정리 1:20 → **약 6:00~6:30**.

---

## 개정 3 (2026-09-25, fix-B1 — Review B M3·L2·L3·L4·L5·L8·L11·L12 수정) **이 절이 위 본문·개정 1·2와 다르면 이 절이 우선한다.**
> 지침 `docs/science/sim-redesign/fix-B-instructions.md`, 보고서 `docs/science/sim-redesign/fix-B1-report.md`.

* **첫 진입 스크롤 없앰(M3)**: 개정 2의 "실험하기에 들어올 때 장면으로 스크롤"을 뺐다(공통 틀과 같게 — 펼쳐진 '🔎 실험 방법 알아 두기'를 먼저 읽게). 달을 누르면 그때 장면이 보이게 맞춘다(전과 같음).
* **계절 대표 3개(L3)**: 굵기 0.035 → 0.05, 불투명 0.55 → 0.9의 **점선**. 색은 달별 실선(계절 색)·하늘과 구별되게 봄·가을 보라 `#7b3fb8`·여름 자홍 `#d81b60`·겨울 청록 `#00838f`(어두움 3D는 밝은 색). 지금 보는 달이 대표 달과 같거나 거의 같아도(3월·9월 등) 셋 다 그린다(3D는 같은 방향의 2% 바깥 구면, 2D는 지금 달 실선 위) — 범례(**지금 달 실선 + 대표 점선 3개**, 이름 함께)와 늘 맞는다.
* **바뀌는 값 이름표 없앰(L4)**: 3D·2D의 "남중 고도 ○○.○°" 이름표를 뺐다(남중 고도 선·호·정남쪽 점선은 그대로, 값은 값 패널에만).
* **처음 시점(L5)**: 교과서·2D처럼 **남쪽 하늘을 바라본 모습**(동쪽 왼쪽, 서쪽 오른쪽) — 북쪽에서 동쪽으로 약 23° 돌려 30° 높이에서 본다("처음 방향으로"·두 번 탭도 같음). 방위·보조선 이름표는 길(선) 위에 그린다(여름 길이 '서' 앞을 지나가도 글자가 보이게).
* **보기 도구 한 줄(L8)**: 가로 태블릿 기본 화면에서 "처음 방향으로 · 이번 달만 · 계절 대표 3개 · 2D로 보기"가 한 줄(버튼 좌우 여백만 줄임, 높이 48px 그대로).
* **크게 보기 막대(L11·L12)**: 막대 윗줄을 머리말(달·계절)과 값 두 칸 한 줄로(막대 186 → 126px, 범례가 있으면 204 → 148px — 3D가 그만큼 커짐). 크게 보기에서는 값 머리말 번호 ② 대신 👀(공통 틀 관찰 카드와 같게). 휴대폰(폭 480px 이하)은 3D 칸을 두 줄 안내 글 위에서 끝내 '북' 이름표가 안내 글에 가리지 않게.
* **2D + 크게 보기(L2)**: 2D 그림 최대 높이를 화면 띠에 맞춰 막대(기록하기)가 스크롤 없이 보이게(가로 태블릿에서 막대가 아래 이동 막대보다 383~444px 아래 → 19px 위).
* **바꾸지 않은 것**: 측정 조건(1~12월 21일)·값·문항·저장 키 `sci621sim3:v1`·기록 구조·`detail`/`detail.qa`(원본 코드와 같은 입력으로 끝까지 진행해 비교 — 글자까지 같음), 궤적 보기 설정 키 `pathMode`, 시간표.

---

## 개정 4 (2026-09-26, 사용자 요청) — 위선 삭제 + 태양 고도 그림 바로잡기 **이 절이 위 본문·개정 1~3보다 우선한다.**
사용자(3D 장면 스크린숏 첨부): "천구의 가로로 그어져 있는 위선은 필요없으니 삭제해줘. 태양고도 측정을 하는 그림이 학생들에게 오개념을 불러일으킬 것 같아. 태양 광선이 막대 끝을 지나 지표면의 그림자 끝과 이루는 각도로 측정하는 것으로 그림을 수정해줘. 지금은 막대의 맨 아래로 광선이 비추고 그때 지표면과의 각도 측정 그림으로 되어 있어"
1. **위선 삭제**: 3D 하늘 반구의 고도 30°·60° 가로 보조선과 그 이름표("30°"·"60°")를 없앤다. 세로 선(정남쪽 자오선)·지평선·방위 이름표는 그대로. 2D(남쪽 하늘 그림)도 같게 가로 격자선을 없애고, 왼쪽 눈금 숫자는 짧은 눈금과 함께 남겨 높이를 읽을 수 있게 한다(Claude 판단 — 사용자에게 알림).
2. **태양 고도 그림**: 남중 때 그리는 그림을 교과서의 측정 방법대로 — 태양 쪽에서 오는 빛줄기가 **막대기 끝을 지나 그림자 끝에 닿는** 선과, **그림자 끝에서** 그 빛줄기와 그림자(땅)가 이루는 각의 호. 옛 그림(막대 밑동에서 태양까지 선·남쪽 지평선까지 회색 선·가운데 큰 호)은 없앤다.
3. **정확성**: 해·빛줄기·그림자·호를 한 태양 방향(그 달 남중)에서 함께 계산 — 그림자 길이 = 막대 높이 ÷ tan(고도), 빛줄기는 햇빛처럼 태양 방향과 평행하고 막대 끝을 정확히 지나며, 호의 각 = 그 달 남중 고도(값 패널 값). 빛줄기는 태양에서 오는 것으로 보이게(같은 단원 탐구 2 `sci-6-2-1-2`의 "평행한 옅은 빛줄기 + 나무 꼭대기를 지나 그림자 끝에 닿는 빛줄기 + 그림자 끝의 호"와 같은 표현).
4. 호·빛줄기에 숫자 이름표는 띄우지 않는다(값은 값 패널에만). 측정 조건·값·문항·저장 키·기록 구조는 그대로.

**Build 결과(2026-09-26, fix-2 — 보고서 `fix-2-report.md`)**
* **위선 삭제**: 3D 고도 30°·60° 가로 원 2개와 이름표 삭제(정남쪽 자오선·지평선·방위 그대로). 2D는 가로 격자선 삭제, 왼쪽 30°·60°·90° 숫자는 짧은 흰 눈금과 함께(90°도 눈금 높이에 맞춤), 정남쪽 점선 그대로.
* **측정기(모형)**: 막대기 1.0 → 3.0(반지름 0.08), 받침판 2.5 × 6.56(두께 0.05) — 12달 남중 그림자가 모두 판 위. 막대기는 판의 남쪽 끝 가까이(원점에서 정남쪽 2.36) — 막대기 끝을 지나는 빛줄기가 모형 속 태양 중심에서 비껴가는 거리를 가장 크게 2.67 → 1.52로 줄이는 자리(가장 낮은·높은 남중 고도에서 같아지게). 해 뜰 무렵·질 무렵 긴 그림자는 판 밖은 땅 위(y = 0.012)에, 하늘 반구 안쪽까지.
* **태양 고도 그림**(남중 때): 막대기 끝을 지나 그림자 끝에 닿는 또렷한 주황 빛줄기(태양 쪽 끝은 빛무리 안에서 옅게 시작) + 막대기 옆을 지나는 옅은 노란 빛줄기 4가닥(모두 태양 방향과 평행) + 그림자 끝의 빨간 호(반투명 채움 + 굵은 테두리, 반지름 = min(2.2, 그림자 × 0.8) — 막대기에 닿지 않음). 옛 그림 3가지(밑동→태양 선, 남쪽 지평선 회색 선, 가운데 큰 호) 삭제. 숫자 이름표 없음. 태양 원반 0.5 → 0.62, 빛무리 2.4 → 3.6. 재생 중에는 남중에 나타나고(값 패널 고도와 함께) 오후에 해가 움직이는 동안은 숨었다가 끝나면 다시(움직이는 그림자와 어긋나지 않게), `show()`는 바로 남중 모습.
* **12달 수치 확인**(장면 물체의 세계 좌표로): 호 각 = 남중 고도(오차 ≤ 2e-14°), 막대기 끝–빛줄기 거리 ≤ 3e-15, 빛줄기 끝 = 그림자 끝, 그림자 길이 = 3.0 ÷ tan(고도)(6월 0.776 ~ 12월 5.412), 방향 정북, 그린 길의 가장 높은 곳·태양 = `lesson-config` 고도. 빛줄기–태양 중심 거리 3·9월 0.02 이하 ~ 6·12월 1.52(원반 0.62 밖, 빛무리 1.8 안).
* **알려진 한계**: 처음 시점이 자오선 면을 약 20° 비스듬히 보므로(시점은 바꾸지 않음) 5~7월은 그림자가 짧아 호가 화면에서 작다(1024×768 크게 보기 6월 반지름 약 14 px). 확대·돌려 보기로는 또렷하다. 필요하면 탐구 2의 "옆에서 본 모습" 칸 같은 보조 그림을 사용자에게 제안.
* 바꾸지 않은 것: 측정 조건·값·문항·저장 키 `sci621sim3:v1`·기록 구조·`detail`/`detail.qa`(12달 기록이 고치기 전과 같음), 궤적 보기, 색, 처음 시점(카메라 위치 같음), `science-sim/`.

**개정 4 보충(2026-09-26 사용자 결정)** — Build 결과 보고의 한계 1(여름 호가 처음 시점에서 작음)에 대한 사용자 답: "2번으로 '옆에서 본 모습' 칸 추가해줘. 추가 태양 광선 가닥 더 있는 것들은 삭제해줘." **이 절이 위 Build 결과보다 우선한다.**
* **'남중할 때 옆에서 본 모습(모형)' 칸 추가**(탐구 2의 칸을 본뜸): 막대기·받침판·그림자·"태양 → 막대기 끝 → 그림자 끝" 빛줄기·그림자 끝의 호('태양 고도')를 자오선 면 정면에서 본 2D 그림. 3D와 같은 남중 태양 방향 값 `noonSun(month)` 하나로 그린다(호 각 = 그 달 남중 고도, 그림자 ÷ 막대기 = 1 ÷ tan(고도), 막대기·받침판 비율 = 3D). 좌우는 탐구 2 약속(3D 처음 시점 화면의 좌우와 같게) → 남중 태양이 늘 화면 왼쪽이라 왼쪽 = 남, 오른쪽 = 북(땅 양끝에 적음, 동쪽에서 본 모습). 숫자 없음. 달을 고르기 전·남중 전에는 안내 글만(값 패널과 같은 때에 나타남).
* **자리**: 값 칸 맨 아래(기록하기 아래) — 기본 화면은 패널, 크게 보기는 조작 칸 안(달 고르기 바로 아래). 장면·버튼을 가리지 않고 3D·2D 모두에서 보인다. 크게 보기에서 가려 있으면 재생 뒤 조작 칸 안에서만 최소로 움직여 보이게 한다(세로 화면 — 달 버튼은 그대로 보임, 페이지는 스크롤되지 않음). 기본 화면은 페이지를 더 움직이지 않는다(칸이 기록하기 바로 아래라 조금 내리면 보임).
* **3D 옅은 평행 빛줄기 4가닥 삭제** — 막대기 끝을 지나 그림자 끝에 닿는 주황 빛줄기 하나만. 막대기 자리·태양 원반·빛무리는 그대로(빛줄기는 태양 원반 안 또는 테두리에서 나온다).
* 12달 확인: 옆 그림 호 각 = 남중 고도(±0.013°, 좌표 소수 둘째 자리), 3D 호 각과의 차 ±0.013°, 빛줄기–막대기 끝 ≤ 0.004(칸 좌표), 그림자 ÷ 막대기 = 1 ÷ tan(고도)(±5e-5) = 3D 비율. 저장 키·기록 구조·`detail`은 그대로(12달 기록이 고치기 전과 같음).

---

## 개정 5 (2026-09-26, 사용자 재수정 요청) — 재생 연출·빛줄기·기록 흐름. **이 절이 위 본문·개정 1~4보다 우선한다.** 측정 조건·값·문항·저장 키·기록 구조는 그대로.
사용자: "이번달만 측정하는게 기본 모드이고, 계절별 대표 궤적은 선택했을 때만 나타나도록 되어 있는거 맞는지 다시 검토해줘." · "태양과 태양광선(주황색 선)이 싱크가 잘 안 맞아. 이거 싱크를 제대로 맞춰줘. 그리고 현재 상태를 보면, 태양이 움직이다가 남중 위치를 지날 때 주황색 선이 한번 나타나고, 태양 움직임 끝난 후 남중 위치로 갔을 때 또 나타나고 2번 나타나는데, 태양 움직임 끝난 후 마지막에 한번 나타나도록 해줘. 그리고 주황색 선이 태양 광선 느낌이 별로 나지 않으니(그냥 마치 태양과 연결한 직선 느낌이야) 좀더 광선 느낌이 나도록 표현해줘. 처음에는 태양이 자연스럽게 궤적을 따라 이동만 하도록 하기." · "'기록하기'를 누르면 자동으로 다음 달로 넘어가는데 이게 오히려 어느달을 측정하는지 헷갈릴 수도 있을것 같아. 기록하기를 누르면 기록만 하고, 기록 후에 다음 달을 학습자가 직접 선택하면 그 달의 태양이 움직이는 것으로 해줘."
1. **궤적 보기**(확인만 — Claude 코드 확인): 기본 "이번 달만"(`pathMode` 기본값 `current`), "계절 대표 3개"는 학생이 눌렀을 때만(고른 것은 그 기기에 기억). 바꾸지 않는다.
2. **재생 연출**: 달을 고르면 태양이 해 뜰 때부터 해 질 때까지 궤적을 따라 **움직이기만** 한다(빛줄기·호·옆 그림·남중 표시 없음). 움직임이 끝난 뒤 태양이 남중 위치에 놓이고 그때 **한 번만** 빛줄기·호(3D)·옆에서 본 모습·값이 나타난다. 해가 진 자리에서 남중으로 **거꾸로 움직이지 않는다**(되감기처럼 보이지 않게).
3. **빛줄기와 태양이 맞게**: 빛줄기가 태양(원반)에서 나오는 것으로 또렷이 보이게 — 12달 모두 빛줄기 선이 태양 원반을 지난다. 호의 각 = 남중 고도(정확성)는 그대로.
4. **광선 느낌**: 태양과 이은 직선처럼 보이지 않고 햇빛이 비쳐 내려오는 느낌(빛나는 줄기·빛이 나아가는 방향 등). 옆에서 본 모습 칸도 같은 느낌으로.
5. **기록 흐름**: '기록하기'를 누르면 기록만 한다(다음 달 자동 선택·재생 없음). 학생이 다음 달을 직접 누르면 그 달의 태양이 움직인다. 안내 문구도 맞게("…기록하기를 누르면 다음 달로 넘어가요" 고침).

**Build 결과(2026-09-26, fix-3 — 보고서 `fix-3-report.md`)**
* **재생 연출**(3D·2D): 달을 누르면 태양이 해 뜨기 조금 전 → 해 진 뒤까지 한 번에 움직인다(1.2초, 빛줄기·호·옆 그림·2D 남중 표시·값 없음). 해가 진 뒤 약 0.15초 쉬고 남중 자리에 태양이 스르르 나타난다(0.32초, 그림자·하늘 밝기도 함께) — 해 진 자리에서 거꾸로 움직이지 않는다. 3D는 이어서 빛줄기가 태양에서 그림자 끝까지 뻗어 나가고(0.38초) 호가 나타난다. 그때 값·옆 그림·기록하기가 한 번에 켜진다(시작 뒤 약 2.2초, 2D는 약 1.75초). 움직임 줄이기·다시 열기(`show`)는 곧바로 남중 모습. 매 프레임 기록: 움직이는 동안 나타난 것 0, 각각 나타난 횟수 1, 태양이 동쪽으로 움직인 프레임 0.
* **싱크**: 3D 측정기를 다시 작게(막대기 3.0 → 0.75, 받침판 1.0 × 1.64, 막대기 자리 정남쪽 0.61) + 태양 원반 0.62 → 0.9(탐구 2와 같은 비율), 빛무리 원반의 2.5배. 빛줄기 선 ↔ 태양 중심 거리 12달 0.004~0.394 = 원반 반지름의 0~44%(고치기 전 0.02~1.52 — 10달이 원반 밖). 호 각 = 남중 고도(오차 ≤ 1.4e-14°), 막대기 끝–빛줄기 ≤ 9e-16, 그림자 = 0.75 ÷ tan(고도)(6월 0.194 ~ 12월 1.353) 그대로. 옆 그림은 막대기 높이 기준으로 그려 크기·수치가 fix-2와 같다.
* **광선 느낌**: 3D 빛줄기 = 빛나는 심(태양 쪽 옅은 노랑 → 땅 쪽 주황) + 부드러운 빛 번짐(늘 카메라 쪽을 보는 판, 양 끝 옅게 — 호를 가리지 않음) + 태양과 막대기 끝 사이의 작은 화살촉(땅 쪽) + 나타날 때 한 번 뻗어 나감. 옆 그림도 같게(빛 번짐은 그림자 끝 쪽에서 사라지고 호는 빛줄기 위, 해 빛무리, 화살촉은 막대기 끝과 그림자 끝 사이, 처음 나타날 때 0.38초 뻗어 나감 — 움직임 줄이기면 바로).
* **기록 흐름**: '기록하기'는 기록만 한다(다음 달 자동 재생 0회). 안내 "✔ ○월을 기록했어요. 다음은 ○월을 눌러 보세요."(마지막은 "12달을 모두 기록했어요."), 다음 달 버튼 테두리가 깜박인다. `intro` 둘째 줄·`app.js` 머리 주석 고침. 빨리 감기 배지에서 낮의 길이를 뺐다(값은 움직임이 끝난 뒤에만). 크게 보기에서 옆 그림을 보이게 하는 조작 칸 스크롤은 달 버튼 12개가 칸 안에 남는 만큼만(review-2 L3).
* 바꾸지 않은 것: 측정 조건·값·문항·저장 키 `sci621sim3:v1`·기록 구조·`detail`/`detail.qa`(같은 입력으로 끝까지 진행해 고치기 전(HEAD)과 글자까지 같음 — 5가지 화면), 궤적 보기(기본 "이번 달만"), 처음 시점, 궤적·계절 색, `science-sim/`.
