# 검토 지침(Review fix-2) — sci-6-2-1-3 위선 삭제 + 태양 고도 그림 + 옆에서 본 모습 (2026-09-26, spec 개정 4·4 보충)

당신은 **Review** 담당이다. **코드를 고치지 않는다**(발견만 기록). 사용자가 "검토 끝나면 바로 배포"를 허락했다 — 학생에게 바로 보일 문제(과학적으로 틀린 그림, 기록이 안 되거나 바뀜, 버튼·값이 가려짐, 페이지가 움직임, 콘솔 오류)를 먼저 찾고, 배포를 막아야 하면 "높음"으로 분명히.

## 먼저 읽기
`CLAUDE.md`(과학 차시 앱 규칙 — "반드시 과학적 사실에 맞게", "장면 속 글자", "태양·빛줄기·그림자처럼 서로 맞아야 하는 그림은 한 상태값에서 함께 계산", "시점을 옮기거나 확대해도 방향이 뒤집히지 않게", "답을 먼저 알려 주지 않는다", 학생 화면에 "지도서" 금지), 이 앱 `spec.md` 끝의 **개정 4·개정 4 보충**(사용자 말 그대로 — 합격 기준), `fix-2-instructions.md`, **`fix-2-report.md`**(바뀐 것·12달 수치·한계), `git diff -- public/apps/sci-6-2-1-3/app.js public/apps/sci-6-2-1-3/style.css`. 같은 단원 탐구 2(`public/apps/sci-6-2-1-2/`)의 '옆에서 본 모습' 칸과 비교한다.
범위 밖: 공통 틀 사본 `science-sim/`(확대 이동 화살표 — 다른 검토가 본다). 화면의 반투명 화살표·"화살표: 화면 옮기기" 안내는 그 작업의 것이니 발견으로 적지 않는다.

## 사용자 요청(합격 기준)
1. "천구의 가로로 그어져 있는 위선은 필요없으니 삭제해줘"
2. "태양 광선이 막대 끝을 지나 지표면의 그림자 끝과 이루는 각도로 측정하는 것으로 그림을 수정해줘"(전에는 막대 밑동으로 광선)
3. "2번으로 '옆에서 본 모습' 칸 추가해줘. 추가 태양 광선 가닥 더 있는 것들은 삭제해줘."

## 확인할 것
1. **과학적 정확성(가장 중요)**: 12달 모두 — 3D·옆 그림에서 빛줄기가 막대기 끝을 지나 그림자 끝에 닿는지, 호가 **그림자 끝**에서 빛줄기와 그림자(땅) 사이의 각인지, 그 각 = 그 달 남중 고도(값 패널·`lesson-config` 값), 그림자 길이 = 막대 높이 ÷ tan(고도), 그림자 방향 = 정북(태양 반대), 3D와 옆 그림이 같은 값에서 나오는지(코드로), 옆 그림의 남·북 방향 표시가 3D 처음 시점과 모순되지 않는지(왼쪽 = 남 — 까닭 확인). 교과서 태양 고도 측정(막대기 그림자 끝과 막대기 끝을 이은 선이 그림자와 이루는 각)과 맞는지. 측정기를 하늘 반구 가운데에서 정남쪽으로 옮기고 키운 모형 단순화가 학생에게 오개념을 줄 만한지(모형 표시·설명). 태양이 그 달의 길 위에 그대로 있는지, 빛줄기가 태양에서 나오는 것으로 보이는지.
2. **요청 반영**: 3D·2D에 가로 보조선·"30°·60°" 이름표가 없는지(자오선·지평선·방위는 그대로), 옛 그림(밑동으로 가는 선·회색 선·가운데 큰 호)이 없는지, 옅은 평행 빛줄기가 0가닥인지, 옆 그림 칸이 기본 화면·크게 보기(조작 칸)·2D에서 보이는지, 숫자 이름표가 없는지(바뀌는 값은 값 패널에만), 달을 고르기 전 안내.
3. **답을 먼저 알려 주지 않음**: 새 안내 글·`aria-label`·옆 그림 이름표가 "높을수록 ~" 같은 결론을 말하지 않는지(관찰 사실만).
4. **흐름·저장**: 12달 기록 → 분석 → 정리까지, 기록 뒤 다시 열기, 새로고침, 크게 보기 켜고 끄기·방향 바꾸기, 2D↔3D, `?no3d=1`, 어두움. 기록·`detail`·`detail.qa`가 고치기 전(`08f2fa8`)과 글자까지 같은지(같은 입력으로 비교). 저장 키 `sci621sim3:v1` 그대로.
5. **배치**: {1024×768, 1180×820, 1366×768, 1920×1080, 768×1024, 810×1080, 390×844, 812×375} × {기본 화면, 크게 보기} — 크게 보기에서 페이지 스크롤 0, 장면 안 막대(값·기록하기)가 보이고 눌림, 옆 그림 칸이 다른 것과 겹치지 않음, 가로 넘침 0, 콘솔 오류 0. 기본 화면에서 옆 그림이 기록하기 아래라 조금 내려야 보이는 것(보고서 한계)이 학생 활동에 문제인지 판단.
6. 접근성(옆 그림 `aria-label`, 대비), `node --check`, `git diff --check`, 임시 코드·`console.log`·`debugger`·`localStorage.clear()` 없음.

## 사용자에게 보여 줄 대표 스크린숏(4장) — `…/scratchpad/review-621-2/showcase/`
1024×768 크게 보기 6월·12월(옆 그림 보이게), 768×1024 크게 보기 3월, 어두움 1장. 파일 이름은 영어·숫자만.

## 테스트 규칙(반드시)
* **자기 정적 서버로 저장소 `public/`을 직접 서빙**: `python3 -m http.server 8787 --bind 127.0.0.1 --directory /Users/sungchul/Desktop/classroom/public` → `http://127.0.0.1:8787/apps/sci-6-2-1-3/index.html`. 비교용 `git archive 08f2fa8 public`을 스크래치에 풀어 8790. dev 서버는 쓰지 않는다. 끝나면 종료.
* 자기 전용 headless Chrome만(포트 **9361**, 프로필은 스크래치 `/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/7bf3a39e-3017-4189-b337-1b82aace66bb/scratchpad/review-621-2/` 안), `--headless=new --use-angle=swiftshader --enable-unsafe-swiftshader --disable-web-security --host-resolver-rules="MAP *.supabase.co 127.0.0.1:9, MAP supabase.co 127.0.0.1:9"`, Node 24 전역 WebSocket으로 CDP, 첫 로드 전부터 CDP `Fetch.enable`(`*supabase.co*`) 가짜 응답, **`Network.setCacheDisabled`로 캐시 끄기**. 실제 Supabase·Gemini 요청 금지, **아무것도 내려받거나 설치하지 않는다**.
* `localStorage.clear()` 금지(`sci621sim3…`·`ssUiPref:` 키만), 다른 탭·프로세스·다른 포트(8785·8786·8796·8797·9356·9359·9360은 다른 작업) 건드리지 않기, git commit/push·실 DB 쓰기·`npm run build` 금지. 저장소 파일은 보고서 하나만 새로 쓴다. 끝나면 자기 Chrome·서버 종료.

## 보고서 `public/apps/sci-6-2-1-3/review-2.md`
심각도별 발견 표(파일:행, 무엇, 재현, 제안), 항목 1~6 판정과 수치, showcase 목록, Supabase 차단 기록, 확인하지 못한 것(실제 iPad 등) — 한국어로 간결하게. 마지막 줄에 **"배포해도 됨 / 고친 뒤 배포"**와 까닭.
