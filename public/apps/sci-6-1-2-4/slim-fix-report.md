# slim-fix-report — sci-6-1-2-4 slim-review 반영 (2026-09-22)

수정 파일: `data/lesson-config.js`, `app.js`, `spec.md`(끝에 개정 절만 추가). `science-sim/`, `class1-record.js`, `config.js`, `index.html`, `style.css`는 건드리지 않았다. 과학 수치(참값 4.2/5.8초, ±0.1초, 반올림 규칙)는 그대로다. git commit/push, 실제 DB 쓰기 없음.

## 항목별 처리
| 항목 | 처리 |
|---|---|
| M1 힌트 2가 답을 누설 | "달리기 시합에서 1등을 어떻게 정했는지 떠올려 보세요."로 바꿈(시간·초시계를 말하지 않음) |
| M2 분석 두 문항이 같은 질문 | q1 "두 자동차 가운데 더 빠른 자동차는?"(내 기록으로 채점, `dataGraded: shorterAt100`), q2 "그렇게 판단한 까닭은?"(걸린 시간이 더 짧기 때문 / 더 길기 때문 / 결승선을 지나 더 멀리 가서 멈췄기 때문, 오답별 피드백) |
| M3 느린 학생 7분 초과 | `trials: 2 → 1`(지도서처럼 자동차마다 한 번). 표는 '걸린 시간' 한 칸, 평균 문장 제거, 막대 제목·다음 단계 잠금 문구·다시 재기 문구 정리. `trials > 1`이면 예전 표(회차+평균)가 그대로 나오게 분기 |
| m1 안내 중복 | 방법 ① 한 문장으로 줄임, 단계 안내 한 줄로 바꿈, 자동차 고르기 아래 "같은 거리를 달려요" 삭제 |
| m2 안전 수칙 | 접혀 있어 그대로 둠(리뷰 의견대로) |
| 저장 키 | `sci612sim4:v2` → `sci612sim4:v3` |

## 검증
자기 전용 headless Chrome(CDP, 임시 프로필) + 로컬 정적 서버. supabase-js CDN 요청은 가짜 클라이언트로, `*.supabase.co` 요청은 전부 가로채 메모리 가짜 응답으로 처리(실제 DB 요청 0건). 앱 키(`sci612sim4:*`)만 지웠고 `localStorage.clear()`는 쓰지 않았다.

| 환경 | 결과 |
|---|---|
| 1024×768 라이트 3D | 완주, 캔버스 1, 가로 스크롤 없음, 콘솔 오류·경고 0, 결과 저장 1건 |
| 768×1024(태블릿 세로) 3D | 같음 |
| 375×812 다크·터치 3D | 같음 |
| 1024×768 `?no3d=1` | 완주, 캔버스 0, 오류 0 |
| 새로고침 복원 | 마친 뒤 새로고침 → 정리하기 단계, 완료 카드, 결론 입력이 가짜 DB에서 복원 |
| 반올림 확인 | 틀린 값(9.9) → "초시계에 나온 숫자를 다시 확인해 보세요.", 맞는 값 → 기록 |
| 분석 채점 | q1 '같다' 오답 피드백 → 내 기록의 짧은 쪽 정답. q2 '더 길기' 오답 피드백 → '더 짧기' 정답 |
| 저장 detail | predict, records(2개, stopwatch 원값), averages(100 cm, 한 번 기록 값), analysis(q1·q2, tries), conclusion, curiosity |

## 새 시간표
추정 방식은 slim-review와 같다(음절 70% 읽기, 보통 450음절/분·60자/분·초시계 6초, 느린 300음절/분·40자/분·초시계 10초).

| 단계 | 기계 시간 | 보통 학생 | 느린 학생 |
|---|---|---|---|
| 예상하기 | 1.2초 | 0:47 | 1:10 |
| 실험하기(2회) | 14.7~16.1초(1회 연출 5.6~7.7초) | 1:36 | 2:13 |
| 기록·분석하기 | 1.6초 | 0:59 | 1:41 |
| 정리하기(+마치기) | 0.4~0.5초 | 1:11 | 1:46 |
| **합계** | **약 18~19초** | **약 4:33** | **약 6:50** |

## 못 한 것
사람이 직접 잰 시간(추정만), 실제 Supabase 저장, 실제 태블릿 터치 드래그, 실험 도중(1회만 기록) 새로고침 복원은 이번에 다시 확인하지 않았다(복원 코드는 바꾸지 않음).
