# fix-1-report — sci-6-2-1-5 리뷰 반영 (2026-09-22)

기준: `review.md`(낮음 5개 + 참고 중 쉽게 고칠 수 있는 것). 수정한 파일은 `app.js`, `data/lesson-config.js`, `style.css`이다.
`spec.md`, `*-instructions.md`, `review.md`, `science-sim/`, `class1-record.js`는 건드리지 않았다(`diff -r`로 정본과 같음 확인). 저장 구조(`detail`)와 저장 키(`sci621sim5:v1`)는 바뀌지 않았다. git·실 DB 사용 없음.

## 낮음 5개
| # | 문제 | 수정 |
|---|---|---|
| 1 | 밝은 모드에서 3D 탁자가 어두운 갈색으로 보임 | 탁자만 `MeshBasicMaterial`로 바꿔 조명 영향을 받지 않게 했다. 밝은 모드는 베이지 `#e3d4b4`, 다크 모드는 `#4d4436`이다. 지구본·전등 조명(평행광)은 그대로 두어 과학 표현은 바뀌지 않는다 |
| 2 | "⭕ 맞았어요! 맞아요! …" 중복 | 분석 1·2의 `correct` 문구 앞 "맞아요! "를 지웠다 |
| 3 | 확대 눈금 끝의 "4"와 "cm"가 붙고, 375px에서 잘림 | viewBox 폭을 508에서 538로 늘리고 "cm"를 오른쪽으로 옮겼다. 측정 결과 "4"와 "cm" 사이 12.4, "cm" 끝 522 < 538 |
| 4 | 막대그래프 두 계열을 주로 색으로 구분함 | 공통 틀은 그대로 두고, 앱이 문서에 빗금 `<pattern>`을 한 번 넣는다. `style.css`에서 `#bar-card`의 '자전축 기울임' 막대와 범례를 빗금으로, '자전축 수직'은 민무늬 + 테두리로 칠한다(다크 모드 색 따로). 범례와 막대 제목에 "(민무늬)", "(빗금)"도 붙였다 |
| 5 | HUD가 '고른 위치'가 아니라 '지구본이 있는 위치'를 보여 줘서 헷갈림 | 첫 줄을 "🌍 지구본: 나 위치 · 자전축 수직 · 그림자 1.4 cm"로 바꿔 지구본 위치임을 밝혔다. 고른 위치가 다르면 둘째 줄에 "➡ 고른 위치: 다 (▶를 누르면 옮겨 가요)"를 보여 준다(3D·2D 공통). ▶를 눌러 옮겨 가면 둘째 줄은 사라진다 |

## 참고 항목 반영
* 분석 2 오답 피드백이 판단 규칙을 알려 주던 문구를 "…그림자 길이와 태양의 남중 고도 사이의 관계를 떠올려 보세요."로 줄였다. 정답 피드백은 그대로다.
* 8칸을 기록한 직후 '다음 단계'를 누르면 완료 알림이 표·그래프를 가리던 문제는 앱에서 막았다. 분석 화면에 들어올 때 알림을 닫는다(공통 틀은 수정하지 않음).
* 공통 틀 관련 참고(관찰 카드에 직전 값이 남음, `?no3d=1` 버튼 문구, 두 탭 동시 사용 때 로컬 기록 사라짐)는 지시대로 손대지 않았다. 두 탭 문제는 Claude가 따로 조사한다.

## 다시 확인한 것
자기 전용 headless Chrome(별도 프로필, CDP 9345)과 자기 정적 서버(8795)로 확인했다. 가짜 세션과 가짜 Supabase(CDP Fetch)를 썼고, 실제 네트워크 요청은 0건이다. 끝난 뒤 두 프로세스를 종료하고 스크래치 폴더를 지웠다.
* 1024×768 3D, 768×1024 3D·터치, 375×812 다크·터치 3D, 768×1024 `?no3d=1` 다크에서 흐름을 끝까지 진행했다. 콘솔 오류 0, 가로 스크롤 0, `app_results` 저장(`completed: true`)을 확인했다.
* 8칸 값: 1.4 1.4 1.4 1.4 / 1.4 0.8 1.4 3.5 cm로 spec과 같다.
* HUD(3D·2D): 나를 기록하고 다를 고르면 "🌍 지구본: 나 위치 · 자전축 수직 / 그림자 1.4 cm / ➡ 고른 위치: 다 (▶를 누르면 옮겨 가요)"가 보인다. 라를 실행한 뒤에는 "🌍 지구본: 라 위치 · 자전축 기울임 (23.5°) / 그림자 3.5 cm"만 보인다.
* 그래프: 계열별 채우기 = 회색(민무늬)과 `url(#s6215-hatch)`(빗금)이다. 밝은 모드·다크 모드 스크린숏에서 모양만으로 구분된다.
* 분석 피드백: "⭕ 맞았어요! 자전축이 수직이면 …"으로 중복이 없다.
* 분석 화면에 들어오면 알림이 닫혀 있다(`toast.hidden = true`).
* 3D 탁자: 밝은 모드 스크린숏에서 밝은 베이지로 보인다.

## 못 한 것
실제 태블릿 터치, 실제 Supabase(RLS), 실제 학생 소요 시간(이번 수정으로 바뀌지 않음), 두 탭 동시 사용(공통 틀 조사 대상).
