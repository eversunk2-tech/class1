# fix-1-report — sci-6-2-2-3 (물질의 연소 · 탐구 3)

Build 서브에이전트, 2026-09-22. 근거: `review.md`(중간 1 · 낮음 4 · 참고 5) + 조정자 지시.
수정 범위는 `public/apps/sci-6-2-2-3/`의 `app.js`, `style.css`, `data/lesson-config.js`뿐이다. spec·지침·review·공통 틀은 고치지 않았다(`diff -r` 정본과 같음). git·실 DB 쓰기 없음.
저장 모양(`records`·`detail`·저장 키 `sci622sim3:v1`)은 바뀌지 않아 키 버전을 올리지 않았다.

## 고친 것
| # | 심각도 | 고친 내용 | 파일 |
|---|---|---|---|
| M1 | 중간 | 나무 부분을 맞힌 뒤 안내: "💡 나무 부분은 아직 불이 붙지 않고 색만 검게 변해요. 더 오래 가열하면 나무 부분에도 불이 붙어요. 물질에 불이 붙기 시작하는 온도를 발화점이라고 해요."(지도서 207쪽). 확인 문구도 "아직 불이 붙지 않고", 장면 상태도 "아직 불꽃 없음 · 색이 검게 변했어요". 분석 2 답("머리가 더 낮은 온도")은 미리 말하지 않는다. | `lesson-config.js`, `app.js` |
| L1 | 낮음 | ㉡ 촛불도 시간 바 78%부터 작아져 96%에서 꺼진다(㉠은 42%). 끝에서는 "㉡ 불꽃 없음 — ㉠보다 늦게 꺼졌어요", 시간 바에 "㉡ 꺼짐" 표시, 꺼진 뒤 흰 김(3D·2D). 확인 문구는 "㉠이 꺼진 뒤에도 더 오래 타다가 나중에 꺼졌어요". 관찰 보기·정답("더 오래 탄다")은 그대로. | `app.js`, `lesson-config.js`, `style.css` |
| L2 | 낮음 | 성냥 머리 부분에 불이 붙은 뒤 잠깐 타고 꺼지며 검게 남는다(모형). 상태 "불꽃 있음 — 불이 붙었어요" → 끝에서 "불이 붙어 타고 꺼졌어요(다 탔어요)". 확인 문구에 "(잠깐 타고 꺼져요)". | `app.js`, `lesson-config.js` |
| L3 | 낮음 | 시간 바 카드 안에 두 촛불 상태 한 줄(㉠·㉡ 아이콘+글자). 덮은 뒤 장면 윗부분을 머리말 바로 아래에 맞춰 장면과 시간 바가 함께 보이게 스크롤. 휴대폰(≤520px)은 장면 210px·시간 바 압축(양 끝 글자 숨김, 빨리 감기 버튼 같은 줄), "빨리 감기(모형)" 배지를 장면 위쪽으로. 가로 태블릿은 장면 높이를 조금 줄여 HUD가 머리말에 가리지 않게. | `app.js`, `style.css` |
| L4 | 낮음 | 실험 2 3D 이름표: "가운데로부터 같은 거리"를 철판 위 점선 앞으로(작게), "알코올램프"를 철판 밖으로. 머리·나무 이름표와 겹치지 않음. | `app.js` |
| 추가 | — | '확인하기' 뒤 '기록하기' 버튼이 아래 단계 버튼(footer)에 가리면 그만큼 올려 보이게(앱 전용, footer 높이만큼 빼고 계산). | `app.js` |
| R1 | 참고 | 머리 부분 안내를 "💡 물질에 불이 붙기 시작하는 온도를 발화점이라고 해요."로. | `lesson-config.js` |
| R3 | 참고 | 결론 비교 도움말에 "(연소: 물질이 산소와 만나 빛과 열을 내면서 타는 현상)" 한 줄. | `lesson-config.js` |
| R4 | 참고 | 시스투스 문장을 "…내뿜는다고 알려져 있어요"로. | `lesson-config.js` |
| R5 | 참고 | "🔎 실험 방법" 안내를 처음에는 접어 둔다(펼치기로 볼 수 있음, 조작 안내는 단계 문구·시간 바 안내에 있음). 실험 단계 화면 글이 약 578 → 437음절로 줄었다. | `app.js` |
| R2 | 참고 | 오개념 카드 위치는 spec 승인안이라 그대로 두었다. | — |

## 다시 확인한 것
자기 전용 headless Chrome(CDP 9373) + 개발 서버(3000) + 가짜 세션·가짜 Supabase(실제 네트워크 요청 0건). 공유 브라우저 창은 쓰지 않았고 `localStorage.clear()`를 쓰지 않았다.
* 전체 흐름(예상 → 4칸 기록 → 분석 2문항 → 결론 → 마치기) 성공, 콘솔 오류 0, 가로 스크롤 0: 1024×768 3D, 768×1024 터치 3D, 375×812 다크·터치 3D, 812×375 터치 3D, 1024×768 다크 `?no3d=1`, 768×1024 다크 `?no3d=1`.
* 기록 값·`detail.records`·`detail.qa`는 이전과 같음(먼저 꺼진다 / 더 오래 탄다 / 불이 붙는다 / 색깔만 검게 변한다), `app_results` `completed: true`.
* 시간 바 값: 40% ㉠ 작아짐·㉡ 탐, 45% ㉠ 꺼짐·㉡ 탐, 100% ㉠ 꺼짐·㉡ "㉠보다 늦게 꺼졌어요". 끝까지 가야 관찰 카드가 나옴(그대로).
* 덮은 뒤 장면·시간 바 위치(화면 안 보이는 영역 = 머리말 아래~footer 위): 375×812 장면 228–438·시간 바 530–580(보이는 영역 223–743), 768×1024 171–581·676–726(165–955), 1024×768 171–474·569–619(165–699), 1366×1024도 둘 다 보임.
* 실제 좌표로 보기→확인하기를 누른 뒤 '기록하기' 아래쪽이 footer 위에 있음(375: 735 < 743, 768: 947 < 955, 1024: 691 < 699).
* 3D↔2D 6번 전환(canvas 1↔0, HUD·시간 바 각 1개), 새로고침 복원, "실험 장치 처음 상태로", 로그인 가림막 — 이전과 같이 정상.
* 3D 실험 2 스크린숏(1024·768): 이름표가 조각·서로와 겹치지 않음. 머리 부분은 불이 붙었다가 꺼진 뒤 검게 남음.

## 남은 것 · 확인하지 못한 것
* 812×375 휴대폰 가로는 공통 머리말(약 165px)이 화면 대부분을 차지해 장면과 시간 바를 함께 볼 수 없다(공통 틀 문제, 앱 범위 밖).
* 실제 iPad 손가락 조작, 실제 Supabase·로그인, 실제 학생 소요 시간(느린 학생은 여전히 7분 경계일 수 있음 — 실험 안내를 접어 약 20~30초 줄였음).
* 스크래치 폴더(`/private/tmp/claude-501/-Users-sungchul-Desktop-classroom/f1b62131-752e-439d-a427-f0c4876f6afe/scratchpad/build-sci-6-2-2-3/`)는 삭제 권한이 거부되어 남아 있을 수 있다(아래 참고).
