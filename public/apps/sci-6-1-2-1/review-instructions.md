# Review 서브에이전트 지침: sci-6-1-2-1

## 목표
독립적으로 검증하고 `public/apps/sci-6-1-2-1/review.md`를 작성한다. **코드는 수정하지 않는다.**

## 읽을 것
`CLAUDE.md`(웹앱 규칙, 과학 차시 앱 규칙), `public/apps/sci-6-1-2-1/spec.md`(확정 결정 절 우선), `build-report.md`, 앱 폴더 전체, 공통 틀 `scripts/templates/science-sim/`. 형식 본보기: `public/apps/sci-6-1-1-3/review.md`.
지도서 원문: `~/Library/CloudStorage/GoogleDrive-sunkyboy@pen.go.kr/내 드라이브/오션초2026/수업/교과서 usb자료/과학 지도서 물체의 운동.pdf` — spec에 적힌 이 차시 쪽 범위와 단원 개관(지도상 유의점·용어 범위). 인쇄 쪽 ↔ PDF 쪽 오프셋은 직접 확인. `python3`의 `fitz`.

## 검증 항목
1. **과학적 정확성 (최우선)**: 화면의 모든 사실·수치·단위·계산·현상·용어·힌트·분석 정답·모범 답안·발전 질문 답을 지도서 원문과 대조하고, 물리적으로 맞는지(거리·시간·속력 관계, 반올림) 직접 계산해 확인. 지도서 밖 주장은 일반 과학 지식으로 판정. 오개념 유발 표현, 다음 차시 용어 스포일러.
* 움직이는 물체/움직이지 않는 물체 판정이 지도서와 일치하는지, 5초 전후 사진 비교가 공정한지(카메라 고정). 공통 틀에 추가된 스냅샷·카운트다운 기능이 다른 앱에 영향 없는지 코드로 확인.

2. **규칙 준수**: 과학 차시 앱 규칙 항목별 체크. 특히 1단원 리뷰에서 반복된 문제 — 힌트가 답을 말함, 발전 질문이 분석 질문·피드백과 겹침, 빨리 감기·단순화 미표시, 수업 목표 활동 누락.
3. **브라우저 동작**: 전체 흐름 끝까지. 태블릿 가로/세로, 휴대폰 375px(가로 스크롤), 다크모드, 새로고침 복원, 콘솔 오류, `?no3d=1`, 3D↔2D 반복 전환, 세로 화면에서 실험 애니메이션이 보이는지, 실시간 연출 시간이 수치와 맞는지.
4. **보안/품질**: 학생 입력 innerHTML(XSS), CDN SRI, 저장 payload 크기(16KB), 접근성, 앱과 틀 사본 일치(`diff -r scripts/templates/science-sim public/apps/sci-6-1-2-1/science-sim`).

## 작업 환경
* 브라우저 창은 다른 에이전트와 공유한다. **자기 탭을 새로 열어(tabs_create) 그 tabId만** 쓰거나, 자기 전용 headless 브라우저(다른 에이전트와 겹치지 않는 포트)를 쓰고 끝날 때 종료. 다른 탭·프로세스를 건드리지 않는다. `localStorage.clear()` 금지(자기 앱 키만 삭제).
* 개발 서버 `http://localhost:3000/class1/apps/sci-6-1-2-1/index.html` (실행 중).
* 임시 파일은 스크래치 디렉터리에만, 끝나면 삭제.
* `public/apps/sci-6-1-2-1/review.md`만 작성. git 조작·실 DB 쓰기 금지.

## review.md 형식
요약(심각도별 개수: 치명/주요/중간/경미/참고) / 과학적 정확성 표(항목, 앱 표현, 지도서 근거 쪽, 판정) / 문제 표(심각도, 위치 파일:줄, 현상, 재현, 수정 제안) / 규칙 준수 체크리스트 / 확인한 것 vs 못 한 것.
