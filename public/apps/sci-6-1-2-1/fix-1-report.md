# fix-1-report.md — sci-6-1-2-1 review 1차 수정

수정 범위: `app.js`, `data/lesson-config.js`만. 공통 틀(`science-sim/`, `scripts/templates/`)은 건드리지 않았다. git 조작·DB 쓰기 없음.
근거 재확인: 지도서 PDF 190·192·194·197쪽을 `fitz`로 다시 뽑아 대조했다(인쇄 쪽 = 0-index + 180).

## 항목별 처리

| # | 처리 | 위치 |
|---|---|---|
| M1 (주요) | 발전 질문 2(팔 흔드는 준비 자세, 상대 운동 설명)를 지웠다. 대신 197쪽 정리 단계 활동 "우리 주변의 운동하는 물체 / 운동하지 않는 물체 분류하기"로 바꿨다. 학생이 실험 장면에 없는 물체를 각각 두 가지 이상 **직접 적고**, 나눈 까닭도 쓴다. 모범 답안은 197쪽 예시 그대로다(달리는 기차, 날아가는 곤충, 자동계단을 타고 올라가는 사람 / 칠판, 쓰레기통, 건물). 기준점·상대성 표현은 없다(192쪽 "직접적으로 다루지 않는다"를 따름). 장면에 이미 나온 물체(달리는 사람, 나무, 책상)는 모범 답안에서 뺐다. | data/lesson-config.js:198-204 |
| D1 (중간) | (a) 3D 비행기 경로를 낮추고 카메라 쪽으로 옮겼다: `loop(0,-2.5,7.5,1.5,…,4.8)`. 나무 꼭대기(약 4.4)보다 높다. 계산으로 확인했다. 3D 칸 비율이 0.6~1.2이면 경로 전체가 사진 안에 들고, 1.8~2.4이면 약 72~81%가 든다. (b) 움직이는 물체가 사진 1·사진 2(5초 뒤)에 모두 넉넉히 들어오지 않으면, 들어오는 때까지 **실제 시간으로** 기다렸다가 사진 1을 찍는다. 이때 초시계 "N초 뒤에 사진 1을 찍어요 / 고른 물체가 화면 안에 들어올 때까지 기다려요 (실제 시간)"를 띄운다. 빨리 감기는 없다. 이름표 자리 때문에 y는 0.14 이상이어야 한다. (c) 그래도 물체가 사진 밖이면 대체 글을 "사진 밖에 있음"으로 바꾸고, 다시 찍으라는 ⚠️ 안내를 보인다. 2D 그림은 하늘이 보이게 예전 높은 경로를 그대로 쓴다(`MOVERS_2D`). | app.js:88, 91, 316-323, 766, 839, 1587-1611, 1636-1652, 1667-1674 |
| D2 (중간) | 분석 맨 앞에 실험관찰 22쪽 ④를 넣었다. "운동하는 물체는 시간이 지났을 때 어떤 위치에 있는 물체?"에 (같은 위치 / 다른 위치) 가운데 고른다. 분석이 4문항에서 5문항이 됐다. | data/lesson-config.js:140-149 |
| D3 (중간) | 힌트 3을 "5초 동안 의자에 계속 앉아 있던 친구와 교실 뒤쪽으로 걸어간 친구가 사진 1·2에서 각각 어디에 있을까요?"로 바꿨다. 팔 흔들기 소재는 없다. 답은 말하지 않는다. | data/lesson-config.js:47 |
| m1 | 세 장면의 물체 순서를 섞어 움직이는 물체가 끝에 몰리지 않게 했다. 정답은 moved로 판정하므로 순서만 바뀐다. | data/lesson-config.js:84-127 |
| m2 | 분석 5(철봉) 보기의 괄호를 뺐다: "운동이 아니다 / 운동이다 / 매달려 있는 시간에 따라 다르다". 근거는 정답 피드백에서 말한다. | data/lesson-config.js:178-181 |
| m3 | 분석 3 "빠르게" 오답 피드백을 바꿨다: "시곗바늘은 아주 천천히 움직였지요. 사진 1과 2에서 무엇이 달라졌는지 떠올려 보세요". 분석 5 오답 피드백도 "5초 간격으로 두 번 찍는다고 생각해 보세요…"로 바꿨다. 둘 다 정답 기준을 직접 말하지 않는다. | data/lesson-config.js:165, 181 |
| m4 | 모형 안내에 "사진을 찍는 동안에만 시간이 흘러요(그 밖에는 멈춘 장면)"를 넣었다. | data/lesson-config.js:75 |
| m5 | M1 수정(197쪽 주변 물체 분류)으로 함께 해결했다. | data/lesson-config.js:198-204 |
| 보기 위치 | 정답 위치를 고르게 나눴다. 분석 1은 2번째, 3은 3번째, 4는 2번째, 5는 1번째이고 분석 2는 여러 개 고르기다. | data/lesson-config.js |
| 저장 호환 | 예전 판에서 저장한 분석 q2~q4 답과 발전 2 답은 새 문항과 맞지 않는다. 그래서 `contentRev` 표시로 한 번만 지운다. | app.js:21-37 |
| i1 (참고) | 자전거 경로를 산책로 바깥 차선(7.8×3.45)으로 옮겼다. 달리는 사람은 안쪽 차선(7.0×2.75)이다. 이제 연못 위를 지나지 않는다(2D·3D 사진으로 확인). | app.js:85-86 |
| i2 (참고) | 틀(sim3d 이름표) 수정은 하지 않았다. D1의 y≥0.14 조건 덕분에 이름표가 잘리는 일이 줄었다. | — |
| i3, i4, i5 | 유지(리뷰 권고대로). | — |

## 제안과 다르게 한 것
- M1: 리뷰는 자동계단 문항이나 허들 문항 하나를 예로 들었다. 나는 197쪽 "주변 물체 분류하기"를 발전 2로 썼다. m5(빠진 197쪽 활동)도 함께 채우고, 학생이 직접 작성하는 활동이 되게 하려는 것이다.
- D2: 분석 2를 앞으로 옮기지 않았다. 22쪽 ④ 문항을 새로 넣었다. 191쪽 평가처럼 "빠르게" 오개념을 다루는 기준 문항은 그대로 두었다.
- D1: (a), (b), (c)를 모두 적용했다. (b)는 장면 시간을 몰래 앞당기지 않는다. 실제 시간으로 기다리고 초시계로 보여 준다.

## 틀 반영 제안
- `experiment.js` scrollIntoViewSafe: `behavior:"smooth"` 스크롤은 숨겨진 탭에서 진행되지 않는다. 검증 중 768×1024에서 사진 찍기 때 3D 칸이 스크롤되지 않은 것도 이 때문이다(보이는 화면에서는 리뷰가 정상 동작을 확인했다). `document.hidden`이면 `behavior:"auto"`로 스크롤하면 좋겠다.
- `sim3d.js` snapshot 이름표(i2): 원 위쪽에 이름표를 둘 자리가 없으면 아래로 옮기는 기준을 이름표 높이로 판단하면 좋겠다.

## 검증 (내 탭 1개, 창이 숨겨진 상태)
- 전체 흐름 끝까지 했다. 예상(힌트 3개) → 26개 물체 실제 5초 촬영·기록(691×837, 3D) → 분석 5문항(오답·`wrongBy`·정답 피드백) → 결론·발전 2개 모범 답안 → 궁금한 점 → 마치기(가짜 save). 완료 카드는 "관찰한 물체 26개, 분석 질문 5/5"였다.
- D1: 691×837(3D 칸 657×366)에서 비행기 5/5, 1280×480(648×298)에서 6/6+3/3, 768×1024에서 1/1이 사진 1·2에 모두 들었다. 1280×480에서는 "사진 1을 찍어요" 기다림이 실제로 작동했다. 추출한 사진으로 비행기와 점선 원을 눈으로 확인했다.
- 375×812, `?no3d=1`, 다크모드: 5단계 모두 scrollWidth 375, 캔버스 0개, body 배경 rgb(17,22,31)이었다. 2D 비행기는 하늘에, 자전거는 산책로 위에 있었다.
- 새로고침 복원(단계, 분석 5, 결론 3칸, 궁금한 점 글)을 확인했다. 콘솔 오류·경고 0개. `node --check` 통과.
- 못 한 것: 창이 숨겨져 있어서 3D 트윈 애니메이션과 부드러운 스크롤을 눈으로 보지 못했다(위 틀 제안 참고). 실제 터치 기기와 로그인 저장도 확인하지 못했다.
- 정리: `sci612sim1:*` 키만 지우고 탭을 닫았다. 사진 수신 스크립트도 멈췄다(스크래치 안).
