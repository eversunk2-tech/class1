# fix-1-report.md — sci-6-2-1-2 리뷰(review.md) 반영

작성: Build 서브에이전트, 2026-09-22. 고친 파일: `app.js`, `style.css`, `data/lesson-config.js`만. spec.md·지침 파일·review.md·`science-sim/`·`class1-record.js`는 건드리지 않았다. git 명령 안 씀, 실 DB 쓰기 안 함. 저장 구조는 그대로라 저장 키(`sci621sim2:v1`)도 그대로다.

## 고친 것

| 리뷰 # | 심각도 | 고친 내용 |
|---|---|---|
| 1 | 중간 | **2D 화면 높이 제한**: 하늘 SVG `max-height: 12vh`, 측정기 SVG `22vh`(가로 좌우 배치일 때 16vh/26vh, 휴대폰 ≤640 px은 제한 없음), 2D 도움말 줄은 숨김(SVG 제목 "하늘(남쪽을 바라본 모습)"·"측정기(옆에서 본 모습)"과 내용이 같음). 세로 태블릿 768×1024 2D: 장면 173–583 px, 시간 바 591–717, 📝 기록하기 858–914 → 하단 버튼 막대(955) 위, 한 화면에 들어옴(전: 장면 713 px, 기록 버튼 y≈1184) |
| 2 | 낮음 | **휴대폰에서 기록할 때 스크롤 줄이기**: ≤520 px에서 3D 장면 300→240 px, 값 패널 한 줄 4칸(글자 작게), 측정값 카드 제목 숨김(세로·좁은 화면 공통), 남중 안내를 기록 버튼 **아래**로 옮김(12:30에서도 버튼 위치가 그대로). 375×812: 시간 바 윗끝부터 기록 버튼 아랫끝까지 347 px라, 시간 바가 머리말 바로 아래 오게 한 번만 내리면 눈금·값·기록 버튼이 모두 한 화면에 보인다(눈금을 누를 때마다 오르내릴 필요 없음). 세로 태블릿 3D도 기록 버튼 857–913 px로 한 화면 |
| 3 | 낮음 | **3D 온도계를 실제 그늘에**: 온도계 둘레에 햇빛 가리개(남·동·서쪽 판 + 지붕, 북쪽만 열림)를 세우고 바닥에 그늘을 칠했다. 이 시각(8:30~15:30) 태양은 방위 110°~240°(남동~남서)라 기하상 가리개 안쪽이 그늘이다(8:30·15:30 광선을 계산해 옆판에 막히는 것 확인). 라벨: "그늘에 둔 온도계 (실제는 땅에서 1.5 m 높이)" |
| 4 | 낮음 | **힌트 1**: "…낮 12시 30분 무렵…" → "해가 뜬 아침부터 한낮을 지나 오후까지 시간이 흐르는 동안, 하늘에서 태양의 높이가 어떻게 달라질지 떠올려 보세요." (특정 시각·변화 방향 없음) |
| 5 | 낮음 | **그래프의 8:30 표시**: 세 그래프 모두 8:30 점을 속이 빈 점선 원으로 그리고 값 옆에 `*`(예: 23.9*), 점 툴팁 "8:30 (모형 추정)", 그래프 아래 범례 "○ 속이 빈 점(8:30)은 모형(추정) 값이에요." 기온 그래프 캡션에 "(세로축은 18 °C부터)"를 더함. (공통 틀 `table-chart.js`는 그대로 두고, 앱에서 그린 뒤 첫 점에 클래스만 붙임) |

참고 항목: 참고-1·2(유지 권고)는 그대로 두었다. 참고-3(3D 글자 선명도)·참고-4(관리자 화면 표시)는 실제 기기·관리자 화면 확인이 필요한 일이고, 참고-5는 spec 수정 사항이라 손대지 않았다.

## 다시 확인한 것
자기 전용 headless Chrome(CDP, 스크래치 프로필, 무작위 포트) + 개발 서버 + 가짜 세션 + Supabase 요청 전부 가로채 가짜 응답(실 DB 요청 0건). 공유 브라우저 탭 안 씀, `localStorage.clear()` 안 씀.
* 전체 흐름 끝까지(예상 → 실험 8칸 → 분석 → 결론·궁금한 점 → 마치기, detail 약 2.7천 자) 5가지: 세로 태블릿 768×1024 **3D**와 **2D**(`?no3d=1`), 375×812 **다크**(모바일·터치 에뮬레이션), 1024×768 3D·2D. 모두 기록 8/8, 값은 spec 표와 같고, **콘솔 오류·경고 0**, 가로 스크롤 0.
* 요소 위치 측정(위 표의 숫자), 1024×768 2D도 장면·시간 바·기록 버튼이 한 화면(시간 바 566–692, 기록 버튼 434–490, 하단 막대 699).
* 스크린숏: 세로 2D, 가로 3D(9:30·12:30·15:30의 가리개·그늘·라벨), 다크 분석 그래프(속이 빈 8:30 점·`*`·범례).
* 힌트 1 새 문구 표시 확인.

## 확인하지 못한 것
* 실제 iPad·휴대폰 터치와 스크롤 느낌, 실제 GPU에서의 3D 글자 선명도.
* 실제 로그인·실제 DB 저장, 관리자 "학생 응답" 화면.
* 8:30 기온 19.5 °C 실제 값(사용자 확인 필요).
* 스크래치 폴더 `build-sci-6-2-1-2`는 삭제 명령이 권한 거부되어 남아 있다(headless Chrome 프로세스는 남은 것 없음).

## 개정 2 (2026-09-22, 사용자 결정: 8:30 삭제)
* **측정 시각 9:30~15:30, 7개**(모두 교과서·실험관찰 예시 값, 추정값 없음). `data/lesson-config.js`의 `times`·`results`에서 `t0830`과 `estimate` 표시를 없앴다. 필수 기록 7칸, 단계 잠금 안내 "눈금 7곳(9:30~15:30)", 완료 문구 "7개 시각".
* **시간 바**: 범위 9:30~15:30(처음·되돌리기 9:30, 버튼 "⏮ 9:30으로"), 눈금 버튼 7개, 레일 작은 눈금 30분 간격 12칸.
* **연속 구간 표**: `data/sun-table.js`를 9:30부터 361개로 잘랐다(9:30~15:30 값은 이전과 같음 — 8:30 보정값은 8:30~9:30 구간에만 쓰였음). 기온 보간(PCHIP)과 그림자 보정은 7점 기준으로 다시 계산된다.
* **추정 표시 삭제**: 값 패널 출처 줄, 눈금 버튼 aria-label, 실험 패널·분석 표의 `*` 각주, 그래프의 속이 빈 점·`*`·범례, detail/qa의 "(모형 추정)"을 모두 없애고 관련 CSS(`.is-est`, `.est-note`, `.est-dot`)도 지웠다. 분석 표 아래에는 "모든 값은 교과서·실험관찰 예시 값(2023년 9월 23일 서울 관측 자료)이에요."를 적었다. 눈금 사이 값은 따로 "🏷 모형: 눈금 사이 값은 살펴보기만 해요(기록하지 않아요)."로 남겼다(모형 안내 문구도 "눈금 사이 값은 모형").
* 그래프 세로축: 그림자 0~20 cm, 기온 20~28 °C(캡션 "세로축은 20 °C부터"), 가로축 9:30~15:30. `index.html` 설명과 머리 주석도 9:30~15:30으로 고쳤다. 저장 키는 `sci621sim2:v1` 그대로(배포 전).
* **다시 확인**(자기 headless Chrome, 가짜 세션·가짜 Supabase, 실 DB 요청 0): 1024×768 3D, 768×1024 3D, 375×812 다크, `?no3d=1`(1024×768·768×1024) 전체 흐름 끝까지 — 기록 7/7, 표·그래프·detail이 7행 모두 교과서 값과 같음, 새로고침 복원(4/7), 콘솔 오류 0, 가로 스크롤 0. 실험·분석·완료 화면의 글자·aria-label·aria-valuetext·title에서 "8:30"·"추정"을 검색해 **0건**. 연속 구간 361분 전수: 눈금 7곳이 표와 같고, 최고 고도 51.7°·최소 그림자 7.9 cm·최고 기온 27.0 °C를 넘지 않음. 세로 태블릿 2D·휴대폰 배치(기록 버튼 위치)도 개정 전과 같다.
* 참고: `build-report.md`는 처음 만들 때의 기록이라 8:30 내용이 그대로 남아 있다(이 절이 최신).
