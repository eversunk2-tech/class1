# fix-1-report.md — sci-6-1-2-5 review 1차 수정

수정한 파일은 `app.js`, `data/lesson-config.js`, `index.html`, `style.css`뿐입니다. `science-sim/` 사본과 `scripts/templates/`는 건드리지 않았습니다. 커밋, 푸시, DB 쓰기는 하지 않았습니다.
지도서 원문은 `fitz`로 다시 확인했습니다. 대조한 곳은 PDF 0-index 42·43·46·47·50·51이고, 인쇄 쪽으로는 222·223·226·227·230·231쪽입니다.

## 항목별 처리

| # | 처리 | 위치 |
|---|---|---|
| M1 | 교통수단 채점을 바꿨습니다. 이제 `a === 이동 거리(km)`이고 `b === 걸린 시간(h)`일 때만 정답입니다. 허용 오차를 없앴고 `transportTolerance`도 지웠습니다. 저장·표·그래프에는 학생이 넣은 값이 아니라 참값 `round1(거리/시간)`이 들어갑니다. 전에 허용 오차로 저장된 값(예: 72.4)도 불러올 때 참값으로 바로잡습니다. 거꾸로 나누면 넣은 수를 보고 판정합니다. 이미 맞힌 카드에서 틀린 식을 넣으면 오답 안내만 나오고 저장값은 그대로입니다 | app.js:866, 881-897 |
| M2 | 분석 1·2(가장 빠른 것 고르기)를 없애고 **빠른 순서대로 쓰기**로 바꿨습니다. 자동차 4대와 교통수단 5가지 모두 학생이 단추를 차례로 눌러 순서를 만들고 확인합니다. 정답 순서는 속력 참값을 정렬해서 만듭니다. 보기 단추는 정답 순서와 다르게 섞어 두었습니다. 틀리면 거꾸로 놓았다는 안내나 "n번째 자리부터 다시 확인" 안내가 나오고 답은 알려 주지 않습니다. 교통수단 두 가지를 골라 **속력으로 비교해 쓰는** 서술 칸도 더했습니다. 제출하면 223쪽 예시 답안 4개가 보입니다. 제출 전에 문장을 가볍게 확인합니다: 이름 2개가 있는지, `km/h`가 붙은 속력 2개가 있는지, 속력 값이 표의 값인지, "A가 B보다 더 빠르다/느리다"의 방향이 맞는지. 이 활동들은 관문(gate)·완료 표시·detail(`order`, `compare`)·요약에 들어갑니다 | app.js:966-1235, 1343-1344, 1370, 1386-1391, 1403 / lesson-config.js:102-137 / index.html:77, 84-85 / style.css 끝 |
| L1 | 정답 피드백에서 "지도서에 따르면"을 빼고 "10 km/h를 m/s로 바꾸면 약 2.8 m/s"라는 근거를 넣었습니다(223쪽 바로잡기) | lesson-config.js:168 |
| L2 | 학생 화면에서 "지도서"를 모두 없앴습니다. "교과서 예시 자료"로 바꿨고, 탐구 3·4에서 잰 내 값과 다를 수 있다는 안내를 더했습니다 | lesson-config.js:57, 206 / index.html:73 / app.js:84 |
| L3 | 이 문항(초록 오답 피드백)은 M2에서 순서 쓰기로 바꾸면서 없어졌습니다. 새 오답 안내는 후보를 좁혀 주지 않습니다 | lesson-config.js:119-120 |
| L4 | 발전 질문 2를 "단위와 함께 바르게 읽어 보고 비교"로 넓혔습니다. 모범 답안에 "이십이 미터 매 초", "사십 미터 매 초"를 넣었습니다(223 스스로 정리 1, 231 해설) | lesson-config.js:190-199 |
| L5 | 앱 코드에서 처리했습니다. `pagehide`와 `visibilitychange(hidden)` 때 예상하기·정리하기·궁금한 점 입력칸의 값을 바로 저장합니다. '처음부터 다시 하기'로 지운 뒤에는 되살리지 않도록 `alive` 표시로 막았습니다. 비교해 쓰기 칸은 입력할 때마다 바로 저장합니다 | app.js:1236-1262 |
| L6 | 초시계 표시를 반올림에서 버림으로 바꿨습니다. 멈춘 값은 정확히 4.2·5.8·5.0입니다 | app.js:486 |
| R1 | 모형 안내에 "5초가 된 순간의 위치를 보여 주려고 멈춘 것처럼 나타냈어요"를 더했습니다 | lesson-config.js:60 |
| R2~R5 | 바꾸지 않았습니다. `build-report.md`는 수정 금지 대상이고, R3은 Embed 단계에서 할 일이며, R4·R5는 리뷰 제안대로 그대로 둡니다 | — |

## 제안과 다르게 한 것 / 더 한 것
* 순서 쓰기를 퀴즈에 더하지 않고 분석 1·2를 **바꿨습니다**. "가장 빠른 것"을 묻는 문항은 순서 쓰기와 겹치고, 정답 피드백이 순서를 알려 주는 문제가 있었기 때문입니다. 남은 분석 질문의 id는 `meaning`, `units`로 새로 붙였습니다. 예전 판에서 저장된 `q1`·`q2` 선택이 새 문항에 잘못 붙지 않게 하려는 것입니다.
* 두 분석 질문의 정답이 모두 두 번째 보기라서, `units`의 정답 보기를 세 번째로 옮겼습니다.
* 분석 `meaning`의 오답 `each` 피드백이 정답("평균적인 빠르기")을 그대로 말하고 있었습니다. 답을 말하지 않는 생각할 거리로 바꿨습니다(lesson-config.js:155).

## 틀 반영 제안
* L5: 지금 정본 `scripts/templates/science-sim/persist.js`에는 이미 `flush`와 떠날 때 자동 저장이 있습니다. 코디네이터가 사본을 동기화하면 app.js:1236-1262의 앱 전용 처리는 없어도 됩니다. 둘을 함께 두어도 같은 값을 쓰므로 문제는 없습니다.

## 검증
자체 헤드리스 Chrome 153(포트 9611, 전용 프로필)으로 확인했고, 끝난 뒤 종료했습니다. 다른 탭과 프로세스는 건드리지 않았습니다. localStorage는 이 앱의 키만 지웠습니다.
* 전체 흐름을 UI로 끝까지 진행했습니다: 예상하기 → 자동차 4대 실행·계산·기록 → 교통수단 5개 → 순서 2개 → 비교 문장 → 퀴즈 → 정리 3개 → 궁금한 점 → 마치기. 1024×768 `?no3d=1`, 768×1024 다크, 375×812 다크(터치 에뮬레이션)에서 했고, 모든 단계에서 `scrollWidth`가 뷰포트 너비와 같았습니다. 콘솔 오류는 0건입니다.
* 3D 1024×768 흐름도 같은 결과였습니다. 이때 콘솔 예외가 1건 나왔는데, 테스트 스크립트가 실행 도중 숨겨진 입력칸에 직접 값을 넣어서 생긴 것입니다. 학생이 할 수 없는 조작이라 앱 문제가 아닙니다.
* M1: `144.8÷2`, `900÷1`은 오답입니다. `5÷4500`은 순서 안내가 나옵니다. 예전 저장값 72.4는 72.5로 바로잡혔습니다. 맞힌 뒤 틀린 식을 넣어도 저장값은 72.5 그대로입니다.
* M2: 거꾸로 놓기, 2번째 자리 오류, 정답을 모두 확인했습니다. 비교 문장은 속력 누락, 틀린 값(35 km/h), 방향 오류("자전거가 배보다 더 빠릅니다")를 막고, 바른 문장은 통과했습니다. 관문 메시지와 새로고침 뒤 복원도 확인했습니다.
* L5: 입력하고 곧바로 새로고침해도 궁금한 점이 남아 있습니다.
* L6: 초록 HUD는 3.8 → 4.0 → 4.2에서 멈추고, 빨강은 5.0에서 멈춥니다. 실제 경과 시간과 맞습니다.
