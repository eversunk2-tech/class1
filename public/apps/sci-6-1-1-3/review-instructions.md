# Review 서브에이전트 지침: sci-6-1-1-3

## 목표
독립적으로 검증하고 `public/apps/sci-6-1-1-3/review.md`를 작성한다. **코드는 수정하지 않는다.**

## 읽을 것
`CLAUDE.md`(웹앱 규칙, 과학 차시 앱 규칙), `public/apps/sci-6-1-1-3/spec.md`(확정 결정 절 우선), `build-report.md`, 앱 폴더 전체, 공통 틀 `scripts/templates/science-sim/`. 본보기로 시범 앱 `public/apps/sci-6-1-1-2/review.md` 형식을 참고.
지도서 원문: `~/Library/CloudStorage/GoogleDrive-sunkyboy@pen.go.kr/내 드라이브/오션초2026/수업/교과서 usb자료/과학 지도서 산과염기.pdf` — 이 차시 지도서 130~137쪽(인쇄 쪽 = PDF 쪽 + 103), 단원 개관(106쪽 지도상 유의점 등). `python3`의 `fitz`로 텍스트 추출.

## 검증 항목
1. **과학적 정확성 (최우선)**: 화면에 나오는 모든 사실·색·현상·분류·용어·힌트·분석 정답·모범 답안·발전 질문 답을 지도서 원문과 대조. 3D/2D 표현이 실제 현상과 맞는지(과장·단순화는 "모형" 표시 여부). 지도서 밖 주장은 일반 과학 지식으로 사실 확인. 오개념 유발 표현. pH·중성·중화 용어 금지 여부.
2. **규칙 준수**: 과학 차시 앱 규칙 항목별 체크(예상 답 누설 금지·힌트, 3D+드래그, 다각적 실험, 기록→표/그래프, 보기 고르기 분석, 결론 후 모범 답안, 발전 질문, 궁금한 점, 다시 하기·앞 단계 이동, localStorage, 결과 저장). 수업 목표(지도서 차시 목표) 활동이 빠지지 않았는지.
3. **브라우저 동작**: 전체 흐름을 끝까지. 태블릿 가로/세로, 휴대폰 375px(가로 스크롤), 다크모드, 새로고침 복원, 콘솔 오류, `?no3d=1`, 3D↔2D 반복 전환, 세로 화면에서 실험 애니메이션이 보이는지. 정적 빌드 경로(`/class1/apps/sci-6-1-1-3/`)에서 자원 로드.
4. **보안/품질**: 학생 입력 innerHTML 삽입(XSS), CDN SRI, 저장 payload 크기(16KB), 접근성(레이블, 포커스, 색만으로 정보 전달 금지), 앱과 공통 틀 사본 일치 여부(`diff -r scripts/templates/science-sim public/apps/sci-6-1-1-3/science-sim`).

## 규칙
* 브라우저 창은 다른 리뷰 에이전트와 공유한다. **자기 탭을 새로 열어(tabs_create) 그 tabId만 사용**하고 다른 탭은 건드리지 않는다. 개발 서버 `http://localhost:3000/class1/apps/sci-6-1-1-3/index.html` 사용 가능(이미 실행 중). 자체 정적 서버를 띄우면 끝날 때 반드시 종료.
* 임시 파일은 스크래치 디렉터리에만, 끝나면 삭제. 테스트로 남긴 localStorage 키는 지운다.
* `public/apps/sci-6-1-1-3/review.md`만 작성. git 조작·실 DB 쓰기 금지.

## review.md 형식
요약(심각도별 개수: 치명/주요/중간/경미/참고) / 과학적 정확성 표(항목, 앱 표현, 지도서 근거 쪽, 판정) / 문제 표(심각도, 위치 파일:줄, 현상, 재현, 수정 제안) / 규칙 준수 체크리스트 / 확인한 것 vs 못 한 것.
