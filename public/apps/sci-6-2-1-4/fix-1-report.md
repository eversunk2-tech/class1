# fix-1-report — sci-6-2-1-4 (review.md 반영)

Build 서브에이전트, 2026-09-22. 바꾼 파일은 `app.js`, `style.css`, `data/lesson-config.js`뿐이다. `spec.md`, 지침 파일, `review.md`, `science-sim/`, `class1-record.js`는 건드리지 않았다(정본과 `diff` 결과 같음). git은 쓰지 않았다.

## 낮음 4건
| # | 수정 내용 |
|---|---|
| L1 그림자가 빛 밖에 생김 | 밝은 빛 자국 둘레에 **흐린 빛(퍼진 빛, 반지름 6 cm, 모형)**을 새로 그렸다. 3D에서는 화면 위와 책상 위 두 곳에 있고, 2D에서는 옆에서 본 흐린 빛줄기·바닥 띠와 위에서 본 흐린 타원이다. 흐린 빛도 1/sin으로 넓어지고 각이 작을수록 흐려진다. 막대에서 옆으로 1.8 cm 떨어진 줄에서 흐린 빛의 반길이는 5.72/sin이고, 그림자 길이는 5·cos/sin이다. 그래서 **10°~90° 모든 각도에서 그림자가 빛이 닿는 곳 안에 생긴다**(10°, 30°, 80°, 90° 캡처로 확인). 수치는 그대로다. 모형 안내에 "밝은 빛 둘레의 흐린 빛 속에 그림자가 생겨요."를 더했다 |
| L2 375px 그래프 글자 | 휴대폰(≤640px)에서 꺾은선그래프의 눈금·값·축 글자를 키웠다. 실제 화면 크기는 눈금 **12.7px**, 점 값 **12.2px**다. 글자를 키우면 회전한 세로축 제목이 눈금 숫자와 겹쳐서, 휴대폰에서는 그 제목을 숨기고 그래프 위에 "세로축: 빛의 세기 · 가로축: 손전등과 화면이 이루는 각(°)" 한 줄을 보인다. 맨 위 값(985)이 잘리지 않게 y축 위 끝을 1100으로 두었다(눈금은 0·500·1000) |
| L3 375px 3D 이름표 겹침 | 좁은 화면에서는 '센서'·'막대' 이름표도 키우고, 겹치지 않는 자리로 옮긴다(막대는 막대 오른쪽 위, 센서는 앞쪽 오른쪽). 넓은 화면으로 바뀌면 원래 자리로 돌아간다. 휴대폰 드래그 안내는 한 줄("👆 드래그: 돌리기 · 두 번 탭: 처음 방향")로 줄여 장면을 덜 가리게 했다. 30°·80°·90°에서 캡처로 확인했다 |
| L4 Firefox 손잡이 | `::-moz-range-thumb`을 `box-sizing: border-box; 44px × 44px`로 바꿨다(Chrome·Safari와 같음) |

## 참고 항목 중 반영한 것
- N1: `?no3d=1`일 때 전환 버튼 문구를 "2D 화면으로 보는 중"으로 바꿨다(WebGL이 없는 기기는 예전처럼 "3D를 쓸 수 없는 기기예요").
- N2: `data/lesson-config.js` 주석의 배경지식 쪽수를 106쪽에서 **107쪽**으로 고쳤다. `spec.md`는 수정 금지라 그대로 두었다(Claude가 고쳐 주세요).
- N3(힌트)는 리뷰 의견대로 그대로 두었다. N4(공통 머리말)는 이 앱 범위 밖이다. N5(시간)는 아래와 같다.
- N6: 스크래치 폴더 `scratchpad/build-sci-6-2-1-4/`는 이번에도 `rm -rf` 권한이 없어 지우지 못했다. 띄운 Chrome은 모두 종료했다.

## 다시 확인한 것(자기 전용 headless Chrome, CDP 9724~9728, 가짜 세션·가짜 Supabase, 실제 DB 요청 0건, 공유 브라우저·`localStorage.clear()` 쓰지 않음)
- 1024×768(3D), 768×1024(3D), 375×812(다크·터치 3D), `?no3d=1`(2D) 네 경우 모두 끝까지 진행했다. **콘솔 오류 0, 모든 단계 가로 스크롤 0**.
- 기록 값: 30° → 500 / 8.7 cm, 80° → 985 / 0.9 cm(spec 개정 1과 같음). 5°·95°를 넣으면 10°·90°로 맞춰진다.
- 키보드, 마우스 끌기, 터치 끌기로 슬라이더가 움직이고 3D·값 패널이 함께 바뀐다.
- 3D↔2D 4번 전환 뒤 캔버스 1개. 새로고침(실험 중·마친 뒤)과 앞 단계 이동 뒤에도 복원된다.
- 추가 기록 6개 제한과 지우기, 분석 오답·정답 피드백을 확인했다. 마치면 `app_results`(`completed: true`, `detail.qa` 6개)가 저장된다. 비로그인이면 로그인 가림막이 뜬다.
- 가장 짧은 경로의 기계 시간은 약 2.4초다. 사람 시간 추정은 바뀌지 않았다(보통 약 4:26, 느린 학생 약 6:50). 모형 안내 한 줄(약 15음절)만 늘었다.

## 확인하지 못한 것
- Firefox와 Safari에서 실제로 어떻게 보이는지(L4는 CSS만 고쳤다), 실제 iPad 터치.
- 실제 Supabase(RLS), 실제 학생 소요 시간.
